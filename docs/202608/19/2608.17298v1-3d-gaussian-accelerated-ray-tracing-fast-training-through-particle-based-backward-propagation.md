---
title: "3D Gaussian Accelerated Ray Tracing: Fast training through particle-based backward propagation"
title_zh: 3D 高斯加速光线追踪：通过基于粒子的反向传播实现快速训练
authors: "Laurent Vit, Oliver Batchelor, Richard Green"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17298v1"
tags: ["query:q3"]
score: 9.0
evidence: 高斯原语的加速光线追踪
tldr: 3DGS在实时渲染中表现出色，但基于光栅化的方法难以处理复杂光影效果。光线追踪虽能解决此问题，但训练效率受限于像素中心的反向传播导致的原子竞争。本文提出3DGART框架，通过将反向传播重组为以基元为中心，利用瓦片-基元映射将梯度计算转化为结构化的聚合过程。该方法在保持光线追踪优势的同时，显著提升了训练速度（约3-4倍），使其效率可与光栅化管线媲美。
selection_source: fresh_fetch
motivation: 传统高斯光线追踪在训练时因像素级反向传播存在严重的原子竞争和线程串行化问题，导致训练成本极高。
method: 提出一种以基元为中心的反向传播机制，通过构建紧凑的中间缓冲区和瓦片-基元映射，让每个线程独立累加单个基元在其覆盖像素上的贡献。
result: 在Mip-NeRF 360数据集上，训练速度比像素级基准提升3-3.5倍，比3DGRT快4倍，且渲染质量有所提高。
conclusion: 3DGART使全光线追踪的高斯训练变得实用，在保留光线追踪处理复杂次级光效优势的同时，达到了与光栅化管线相当的运行效率。
---

## 摘要
3D 高斯泼溅（3D Gaussian Splatting）使高斯原语成为实时新视角合成的高效表示，但其基于光栅化的公式依赖于屏幕空间近似，这限制了准确的视点相关排序以及反射、折射和阴影等二次光线效果的集成。高斯光线追踪通过评估显式的光线-原语相交解决了这些限制，但其训练成本仍然很高。我们观察到，主要的瓶颈不仅在于光线遍历，还在于以像素为中心的反向传播。在这种模式下，许多线程并发地将梯度累加到相同的原语参数中，导致严重的原子竞争和线程串行化。我们提出了 3DGART，这是一个用于光线追踪高斯渲染的实用训练框架。我们的核心思想是围绕原语而非像素重新组织反向传播。利用保守的透视校正屏幕空间边界，我们构建了一个紧凑的中间缓冲区和瓦片-原语映射，允许每个线程在瓦片内累加一个原语对其覆盖像素的贡献。这将梯度计算从高竞争的散射（scatter）操作转变为结构化的类聚集（gather-like）过程。在 Mip-NeRF 360 数据集上，3DGART 相比于逐像素基准实现了约 3-3.5 倍的原始训练加速，相比于 3DGRT 实现了约 4 倍的加速，同时提升了质量。更重要的是，3DGART 使全光线追踪的高斯训练变得实用，在保持光线追踪优势的同时，达到了与基于光栅化的流水线相当的运行时间。

## Abstract
3D Gaussian Splatting has made Gaussian primitives a highly efficient representation for real-time novel view synthesis, but its rasterisation-based formulation relies on screen-space approximations that limit accurate view-dependent ordering and the integration of secondary ray effects such as reflections, refractions, and shadows. Gaussian ray tracing addresses these limitations by evaluating explicit ray-primitive intersections, yet it remains costly to train. We observe that the main bottleneck is not ray traversal alone, but the pixel-centric backward propagation, where many threads concurrently accumulate gradients into the same primitive parameters, causing severe atomic contention and thread serialisation.   We present 3DGART, a practical training framework for ray-traced Gaussian rendering. Our key idea is to reorganise backward propagation around primitives rather than pixels. Using conservative perspective-correct screen-space bounds, we build a compact intermediate buffer and a tile-primitive mapping that allows each thread to accumulate the contribution of one primitive over its covered pixels within a tile. This transforms gradient computation from a contention-heavy scatter operation into a structured gather-like process. On Mip-NeRF 360, 3DGART achieves an $\approx 3-3.5\times$ raw training speedup over per-pixel baseline and $\approx4 \times$ over 3DGRT on Mip-NeRF 360 while improving quality. More importantly, 3DGART makes fully ray-traced Gaussian training practical, reaching runtimes competitive with rasterisation-based pipelines while preserving benefits of ray tracing.

---

## 论文详细总结（自动生成）

以下是对论文《3D Gaussian Accelerated Ray Tracing: Fast training through particle-based backward propagation》的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：3D 高斯泼溅（3DGS）凭借其基于光栅化的渲染管线实现了极高的实时渲染速度。然而，光栅化依赖于屏幕空间近似，难以处理精确的视点排序以及复杂的次级光影效果（如反射、折射和阴影）。
*   **核心问题**：虽然“高斯光线追踪”可以解决上述物理准确性问题，但其**训练成本极高**。作者发现瓶颈不在于光线遍历（Ray Traversal），而在于**以像素为中心的反向传播机制**。在这种模式下，大量线程（像素）同时尝试更新同一个高斯基元的参数，导致严重的原子竞争（Atomic Contention）和硬件线程串行化，极大地拖慢了训练速度。
*   **整体含义**：本文旨在通过重构反向传播算法，使全光线追踪的高斯训练在速度上能与传统光栅化方法竞争，从而推动高质量、具备复杂光效的神经渲染技术走向实用。

### 2. 方法论：核心思想与关键技术
3DGART 的核心是将反向传播从“以像素为中心”转变为**“以基元（高斯粒子）为中心”**。
*   **粒子中心反向传播（Particle-based Backward）**：
    *   **重组计算**：不再让每个像素线程去寻找并更新它所受影响的高斯，而是让每个线程负责一个高斯基元，计算该基元对其覆盖的所有像素的梯度贡献。
    *   **瓦片-基元映射（Tile-Primitive Mapping）**：利用保守的透视校正屏幕空间边界，将图像划分为瓦片（Tiles）。通过构建紧凑的中间缓冲区，记录每个瓦片内受影响的高斯。
    *   **从“散射”到“聚集”**：将原本高竞争的“散射（Scatter）”操作（多像素写一高斯）转化为结构化的“类聚集（Gather-like）”过程。每个线程在瓦片内部独立累加单个基元的梯度，最后再统一更新全局参数，显著减少了原子操作冲突。
*   **算法流程**：
    1.  **前向传播**：执行光线追踪，记录光线与高斯的相交信息及贡献权重。
    2.  **预处理**：计算高斯在屏幕上的保守边界，并进行瓦片化排序。
    3.  **反向传播内核**：每个 CUDA 线程处理一个“高斯-瓦片”对，遍历该瓦片内受影响的像素，累加梯度。

### 3. 实验设计
*   **数据集**：主要使用 **Mip-NeRF 360** 数据集（包含室内和室外场景），这是衡量新视角合成质量的标准基准。
*   **Benchmark（基准）**：
    *   **3DGS (Original)**：基于光栅化的原始 3D 高斯泼溅。
    *   **3DGRT**：现有的高斯光线追踪框架。
    *   **Per-pixel RT Baseline**：作者实现的基于像素中心反向传播的光线追踪基准，用于对比消融。
*   **对比指标**：渲染质量（PSNR, SSIM, LPIPS）和训练效率（每迭代耗时、总训练时间）。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA RTX 4090** GPU 上进行。
*   **训练时长**：
    *   3DGART 在 Mip-NeRF 360 数据集上完成 30,000 次迭代大约需要 **30-40 分钟**。
    *   相比之下，传统的逐像素光线追踪方法耗时显著更长（约 3-4 倍的时间成本）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 Mip-NeRF 360 的所有 9 个场景。
*   **消融实验**：作者对**瓦片大小（Tile Size）**、**排序策略**以及**反向传播模式**进行了详细的消融研究，验证了粒子中心化方案的有效性。
*   **充分性评价**：实验设计较为充分，不仅对比了质量，还深入分析了内存占用和计算延迟。通过与当前最先进的光栅化和光线追踪方法对比，证明了其在效率上的重大突破。

### 6. 主要结论与发现
*   **速度飞跃**：3DGART 相比于逐像素反向传播的光线追踪实现了 **3-3.5 倍** 的加速，相比于 3DGRT 实现了约 **4 倍** 的加速。
*   **质量提升**：在保持或超越原始 3DGS 图像质量的同时，解决了光栅化在处理大倾角或复杂遮挡时的伪影问题。
*   **实用性**：证明了全光线追踪的高斯训练可以达到与光栅化管线相媲美的运行效率，使得在训练阶段集成次级光效（如阴影、反射）变得可行。

### 7. 优点
*   **解决核心瓶颈**：敏锐地捕捉到原子竞争是训练慢的元凶，并给出了优雅的工程解决方案。
*   **硬件友好**：通过瓦片化和局部累加，更好地利用了 GPU 的并行架构和缓存机制。
*   **兼容性**：该框架保留了光线追踪的所有优点（如精确排序、易于扩展次级光线），同时消除了其训练慢的劣势。

### 8. 不足与局限
*   **内存开销**：为了实现高效的反向传播，需要存储中间缓冲区（如瓦片-基元映射），这增加了显存占用。
*   **场景依赖**：在极度密集或高斯重叠极其严重的场景下，瓦片化带来的增益可能会因处理冗余像素而有所稀释。
*   **次级光效展示较少**：虽然论文强调了光线追踪对次级光效的支持，但实验部分仍主要集中在标准的新视角合成（主要体现初级光线），对反射/折射等复杂场景的深度训练实验尚有提升空间。

（完）
