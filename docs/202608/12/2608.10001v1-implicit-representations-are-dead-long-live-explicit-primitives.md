---
title: "Implicit representations are dead. Long live explicit primitives!"
title_zh: 隐式表示已死。显式原语万岁！
authors: "Nil Stolt-Ansó, Maik Dannecker, Wenqi Huang, Andras Jakab, Daniel Rueckert"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.10001v1"
tags: ["query:q1"]
score: 8.0
evidence: 医学体积的隐式神经表示与基于高斯的基元
tldr: 随着医疗影像分辨率不断提高，传统的隐式神经表示（INR）因计算成本高、内存需求大且优化缓慢而面临瓶颈。本文提出并评估了基于显式高斯原语的表示方法，通过在2D病理切片和3D肺部CT数据集上的实验，证明了该方法在保持高保真度的同时，显著降低了优化时间和内存消耗。研究对比了显式与隐式方法的数学特性，展示了显式原语在医疗影像处理中的巨大潜力，为未来高分辨率医学数据表示提供了新方向。
selection_source: fresh_fetch
motivation: 隐式神经表示在处理高分辨率医疗影像时存在计算开销大、优化时间长和内存占用高等显著瓶颈。
method: 采用显式高斯原语替代全局多层感知机，并对2D显微组织学和3D肺部CT数据进行跨维度性能评估。
result: 高斯表示在所有压缩倍率下均能达到或超过隐式方法的重建质量，且优化速度更快、内存需求更低。
conclusion: 显式高斯原语凭借其优异的计算性能和数学特性，是未来高分辨率医疗影像表示研究中极具吸引力的方向。
---

## 摘要
医学数据的连续参数化已成为一种强大的分辨率无关图像表示范式。虽然隐式神经表示（Implicit Neural Representations）提供了高保真度和紧凑的存储，但其对全局多层感知机（MLP）的依赖导致了巨大的计算开销、高内存需求以及漫长的优化时间。随着医学影像趋向于更详细、更高分辨率的体数据，这些成本对隐式方法的适用性构成了重大瓶颈。最近，基于高斯的显式原语通过将深度网络评估替换为局部的、易于光栅化的原语，彻底改变了表示学习范式。在本文中，我们针对医学影像应用，对高斯表示与隐式方法进行了全面的跨维度评估。首先，我们概述了显式原语所具备的、超出隐式神经范式能力的数学特性。随后，我们在两个具有挑战性的图像数据集上基准测试了计算性能：2D 显微组织学和 3D 肺部计算机断层扫描（CT）。我们的实验表明，在所有压缩倍率下，高斯表示的重建指标始终与隐式方法持平或更优，同时表现出显著更低的优化时间和内存需求。结合显式原语所提供的引人注目的数学特性，这些发现推动了高斯表示的更广泛采用，并将其定位为医学影像未来研究的一个极具吸引力的方向。

## Abstract
Continuous parameterization of medical data has emerged as a powerful paradigm for resolution-independent image representation. While Implicit Neural Representations offer high fidelity and compact storage, their reliance on global Multi-Layer Perceptrons incurs sizeable computational costs, large memory requirements, and extensive optimization times. As medical imaging trends towards ever-more detailed, high-resolution volumes, these costs impose significant bottlenecks in the applicability of implicit approaches. Recently, explicit Gaussian-based primitives have revolutionized the representation learning paradigm by trading deep network evaluations for localized, rasterization-friendly primitives. In this paper, we present a comprehensive, cross-dimensional evaluation of Gaussian representations against implicit approaches for medical imaging applications. First, we outline a theoretical overview on the mathematical properties offered by explicit primitives beyond what is capable under the implicit neural paradigm. Subsequently, we benchmark the computational performance on two demanding image datasets: 2D microscopy histology and 3D lung Computed Tomography (CT). Our experiments demonstrate that Gaussian representations consistently match or surpass reconstruction metrics compared to implicit methods across all compression factors, while displaying significantly lower optimization times, and memory requirements. Together with the compelling mathematical properties offered by explicit primitives, these findings motivate the wider adoption of Gaussian representations and position them as an attractive direction for future research in medical imaging.

---

## 论文详细总结（自动生成）

这篇论文对医学影像表示领域从“隐式神经表示（INR）”向“显式高斯原语（Explicit Gaussian Primitives）”的范式转移进行了深入探讨和实证研究。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着医学影像（如病理切片、CT、MRI）分辨率的不断提升，传统的**隐式神经表示（INR）**（依赖全局多层感知机 MLP）在处理大规模数据时面临严重的计算瓶颈，包括优化时间极长、内存占用高以及缺乏几何可解释性。
*   **研究动机**：受 3D Gaussian Splatting 的启发，作者试图证明显式高斯原语在医学影像中不仅能提供更高的重建质量，还能在计算效率上实现数量级的提升，并具备 INR 难以企及的数学解析特性。

### 2. 方法论
*   **统一框架**：论文将字典学习、INR、特征网格和显式原语统一在一个基函数展开公式下：$v(x) = \sum_{k \in N(x)} w_k(x) c_k$。不同方法的区别仅在于基函数 $w_k$ 的来源、空间支撑集以及优化对象。
*   **核心思想**：放弃深度 MLP，改用一组可优化的**各向异性高斯分布**作为显式基函数。
*   **关键技术细节**：
    *   **高斯基函数**：每个原语由中心 $\mu$、协方差矩阵 $\Sigma$（通过 Cholesky 分解保证正定性）和系数 $c$ 组成。
    *   **两种权重方案**：评估了“加性（Additive）”和“单位划分（PoU）”两种方案，后者在处理大面积均匀区域时效率更高。
    *   **数学特性**：显式高斯具有**卷积闭包**（方便建模传感器模糊/PSF）、**空间-频谱对偶性**（方便傅里叶域处理）、**解析 Lipschitz 连续性**（方便正则化）以及**闭式导数与体积计算**。

### 3. 实验设计
*   **数据集**：
    *   **2D BACH (Breast Cancer Histology)**：高分辨率乳腺癌组织学图像（2048x1536），包含复杂的纹理和均匀区域。
    *   **3D NLST (National Lung Screening Trial)**：3D 肺部 CT 扫描（151x512x512），包含复杂的解剖结构和噪声。
*   **Benchmark 与对比方法**：
    *   **全隐式方法**：Fourier Features (FF-ReLU), SIREN, WIRE。
    *   **特征网格方法**：Neural Volumes, K-Planes。
    *   **显式方法**：Gaussian (Additive), Gaussian (PoU)。
*   **评估指标**：峰值信噪比 (PSNR)、优化耗时（达到收敛的时间）、反向传播时的单坐标内存占用。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量。
*   **训练细节**：使用了 ADAM 优化器，Batch Size 为 200k，采用混合精度训练。
*   **效率量化**：虽然未给总时长，但给出了相对比例——高斯方法比特征网格快 2-5 倍，比全隐式方法快 5-100 倍。

### 5. 实验数量与充分性
*   **实验规模**：针对每个设置进行了 $n=10$ 次重复实验以保证统计显著性。
*   **压缩倍率**：对比了 5x、10x、20x 三种不同的压缩因子，涵盖了从高保真到高压缩的场景。
*   **公平性**：所有基准方法都经过了超参数网格搜索（如学习率、频率参数 $\omega$ 等），确保了对比的客观性。实验跨越了 2D 和 3D 维度，验证了方法的通用性。

### 6. 主要结论与发现
*   **性能领先**：在所有压缩倍率下，显式高斯表示（尤其是 PoU 方案）的 PSNR 均持平或优于最先进的隐式方法（如 WIRE）。
*   **效率碾压**：显式方法在优化速度上比 INR 快 1-2 个数量级，内存需求降低了 10-200 倍。
*   **扩展性强**：显式原语能够根据信号复杂度自适应分配容量（通过自适应加密），而不会像 MLP 那样产生全局扰动或灾难性遗忘。

### 7. 优点与亮点
*   **理论完备**：不仅做了实验，还从数学角度系统梳理了显式原语在医学影像中的优势（如解析 PSF 建模、闭式 Lipschitz 约束）。
*   **实用性强**：直接解决了医学影像大数据量与 INR 计算成本之间的矛盾，为临床应用（如快速配准、稀疏重建）扫清了障碍。
*   **范式统一**：将多种表示方法纳入统一框架，有助于读者理解不同技术路径的本质联系。

### 8. 不足与局限
*   **生成模型挑战**：作者承认显式原语在生成式建模（如 GAN/Diffusion）方面仍处于起步阶段，因为其非网格化的拓扑结构与目前的网格化生成架构存在冲突。
*   **硬件依赖**：虽然内存占用低，但显式原语的效率高度依赖于高性能的光栅化算子，这在某些非 CUDA 环境下可能受限。
*   **实验覆盖**：虽然涵盖了 CT 和组织学图像，但未涉及 MRI 或超声等其他模态，不同模态的噪声特性可能对高斯拟合产生不同影响。

（完）
