---
title: View-Adaptive Renderer for View-Consistent 2D-to-3D Generation
title_zh: 面向视角一致性 2D 到 3D 生成的视角自适应渲染器
authors: "U-Chae Jun, Jaeeun Ko, Jiwoo Kang"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09110v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于NeRF重建的视点自适应神经渲染
tldr: 本研究针对单图3D重建中多视角合成不一致的问题，提出了一种视角自适应神经渲染框架。该框架通过独立的视角自适应渲染器修正视角误差，并利用全局特征骨干保持结构一致性，同时引入自注意力融合模块整合多视角信息。该方法无需复杂的扩散模型监督，仅凭光度渲染损失和轻量化正则项即可实现高保真重建，在保证精度的同时显著提升了效率。
selection_source: fresh_fetch
motivation: 旨在解决单图3D重建过程中，因多视角合成存在投影歧义而导致的视觉不一致和几何重建误差。
method: 提出视角自适应渲染器独立修正视角误差，并结合全局特征骨干与自注意力融合模块来确保结构一致性。
result: 实验证明该方法在不使用扩散模型监督的情况下，仅靠光度损失即可达到接近最先进水平的重建保真度。
conclusion: 该框架在精度与效率之间取得了良好平衡，为处理不一致输入下的鲁棒3D重建提供了实用的解决方案。
---

## 摘要
从单张图像重建 3D 形状仍然是计算机视觉中一个基础且具有挑战性的问题。传统的单目 3D 生成流水线通常先从单张输入图像合成多个视角，然后再应用基于神经辐射场（NeRF）的重建。然而，固有的投影歧义往往会导致生成的视角之间出现视觉不连续性，从而导致重建的 3D 模型不准确。目前的解决方案要么带来了显著的额外计算负担，要么未能充分解决合成视角之间的实际不一致性。为了解决这些局限性，我们提出了一种新型的视角自适应神经渲染框架，即使在给定部分不一致的多视角输入时，也能实现鲁棒的 3D 重建。我们的方法引入了视角自适应神经渲染器，在独立纠正与视角相关的误差的同时，共享全局特征骨干网络以保持结构连贯性。此外，我们提出了一个自注意力融合模块，能够自适应地整合多视角信息，在不严重依赖间接正则化或高计算强度方法的情况下确保几何一致性。通过广泛的实验，我们证明了我们的方法能够持续提高 3D 重建的保真度。重要的是，我们的方法在没有基于扩散的 SDS 监督的情况下，主要依靠光度渲染损失和轻量级注意力正则化器，实现了接近最先进水平的性能。这种准确性与效率之间的平衡使得所提出的框架在实际应用中具有很高的实用性。

## Abstract
Reconstructing 3D shapes from a single image remains a fundamental yet challenging problem in computer vision. Traditional monocular 3D generation pipelines typically synthesize multiple views from a single input image before applying Neural Radiance Field (NeRF)-based reconstruction. However, inherent projective ambiguities often produce visual discontinuities across generated viewpoints, leading to inaccuracies in reconstructed 3D models. Current solutions either incur significant additional computational burdens or fail to adequately resolve practical inconsistencies between synthesized views. To address these limitations, we propose a novel viewpoint-adaptive neural rendering framework that enables robust 3D reconstruction even when given partially inconsistent multi-view inputs. Our approach introduces view-adaptive neural renderers that independently correct viewpoint-dependent errors while simultaneously sharing a global feature backbone to preserve structural coherence. Furthermore, we propose a self-attention fusion module that adaptively integrates multi-view information, ensuring geometric consistency without relying heavily on indirect regularizations or computationally intensive methods. Through extensive experiments, we demonstrate that our method consistently improves 3D reconstruction fidelity. Importantly, our approach achieves near state-of-the-art performance without diffusion-based SDS supervision, relying primarily on photometric rendering loss with lightweight attention regularizers. This balance between accuracy and efficiency makes the proposed framework highly practical for real-world applications.

---

## 论文详细总结（自动生成）

以下是对论文《View-adaptive Renderer for View-consistent 2D-to-3D Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：从单张图像进行 3D 重建时，主流方法通常先生成多视角图像再利用 NeRF 进行重建。然而，由于投影歧义，生成的多个视角之间往往存在**视觉不一致性**（如纹理错位、几何变形），导致最终重建的 3D 模型出现表面模糊或几何畸变。
*   **研究动机**：现有的解决视角不一致的方法（如 SDS 监督）计算开销巨大，且难以从根本上解决局部不一致。本文旨在开发一种能够显式处理视角差异、在不牺牲全局连贯性的前提下融合局部修正的鲁棒渲染框架。

### 2. 方法论
核心思想是**“解耦局部误差与全局结构”**，通过视角自适应分支修正特定视角的错误，并通过注意力机制进行全局融合。
*   **视角自适应神经表示**：
    *   **共享骨干网络**：使用一个共享的 MLP 提取全局几何和外观先验。
    *   **视角自适应 MLP 分支**：为每个输入视角分配独立的轻量级 MLP 分支，专门学习并修正该视角特有的不一致性（扰动 $\delta_i$）。
*   **自注意力融合模块 (Self-Attention Fusion)**：
    *   利用多头自注意力机制（MHSA）对各视角的特征进行加权聚合。
    *   **公式化表达**：通过计算视角间的注意力权重 $\alpha_{ij}$，动态抑制噪声视角，增强可靠视角的贡献。
*   **损失函数设计**：
    *   **光度渲染损失 ($L_{render}$)**：最小化合成图像与输入图像的像素差异。
    *   **熵正则化 ($L_{entropy}$)**：防止注意力过度集中于单一视角，确保多视角信息平稳整合。
    *   **一致性损失 ($L_{consistency}$)**：最小化融合特征的方差，强制各视角向全局一致的表示对齐。
*   **三阶段训练策略**：全局预训练（初始化结构） $\rightarrow$ 视角自适应微调（修正局部） $\rightarrow$ 自注意力融合（精炼一致性）。

### 3. 实验设计
*   **数据集**：使用 Google Scanned Objects (GSO) 中的 50 个 3D 对象实例进行评估（这些实例在多视角生成模型的训练集之外，确保了评估的公正性）。
*   **Benchmark 与对比方法**：
    *   **基础渲染器**：InstantNGP 和 NeuS。
    *   **SOTA 对比**：RealFusion、Zero-1-to-3、SyncDreamer 和 Wonder3D。
*   **评估指标**：
    *   **图像质量**：PSNR、SSIM、LPIPS。
    *   **几何精度**：倒角距离 (Chamfer Distance, CD)、体积交并比 (IoU)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 3090 GPU**。
*   **参数设置**：Batch size 为 1024 条射线；预训练 100k 次迭代，微调 200k 次迭代。
*   **效率表现**：相比于使用 SDS 损失的方法，该方法在仅使用渲染损失时，训练时间缩短了约 **87%**（从约 54 分钟降至 7 分钟），同时保持了相近的精度。

### 5. 实验数量与充分性
*   **实验规模**：针对 50 个不同类别的 GSO 对象进行了详尽测试。
*   **消融实验**：系统验证了视角自适应分支、自注意力融合、熵正则化和一致性损失四个组件的累积贡献，证明了每个模块的必要性。
*   **鲁棒性测试**：测试了在不同视角数量（4、8、12、16 个视角）下的表现，证明了该方法在**稀疏视角（如仅 4 个视角）**下的性能提升最为显著。
*   **公平性**：在相同的 NeuS 后端下对比了不同生成模型的效果，实验设计客观且对比基准统一。

### 6. 主要结论与发现
*   **视角自适应的有效性**：通过为每个视角分配专门的修正层，可以显著减少因输入图像不一致导致的重建伪影。
*   **摆脱重度监督**：证明了在合理的架构设计下，仅靠光度损失（Photometric Loss）即可实现高保真重建，无需依赖计算昂贵的扩散模型（SDS）监督。
*   **稀疏视角优势**：在输入视角极少的情况下，该方法比传统 NeRF 具有更强的鲁棒性和几何恢复能力。

### 7. 优点
*   **高效性**：大幅降低了 3D 重建的计算开销，提升了实际应用的可行性。
*   **灵活性**：作为一个通用的插件式框架，可以轻松集成到 InstantNGP、NeuS 甚至 3D Gaussian Splatting 等不同 3D 表示中。
*   **理论支撑**：论文提供了视角不一致性对重建误差影响的理论分析（引理 1 和定理 1），增强了方法的说服力。

### 8. 不足与局限
*   **极端噪声敏感**：如果输入的多视角图像存在极度严重的噪声或完全错误的几何关系，该框架的修正能力可能达到上限。
*   **背景复杂度限制**：在处理具有高度复杂背景或遮挡关系的场景时，重建的可靠性会有所下降。
*   **相机参数依赖**：虽然能修正图像内容的不一致，但仍高度依赖于初始相机参数的准确性。

（完）
