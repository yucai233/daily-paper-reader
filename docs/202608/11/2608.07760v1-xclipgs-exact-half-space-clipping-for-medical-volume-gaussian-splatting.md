---
title: "XClipGS: Exact Half-Space Clipping for Medical Volume Gaussian Splatting"
title_zh: XClipGS：用于医学体数据高斯泼溅的精确半空间裁剪
authors: "Zhongpai Gao, Benjamin Planche, Meng Zheng, Anwesa Choudhuri, Chaoyi Zhou, Terrence Chen, Ziyan Wu"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07760v1"
tags: ["query:q7"]
score: 9.0
evidence: 使用高斯泼溅代理对体积医学扫描进行交互式渲染
tldr: 本研究提出XClipGS，旨在解决医学体积高斯泼溅中的精确裁剪与内部结构重建问题。通过将半空间限制的高斯射线积分分解为2D足迹与条件高斯累积分布函数，实现了无需额外参数的闭式像素级裁剪算子。该方法支持微分并利用多视图监督内部解剖，在保持超实时渲染速度的同时，显著提升了裁剪面的保真度并减少了伪影泄漏。
selection_source: fresh_fetch
motivation: 传统高斯泼溅在医学影像裁剪时无法精确处理被截断的基元，且难以恢复未被外部视图覆盖的内部解剖细节。
method: 提出一种基于局部仿射模型的闭式像素级裁剪算子，并结合多距离、多轴向的参考视图对隐藏内部进行监督训练。
result: 实验表明XClipGS在PSNR和SSIM上均达到最优，渲染速度超过650 FPS，且在裁剪面伪影抑制方面表现卓越。
conclusion: XClipGS为医学体积数据的交互式可视化提供了一种高效且精确的裁剪方案，成功解决了基元截断处理与内部结构监督的挑战。
---

## 摘要
高斯泼溅（Gaussian-splatting）代理实现了医学体扫描数据的交互式渲染，但裁剪平面会暴露未受外部视图训练约束的解剖结构，并与传统泼溅方法只能整体保留或丢弃的基元相交。我们提出了 XClipGS（精确裁剪），将其视为两个独立的问题：渲染时的裁剪算子和隐藏内部的监督。在 EWA 泼溅所使用的局部仿射模型下，受半空间限制的高斯的射线积分可以精确地分解为其普通的二维足迹（footprint）和一个条件高斯累积分布函数（CDF），后者的参数在像素坐标中是仿射的。由此产生的闭式逐像素算子无需引入学习的裁剪参数或辅助网络，并且对于基元和平面保持可微。我们使用具有不同裁剪平面轴和偏移量的多距离参考视图，通过相同的算子来监督内部。我们还引入了一种配对的裁剪/未裁剪切面协议，包含差分参考切割误差（CDE）和剔除侧泄漏（Leak），因为全局图像指标会稀释平面附近的误差。在八个使用未参与训练的平面偏移量的 CT 和 MRI 体数据上，XClipGS 在每个体数据上都获得了最高的 PSNR（33.56 dB，而 ClipGS 为 32.34 dB），同时渲染速度超过 650 FPS（远高于实时水平），而对比方法为 278 FPS。在体素轴切面视图上，它将平均波段 SSIM 从 0.809 提高到 0.860，且泄漏量减少了约 40 倍。在无需重新训练的情况下，它在任意法向平面上的所有四项指标中也达到了最佳平均值；在固定内部的情况下，它的切面保真度与 RaRa 相当，但泄漏量减少了约 16 倍。项目页面：https://gaozhongpai.github.io/XClipGS/

## Abstract
Gaussian-splatting proxies enable interactive rendering of volumetric medical scans, but a clipping plane exposes anatomy not constrained by external-view training and intersects primitives that conventional splatting can only keep or drop whole. We present XClipGS (eXact Clipping), which treats these as two separate problems: the render-time clip operator and supervision of the hidden interior. Under the local affine model used by EWA splatting, the ray integral of a half-space-restricted Gaussian factorizes exactly into its ordinary 2D footprint and a conditional Gaussian CDF whose argument is affine in pixel coordinates. The resulting closed-form per-pixel operator introduces no learned clipping parameters or auxiliary network and remains differentiable with respect to the primitive and plane. We use multi-distance reference views with varied clipping-plane axes and offsets to supervise the interior through the same operator. We also introduce a paired clipped/unclipped cut-face protocol with difference-referenced cut error (CDE) and culled-side leakage (Leak), because global image metrics dilute errors near the plane. On eight CT and MRI volumes with plane offsets not used for training, XClipGS attains the highest PSNR on every volume (33.56 versus 32.34 dB for ClipGS) while rendering at over 650 FPS, far above real time, versus 278 FPS. On voxel-axis cut-face views, it raises average band SSIM from 0.809 to 0.860 and leaks roughly 40 times less. Without retraining, it also achieves the best average across all four metrics on arbitrary-normal planes; on a fixed interior, it matches RaRa's face fidelity with about 16 times less leakage. Project page: https://gaozhongpai.github.io/XClipGS/

---

## 论文详细总结（自动生成）

这篇论文提出了 **XClipGS**，一种针对医学体数据高斯泼溅（Gaussian Splatting, GS）的精确半空间裁剪方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在医学影像（如 CT/MRI）的交互式可视化中，用户经常需要使用“裁剪平面”来观察内部解剖结构。然而，传统的高斯泼溅在处理裁剪时存在两个主要痛点：
    1.  **基元截断不精确**：传统方法对被平面穿过的基元通常采取“整体保留或丢弃”的二元策略，导致裁剪边缘出现锯齿或空洞。
    2.  **内部结构缺乏监督**：现有的 GS 模型多从外部视角训练，内部解剖结构未被充分约束，导致裁剪后暴露的内部细节模糊或错误。
*   **整体含义**：论文旨在通过数学上的精确推导和改进的训练策略，实现高保真、超实时的医学体数据交互式裁剪渲染。

### 2. 论文提出的方法论
*   **核心思想**：将裁剪问题分解为渲染时的**精确算子（Operator）**和对**隐藏内部的监督（Supervision）**。
*   **关键技术细节**：
    *   **解析半空间裁剪算子**：在 EWA 泼溅的局部仿射模型下，论文证明了受限高斯的射线积分可以精确分解为：**原始 2D 足迹（Footprint） × 条件高斯累积分布函数（CDF）**。
    *   **闭式表达式**：该 CDF 的参数在像素坐标中是仿射的，这意味着裁剪操作不需要额外的学习参数或辅助神经网络（如 MLP），且对基元参数和平面参数均可微。
    *   **裁剪感知内部监督**：利用参考体渲染器生成多距离、多轴向、多偏移量的裁剪视图作为训练数据。通过可微裁剪算子，梯度可以直接流向被暴露的内部基元，从而学习到真实的内部解剖结构。
    *   **无需重训的泛化**：由于算子是解析的，训练完成后的模型可以支持任意法向和偏移量的裁剪平面。

### 3. 实验设计
*   **数据集**：使用了 8 个临床体数据（6 个 CT，2 个 MRI），涵盖腹部、肠道、膝关节、下肢、血管、心脏、鼻部和手部。
*   **Benchmark 与对比方法**：
    *   **对比系统**：**ClipGS**（基于 3DGS 的医学裁剪基准）。
    *   **对比算子**（在相同骨干网络下）：**Hard Cull (HC)**（硬剔除）、**Moment-Matched (MM)**（矩匹配近似）、**RaRa Clipper**（基于射线-椭球弦长比例的近似）。
*   **评估指标**：除了常规的 PSNR、SSIM、LPIPS 外，专门引入了针对裁剪面的指标：
    *   **$S_b$ (Band SSIM)**：裁剪面带状区域的结构相似性。
    *   **CDE (Cut Error)**：差分参考切割误差，衡量切割位置的准确度。
    *   **Leak (Leakage)**：剔除侧的能量泄漏，衡量伪影。

### 4. 资源与算力
*   **GPU 型号**：实验使用了 **NVIDIA A100-SXM4-80GB** 进行渲染速度（FPS）测试。
*   **训练时长**：所有模型均训练了 **30,000 次迭代**。
*   **渲染分辨率**：测试在 **1600 × 1600** 的高分辨率下进行。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同的解剖部位数据集上进行了全面测试，涵盖了 held-out（预留）的平面偏移量和完全未见过的任意法向平面。
*   **消融实验**：
    *   验证了“裁剪感知监督”的重要性（不加该监督，PSNR 下降达 8.3 dB）。
    *   进行了“算子交换”实验，在固定内部结构的前提下对比不同裁剪规则的优劣。
*   **充分性评价**：实验设计非常严谨，通过配对的裁剪/未裁剪视图消除了重建误差对裁剪评估的干扰。使用了参数敏感性分析验证了 Leak 和 CDE 指标的稳定性，实验结果具有高度的客观性和公平性。

### 6. 论文的主要结论与发现
*   **性能卓越**：XClipGS 在所有 8 个数据集上的 PSNR 均排名第一（平均 33.56 dB，比 ClipGS 高 1.22 dB）。
*   **速度极快**：渲染速度超过 **650 FPS**，比 ClipGS（278 FPS）快 2.4 倍，完全满足超实时交互需求。
*   **伪影极低**：在裁剪面伪影（Leakage）方面，比 MM 方法减少约 15 倍，比 RaRa 减少约 16 倍，比 ClipGS 减少约 40 倍。
*   **泛化性强**：在训练时仅见过轴向裁剪平面的情况下，模型能完美处理任意斜向裁剪平面。

### 7. 优点
*   **数学严谨性**：提出了基于仿射 EWA 模型的解析解，避免了启发式近似或昂贵的神经网络推理。
*   **端到端可微**：算子支持反向传播，使得内部结构的监督训练变得高效。
*   **轻量化**：不增加存储负担，不引入辅助网络，仅在渲染时增加极小的计算开销。
*   **局部化评估**：提出的 CDE 和 Leak 指标解决了全局指标（如 PSNR）对裁剪边缘误差不敏感的问题。

### 8. 不足与局限
*   **仿射模型限制**：该精确解是基于局部仿射 EWA 投影模型的，如果使用完全透视投影模型（如 3DGEER），则需要重新推导系数。
*   **训练依赖**：虽然推理时支持任意平面，但高质量的内部重建仍依赖于训练阶段包含多样化的裁剪视图。
*   **计算平台**：虽然渲染很快，但训练过程仍需要高性能 GPU 产生参考视图并进行优化。

（完）
