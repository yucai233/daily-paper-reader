---
title: "ACA-GS: Adaptive-Capacity Anchored Gaussian Splatting for Compact Dynamic Radiance Fields"
title_zh: ACA-GS：用于紧凑型动态辐射场的自适应容量锚定高斯泼溅
authors: "Seunghyeon Song, Joo Chan Lee, Chanung Park, Jun Young Jeong, Minseo Lee, Eunbyung Park, Jong Hwan Ko"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04581v1"
tags: ["query:q1"]
score: 8.0
evidence: 动态辐射场与神经高斯泼溅架构
tldr: 针对4D高斯泼溅（4DGS）在运动表达与存储效率之间的权衡问题，本文提出ACA-GS框架。该方法打破了传统基于锚点设计的固定参数限制，通过动态分配表示容量来优化存储。核心贡献包括自适应锚点基数（调整每个锚点的神经高斯数量）和自适应特征掩码（调节特征通道），在保持高保真渲染的同时，显著降低了内存占用，在MPEG等数据集上实现了比现有技术高1.5倍的压缩率。
selection_source: fresh_fetch
motivation: 现有的基于锚点的4DGS方法采用固定的参数配置，导致在处理复杂运动时为了提升精度而过度增加锚点密度，造成严重的内存浪费。
method: 提出一种自适应容量框架，通过动态调整每个锚点生成的神经高斯数量以及自适应屏蔽特征通道，实现计算资源与局部时空需求的精准匹配。
result: 在MPEG、Panoptic Sports和N3DV等多个数据集上的实验表明，该方法在不损失视觉质量的前提下大幅减少了存储开销，压缩效率提升显著。
conclusion: ACA-GS通过灵活的容量分配机制，有效解决了动态辐射场中的冗余问题，为实现紧凑且高效的4D场景表示提供了新方案。
---

## 摘要
4D高斯泼溅（4DGS）的最新进展实现了高保真、实时的时空渲染，但揭示了运动表达能力与存储效率之间的基本权衡。虽然基于锚点的设计通过锚点级参数共享实现了紧凑性，但其僵化的统一参数化强制要求每个锚点具有固定的神经高斯（Neural Gaussian）数量和特征预算。因此，保真度不足的问题是通过过高的锚点密度来解决的，而不是通过轻量化、有针对性地增加神经高斯数量或特征容量，从而导致了内存浪费。为了克服这种僵化性，我们引入了一种自适应容量的锚点框架，根据局部时空需求动态分配表示容量。自适应锚点基数（Adaptive Anchor Cardinality）改变了每个锚点的神经高斯数量，将基元集中在几何或运动复杂度高的区域，同时抑制冗余。与此同时，自适应锚点特征掩码（Adaptive Anchor Feature Masking）调节锚点级特征通道，为复杂区域分配丰富的特征，为简单区域分配轻量化表示。在 MPEG、Panoptic Sports 和 N3DV 数据集上的实验表明，在不降低视觉质量的情况下，存储空间显著减少。值得注意的是，在具有复杂运动的挑战性 MPEG 序列上，我们的方法在保持相当质量的同时，实现了比最先进的基于锚点的方法高出 1.5 倍的压缩率。

## Abstract
Recent advances in 4D Gaussian Splatting (4DGS) enable high-fidelity, real-time spatiotemporal rendering, but expose a fundamental trade-off between motion expressiveness and storage efficiency. While anchor-based designs achieve compactness through anchor-level parameter sharing, their rigid uniform parametrization enforces fixed Neural Gaussian counts and feature budgets per anchor. Consequently, insufficient fidelity is addressed by excessive anchor density, rather than lightweight, targeted increases in Neural Gaussian count or feature capacity, resulting in memory waste. To overcome this rigidity, we introduce an adaptive-capacity anchor-based framework that dynamically allocates the representational capacity based on local spatiotemporal demands. Adaptive Anchor Cardinality varies the number of Neural Gaussians per anchor, concentrating primitives in regions of high geometric or motion complexity while suppressing redundancy. In parallel, Adaptive Anchor Feature Masking modulates anchor-level feature channels, assigning rich features to complex regions and lightweight representations to simpler ones. Experiments on MPEG, Panoptic Sports, and N3DV datasets demonstrate substantial storage reduction without degrading visual quality. Notably, on challenging MPEG sequences with complex motion, our method achieves up to 1.5x higher compression than state-of-the-art anchor-based methods while preserving comparable quality.

---

## 论文详细总结（自动生成）

### ACA-GS：用于紧凑型动态辐射场的自适应容量锚定高斯泼溅论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的4D高斯泼溅（4DGS）技术在**渲染质量**与**存储效率**之间存在严重权衡。
*   **研究背景**：基于锚点（Anchor-based）的方法虽然通过参数共享提升了紧凑性，但其结构过于僵化——每个锚点被分配了**固定数量**的神经高斯基元和**统一长度**的特征通道。
*   **动机**：这种“一刀切”的配置导致简单区域（如背景）存在大量冗余，而复杂运动区域为了弥补精度不足不得不增加锚点密度，造成内存浪费。本文旨在实现一种能根据局部时空需求动态分配资源的自适应框架。

#### 2. 方法论
核心思想是引入**自适应容量（Adaptive Capacity）**机制，从基数和特征两个维度优化资源分配：
*   **自适应锚点基数（AAC）**：
    *   **重要性评估**：引入“累积视点相关透明度”作为重要性指标，通过跨视点和跨时间聚合透明度贡献，识别低贡献的基元。
    *   **基元重定位**：将低重要性的神经高斯从冗余区域“杀死”，并重新分配到几何或运动复杂的活跃锚点附近，通过扰动初始化位置来增强局部细节。
    *   **门控掩码**：为了保持批处理效率，预设最大基数上限，通过学习二进制门控掩码来激活实际需要的基元。
*   **自适应锚点特征掩码（AAFM）**：
    *   **特征分解**：将锚点特征分为“基础段”（保证稳定性）和“自适应段”（可屏蔽）。
    *   **学习掩码**：利用直通估计器（STE）学习通道掩码，通过正则化损失压制冗余通道。
*   **条件正则化（Conditional Regularization）**：
    *   为了防止时间特征（处理运动）去补偿因空间特征屏蔽带来的损失，引入条件损失函数，确保在动态区域保留足够的特征容量，而在静态简单区域强制稀疏化。

#### 3. 实验设计
*   **数据集**：
    *   **MPEG**（Bartender, Cinema）：包含复杂且快速的运动。
    *   **Panoptic Sports**：多视角体育运动场景。
    *   **N3DV**：经典的动态场景数据集。
*   **基准方法（Benchmarks）**：
    *   **非压缩/弱压缩方法**：4DGS, STG, E-D3DGS, Dynamic 3DGS。
    *   **高压缩方法**：CSTG+PP, GIFStream（当前SOTA锚点压缩方法）。
*   **评价指标**：PSNR、SSIM、LPIPS（视觉质量）以及存储大小（MB）。

#### 4. 资源与算力
*   **渲染性能**：文中提到在MPEG数据集上可达到 70-80 FPS 的实时渲染速度。
*   **训练算力**：**论文未明确说明具体的 GPU 型号、数量及总训练时长**。仅提到 AAC 机制从 3,000 次迭代开始每 500 次触发一次，AAFM 则从训练开始即激活。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流动态场景数据集，进行了详尽的定量对比。
*   **消融实验**：非常充分。分别验证了 AAC、AAFM、压缩管线、最大基数限制、特征通道数以及各项超参数（如 $\lambda_M, \lambda_{CR}$）对结果的影响。
*   **客观性**：通过率失真曲线（Rate-Distortion Curve）直观展示了在相同存储预算下，本文方法在 PSNR 上的显著优势，对比实验较为公平。

#### 6. 主要结论与发现
*   **压缩效率**：在保持同等视觉质量的前提下，存储空间比 SOTA 方法（GIFStream）进一步减少了 **35% 到 42%**。
*   **运动处理**：在 MPEG 等快速运动场景中，自适应资源分配的优势尤为明显，压缩率提升达 **1.5 倍**。
*   **资源分布**：可视化结果证实，模型成功地在静态简单区域减少了资源，而在动态复杂区域集中了更多的基元和特征。

#### 7. 优点与亮点
*   **灵活性**：打破了锚点方法的结构僵化，实现了细粒度的资源调度。
*   **协同效应**：AAC 减少了基元冗余，AAFM 减少了特征冗余，两者结合在不增加锚点总数的情况下提升了表达力。
*   **端到端优化**：通过 STE 和条件正则化，使复杂的离散掩码学习能够融入连续的梯度优化过程。

#### 8. 不足与局限
*   **超参数依赖**：虽然实验证明其具有一定的鲁棒性，但最大基数 $\tau$ 和重定位比例 $\rho$ 等参数仍需根据场景预设。
*   **伪影风险**：消融实验显示，若基数设置过高，可能会出现浮动伪影（Floater artifacts）。
*   **长序列挑战**：目前主要针对短视频序列，对于超长序列的帧间一致性和存储扩展性仍有待进一步研究。

（完）
