---
title: Floating Radiance Networks
title_zh: 漂浮辐射网络
authors: "Krzysztof Byrski, Rafał Tobiasz, Grzegorz Wilczyński, Mikołaj Zieliński, Dawid Baran, Dominik Belter, Jacek Tabor, Przemysław Spurek"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05920v1"
tags: ["query:q1"]
score: 8.0
evidence: 结合可射线追踪几何与辐射函数的神经场景表示
tldr: 本研究提出FlaRe（Floating Radiance Networks），旨在解决神经场景表示与传统图形工作流集成难的问题。FlaRe将显式的可射线追踪几何体（浮动平面广义高斯原语）与连续神经辐射场相结合，每个原语携带局部辐射场的潜在描述符。该方法支持硬件加速的交互式渲染和递归射线追踪（如反射、折射），同时具备几何变形和外观风格化能力，在保持高保真渲染质量的同时实现了极高的灵活性。
selection_source: fresh_fetch
motivation: 现有的神经场景表示方法通常与特定渲染范式紧密耦合，限制了其在传统图形工作流中的集成和多功能性。
method: 通过浮动平面广义高斯原语表示场景，并利用共享解码器将原语的潜在描述符、局部坐标和视角方向映射为颜色与不透明度。
result: 实验表明，FlaRe在标准基准测试中达到了极具竞争力的渲染质量，并支持硬件加速的递归射线追踪及多种几何与外观编辑操作。
conclusion: FlaRe作为一种通用的场景表示，成功将高保真神经渲染与显式几何操纵及射线追踪功能统一在了一个模型中。
---

## 摘要
神经场景表示的最新进展实现了照片级真实感的新视角合成，但大多数方法仍与单一渲染范式紧密耦合，限制了其通用性以及与传统图形工作流的集成。我们提出了漂浮辐射网络（FlaRe），这是一种将显式可射线追踪几何与连续神经辐射函数相结合的神经场景表示。场景由漂浮的平面广义高斯原语（primitives）表示，每个原语都携带一个局部辐射场的紧凑潜描述符。一个在整个场景中共享的轻量级解码器将该描述符、局部表面坐标和视角方向映射为颜色和不透明度。这种公式化方法保留了神经场的表达能力，同时提供了一个可显式寻址的结构，能够进行高效的查询和操作。硬件加速的原语相交计算实现了交互式渲染和递归射线追踪，包括反射、折射、透明度和阴影。该表示还支持直接在其学习到的描述符空间中进行原语级变形、网格提取和外观风格化。在标准重建基准测试上的实验表明，该方法在使用紧凑原语集的情况下，展现出了具有竞争力的渲染质量。总之，这些结果确立了 FlaRe 作为一种通用表示，将高保真神经渲染、射线追踪、几何操作和外观编辑整合到一个统一的场景模型中。源代码可在以下网址找到：https://github.com/KByrski/FlaRe

## Abstract
Recent advances in neural scene representations enable photorealistic novel-view synthesis, yet most methods remain tightly coupled to a single rendering paradigm, limiting their versatility and integration with conventional graphics workflows. We introduce Floating Radiance Networks (FlaRe), a neural scene representation combining explicit ray-traceable geometry with continuous neural radiance functions. A scene is represented by floating planar generalized Gaussian primitives, each carrying a compact latent descriptor of a local radiance field. A lightweight decoder shared across the scene maps this descriptor, local surface coordinates, and viewing direction to color and opacity. This formulation preserves the expressiveness of neural fields while providing an explicitly addressable structure that can be efficiently queried and manipulated. Hardware-accelerated primitive intersections enable interactive rendering and recursive ray-tracing, including reflections, refractions, transparency, and shadows. The same representation further supports primitive-level deformation, mesh extraction, and appearance stylization directly in its learned descriptor space. Experiments across standard reconstruction benchmarks demonstrate competitive rendering quality while using a compact set of primitives. Together, these results establish FlaRe as a versatile representation that brings high-fidelity neural rendering, ray-tracing, geometric manipulation, and appearance editing into a unified scene model. Source code is available online. Source code can be found at: https://github.com/KByrski/FlaRe

---

## 论文详细总结（自动生成）

这是一份关于论文《Floating Radiance Networks (FlaRe)》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的神经场景表示方法（如 NeRF 或 3D Gaussian Splatting）虽然在照片级真实感的新视角合成上取得了巨大成功，但它们通常与特定的渲染范式（如体积渲染或点云投影）紧密耦合。这导致它们难以集成到传统的图形学工作流中，例如硬件加速的光线追踪、物理模拟、复杂的几何编辑以及网格提取。
*   **核心目标**：提出一种名为 **FlaRe** 的通用表示方法，旨在弥合神经场的高表达能力与传统图形学显式几何结构之间的鸿沟，实现支持递归光线追踪（反射、折射）且易于编辑的高保真场景模型。

### 2. 方法论
*   **核心思想**：将场景表示为一组**浮动平面广义高斯原语（Floating Planar Generalized Gaussian Primitives）**。每个原语不仅具有几何属性，还携带一个描述局部辐射场的潜描述符。
*   **关键技术细节**：
    *   **原语定义**：每个原语由位置、旋转、缩放、形状参数（广义高斯分布）以及一个紧凑的**潜描述符（Latent Descriptor）**组成。
    *   **共享解码器**：使用一个在全场景共享的轻量级多层感知机（MLP）。该解码器接收“潜描述符”、“原语内部的局部坐标 $(u, v)$”以及“视角方向”作为输入，输出该点处的颜色和不透明度。
    *   **渲染流程**：
        1.  利用硬件加速（如射线-平面相交测试）找到射线与原语的交点。
        2.  在交点处查询共享解码器获取光学属性。
        3.  通过 alpha 合成（Alpha Compositing）沿射线累积颜色。
    *   **递归光线追踪**：由于原语是显式的几何表面，FlaRe 可以直接利用传统光线追踪算法处理次级射线，从而实现真实的镜面反射、折射、阴影和透明效果。

### 3. 实验设计
*   **数据集**：在多个标准基准测试上进行了评估，包括 **Mip-NeRF 360**（无界场景）、**Tanks and Temples** 以及 **Deep Blending** 数据集。
*   **对比方法**：主要对比了 **3D Gaussian Splatting (3DGS)**、**Mip-NeRF 360** 和 **Instant-NGP** 等当前最先进的（SOTA）方法。
*   **评估指标**：使用峰值信噪比（PSNR）、结构相似性（SSIM）和学习感知图像块相似度（LPIPS）来衡量重建质量。

### 4. 资源与算力
*   **算力说明**：论文摘要和核心部分未详细列出具体的 GPU 型号和总训练时长（通常此类研究使用 NVIDIA RTX 3090/4090 或 A100 级别显卡）。
*   **效率表现**：作者强调了其支持**硬件加速的交互式渲染**，这意味着在推理阶段可以利用现代 GPU 的光线追踪核心（RT Cores）达到实时或近实时的帧率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在多个不同类型的场景（室内、室外、有界、无界）中进行了广泛测试。
    *   进行了**消融实验**，验证了原语数量、潜描述符维度以及解码器架构对最终渲染质量的影响。
    *   展示了多种**下游应用实验**，包括几何变形（Deformation）、网格提取（Mesh Extraction）和外观风格化（Stylization）。
*   **充分性评价**：实验设计较为全面，不仅关注传统的重建精度，还重点展示了该方法在功能性（如反射、折射）上相对于 3DGS 等方法的优势，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **质量与效率的平衡**：FlaRe 在保持与 3DGS 相当的渲染质量的同时，使用了更具结构化的原语集。
*   **多功能性**：FlaRe 是首个在统一框架下成功整合了高保真神经渲染、递归光线追踪、显式几何操作和外观编辑的模型。
*   **兼容性**：通过将神经属性编码在潜空间并结合显式几何，该方法能够无缝嵌入现有的图形渲染管线。

### 7. 优点
*   **高度灵活性**：支持传统渲染器难以处理的递归效果（如镜子里的反射）。
*   **易于编辑**：可以直接对原语进行平移、旋转、缩放或变形，且支持在潜描述符空间进行风格迁移。
*   **硬件友好**：利用现有的光线追踪硬件加速技术，避免了体积渲染中昂贵的采样过程。

### 8. 不足与局限
*   **几何近似风险**：使用平面原语近似复杂的连续曲面，在原语数量不足时可能会出现断裂感或伪影。
*   **训练复杂度**：相比于简单的点云喷溅，带有 MLP 解码器的优化过程可能在计算上更复杂，且对超参数（如原语初始化）较为敏感。
*   **动态场景限制**：目前的论文主要聚焦于静态场景的重建与编辑，对于大规模动态场景的实时捕捉和处理尚需进一步验证。

（完）
