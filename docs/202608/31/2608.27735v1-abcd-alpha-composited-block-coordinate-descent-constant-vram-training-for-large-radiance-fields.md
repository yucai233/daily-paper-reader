---
title: "ABCD: Alpha-Composited Block Coordinate Descent: Constant-VRAM Training for Large Radiance Fields"
title_zh: ABCD：Alpha 混合块坐标下降：大规模辐射场的常数显存训练
authors: "Ka Heng Shiu, Kartic Subr"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27735v1"
tags: ["query:q7"]
score: 8.0
evidence: 加速大型辐射场的训练与渲染
tldr: ABCD 是一种针对大规模辐射场（如 3D 高斯泼溅）的显存恒定训练框架。它将训练过程重新表述为空间分区上的块坐标下降，每次仅激活一个分块，并利用 Alpha 混合的结合律将非活动区域预渲染为背景/前景图像。该方法使峰值显存与场景总规模解耦，实现了在有限显存的 GPU 上训练超大规模场景，且重建质量损失极小。
selection_source: fresh_fetch
motivation: 解决大规模辐射场训练中显存需求随场景规模增长，导致有限显存设备无法训练超大场景的问题。
method: 利用 Alpha 混合的结合律，将非活动空间块预渲染并折叠为前景和背景图像，从而在训练单个块时实现常数级显存占用。
result: "实验表明该方法在显著降低显存需求的同时，重建质量与完整 3DGS 相比 PSNR 下降不到 5%。"
conclusion: ABCD 框架为在显存受限的硬件上训练超大规模辐射场提供了一种高效且保真的外存训练方案。
---

## 摘要
我们提出了 ABCD（Alpha 混合块坐标下降），这是一种针对 Alpha 混合辐射场的外存（out-of-core）训练框架，本文以 3D 高斯泼溅（3D Gaussian Splatting）为例进行了实例化。我们的方法将训练重新表述为空间分区上的块坐标下降：一次只有一个参数块处于激活状态，而所有其他块都被冻结。通过利用 Alpha 混合的结合律，这些非激活区域可以被预渲染并合并为前景和背景 RGBA 图像。因此，对于固定的分区大小和图像分辨率，峰值显存（VRAM）相对于总场景范围变为 O(1)，而不是随完整场景大小增长。这使得显存有限的 GPU 能够训练那些原本无法放入内存的场景。在实验中，我们的方法很好地保持了 3DGS 的重建质量，PSNR 下降不到 5%，而消除了混合机制的 ABCD 则会遭受约 40% 的性能下降。我们的代码可在 https://github.com/shiukaheng/abcd 找到。

## Abstract
We present ABCD (Alpha-Composited Block Coordinate Descent), an out-of-core training framework for alpha-composited radiance fields, instantiated here for 3D Gaussian Splatting. Our method reformulates training as block coordinate descent over spatial partitions: only one block of parameters is active at a time, while all others are frozen. By exploiting the associativity of alpha blending, these inactive regions can be pre-rendered and collapsed into foreground and background RGBA images.   As a result, for fixed partition size and image resolution, peak VRAM becomes O(1) with respect to total scene extent, rather than growing with full scene size. This enables GPUs with limited memory to train scenes that would otherwise not fit in core. In experiments, our method closely preserves the reconstruction quality of 3DGS, with less than 5% PSNR degradation, while ABCD with compositing ablated suffers roughly 40% degradation.   Our code can be found at https://github.com/shiukaheng/abcd

---

## 论文详细总结（自动生成）

以下是对论文《ABCD: Alpha-Composited Block Coordinate Descent: Constant-VRAM Training for Large Radiance Fields》的结构化深入总结：

### 1. 研究动机与核心问题
*   **核心问题**：随着辐射场（如 3D Gaussian Splatting, 3DGS）应用场景向大规模城市级扩展，训练所需的显存（VRAM）呈爆炸式增长，超出了单体 GPU 的承载能力。
*   **背景与挑战**：
    *   传统的“分块训练再合并”方法（如 NeRF-XL）会导致严重的边界伪影，因为每个分块在训练时缺乏全局上下文，会试图补偿缺失的几何信息。
    *   现有的分布式训练方案成本高昂，且对硬件要求苛刻。
*   **整体含义**：ABCD 旨在通过一种数学上严谨的优化框架，实现在有限显存下训练理论上无限大的场景，且不损失重建质量。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将训练过程建模为**空间分区上的块坐标下降（Block Coordinate Descent, BCD）**。在任一时刻，仅激活一个空间块的参数进行优化，其余块保持冻结。
*   **关键技术：Alpha 混合的结合律**：
    *   利用 Alpha 混合（Alpha Compositing）的结合律，将冻结的非活动区域预渲染并折叠为两张 RGBA 图像：**前景图 (F)** 和 **背景图 (B)**。
    *   对于给定的相机视角，位于活动块之前的几何体渲染为 F，位于其后的渲染为 B。
*   **算法流程**：
    1.  **初始化**：将场景划分为规则的 3D 网格块。
    2.  **预渲染**：为每个可见的相机-分区对生成并存储压缩的 F 和 B 图像。
    3.  **块优化**：选择一个活动块 $\theta_{active}$，加载对应的 F 和 B，仅对该块进行 $T$ 步梯度下降。
    4.  **更新与循环**：完成优化后，刷新该块的缓存，切换到下一个块。
*   **显存优势**：峰值显存仅取决于单个块的大小和两张图像的分辨率，相对于场景总规模呈 $O(1)$ 复杂度。

### 3. 实验设计
*   **数据集/场景**：使用了原始 NeRF 数据集中的经典场景：`garden`（花园）和 `kitchen`（厨房）。
*   **基准测试 (Benchmark)**：以标准的 **3D Gaussian Splatting (3DGS)** 作为质量上限参考。
*   **对比方法**：
    *   **Standard 3DGS**：全量参数驻留显存训练。
    *   **ABCD (Full)**：本文提出的带 Alpha 混合补偿的方法。
    *   **ABCD (Ablated)**：消融实验，即简单的分块训练，不使用前景/背景图像补偿。

### 4. 资源与算力
*   **显存 (VRAM)**：ABCD 的平均峰值显存约为 **0.785 GB**，而标准 3DGS 为 0.925 GB。在小场景下优势不明显，但证明了显存占用与分块大小挂钩。
*   **系统内存 (RAM)**：ABCD 使用了约 **21.38 GB**，显著高于 3DGS（5.81 GB），主要用于缓存预渲染的图像。
*   **训练时长**：ABCD 的训练时间（约 1061 秒）远长于 3DGS（约 83 秒），增加了约 12 倍。这主要是由于频繁的磁盘/内存 I/O 以及小规模分块导致 GPU 算力利用率（Rasterizer occupancy）不足。
*   **GPU 型号**：文中未明确提及具体的 GPU 型号，但提供了详细的显存占用数值。

### 5. 实验数量与充分性
*   **实验规模**：主要针对两个代表性场景进行了深入对比，并包含了一组关键的消融实验。
*   **充分性评价**：
    *   **逻辑充分**：消融实验有力地证明了 Alpha 混合补偿是消除分块伪影的关键。
    *   **规模局限**：实验场景相对较小（200 个相机），尚未在真正的城市级超大规模场景上验证其 $O(1)$ 显存的渐进优势。作者在文中也承认了这一点，将其归因为工程实现和时间限制。

### 6. 主要结论与发现
*   **质量保持**：ABCD 成功解决了分块训练的伪影问题，PSNR 仅比全量训练下降了约 **3.1%**，而无补偿的分块训练 PSNR 下降了约 37%。
*   **显存解耦**：证明了可以通过增加存储和计算时间来换取极低的显存占用，使低端显卡训练大场景成为可能。
*   **训练轨迹**：ABCD 的损失函数下降曲线与标准 3DGS 高度趋同，验证了块坐标下降在辐射场优化中的有效性。

### 7. 优点
*   **理论严谨**：利用 Alpha 混合的数学特性（结合律）解决了分块训练的上下文缺失问题。
*   **硬件普惠**：将大场景训练的门槛从昂贵的高显存 GPU 转向了廉价的内存和磁盘存储。
*   **通用性**：虽然以 3DGS 为例，但该框架理论上适用于任何基于 Alpha 混合的辐射场表示。

### 8. 不足与局限
*   **时间开销巨大**：由于需要频繁预渲染和加载缓存图像，训练速度显著变慢。
*   **系统内存压力**：虽然节省了 VRAM，但对系统 RAM 或磁盘空间的需求大幅增加。
*   **实验场景偏小**：目前的实验尚未完全体现出其在“超大规模”场景下的渐进式显存优势，小场景下的显存节省被优化器状态等固定开销掩盖。
*   **分块边界处理**：目前仅根据高斯中心点进行硬分区，忽略了高斯协方差跨越边界的情况，可能导致微小的边界不连续。

（完）
