---
title: "QuARC-GS: Quantized Anchored Residual Coding for Compact Dynamic Scene Streaming with Gaussian Splatting"
title_zh: QuARC-GS：基于量化锚定残差编码的紧凑型动态场景高斯泼溅流式传输
authors: "Vu Trung Nghia Nguyen, Yuchen Wang, Kyung Chul Lee, Kevin C. Zhou"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.18285v1"
tags: ["query:q7"]
score: 8.0
evidence: 通过量化感知优化加速动态场景渲染
tldr: 针对动态3D场景流式传输中存储需求大、重建速度慢的挑战，本文提出QuARC-GS框架。该方法基于高斯泼溅技术，通过单一参考帧和高度压缩的逐帧残差来表示场景。核心贡献包括量化感知锚点变形和变化门控加密策略，在保持高质量重建和训练速度的同时，将每帧存储空间降低了高达11倍，实现了超高压缩率的自由视角视频流传输。
selection_source: fresh_fetch
motivation: 现有的动态3D场景表示技术在长视频流式传输中面临巨大的存储压力和实时渲染挑战。
method: 提出一种量化感知优化框架，利用量化锚点变形抑制微小运动，并结合变化门控加密策略仅在真实变化区域增加高斯点。
result: 实验表明，QuARC-GS在保持竞争力的重建质量和训练速度的前提下，比现有最先进方法的每帧存储量减少了高达11倍。
conclusion: QuARC-GS通过高效的残差编码和量化策略，为紧凑且高质量的动态场景流式传输提供了一种有效的解决方案。
---

## 摘要
3D场景表示技术（如神经辐射场 (NeRF) 和高斯泼溅 (Gaussian Splatting)）在动态视图合成方面取得了实质性进展，实现了从任意视角的高质量渲染。最近，这些技术已扩展到动态3D场景；然而，由于详细场景表示的巨大存储需求以及对高重建/渲染速度的要求，实现可持续的在线自由视角视频 (FVV) 流式传输仍然具有挑战性，特别是对于较长的视频。为了应对这些挑战，我们提出了 QuARC-GS（量化锚定残差编码高斯流），这是一种用于在线动态场景重建的量化感知 4D 场景优化框架，在保持重建速度和质量的同时实现了超高压缩率。QuARC-GS 使用单个标准帧和高度压缩的逐帧残差来表示场景。具体而言，我们通过针对运动、外观和稠密化的两种互补策略来压缩每个残差。我们引入了量化感知锚定变形，它在抑制微不足道的运动更新的同时保留有意义的变形，从而在低存储流式传输下保持重建质量。此外，我们设计了一种变化门控稠密化策略，仅在表现出真实时间变化的区域分配新的高斯分布，从而有效消除了冗余的外观更新并降低了存储开销。在广泛使用的数据集上进行的广泛实验表明，与现有最先进技术相比，QuARC-GS 在实现具有竞争力的重建质量和训练速度的同时，将每帧存储空间减少了高达 11 倍。

## Abstract
3D scene representation techniques such as neural radiance fields (NeRFs) and Gaussian splatting have made substantial progress in novel view synthesis, achieving high-quality renderings from arbitrary view angles. More recently, such techniques have been extended to dynamic 3D scenes; however, achieving sustainable online free-viewpoint video (FVV) streaming remains challenging, especially for longer videos, due to significant storage demands of detailed scene representations and high reconstruction/rendering speed needs. To address these challenges, we propose Quantized Anchored Residual Coding Gaussian Streaming (QuARC-GS), a quantization-aware 4D scene optimization framework for online dynamic scene reconstruction that achieves ultra-high compression while maintaining reconstruction speed and quality. QuARC-GS represents a scene using a single canonical frame and highly compressed per-frame residuals. Specifically, we compress each residual through two complementary strategies targeting motion, appearance, and densification. We introduce quantization-aware anchor deformation, which suppresses insignificant motion updates while preserving meaningful deformations, maintaining reconstruction quality under low-storage streaming. Furthermore, we design a change-gated densification strategy that allocates new Gaussians only in regions exhibiting genuine temporal changes, effectively eliminating redundant appearance updates and reducing storage overhead. Extensive experiments on widely used datasets demonstrate that QuARC-GS enables competitive reconstruction quality and training speed while cutting per-frame storage by up to 11$\times$ compared to the state-of-the-art.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《QuARC-GS: Quantized Anchored Residual Coding for Compact Dynamic Scene Streaming with Gaussian Splatting》** 的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 3D 高斯泼溅（3DGS）技术的发展，自由视角视频（FVV）的实时渲染已成为可能。然而，在**在线流式传输**场景下，动态 3D 场景表示面临两大挑战：
*   **存储需求巨大**：详细的 4D 场景表示（随时间变化的数百万个高斯点）会产生海量数据，难以在有限带宽下传输。
*   **重建与渲染速度平衡**：需要在极短的时间内完成逐帧更新，同时保持高保真度的渲染效果。
现有方法虽尝试通过锚点或残差编码来压缩，但在运动量化和冗余外观更新方面仍存在优化空间。

### 2. 方法论：核心思想与关键技术
QuARC-GS 提出了一种量化感知的 4D 场景优化框架，核心思想是利用**单一参考帧（Canonical Frame）加高度压缩的逐帧残差**来表示动态序列。

*   **分层锚点变形（Hierarchical Anchor Deformation）**：
    *   将场景运动建模为多级（通常为 3 级）锚点的变形。每个高斯点通过 1-NN 搜索绑定到最近的锚点，通过聚合锚点的平移（$\delta t$）和旋转（$\delta r$）残差来计算自身位姿。
*   **量化感知优化（Quantization-aware Optimization）**：
    *   **核心技术**：引入直通估计器（Straight-Through Estimator, STE）。在训练的前向传播中对锚点残差进行量化（Rounding），使模型在训练阶段就“感知”到传输时的精度损失。
    *   **效果**：微小的运动会被量化为零（恒等变换），从而在熵编码时大幅降低存储开销，同时梯度能绕过量化算子更新全精度参数。
*   **变化门控稠密化（Change-gated Densification）**：
    *   **核心技术**：计算当前渲染图与前一帧目标图之间的视空间梯度差值。
    *   **逻辑**：仅在变形无法解释的、具有显著时间变化的区域（如新出现的物体或复杂光影变化）增加新的高斯点。这避免了在静态或已拟合区域冗余地添加点，控制了模型随时间的膨胀。

### 3. 实验设计
*   **数据集**：
    *   **N3DV**：包含 6 个室内动态场景，分辨率 2704×2028，300 帧。
    *   **Meeting Room**：包含 3 个真实世界动态场景，分辨率 1280×720。
*   **Benchmark（基准指标）**：PSNR、SSIM、LPIPS（质量）；Storage/KB（存储）；Train Time（训练耗时）；Render FPS（渲染速度）。
*   **对比方法**：
    *   **在线方法**：3DGStream、ReCon-GS、QUEEN、4DGC、ComGS、HiCoM 等。
    *   **离线方法**：4DGS、Swift4D、SplineGS 等。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX PRO 6000 GPU**，运行环境为 Ubuntu 24.04。
*   **训练时长**：
    *   **初始帧**：优化 15,000 次迭代（含噪声注入以增强鲁棒性）。
    *   **后续帧**：每帧进行 100 次量化变形优化和 100 次门控稠密化优化。
    *   **总耗时**：在 N3DV 数据集上，每帧平均训练时间约为 **5.0 秒**（含首帧训练分摊），Meeting Room 约为 **3.3 秒**。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流多视角动态数据集的 9 个不同场景上进行了全面测试。
*   **消融实验**：专门针对“量化感知”和“变化门控”两个核心组件进行了消融，验证了它们分别对降低运动载荷和控制模型增长的贡献。
*   **率失真分析**：通过调整量化步长（$\Delta t$）绘制了率失真曲线（Rate-Distortion Curve），寻找存储与质量的最佳平衡点。
*   **评价**：实验设计客观、公平，对比了最新的 SOTA 方法，并对部分关键对比方法（如 ReCon-GS）在相同环境下进行了复现，确保了结果的可比性。

### 6. 主要结论与发现
*   **极致压缩**：QuARC-GS 在保持与 SOTA 相当的渲染质量（PSNR 约 32dB）时，每帧存储仅需 **18-36 KB**，比 ReCon-GS 等方法降低了约 **11 倍**。
*   **模型稳定性**：变化门控稠密化成功抑制了高斯点数量的无序增长，使长序列流式传输更具可持续性。
*   **速度优势**：在大幅压缩的同时，保持了约 240-270 FPS 的实时渲染速度。

### 7. 优点（亮点）
*   **训练/传输一致性**：通过 STE 量化感知训练，解决了传统方法中“训练时全精度、传输时量化”导致的精度下降问题。
*   **零成本绑定**：锚点绑定基于确定性种子和几何结构，无需额外存储绑定关系。
*   **高效的冗余抑制**：结合了运动层面的量化和外观层面的门控，双管齐下实现紧凑表示。

### 8. 不足与局限
*   **首帧开销**：初始帧需要较长时间的预训练（15,000 次迭代），无法做到完全的“即开即看”。
*   **实时重建差距**：虽然每帧 3-5 秒的重建速度在学术界领先，但对于真正的超低延迟实时交互（如直播）仍有一定距离。
*   **量化步长敏感性**：量化步长需要手动调节以适应不同场景，缺乏一种完全自适应的比特分配机制。

（完）
