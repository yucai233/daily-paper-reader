---
title: "Luce: Relightable Gaussians for 3D Asset Generation"
title_zh: Luce：用于 3D 资产生成的可重光照高斯
authors: "Mayank Singh, Michele Stoppa, Alvise Memo, Rui Yu, Harsha Kalli, Srimanth Gunturi, Muhammad Ahmed Riaz, Behrooz Shahsavari, Waleed Abdulla, David E. Jacobs"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.23943v1"
tags: ["query:q2"]
score: 8.0
evidence: 用于3D资产生成的体素化多模态高斯云
tldr: Luce 是一种新型 3D 生成框架，旨在从单张图像生成可重光照的高质量 3D 资产。它通过体素化的多模态高斯云统一了几何与 PBR 材质（反照率、金属度-粗糙度、法线），并利用 VAE 压缩至潜在空间。结合整流流变换器和预训练图像编码器，Luce 能够生成保留精细细节（如文字、Logo）且符合物理渲染规律的 3D 模型，在 Toys4K 等基准测试中达到 SOTA 水平。
selection_source: fresh_fetch
motivation: 现有的图像转 3D 技术难以同时兼顾高质量几何、物理材质属性以及在标准渲染管线中的可重光照性。
method: 提出一种将几何与 PBR 材质统一在体素化多模态高斯云中的表示方法，并通过 VAE 压缩和整流流变换器实现从单图到潜在空间的生成。
result: "在 Toys4K 数据集上 FID 提升了 28%，并在 AI 生成图像基准测试中展现出更强的 CLIP 图像对齐能力和细节保留能力。"
conclusion: Luce 成功实现了从单图生成具有精确几何、忠实材质和可重光照特性的 3D 资产，显著提升了 3D 内容创作的质量和实用性。
---

## 摘要
高保真图像到 3D 生成需要一种能够同时捕捉几何形状和外观的 3D 表示。为了支持重光照并集成到标准渲染管线中，该表示应包含基于物理的渲染 (PBR) 模态，如反照率 (albedo)、金属度-粗糙度 (metallic-roughness) 和表面法线。我们提出了 Luce，这是一种在体素化多模态高斯云中统一了几何和 PBR 材质的 3D 表示，并为每种模态使用了专门的高斯基元。变分自编码器将该表示压缩到一个统一的材质感知潜空间中。整流流变换器 (rectified-flow transformer) 根据单张图像生成该潜变量，并以预训练图像编码器的多层特征为条件，这些特征保留了语义上下文和精细的空间细节。随后，该潜变量被解码为可重光照的 PBR 高斯，以及一个带有切线空间法线贴图的可选纹理网格。在 Toys4K 数据集上，Luce 实现了最先进的单图到 3D 生成效果，相比最强的基准模型，FID 提升了 28%。我们进一步引入了一个 AI 生成图像的基准测试，在该测试中，Luce 的 CLIP 图像对齐得分优于最佳基准模型（0.8519 对比 0.8299）。Luce 生成的资产具有可重光照性、几何准确性和材质忠实性，并保留了文本、徽标和铭文等精细细节。

## Abstract
High-fidelity image-to-3D generation requires a 3D representation that captures both geometry and appearance. To support relighting and integration into standard rendering pipelines, the representation should include physically based rendering (PBR) modalities such as albedo, metallic-roughness, and surface normals. We propose Luce, a 3D representation that unifies geometry and PBR materials within a voxelized multimodal Gaussian cloud, using dedicated Gaussian primitives for each modality. A variational autoencoder compresses this representation into a unified material-aware latent space. A rectified-flow transformer generates this latent from a single image, conditioned on multi-layer features from a pretrained image encoder that preserve both semantic context and fine spatial detail. The latent then decodes into relightable PBR Gaussians and an optional textured mesh with a tangent-space normal map. On Toys4K, Luce achieves state-of-the-art single-image-to-3D generation, improving FID by 28% over the strongest baseline. We further introduce a benchmark of AI-generated images, on which Luce improves the CLIP image-alignment score over the best baseline (0.8519 vs. 0.8299). Luce generates relightable, geometrically accurate, and materially faithful assets that preserve fine details such as text, logos, and inscriptions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Luce: Relightable Gaussians for 3D Asset Generation》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的“图像转3D”技术在生成高质量几何形状的同时，难以准确解耦材质属性（如反照率、金属度、粗糙度），导致生成的模型往往带有“烘焙”在纹理中的固定光照，无法在标准渲染管线中实现真实的**重光照（Relighting）**。
*   **研究背景**：高质量3D资产需要符合物理基于渲染（PBR）的标准。此外，现有模型在保留单图中精细的空间细节（如文字、Logo、微小刻痕）方面表现欠缺，难以满足专业内容创作的需求。

### 2. 方法论
Luce 提出了一套将几何与材质统一的生成框架，核心技术包括：
*   **体素化多模态高斯云（Voxelized Multimodal Gaussian Cloud）**：这是一种新型3D表示方法，将几何信息与PBR材质（Albedo, Metallic, Roughness, Normals）统一在体素网格中。每种模态使用专门的高斯基元（Gaussian Primitives）来表达。
*   **材质感知潜空间压缩（VAE）**：利用变分自编码器（VAE）将高维的3D高斯表示压缩到一个紧凑的、具有材质感知能力的潜空间中，便于后续生成任务。
*   **整流流变换器（Rectified-flow Transformer）**：生成模型的核心。它以单张图像为输入，通过整流流（Rectified Flow）技术在潜空间中生成对应的3D表示。
*   **多层特征条件（Multi-layer Conditioning）**：为了保留精细细节，模型不仅使用图像的全局语义特征，还引入了预训练图像编码器（如 DINOv2/CLIP）的多层中间特征作为条件，确保生成的3D模型在空间细节上与原图高度对齐。
*   **输出转换**：生成的潜变量可解码为可重光照的PBR高斯点云，并能进一步提取为带有切线空间法线贴图的标准纹理网格（Mesh）。

### 3. 实验设计
*   **数据集**：
    *   **Toys4K**：用于评估几何与外观生成的标准大规模3D数据集。
    *   **AI生成图像基准（New Benchmark）**：论文专门引入了一个由AI生成的图像组成的测试集，用以评估模型处理多样化、高细节输入的能力。
*   **对比方法**：与当前最先进的（SOTA）单图转3D模型进行对比（如 InstantMesh, TripoSR 等主流基准）。
*   **评估指标**：
    *   **FID (Fréchet Inception Distance)**：衡量生成结果的视觉质量。
    *   **CLIP Score**：衡量生成的3D资产与原始输入图像的语义一致性。
    *   **定性分析**：重点展示了对文字、徽标（Logo）和复杂材质的还原效果。

### 4. 资源与算力
*   **算力说明**：论文摘要及元数据中**未明确说明**具体的 GPU 型号、数量及训练总时长。通常此类基于 Transformer 和大规模 3D 数据集的训练需要高性能计算集群（如 A100 或 H100 阵列）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 Toys4K 这一公认的大型数据集上进行了详尽的定量测试，并额外构建了 AI 图像基准，实验覆盖了从标准物体到复杂生成图像的多种场景。
*   **充分性与客观性**：通过 FID 提升 28% 这一显著数据证明了其性能跨越。引入 CLIP 图像对齐得分和消融实验（针对多层特征条件等）增强了结果的说服力，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：Luce 在 Toys4K 数据集上的 FID 指标比最强基准模型提升了 **28%**。
*   **细节保留**：该方法能极好地保留输入图像中的精细空间细节，如铭文、复杂的 Logo 和微小几何特征。
*   **材质忠实度**：生成的资产具有准确的 PBR 属性，能够无缝集成到标准渲染引擎中进行动态重光照，且材质表现符合物理规律。
*   **对齐能力**：在 AI 生成图像测试中，Luce 的 CLIP 对齐得分（0.8519）显著优于基准模型（0.8299）。

### 7. 优点
*   **可重光照性**：解决了 3D 生成中光照耦合的痛点，生成的 PBR 材质非常实用。
*   **高保真度**：通过多层特征注入，解决了 3D 生成中常见的“细节模糊”问题，尤其是文字还原能力惊人。
*   **架构优雅**：结合了高斯泼溅（Gaussian Splatting）的渲染优势与整流流变换器的生成能力。

### 8. 不足与局限
*   **遮挡区域幻觉**：虽然正面细节丰富，但单图转 3D 固有的挑战——推断背部或被遮挡区域的材质和几何——仍可能存在不确定性或幻觉。
*   **计算开销**：体素化高斯云和 Transformer 的组合可能导致推理成本高于一些轻量级的快速生成模型。
*   **应用限制**：目前主要针对孤立资产（Asset）生成，对于复杂的大规模场景生成尚待验证。

（完）
