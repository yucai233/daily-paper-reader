---
title: "DeGS: A Scalable 3DGS Architecture via Decoupled Workload Parsing and Reorganization"
title_zh: DeGS：通过解耦工作负载解析与重组实现的可扩展 3DGS 架构
authors: "Minnan Pei, Gang Li, Zeyu Zhu, Siting Wang, Junwen Si, Zhuoran Song, Yu Feng, Fangxin Liu, Xiaoyao Liang, Jian Cheng"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02099v1"
tags: ["query:q7"]
score: 8.0
evidence: 用于高效3D高斯泼溅推理的可扩展架构
tldr: "针对3D高斯泼溅（3DGS）加速器在并行扩展时处理单元（PE）利用率低的问题，本文提出了DeGS架构。该架构通过解耦传统的“检查-混合”数据流，将其重构为工作负载解析、重组和混合三个阶段，有效消除了空间和时间冗余。在28nm工艺下，DeGS在吞吐量、能效和扩展性方面显著优于现有SOTA加速器，即使在1024个PE的大规模扩展下仍能保持80%以上的利用率。"
selection_source: fresh_fetch
motivation: 现有3DGS加速器因耦合的渲染数据流导致PE在处理不规则覆盖和异步终止时利用率低下，限制了硬件的并行扩展能力。
method: 提出一种解耦数据流架构，将渲染过程重构为连续的工作负载解析、重组和混合阶段，从而在混合前生成紧凑且无冲突的任务。
result: 实验表明，DeGS在多种分辨率下比现有SOTA加速器实现了最高7.25倍的吞吐量提升和6.02倍的端到端加速。
conclusion: DeGS通过解耦和重组工作负载，成功解决了3DGS渲染中的冗余与依赖问题，为高性能可扩展的3D渲染硬件设计提供了新思路。
---

## 摘要
3D 高斯泼溅 (3DGS) 已成为实时新视角合成的领先技术，然而现有的 3DGS 加速器面临架构可扩展性差的问题：在渲染过程中增加处理单元 (PE) 的数量仅能带来微小的性能提升。我们发现其根本原因在于紧耦合的“边检查边混合”数据流，这加剧了由不规则高斯覆盖带来的空间冗余以及并行执行下异步像素级终止带来的时间冗余所导致的 PE 利用率不足。为解决这一问题，我们提出了 DeGS，一种用于高效 3DGS 推理的可扩展架构。为了系统地消除渲染中固有的冗余，DeGS 采用了解耦的数据流，将标准渲染过程中耦合的 α 检查、透射率检查和 α 混合重构为连续的工作负载解析、重组和混合阶段。这使得碎片化、长度可变且具有时间依赖性的工作负载在混合之前能够被重组为紧凑、无冲突且密集的工作负载，从而显著提高并行混合期间的 PE 利用率。在 28 nm 工艺下实现，DeGS 在不同场景和分辨率（720p 到 8K）下，相比最先进的 3DGS 加速器（GSCore、GBU、GCC），实现了 2.36 倍至 7.25 倍的吞吐量、1.82 倍至 6.02 倍的端到端加速以及 1.59 倍至 4.42 倍的能效提升。此外，从 16 个 PE 扩展到 1024 个 PE，DeGS 在高分辨率下仍能保持 80% 以上的 PE 利用率，显著优于现有加速器。

## Abstract
3D Gaussian Splatting (3DGS) has emerged as a leading technique for real-time novel view synthesis, yet existing 3DGS accelerators suffer from poor architectural scalability: increasing the number of PEs leads to marginal performance improvement during rendering. We identify that the root cause is the tightly coupled ``checking-while-blending'' dataflow, which exacerbates PE underutilization caused by spatial redundancy from irregular Gaussian coverage and temporal redundancy from asynchronous pixel-wise termination under parallel execution.   To address this issue, we propose DeGS, a scalable architecture for efficient 3DGS inference. To systematically eliminate the redundancies inherent in rendering, DeGS exploits a decoupled dataflow, restructuring the coupled $α$-checking, transmittance checking, and $α$-blending of the standard rendering process into consecutive workload parsing, reorganization, and blending stages. This allows the fragmented, length-variable, and temporal-dependent workloads to be reorganized into compact, conflict-free, and dense workloads prior to blending, thereby significantly improving PE utilization during parallel blending. Implemented in 28 nm technology, DeGS achieves 2.36$\times$--7.25$\times$ throughput, 1.82$\times$--6.02$\times$ end-to-end speedup, and 1.59$\times$--4.42$\times$ energy efficiency over state-of-the-art 3DGS accelerators (GSCore, GBU, GCC) across diverse scenes and resolutions (720p to 8K). Moreover, scaling from 16 to 1024 PEs, DeGS maintains over 80\% PE utilization at high resolutions, significantly outperforming existing accelerators.

---

## 论文详细总结（自动生成）

这是一份关于论文《DeGS: A Scalable 3DGS Architecture via Decoupled Workload Parsing and Reorganization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 高斯泼溅（3DGS）硬件加速器在增加处理单元（PE）数量时，性能提升边际递减，即**架构可扩展性差**。
*   **研究动机**：作者发现根源在于传统的“边检查边混合”（checking-while-blending）耦合数据流。这种模式导致了两种冗余：
    *   **空间冗余**：高斯球投影形状不规则，导致大量 PE 在处理包围盒内的无效像素。
    *   **时间冗余**：不同像素的 α 混合终止时间不同，并行执行时先完成的 PE 必须等待长尾像素，导致资源闲置。
*   **目标**：设计一种能够消除这些冗余并支持大规模 PE 扩展的高效渲染架构。

### 2. 方法论与关键技术
DeGS 的核心思想是将渲染过程**解耦**为三个独立阶段，将冗余识别从计算阵列中剥离：
*   **跨度解析（Span Parsing）**：
    *   利用解析几何方法，将 2D 高斯椭圆的有效区域转化为 1D 扫描线跨度（Spans）。
    *   通过预计算几何参数，直接计算每一行有效像素的起点和长度，避免了对无效像素的逐点探测。
*   **任务重组（Task Reorganization）**：
    *   将变长的跨度划分为固定长度的段（Segments）。
    *   **冲突感知调度**：通过 Scoreboard 机制管理像素更新依赖，确保深度顺序的同时，允许不同冲突域的任务乱序发射，填补时间空隙。
    *   **任务打包**：将碎片化的有效任务压缩成密集的固定宽度数据包。
*   **混合执行（Blending Execution）**：
    *   后端 PE 阵列仅接收经过验证的、无冲突的密集数据包。
    *   使用闭式解（Closed-form）重建二次项值，配合查找表（LUT）进行 α 计算和颜色混合，极大提高了 PE 利用率。

### 3. 实验设计
*   **数据集与场景**：选取了 Tanks & Temples、Deep Blending 和 Mip-NeRF 360 等标准基准测试集，涵盖室内外多种复杂场景。
*   **分辨率范围**：从 720p 扩展至 8K，以测试不同负载下的扩展性。
*   **对比方法（Benchmark）**：
    *   **GSCore**：最早的专用 3DGS 加速器（类 GPU 瓦片流）。
    *   **GBU**：基于行扫描的边缘 GPU 插件。
    *   **GCC**：基于高斯维度和跨阶段组织的架构。
    *   **ORANGE**：当代基于 NPU（GEMM 友好型）的渲染基准。

### 4. 资源与算力说明
*   **硬件实现**：采用 **TSMC 28nm** 工艺节点，目标频率 **1.0 GHz**。
*   **算力配置**：评估了从 **16 PE 到 1024 PE** 的不同设计规模。
*   **存储**：片上 SRAM 预算约为 130 KB（包括高斯缓冲区、像素缓冲区等）。
*   **训练/参考生成**：使用 **NVIDIA RTX 3090 GPU** 运行原始 3DGS 管道（30K 迭代）生成参考图像，用于质量对比。
*   **仿真**：开发了周期精确（Cycle-accurate）的硬件模拟器，建模了 LPDDR5 内存带宽（6400 MT/s）。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度评估，包括端到端延迟、稳态吞吐量、能效比、PE 利用率分析。
*   **消融实验**：分别验证了跨度解析（SP）和任务重组（TR）对性能和功耗的贡献。
*   **敏感性分析**：针对内存带宽、任务库分区数量、段长度、瓦片（Tile）大小进行了详尽的参数化测试。
*   **动态场景测试**：额外在 4DGS（动态高斯）负载上验证了架构的通用性。
*   **评价**：实验设计非常全面，覆盖了从低端边缘设备到高性能渲染服务器的潜在应用场景，对比公平且客观。

### 6. 主要结论与发现
*   **性能飞跃**：DeGS 实现了 2.36×–7.25× 的吞吐量提升和 1.82×–6.02× 的端到端加速。
*   **高扩展性**：在 1024 PE 的大规模配置下，DeGS 在高分辨率渲染中仍能保持 **80% 以上** 的 PE 利用率，而传统架构会跌至 10%–30%。
*   **能效优势**：相比 SOTA 加速器，能效提升了 1.59×–4.42×。
*   **质量保持**：通过混合精度支持，DeGS 的渲染质量（PSNR/SSIM/LPIPS）与 GPU 参考值几乎一致，无视觉可见伪影。

### 7. 优点与亮点
*   **架构创新**：首次系统性地提出了通过“解耦”来解决 3DGS 渲染中的空间和时间冗余问题。
*   **数学驱动**：利用椭圆解析几何优化硬件光栅化路径，比暴力探测更科学。
*   **调度优化**：任务重组机制有效解决了并行渲染中的 Read-Modify-Write 依赖冲突，这是提升 PE 利用率的关键。
*   **适应性强**：能够良好支持从 720p 到 8K 的分辨率跨度，且对动态场景（4DGS）同样有效。

### 8. 不足与局限
*   **硬件复杂度**：引入了复杂的 Packing Scheduler 和 Span Engine，虽然面积增加可控，但增加了逻辑设计的复杂度。
*   **数值精度风险**：虽然实验证明误差极小，但在极端长尾的高斯分布或超高精度需求下，LUT 和定点数运算可能存在微小偏差。
*   **前端瓶颈**：在极低分辨率且 PE 数量极多时，前端的跨度生成速度可能成为新的瓶颈（尽管这种情况在实际应用中较少见）。

（完）
