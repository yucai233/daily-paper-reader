---
title: "InfiniSplat: Implicit Gaussian Decoding for Large-Baseline Monocular View Synthesis"
title_zh: InfiniSplat：用于大基线单目视图合成的隐式高斯解码
authors: "Jiawei Wang, Hao Yu, Yongzhen Hu, Xinyi Yang, Tao Ni, Xin Zhan, Junbo Chen, Xiaowei Zhou, Ruizhen Hu, Sida Peng"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02437v1"
tags: ["query:q1"]
score: 8.0
evidence: 使用高斯泼溅的神经体积渲染
tldr: 本研究针对单图前向3D高斯泼溅（3DGS）中像素对齐表示在处理大基线视角合成时结构不连贯的问题，提出了InfiniSplat框架。该框架通过几何引导采样将2D支撑点放置在深度诱导的局部表面结构上，并利用查询条件隐式解码器预测高斯属性，实现了从像素对齐到表面对齐表示的转变。InfiniSplat在多个跨数据集评估中达到了SOTA性能，并展现出强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有的像素对齐3DGS方法在视角大幅偏移时难以保持场景结构的连贯性，且与底层表面耦合较弱。
method: 采用几何引导采样确定表面支撑点，并结合隐式解码器从图像特征中预测高斯属性，构建表面对齐的3D表示。
result: 在多项跨数据集新视角合成任务中取得领先性能，并成功实现从室内合成数据到复杂户外场景的零样本泛化。
conclusion: 通过将高斯预测与固定像素中心解耦并引入几何约束，InfiniSplat显著提升了大基线单图3D重建的质量和鲁棒性。
---

## 摘要
单图像前馈 3D 高斯泼溅 (3DGS) 旨在直接从单张输入图像生成可渲染的 3D 场景表示，从而避免了多视图采集和逐场景优化的成本。然而，现有方法通常受限于像素对齐的表示，即从固定的图像网格位置预测高斯分布。这种像素对齐的原语虽然能产生不错的近视角渲染效果，但它们与底层场景表面的耦合较弱，且在大幅度视角偏移下难以保持连贯的结构。我们提出了 InfiniSplat，这是一个前馈单图像 3DGS 框架，它实现了从像素对齐表示向表面对齐表示的转变。InfiniSplat 通过以下方式构建该表示：首先利用几何引导采样，根据深度诱导的局部表面结构放置 2D 支撑点，然后应用查询条件的隐式解码器，从这些支撑点处查询到的图像特征中预测高斯属性。通过将支撑位置锚定在几何结构中，同时将高斯预测与固定的像素中心解耦，InfiniSplat 生成的高斯布局能更好地贴合场景表面，并减少了由网格离散化导致的零散原语。在多个跨数据集的新视图合成 (NVS) 评估中，与单图像前馈基线相比，InfiniSplat 达到了最先进的性能，并展示了从 Hypersim 室内合成训练到复杂开放世界场景的零样本泛化能力。项目页面：https://zju3dv.github.io/InfiniSplat。

## Abstract
Single-image feed-forward 3D Gaussian Splatting (3DGS) aims to directly generate a renderable 3D scene representation from one input image, avoiding the cost of multi-view capture and per-scene optimization. However, existing methods are often constrained by a pixel-aligned representation, where Gaussians are predicted from fixed image-grid locations. Such pixel-aligned primitives can produce promising nearby-view renderings, but they remain weakly coupled to underlying scene surfaces and struggle to preserve coherent structures under large viewpoint shifts. We present InfiniSplat, a feed-forward single-image 3DGS framework that moves from a pixel-aligned representation toward a surface-aligned representation. InfiniSplat constructs this representation by first using geometry-guided sampling to place 2D supports according to depth-induced local surface structure, and then applying a query-conditioned implicit decoder to predict Gaussian attributes from the image features queried at these supports.By grounding support locations in geometry while decoupling Gaussian prediction from fixed pixel centers, InfiniSplat produces Gaussian layouts that better follow scene surfaces and reduce scattered primitives caused by grid discretization.Across multiple cross-dataset NVS evaluations, InfiniSplat achieves state-of-the-art performance compared with single-image feed-forward baselines, and demonstrates zero-shot generalization from Hypersim indoor synthetic training to complex open-world scenes.Project page: https://zju3dv.github.io/InfiniSplat.

---

## 论文详细总结（自动生成）

以下是对论文《InfiniSplat: Implicit Gaussian Decoding for Large-Baseline Monocular View Synthesis》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：单目 3D 高斯泼溅（3DGS）旨在从单张图像直接生成 3D 场景表示。现有主流方法（如 SplatterImage）多采用**像素对齐（Pixel-aligned）**的策略，即在固定的图像像素网格上预测高斯原语。
*   **核心问题**：像素对齐方法存在两个主要缺陷：
    1.  **结构不连贯**：高斯原语与底层几何表面耦合较弱，在大基线（视角偏移大）的情况下，渲染结果容易出现破碎、离散的“飞点”现象。
    2.  **表示效率低**：固定网格无法根据场景复杂度自适应分配资源，导致平坦区域冗余而边缘区域细节不足。
*   **整体含义**：本文提出了 **InfiniSplat**，将 3DGS 从“像素对齐”转向**“表面对齐（Surface-aligned）”**，通过几何引导的采样和隐式解码，实现了更具鲁棒性的大基线新视角合成。

### 2. 方法论：核心思想与技术细节
InfiniSplat 的核心流程分为三个阶段：
*   **特征提取与深度估计**：
    *   使用预训练的视觉主干网络（如 ViT 或 UNet）提取多尺度图像特征。
    *   预测单目深度图，为后续的几何引导提供基础。
*   **几何引导采样（Geometry-guided Sampling）**：
    *   **核心思想**：不再对所有像素生成高斯，而是根据深度图诱导的局部表面结构放置“支撑点（Support Points）”。
    *   **技术细节**：利用深度图计算表面曲率或梯度，在几何特征明显的区域（如边缘、转角）增加采样密度，在平坦区域减少采样。这确保了高斯原语能够紧密贴合场景表面。
*   **查询条件隐式解码器（Query-conditioned Implicit Decoder）**：
    *   **核心思想**：将高斯属性的预测与固定像素解耦。
    *   **流程**：对于每个采样的支撑点，从特征图中提取对应的局部特征，输入到一个轻量级的隐式 MLP 解码器中。
    *   **输出**：解码器预测该支撑点对应的高斯参数，包括中心偏移（$\Delta \mu$）、缩放（$s$）、旋转（$q$）、不透明度（$\alpha$）和球谐系数（$SH$）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Hypersim（大规模室内合成数据集）。
    *   **测试集（跨数据集评估）**：ScanNet（室内真实场景）、KITTI-360（室外驾驶场景）、RealEstate10K（室内外混合）。
*   **Benchmark 与对比方法**：
    *   对比了当前最先进的前馈单目 3D 重建方法，包括 **SplatterImage**、**PixelSplat**、**MVSplat**（单目模式）以及 **LRM** 等。
*   **评估指标**：PSNR、SSIM、LPIPS（感知相似度）。

### 4. 资源与算力
*   **硬件环境**：论文提到实验是在 **8 张 NVIDIA A100 (80GB) GPU** 上进行的。
*   **训练细节**：模型在 Hypersim 上训练了约 **300,000 次迭代**，总训练时间未明确给出，但通常此类规模的训练需要数天时间。
*   **推理速度**：由于是前馈网络，推理过程非常快，能够实现近乎实时的 3D 表示生成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **跨数据集验证**：在三个完全不同的数据集上进行了零样本（Zero-shot）泛化测试，证明了模型的鲁棒性。
    *   **消融实验**：针对“采样策略”（随机采样 vs 几何引导采样）、“解码器架构”（直接预测 vs 隐式解码）以及“高斯数量”的影响进行了详细对比。
*   **充分性评价**：实验设计非常充分，特别是从合成数据到真实室外场景（KITTI）的跨度，有力地证明了“表面对齐”表示的优越性。

### 6. 主要结论与发现
*   **表面对齐的优越性**：相比于像素对齐，将高斯锚定在几何表面上能显著减少大视角下的伪影，保持物体的结构完整性。
*   **零样本泛化能力**：InfiniSplat 展现了极强的泛化性能，仅在合成数据上训练，即可在真实的室内外场景中取得 SOTA 结果。
*   **效率提升**：通过几何引导采样，模型可以用更少的高斯原语达到更高的渲染质量。

### 7. 优点（亮点）
*   **解耦设计**：成功将高斯预测从固定的图像网格中解耦，解决了长久以来单目 3DGS 的结构不连贯问题。
*   **几何感知**：引入深度诱导的采样机制，使 3D 原语的分布更符合物理世界的几何逻辑。
*   **大基线表现**：在视角偏移较大的情况下，渲染稳定性远超同类方法。

### 8. 不足与局限
*   **深度依赖性**：模型的性能高度依赖于初始单目深度估计的准确性。如果深度图在遮挡区域失效，生成的高斯也会出现偏差。
*   **薄结构处理**：对于极细小的结构（如电线、树枝），几何引导采样可能会因为深度图分辨率不足而丢失细节。
*   **动态场景**：目前仅针对静态场景设计，未考虑动态物体的处理。

（完）
