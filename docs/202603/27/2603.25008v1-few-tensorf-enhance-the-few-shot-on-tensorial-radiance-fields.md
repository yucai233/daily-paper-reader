---
title: "Few TensoRF: Enhance the Few-shot on Tensorial Radiance Fields"
title_zh: Few TensoRF：增强张量辐射场的少样本性能
authors: "Thanh-Hai Le, Hoang-Hau Tran, Trong-Nghia Vu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25008v1"
tags: ["query:q8"]
score: 8.0
evidence: 使用张量辐射场加速渲染速度
tldr: Few TensoRF 结合了 TensoRF 的高效张量表示与 FreeNeRF 的频率驱动正则化，旨在解决少样本视角下的 3D 重建难题。通过引入频率掩码和遮挡掩码，该方法在保持 10-15 分钟快速训练的同时，显著提升了稀疏输入下的重建质量和稳定性。在 Synthesis NeRF 和 THuman 2.0 数据集上的实验证明，其在 PSNR 指标上优于原始 TensoRF，是兼顾效率与效果的少样本 3D 重建方案。
selection_source: fresh_fetch
motivation: 针对 TensoRF 在少样本输入下重建质量不稳定且易产生伪影的问题，寻求一种高效且稳健的正则化改进方案。
method: 将 TensoRF 的张量分解表示与 FreeNeRF 的频率掩码及遮挡掩码技术相结合，以约束稀疏视角下的辐射场学习。
result: 在 Synthesis NeRF 基准上将平均 PSNR 从 21.45 dB 提升至最高 24.52 dB，且在人体重建任务中仅需 8 张图像即可获得高质量结果。
conclusion: Few TensoRF 成功实现了在极短训练时间内完成高质量的少样本 3D 重建，展现了其在实时应用中的巨大潜力。
---

## 摘要
本文提出了 Few TensoRF，这是一个结合了 TensoRF 高效的基于张量的表示与 FreeNeRF 频率驱动的少样本正则化的三维重建框架。该方法利用 TensoRF 显著加快了渲染速度，并通过引入频率掩码和遮挡掩码，提高了在稀疏输入视角下的稳定性和重建质量。在 Synthesis NeRF 基准测试上的实验表明，Few TensoRF 方法将平均峰值信噪比（PSNR）从 21.45 dB（TensoRF）提升至 23.70 dB，微调版本更是达到了 24.52 dB，同时保持了 TensoRF 约 10-15 分钟的快速训练时间。在 THuman 2.0 数据集上的实验进一步证明了其在人体重建中具有竞争力的性能，仅需 8 张输入图像即可达到 27.37 - 34.00 dB。这些结果凸显了 Few TensoRF 是一个针对多种场景的实时三维重建的高效且数据有效的解决方案。

## Abstract
This paper presents Few TensoRF, a 3D reconstruction framework that combines TensorRF's efficient tensor based representation with FreeNeRF's frequency driven few shot regularization. Using TensorRF to significantly accelerate rendering speed and introducing frequency and occlusion masks, the method improves stability and reconstruction quality under sparse input views. Experiments on the Synthesis NeRF benchmark show that Few TensoRF method improves the average PSNR from 21.45 dB (TensorRF) to 23.70 dB, with the fine tuned version reaching 24.52 dB, while maintaining TensorRF's fast \(\approx10-15\) minute training time. Experiments on the THuman 2.0 dataset further demonstrate competitive performance in human body reconstruction, achieving 27.37 - 34.00 dB with only eight input images. These results highlight Few TensoRF as an efficient and data effective solution for real-time 3D reconstruction across diverse scenes.

---

## 论文详细总结（自动生成）

以下是对论文《Few TensoRF: Enhance the Few-shot on Tensorial Radiance Fields》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的神经辐射场（NeRF）在处理少样本（Few-shot）输入时表现不佳，容易产生高频伪影且训练耗时。
*   **核心问题**：现有的改进方案中，TensoRF 虽然通过张量分解极大地提升了训练速度，但在稀疏视角下极不稳定；而 FreeNeRF 虽然通过频率正则化解决了少样本问题，但计算效率较低。
*   **整体含义**：本文旨在结合 TensoRF 的**高效张量表示**与 FreeNeRF 的**频率驱动正则化技术**，开发一种既能快速训练，又能在极少数输入图像（如 3-9 张）下生成高质量 3D 重建结果的框架。

### 2. 论文提出的方法论
Few TensoRF 在 TensoRF 的基础上引入了三项核心改进：
*   **张量组件的频率掩码（Frequency Masking Tensor Components）**：针对密度张量（$A$）和外观张量（$A_c$）应用动态频率掩码。在训练初期屏蔽高频信号，引导模型先学习低频结构，随着迭代增加逐渐引入高频细节，从而避免过快收敛导致的伪影。
*   **外观网格与视角的频率掩码**：对外观网格 $G_c$ 和视角方向 $d$ 的位置编码进行过滤，减轻 MLP 在处理高频信号时的过拟合风险。
*   **遮挡正则化（Occlusion Regularization）**：引入遮挡损失，抑制相机附近的“浮空物”（floaters）密度，强制模型在更远、更合理的距离解释场景几何。
*   **核心架构**：沿用 TensoRF 的向量-矩阵（VM）分解技术，将 4D 张量分解为低秩组件，以降低内存占用并加速计算。

### 3. 实验设计
*   **数据集**：
    *   **Synthesis NeRF**：包含 Lego、Chair、Drums 等 8 个标准合成场景，用于评估少样本性能。
    *   **THuman 2.0**：人体扫描数据集，用于测试复杂人体结构的重建能力。
*   **基准（Benchmark）**：主要使用 **PSNR**（峰值信噪比）作为图像质量评价指标。
*   **对比方法**：
    *   **FreeNeRF**（少样本 SOTA 方法，分别测试了 1.5 万次和 5 万次迭代的版本）。
    *   **TensoRF**（原始张量辐射场方法）。
    *   **Few TensoRF (Raw & Fine-tuned)**：本文提出的方法及其微调版本。

### 4. 资源与算力
*   **硬件平台**：Google Colab 环境，配备 **NVIDIA T4 Tesla GPU**。
*   **训练参数**：
    *   迭代次数：15,000 次。
    *   光线批大小（Ray batch size）：1024。
    *   优化器：Adam。
*   **训练时长**：Few TensoRF 仅需 **10-15 分钟** 即可完成训练，而 FreeNeRF（5 万次迭代）需要约 4 小时 55 分钟。

### 5. 实验数量与充分性
*   **实验规模**：在 Synthesis NeRF 的 8 个场景上进行了详尽对比，并在 THuman 2.0 数据集的 2 个特定对象（0300 和 0525）上进行了人体实验。
*   **充分性评价**：
    *   **客观性**：通过统一迭代次数（1.5 万次）公平对比了不同方法的效率与质量。
    *   **局限性**：人体实验仅覆盖了 500 个对象中的 2 个，样本量较小，不足以构成全面的人体基准测试。
    *   **消融/变体**：对比了原始版本与微调版本，证明了频率掩码比例对结果的影响。

### 6. 论文的主要结论与发现
*   **性能提升**：在 Synthesis NeRF 上，Few TensoRF 将平均 PSNR 从 TensoRF 的 21.45 dB 提升至 **24.52 dB**（微调版）。
*   **效率优势**：在保持与 TensoRF 相当的极速训练时间（约 15 分钟）的同时，图像质量接近或超过了训练数小时的 FreeNeRF。
*   **少样本能力**：在 THuman 2.0 数据集上，仅使用 **8 张图像** 即可实现 27.37-34.00 dB 的重建效果，证明了其在稀疏数据下的稳健性。

### 7. 优点
*   **高效平衡**：成功解决了“训练速度”与“少样本质量”之间的矛盾。
*   **即插即用**：频率掩码和遮挡正则化逻辑简单，易于集成到现有的张量分解框架中。
*   **多场景适用**：不仅在刚性物体（Blender 数据集）上表现良好，在复杂的人体几何重建上也展现了潜力。

### 8. 不足与局限
*   **特定场景失效**：在“Drums”（鼓）场景中表现欠佳，推测是由于该场景细节过于复杂且存在大量遮挡，模型难以处理。
*   **人体噪声问题**：虽然在 THuman 2.0 上 PSNR 较高，但生成的 3D 网格在视觉上仍存在明显的噪声，且人体实验的覆盖范围有限。
*   **超参数敏感性**：频率掩码的可见比例（visible ratio）等参数可能需要针对不同场景进行手动微调才能达到最优效果。

（完）
