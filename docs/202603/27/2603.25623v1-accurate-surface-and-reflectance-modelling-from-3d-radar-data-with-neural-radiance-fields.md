---
title: Accurate Surface and Reflectance Modelling from 3D Radar Data with Neural Radiance Fields
title_zh: 基于神经辐射场的 3D 雷达数据精确表面与反射建模
authors: "Judith Treffler, Vladimír Kubelka, Henrik Andreasson, Martin Magnusson"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25623v1"
tags: ["query:q6"]
score: 8.0
evidence: 针对雾、烟等低能见度环境的神经辐射场
tldr: 针对自动驾驶在恶劣天气下感知受限的问题，本文提出一种基于神经辐射场（NeRF）的3D雷达建模方法。该方法利用混合特征编码学习连续的有符号距离函数（SDF），同时建模场景几何与视点相关的雷达反射强度。通过处理稀疏且多噪的雷达点云，实现了比传统方法更精确、平滑的3D表面重建，在低能见度环境下具有显著优势。
selection_source: fresh_fetch
motivation: 旨在解决雷达数据因稀疏和噪声导致难以进行可靠3D表面重建的问题，以提升自动驾驶系统在恶劣环境下的鲁棒性。
method: 提出一种神经隐式方法，结合混合特征编码学习连续SDF，并同步建模视点相关的雷达反射特性。
result: 实验表明该方法在雷达数据上的重建精度和表面平滑度优于现有的激光雷达重建算法，且能有效处理极稀疏的点云。
conclusion: 神经隐式表示在处理稀疏雷达数据方面比传统显式SDF和网格化技术更具优势，为复杂环境下的3D映射提供了新方案。
---

## 摘要
鲁棒的场景表示对于自主系统在具有挑战性的低能见度环境中安全运行至关重要。在这些条件下，雷达由于对雾、烟或灰尘等环境因素具有韧性，相比摄像头和激光雷达具有明显优势。然而，雷达数据本质上是稀疏且多噪声的，这使得可靠的 3D 表面重建具有挑战性。为了应对这些挑战，我们提出了一种基于雷达点云进行 3D 映射的神经隐式方法，该方法联合建模了场景几何和视点相关的雷达强度。我们的方法利用内存高效的混合特征编码来学习用于表面重建的连续有符号距离场 (SDF)，同时捕捉雷达特有的反射特性。我们证明，与应用于雷达数据的现有基于激光雷达的重建方法相比，我们的方法能产生更平滑、更准确的 3D 表面重建，并且能够重建视点相关的雷达强度。我们还表明，通常情况下，随着输入点云变得更加稀疏，与传统的显式 SDF 和网格划分技术相比，神经隐式表示能够渲染出更真实的表面。

## Abstract
Robust scene representation is essential for autonomous systems to safely operate in challenging low-visibility environments. Radar has a clear advantage over cameras and lidars in these conditions due to its resilience to environmental factors such as fog, smoke, or dust. However, radar data is inherently sparse and noisy, making reliable 3D surface reconstruction challenging. To address these challenges, we propose a neural implicit approach for 3D mapping from radar point clouds, which jointly models scene geometry and view-dependent radar intensities. Our method leverages a memory-efficient hybrid feature encoding to learn a continuous Signed Distance Field (SDF) for surface reconstruction, while also capturing radar-specific reflective properties. We show that our approach produces smoother, more accurate 3D surface reconstructions compared to existing lidar-based reconstruction methods applied to radar data, and can reconstruct view-dependent radar intensities. We also show that in general, as input point clouds get sparser, neural implicit representations render more faithful surfaces, compared to traditional explicit SDFs and meshing techniques.

---

## 论文详细总结（自动生成）

以下是对论文《Accurate Surface and Reflectance Modelling from 3D Radar Data with Neural Radiance Fields》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶和自主系统领域，传统的摄像头和激光雷达（LiDAR）在雾、烟、尘等低能见度环境下表现受限。雷达（Radar）虽能穿透这些介质，但其生成的3D点云具有**极高的噪声（如多路径反射）和稀疏性**，导致难以进行精确的3D表面重建。
本文的核心问题是：**如何利用神经隐式表示（Neural Implicit Representations）从稀疏且多噪的3D雷达数据中，同时恢复精确的场景几何表面和视点相关的反射强度（Intensity）？** 这对于提升机器人在恶劣环境下的定位与场景理解能力具有重要意义。

### 2. 论文提出的方法论：3QFPI
论文提出了一种名为 **3QFPI**（3QFP with Intensities）的框架，其核心思想是将场景建模为连续的有符号距离场（SDF）和强度场。

*   **核心架构**：
    *   **SDF网络**：基于3QFP架构，利用**三四叉树（Tri-Quadtree）**和**傅里叶特征位置编码（Fourier Feature Positional Encoding）**的混合特征编码。这种结构内存效率高，能学习连续的SDF值，用于提取平滑的表面网格。
    *   **强度网络（Intensity Network）**：受NeuS2启发，该网络预测给定3D点在特定观测方向下的雷达回波强度。它以SDF网络的几何特征、表面法线、观测方向（经球谐函数编码）和位置信息作为输入。
*   **关键技术细节**：
    *   **雷达方程建模**：强度网络隐式地学习了雷达方程中的参数（如发射功率、增益、雷达散射截面σ），从而能够处理视点相关的反射特性。
    *   **采样策略**：在雷达点附近和传感器与点之间的空旷空间（Free Space）进行采样。
*   **损失函数**：
    *   **SDF损失**：使用二元交叉熵（BCE）损失，将SDF值映射到[0,1]区间，加速收敛。
    *   **强度损失**：采用 **L1损失**，实验证明其对雷达数据中的离群点（Outliers）比L2损失更具鲁棒性。

### 3. 实验设计
*   **数据集**：
    1.  **Radar Forest数据集**：使用Hugin A3雷达（48Tx/48Rx天线）。
    2.  **SNAIL-Radar公开数据集**：使用Oculii Eagle雷达（6Tx/8Rx天线），代表了不同噪声特性的传感器。
    3.  **自定义角反射器数据集**：专门用于验证视点相关强度的恢复能力。
*   **Benchmark（基准）**：以同步采集的**激光雷达（LiDAR）点云**作为几何真值（Ground Truth）。
*   **对比方法**：
    *   传统显式方法：$\alpha$-shapes、球旋转算法（BPA）、泊松表面重建（Poisson）。
    *   显式TSDF方法：VDBFusion。
    *   神经隐式方法：SHINE-Mapping（原为激光雷达设计）。

### 4. 资源与算力
论文**未明确说明**具体的GPU型号和数量。
*   **训练细节**：提到训练迭代次数为4000次，学习率为$10^{-3}$，在1000次迭代后冻结SDF网络以稳定强度网络的训练。
*   **内存效率**：论文强调了其方法的内存优势，3QFPI的地图大小仅为SHINE-Mapping的20%-30%，且远小于原始点云存储需求。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同型号雷达的多个场景（森林、篮球场、塔楼等）下进行了测试。
*   **消融实验**：针对强度网络的输入（是否包含SDF、法线、几何特征）进行了对比实验。
*   **鲁棒性测试**：通过抽取每第n帧数据（稀疏化输入）来测试模型在极稀疏数据下的表现。
*   **评价指标**：除了常规的精度（Accuracy）、完成度（Completion）和F-score，还引入了**伽马分布（Gamma Distribution）拟合**来量化表面的局部平滑度，以及平均绝对误差（MAE）衡量强度预测。
*   **充分性评价**：实验设计较为全面，涵盖了不同硬件、不同稀疏程度和消融分析，结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **几何重建更优**：3QFPI在雷达数据上的表现优于传统的泊松重建和专门的激光雷达神经映射方法，生成的表面更平滑且更符合物理真实的平面（如墙面、地面）。
*   **强度预测准确**：模型能成功预测随入射角变化的雷达回波强度，MAE误差较小。
*   **稀疏鲁棒性**：随着输入数据变稀疏，神经隐式方法（3QFPI和SHINE）的性能下降速度远慢于传统显式方法。
*   **内存高效**：三四叉树结构在保持高分辨率重建的同时，显著降低了显存占用。

### 7. 优点（亮点）
*   **首创性**：据作者称，这是首个仅利用3D雷达点云实现3D表面重建并同时建模视点相关强度的神经隐式方法。
*   **噪声处理能力**：通过隐式表示有效地过滤了雷达特有的多路径噪声，解决了传统方法容易产生“碎片化”网格的问题。
*   **多维度评估**：引入了表面法线夹角的分布分析，为雷达制图质量提供了更客观的评价维度。

### 8. 不足与局限
*   **数据依赖性**：重建质量高度依赖于输入雷达数据的初始质量（如SNAIL数据集上的误差明显高于Radar Forest）。
*   **物理建模简化**：目前尚未显式建模雷达的**宽波束效应（Wide Beam）**和复杂的**多路径反射**物理过程，这可能限制其在极复杂室内环境下的表现。
*   **实时性未提及**：虽然提到了内存效率，但未详细讨论推理和训练的实时性，这对于自动驾驶应用至关重要。

（完）
