---
title: "GaussianPile: A Unified Sparse Gaussian Splatting Framework for Slice-based Volumetric Reconstruction"
title_zh: GaussianPile：一种用于基于切片的体积重建的统一稀疏高斯泼溅框架
authors: "Di Kong, Yikai Wang, Wenjie Guo, Yifan Bu, Boya Zhang, Yuexin Duan, Xiawei Yue, Wenbiao Du, Yiman Zhong, Yuwen Chen, Cheng Ma"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20611v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于切片体积重建和渲染的高斯泼溅技术
tldr: GaussianPile是一个统一的稀疏高斯泼溅框架，专门用于基于切片的体积重建（如显微镜和超声）。它结合了成像系统感知模型，通过切片堆叠策略、可微分投影算子和联合优化管线，在保持内部结构细节的同时实现了极高的压缩率。该方法在保证诊断保真度的前提下，显著提升了重建速度和渲染效率，为体积数据的部署和探索提供了实用方案。
selection_source: fresh_fetch
motivation: 传统的体积成像表示方法在处理基于切片的数据时，难以在保持内部结构细节的同时实现高效的压缩和实时渲染。
method: 提出了一种包含切片感知堆叠策略、模拟成像系统点扩散函数的可微投影算子以及联合优化管线的统一高斯泼溅框架。
result: 在显微镜和超声数据集上实现了比NeRF快11倍的重建速度，并相对于体素网格达到了16倍的压缩比。
conclusion: GaussianPile为基于切片的体积数据提供了一种高效、高保真且易于部署的重建与压缩新途径。
---

## 摘要
基于切片的体积成像应用广泛，它需要能够在保持内部结构以供分析的同时进行大幅压缩的表示方法。我们介绍了 GaussianPile，它将 3D 高斯泼溅（3D Gaussian splatting）与成像系统感知的聚焦模型相结合，以应对这一挑战。我们提出的方法引入了三项关键创新：(i) 一种切片感知堆叠策略，通过放置各向异性 3D 高斯来建模跨切片贡献；(ii) 一种可微投影算子，用于编码成像采集系统的有限厚度点扩散函数；(iii) 一种紧凑编码和联合优化流水线，可同时重建和压缩高斯集合。我们基于 CUDA 的设计保留了高斯原语的压缩和实时渲染效率，同时保留了高频内部体积细节。在显微镜和超声数据集上的实验表明，我们的方法降低了存储和重建成本，保持了诊断保真度，并实现了快速 2D 可视化以及 3D 体素化。在实践中，它在短短 3 分钟内即可提供高质量结果，比基于 NeRF 的方法快达 11 倍，并实现了相对于体素网格一致的 16 倍压缩，为基于切片的体积数据集的可部署压缩和探索提供了一条实用路径。

## Abstract
Slice-based volumetric imaging is widely applied and it demands representations that compress aggressively while preserving internal structure for analysis. We introduce GaussianPile, unifying 3D Gaussian splatting with an imaging system-aware focus model to address this challenge. Our proposed method introduces three key innovations: (i) a slice-aware piling strategy that positions anisotropic 3D Gaussians to model through-slice contributions, (ii) a differentiable projection operator that encodes the finite-thickness point spread function of the imaging acquisition system, and (iii) a compact encoding and joint optimization pipeline that simultaneously reconstructs and compresses the Gaussian sets. Our CUDA-based design retains the compression and real-time rendering efficiency of Gaussian primitives while preserving high-frequency internal volumetric detail. Experiments on microscopy and ultrasound datasets demonstrate that our method reduces storage and reconstruction cost, sustains diagnostic fidelity, and enables fast 2D visualization, along with 3D voxelization. In practice, it delivers high-quality results in as few as 3 minutes, up to 11x faster than NeRF-based approaches, and achieves consistent 16x compression over voxel grids, offering a practical path to deployable compression and exploration of slice-based volumetric datasets.

---

## 论文详细总结（自动生成）

以下是对论文《GaussianPile: A Unified Sparse Gaussian Splatting Framework for Slice-based Volumetric Reconstruction》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代生物医学成像（如3D显微镜、体积超声）产生海量数据，给存储、传输和分析带来巨大挑战。
*   **研究动机**：
    *   **传统编解码器（JPEG/HEVC）**：主要针对2D或视频序列，缺乏对3D体积数据结构冗余的优化。
    *   **隐式神经表示（INR）**：虽然压缩率高，但训练/推理极慢，且容易丢失高频细节。
    *   **标准3D高斯泼溅（3DGS）**：专为建模物体表面设计，忽略了内部体积信息，直接用于切片成像会产生严重的伪影（如“浮动”伪影）。
*   **目标**：开发一种既能保持内部结构高保真度，又能实现高压缩率和实时渲染的统一体积表示框架。

### 2. 方法论
GaussianPile 的核心思想是将成像系统的物理特性（聚焦模型）引入 3DGS 框架：
*   **聚焦感知建模（Focus-aware Modeling）**：将成像系统的点扩散函数（PSF）建模为各向异性高斯函数，定义“敏感度图”来描述轴向（切片厚度）的聚焦能力。
*   **高斯堆叠策略（Gaussian Piling）**：
    *   **轴向重参数化**：通过在协方差矩阵中注入轴向分辨率项，将 3D 高斯原语转换为“聚焦高斯”（Focus Gaussian），使其符合有限切片厚度的物理特性。
    *   **不透明度调制**：根据原语距离焦平面的距离衰减其透明度，有效抑制离焦干扰和“鬼影”。
    *   **屏幕空间投影与加性光栅化**：不同于标准 3DGS 的 Alpha 混合，该方法采用**加性累加（Additive Accumulation）**，模拟体积成像中光强积分的物理过程。
*   **压缩管线**：
    *   **稀疏表示**：利用高斯原语的连续性，用远少于体素数量的原语表示场景。
    *   **量化与编码**：采用 Morton 空间排序、12-14位自适应量化、差分编码及 LZMA 熵压缩，实现极高的存储效率。
*   **可微体素化**：通过 CUDA 加速的体素化算子，支持从高斯原语快速重建 3D 体素网格。

### 3. 实验设计
*   **数据集**：
    *   **超声**：TDSC-ABUS（30例自动3D乳腺超声）。
    *   **显微镜**：rDL-LSM（30例光片显微镜数据）、TNNI1/hiPSC 细胞数据、不同噪声水平的赤拟谷盗胚胎数据。
    *   **其他**：ssEM（序列切片电子显微镜）、MIR-OCT（中红外光学相干断层扫描）。
*   **对比方法（Benchmarks）**：
    *   传统方法：HEVC。
    *   SOTA INR 方法：INIF、CoordNet、NeurComp。
    *   改进版 3DGS：针对切片成像适配的标准 3DGS。
*   **评估指标**：2D/3D PSNR、SSIM、训练时间、存储大小、压缩比。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A800 GPU**。
*   **训练时长**：
    *   GaussianPile 在 **3-13 分钟**内即可收敛（平均约 8 分钟）。
    *   相比之下，INR 方法（如 INIF）通常需要 1 小时左右，GaussianPile 的重建速度提升了约 **5-11 倍**。
*   **渲染效率**：支持 >100 FPS 的实时查询和渲染。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从宏观（超声）到微观（细胞、电子显微镜）以及工业检测（OCT）的多种模态，样本量充足（数十个案例）。
*   **消融实验**：详细探讨了切片厚度参数（$\sigma_z$）、初始化策略（随机 vs. 网格）、原语数量（M）以及量化模块对结果的影响。
*   **公平性**：对比了多种不同原理的方法（传统、神经隐式、显式高斯），并提供了 2D 渲染和 3D 重建的双重质量评估，实验设计客观全面。

### 6. 主要结论与发现
*   **性能领先**：在所有测试的数据集上，GaussianPile 在 PSNR 和 SSIM 上均达到或超过了 SOTA 方法。
*   **高效压缩**：实现了相对于原始体素网格平均 **16 倍**（最高 26 倍）的压缩比，且不损失诊断保真度。
*   **3D 一致性**：物理感知模型确保了 3D 结构的连续性，解决了标准 3DGS 在切片间不连续和产生浮动伪影的问题。
*   **下游任务友好**：重建后的数据在分割任务（Dice/IoU 指标）中表现更好，证明了其对结构细节的保留能力。

### 7. 优点（亮点）
*   **物理驱动**：并非简单的黑盒拟合，而是将成像系统的 PSF 物理特性融入渲染方程，具有更强的解释性和泛化性。
*   **速度极快**：基于 CUDA 优化的加性光栅化算子，使 3D 体积重建进入了“分钟级”时代。
*   **端到端优化**：同时实现了重建、渲染和压缩的联合优化。
*   **通用性强**：能够处理高度各向异性（轴向分辨率远低于平面分辨率）的各种切片成像模态。

### 8. 不足与局限
*   **PSF 假设简化**：目前假设 PSF 是空间不变的，而实际光学系统可能存在空间变化的像差。
*   **噪声敏感性**：在极端噪声或严重欠采样的情况下，优化过程可能会导致结果过度平滑。
*   **迭代开销**：虽然比 NeRF 快，但仍需针对每个新体积进行迭代优化，尚未实现“一键式”的前向推理重建。
*   **参数依赖**：需要预知或估计成像系统的切片厚度参数（$\sigma_z$），尽管论文给出了估计建议，但在某些未知系统中可能难以获取最优值。

（完）
