---
title: "GaussianSSC: Triplane-Guided Directional Gaussian Fields for 3D Semantic Completion"
title_zh: GaussianSSC：用于三维语义场景补全的三平面引导有向高斯场
authors: "Ruiqi Xian, Jing Liang, He Yin, Xuewei Qi, Dinesh Manocha"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21487v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于场景补全的学习型逐体素高斯场
tldr: GaussianSSC 是一种用于 3D 语义场景补全（SSC）的新方法。它采用两阶段、基于网格和三平面引导的架构，通过引入“高斯锚定”实现亚像素级的图像特征聚合，并利用学习到的各向异性高斯场来优化三平面特征。该方法在保持三平面表示效率的同时，增强了对表面切向、尺度和遮挡的建模能力，在 SemanticKITTI 基准测试中显著提升了占用预测和语义补全的性能。
selection_source: fresh_fetch
motivation: 旨在解决现有 SSC 方法中体素与图像对齐不精确以及传统网格表示难以捕捉复杂几何细节和遮挡关系的问题。
method: 提出高斯锚定机制进行亚像素图像特征聚合，并结合高斯-三平面精细化模块，通过局部采集和全局聚合来优化各向异性高斯场特征。
result: "在 SemanticKITTI 数据集上，该方法在第一阶段占用预测（+1.8% IoU）和第二阶段语义预测（+0.8% mIoU）上均超越了现有最先进水平。"
conclusion: GaussianSSC 证明了将高斯场引入三平面框架能有效提升 3D 场景补全的几何精度和语义一致性。
---

## 摘要
我们提出了 GaussianSSC，这是一种两阶段、原生网格且由三平面（triplane）引导的语义场景补全（SSC）方法，该方法在不替换体素网格或维护独立高斯集合的情况下，引入了高斯分布的优势。我们引入了“高斯锚定”（Gaussian Anchoring），这是一种在融合的 FPN 特征上进行的亚像素级、高斯加权图像聚合，它加强了体素与图像的对齐，并提升了单目占用估计的性能。我们进一步将点状体素特征转换为学习到的逐体素高斯场，并通过三平面对齐的“高斯-三平面细化”（Gaussian--Triplane Refinement）模块来优化三平面特征，该模块结合了局部收集（以目标为中心）和全局聚合（以源为中心）。这种有向的各向异性支持捕捉了表面切向、尺度以及感知遮挡的不对称性，同时保留了三平面表示的高效性。在 SemanticKITTI 数据集上，GaussianSSC 相比最先进的基准模型，在第一阶段占用估计中提升了 +1.0% 的召回率、+2.0% 的精确率和 +1.8% 的 IoU，并在第二阶段语义预测中提升了 +1.8% 的 IoU 和 +0.8% 的 mIoU。

## Abstract
We present \emph{GaussianSSC}, a two-stage, grid-native and triplane-guided approach to semantic scene completion (SSC) that injects the benefits of Gaussians without replacing the voxel grid or maintaining a separate Gaussian set. We introduce \emph{Gaussian Anchoring}, a sub-pixel, Gaussian-weighted image aggregation over fused FPN features that tightens voxel--image alignment and improves monocular occupancy estimation. We further convert point-like voxel features into a learned per-voxel Gaussian field and refine triplane features via a triplane-aligned \emph{Gaussian--Triplane Refinement} module that combines \emph{local gathering} (target-centric) and \emph{global aggregation} (source-centric). This directional, anisotropic support captures surface tangency, scale, and occlusion-aware asymmetry while preserving the efficiency of triplane representations. On SemanticKITTI~\cite{behley2019semantickitti}, GaussianSSC improves Stage~1 occupancy by +1.0\% Recall, +2.0\% Precision, and +1.8\% IoU over state-of-the-art baselines, and improves Stage~2 semantic prediction by +1.8\% IoU and +0.8\% mIoU.

---

## 论文详细总结（自动生成）

### GaussianSSC：用于三维语义场景补全的三平面引导有向高斯场——论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：单目 3D 语义场景补全（SSC）旨在从单张 RGB 图像推断完整的 3D 几何和语义。现有方法面临两大挑战：
    1.  **对齐不精：** 单目提升（Lifting）过程中，由于深度歧义，图像特征与 3D 体素的关联往往存在亚像素偏差。
    2.  **表示局限：** 传统的体素或三平面（Triplane）网格采用均匀分布，难以捕捉现实场景中各向异性（如细长物体、墙面切向）的结构特征，且对遮挡区域的推理能力有限。
*   **研究动机**：引入 3D 高斯分布的灵活性（可学习均值、协方差和各向异性支持），在不抛弃高效网格架构的前提下，增强模型对几何细节和遮挡关系的建模能力。

#### 2. 方法论
论文提出了一个两阶段、原生网格且由三平面引导的框架 **GaussianSSC**：
*   **第一阶段：占用预测与高斯锚定（Gaussian Anchoring）**
    *   **核心思想**：改进图像到体素的特征聚合。
    *   **关键技术**：不再简单地采样投影点处的单个像素，而是为每个体素学习一个 2D 高斯窗口，在融合的 FPN 特征图上进行加权聚合。通过学习偏移量（$\delta$）、尺度（$\Sigma$）和不透明度（$\alpha$），实现亚像素级的精确对齐。
*   **第二阶段：语义补全与高斯-三平面精细化（Gaussian-Triplane Refinement）**
    *   **核心思想**：利用高斯场优化三平面特征。
    *   **关键技术**：
        1.  将体素特征转换为学习到的 3D 高斯原语。
        2.  **局部采集（Local Gathering）**：以目标为中心，聚合高斯场内的邻域证据，减少量化伪影。
        3.  **全局聚合（Global Aggregation）**：以源为中心，根据高斯分布将特征分发给邻居，增强长程一致性。
        4.  通过混合因子 $\beta$ 融合两种策略，生成最终的语义特征。

#### 3. 实验设计
*   **数据集**：**SemanticKITTI**（自动驾驶场景的基准数据集）。
*   **Benchmark 设置**：在 51.2m x 51.2m x 6.4m 的范围内，划分为 256 x 256 x 32 的体素网格（分辨率 0.2m）。
*   **对比方法**：
    *   **单目 SSC 基准**：MonoScene, TPVFormer, VoxFormer, OccFormer, MonoOcc, Symphonies, ET-Former 等。
    *   **指标**：占用预测的 IoU/Precision/Recall，以及语义补全的 mIoU 和各类别 IoU。

#### 4. 资源与算力
*   **硬件使用**：
    *   **训练与评估**：NVIDIA RTX A5000 GPU (Intel Xeon W-2255 CPU)。
    *   **效率基准测试**：NVIDIA RTX A6000 GPU。
*   **算力细节**：论文未明确给出具体的训练总时长，但提供了推理延迟（Latency）对比：GaussianSSC 约为 409.8ms，略高于 ET-Former（356.6ms），但远低于 Symphonies（612ms）。显存占用约为 2.8GB。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 SemanticKITTI 的验证集和测试集上进行了全面对比。
    *   **消融实验**：针对第一阶段的高斯锚定、负采样（Negative Sampling）以及第二阶段的局部/全局精细化策略（$\beta$ 参数）分别做了多组实验。
*   **充分性与公平性**：实验设计较为充分。为了公平对比，作者采用了与 ET-Former 相同的骨干网络（ResNet-50）和深度估计器（MobileStereoNet），确保性能提升源于所提模块而非更强的预训练模型。

#### 6. 主要结论与发现
*   **性能提升**：在第一阶段占用估计中，IoU 提升了 **1.8%**；在第二阶段语义预测中，IoU 提升了 **1.8%**，mIoU 提升了 **0.8%**。
*   **结构化优势**：高斯锚定显著提升了精确率（Precision +2.0%），说明其能有效减少由于对齐错误导致的误报。
*   **几何捕捉**：可视化结果显示，该方法在处理建筑物、车辆、植被等具有明显几何特征的类别时表现更优，尤其是在遮挡区域的补全更加连贯。

#### 7. 优点
*   **混合架构创新**：巧妙地将 3D 高斯的各向异性优势引入到成熟的三平面/体素网格框架中，避免了维护大规模独立高斯点云的计算负担。
*   **亚像素对齐**：通过学习高斯窗口而非硬性投影，解决了单目视觉中常见的投影量化误差问题。
*   **即插即用潜力**：其高斯精细化模块具有较强的通用性，可作为现有网格化 SSC 方法的增强插件。

#### 8. 不足与局限
*   **时序缺失**：目前仅基于单帧 RGB 输入，未利用视频序列中的时序信息，这在动态驾驶场景中可能限制对移动物体的建模。
*   **计算开销**：虽然比某些复杂模型快，但相比纯三平面方法，引入高斯聚合增加了约 15% 的推理延迟和少量显存占用。
*   **极端遮挡挑战**：在极度遮挡或视野外区域，模型虽然尝试补全，但仍存在多解性（Ambiguity），可能产生过度平滑或不准确的几何推断。

（完）
