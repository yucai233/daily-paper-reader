---
title: "GTSR: Subsurface Scattering Awared 3D Gaussians for Translucent Surface Reconstruction"
title_zh: GTSR：用于半透明表面重建的感知次表面散射 3D 高斯
authors: "Youwen Yuan, Xi Zhao"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22036v1"
tags: ["query:q4"]
score: 8.0
evidence: 为半透明物体建模次表面散射和内部高斯
tldr: 针对半透明物体三维重建中计算成本高且难以建模光学特性的挑战，本文提出GTSR框架。该方法基于3D高斯泼溅（3DGS），通过结合表面和内部两组高斯球来分别模拟表面反射和次表面散射。利用菲涅尔项进行颜色融合，并引入Disney BSDF模型与延迟渲染以增强几何约束。实验证明，GTSR在保持实时渲染性能的同时，在重建精度上优于现有方法，并能适应多种半透明材质。
selection_source: fresh_fetch
motivation: 传统的半透明物体重建方法计算开销大，而高效的3DGS方法因缺乏对次表面散射等光学特性的建模而难以处理此类物体。
method: 提出一种双层高斯结构（表面与内部），利用菲涅尔项进行融合，并结合Disney BSDF模型与延迟渲染来优化法线和深度约束。
result: 在NeuralTO Syn数据集及扩展材质上，该方法在重建质量上超越了基准模型，并实现了出色的实时渲染性能。
conclusion: GTSR成功将3DGS扩展到半透明物体领域，通过物理启发式的建模实现了高效且高精度的几何与外观重建。
---

## 摘要
从多视图图像中重建半透明物体是一个难题。此前，研究人员使用了可微路径追踪和神经隐式场，这些方法需要较高的计算成本。最近，许多基于 3DGS 流水线的工作在不透明物体重建方面取得了良好的效果，且效率更高。然而，这些方法难以处理半透明物体，因为它们没有考虑半透明物体的光学特性。在本文中，我们提出了一种新型的基于 3DGS 的流水线（GTSR），用于重建半透明物体的表面几何。GTSR 结合了两组高斯函数：表面高斯和内部高斯，分别用于建模光线穿过半透明物体时的表面颜色和散射颜色。为了渲染半透明物体的外观，我们引入了一种利用菲涅耳（Fresnel）项来混合这两组高斯的方法。此外，为了改进非轮廓区域的重建细节，我们引入了带有延迟渲染的 Disney BSDF 模型，以增强法线和深度的约束。实验结果表明，我们的方法在 NeuralTO Syn 数据集上优于基准重建方法，同时展现出卓越的实时渲染性能。我们还通过具有不同材料属性的新半透明物体扩展了数据集，并证明了我们的方法能够适应不同的半透明材料。

## Abstract
Reconstructing translucent objects from multi-view images is a difficult problem. Previously, researchers have used differentiable path tracing and the neural implicit field, which require relatively large computational costs. Recently, many works have achieved good reconstruction results for opaque objects based on a 3DGS pipeline with much higher efficiency. However, such methods have difficulty dealing with translucent objects, because they do not consider the optical properties of translucent objects. In this paper, we propose a novel 3DGS-based pipeline (GTSR) to reconstruct the surface geometry of translucent objects. GTSR combines two sets of Gaussians, surface and interior Gaussians, which are used to model the surface and scattering color when lights pass translucent objects. To render the appearance of translucent objects, we introduce a method that uses the Fresnel term to blend two sets of Gaussians. Furthermore, to improve the reconstructed details of non-contour areas, we introduce the Disney BSDF model with deferred rendering to enhance constraints of the normal and depth. Experimental results demonstrate that our method outperforms baseline reconstruction methods on the NeuralTO Syn dataset while showing great real-time rendering performance. We also extend the dataset with new translucent objects of varying material properties and demonstrate our method can adapt to different translucent materials.

---

## 论文详细总结（自动生成）

以下是对论文《GTSR: Subsurface Scattering Awared 3D Gaussians for Translucent Surface Reconstruction》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：从多视图图像重建半透明物体（如玉石、果汁、蜡烛等）具有极高挑战性。光线会穿透物体表面并在内部发生复杂的折射和散射（次表面散射），这违反了传统重建方法中“表面颜色具有多视图一致性”的假设。
*   **核心问题**：
    1.  **优化冲突**：现有的 3D 高斯泼溅（3DGS）方法为了拟合半透明外观，往往会在物体内部生成大量高斯球，并降低表面高斯球的透明度，导致提取的表面几何结构破碎且不准确。
    2.  **计算成本**：基于可微路径追踪或神经隐式场（如 NeuS）的方法虽然能处理半透明，但训练耗时长达数小时甚至数天。
*   **整体含义**：本文提出 GTSR 框架，旨在利用 3DGS 的高效性，通过物理启发式的建模方法，实现高精度的半透明物体表面重建与实时渲染。

### 2. 论文提出的方法论
GTSR 框架的核心思想是将物体外观解耦为表面反射和内部散射，并引入物理渲染约束。
*   **双层高斯表示**：
    *   **表面高斯（Surface Gaussians）**：密集且高不透明度，专门负责建模几何形状和表面反射颜色。
    *   **内部高斯（Interior Gaussians）**：位于物体内部，专门负责建模次表面散射效果。
*   **菲涅尔项融合（Fresnel Blending）**：
    *   为了让内部高斯可见而不破坏表面几何，引入菲涅尔项来动态调整表面高斯的透明度。当视线垂直于表面时，表面变得更透明，从而模拟光线穿透效果。
*   **基于 Disney BSDF 的延迟渲染**：
    *   将外观分解为镜面反射（Specular）、电介质漫反射（Dielectric Diffuse）和散射（Scattering）三部分。
    *   采用**延迟渲染（Deferred Rendering）**技术，先渲染深度、法线、基础色和粗糙度图，再在屏幕空间进行 PBR 计算。这种方式比前向渲染更稳定，能为法线和深度提供更强的几何约束。
*   **损失函数**：结合了光度损失（SH 渲染与 PBR 渲染）、几何一致性损失（法线与深度约束）以及内部高斯约束（确保内部高斯不溢出表面）。

### 3. 实验设计
*   **数据集**：
    *   **NeuralTO Syn**：包含 6 个半透明物体（元宝、中国龙、古龙、熊、指甲、果汁）。
    *   **扩展数据集**：作者自行渲染了具有不同材质属性（不同散射强度和颜色）的新场景以验证适配性。
    *   **真实世界数据**：在黑暗环境下使用闪光灯拍摄的真实半透明物体。
*   **Benchmark 与对比方法**：
    *   **几何重建对比**：NeuS, HF-NeuS, Ref-NeuS, NeuralTO（之前的 SOTA）, 2DGS, PGSR。
    *   **新视角合成对比**：NeRF, 3DGS, SSS, GaussianShader 等。
*   **评价指标**：Chamfer Distance (CD) 衡量几何精度；PSNR, SSIM, LPIPS 衡量渲染质量。

### 4. 资源与算力
*   **硬件**：Nvidia RTX A6000 GPU。
*   **训练时长**：约 **2.5 小时**。
*   **显存占用**：小于 **8 GB**。
*   **效率对比**：相比于基于 NeuS 的方法（通常需 10 小时以上），GTSR 在保证精度的同时显著提升了速度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个标准合成场景、多个扩展材质场景、消融实验以及 3 个真实世界场景。
*   **充分性**：
    *   **消融实验**：分别验证了 PBR 模块、菲涅尔项和内部高斯的作用，证明了每个组件对几何精度的贡献。
    *   **客观性**：对比了从神经隐式场到最新 3DGS 变体的各类方法，实验设计较为公平，且在几何和渲染两个维度都进行了量化评估。

### 6. 论文的主要结论与发现
*   **几何精度突破**：GTSR 在 NeuralTO 数据集上的 Chamfer Distance 达到了最低（平均值显著优于 PGSR 和 2DGS），解决了半透明物体表面“空洞”和“凹凸不平”的问题。
*   **渲染质量提升**：在 PSNR 等指标上超越了 3DGS 原型，证明了物理启发式建模有助于更准确地拟合复杂外观。
*   **解耦成功**：通过双层高斯和 PBR 模块，成功实现了几何形状与材质属性的有效分离。

### 7. 优点
*   **物理启发式设计**：巧妙地利用菲涅尔项解决了 3DGS 在处理半透明物体时的优化冲突。
*   **高效性**：在保持 3DGS 实时渲染优势的同时，达到了与重型神经隐式场相当甚至更好的重建精度。
*   **稳定性**：引入延迟渲染不仅降低了计算开销，还通过像素级法线优化增强了训练稳定性。

### 8. 不足与局限
*   **光照假设限制**：目前假设为**同轴光源（Co-located Light）**，即光源随相机移动且无环境光。这限制了其在复杂环境光下的应用。
*   **物理模型简化**：简化了光线在物体内部的折射和背面照明（Back-side illumination），对于像玻璃这种极高透明度或具有复杂折射率变化的物体，效果可能受限。
*   **精细纹理流失**：在某些极细微的几何细节（如龙鳞）上，由于材质解耦不够彻底，仍存在轻微的模糊现象。

（完）
