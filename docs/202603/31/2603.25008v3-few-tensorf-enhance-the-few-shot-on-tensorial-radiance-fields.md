---
title: "Few TensoRF: Enhance the Few-shot on Tensorial Radiance Fields"
title_zh: Few TensoRF：增强张量辐射场上的少样本性能
authors: "Thanh-Hai Le, Hoang-Hau Tran, Trong-Nghia Vu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25008v3"
tags: ["query:q1"]
score: 8.0
evidence: 结合张量辐射场与频率驱动正则化以实现快速渲染
tldr: 本研究提出 Few TensoRF，旨在解决少样本视角下的 3D 重建难题。该框架结合了 TensoRF 的高效张量表示与 FreeNeRF 的频率驱动正则化技术，通过引入频率和遮挡掩码，在稀疏输入下显著提升了重建质量和稳定性。实验表明，该方法在保持 10-15 分钟快速训练的同时，大幅提高了 PSNR 指标，为实时、高效的少样本 3D 重建提供了有力方案。
selection_source: fresh_fetch
motivation: 针对 TensoRF 在稀疏输入视角下重建质量不稳定且容易过拟合的问题，寻求一种兼顾速度与精度的少样本 3D 重建方法。
method: 将 TensoRF 的张量分解表示与 FreeNeRF 的频率正则化及遮挡掩码机制相结合，以增强模型在少样本场景下的泛化能力。
result: 在 Synthesis NeRF 基准上将平均 PSNR 从 21.45 dB 提升至最高 24.52 dB，且在 THuman 2.0 数据集上仅需 8 张图像即可实现高质量重建。
conclusion: Few TensoRF 证明了通过结合张量表示与频率正则化，可以在极短的训练时间内实现高效且稳健的少样本 3D 场景重建。
---

## 摘要
本文提出了 Few TensoRF，这是一个结合了 TensoRF 高效的基于张量的表示与 FreeNeRF 频率驱动的少样本正则化的三维重建框架。该方法利用 TensoRF 显著加快了渲染速度，并通过引入频率掩码和遮挡掩码，提高了在稀疏输入视角下的稳定性和重建质量。在 Synthesis NeRF 基准测试上的实验表明，Few TensoRF 方法将平均峰值信噪比（PSNR）从 21.45 dB（TensoRF）提升至 23.70 dB，微调版本更是达到了 24.52 dB，同时保持了 TensoRF 约 10-15 分钟的快速训练时间。在 THuman 2.0 数据集上的实验进一步证明了其在人体重建方面的竞争性能，仅需 8 张输入图像即可达到 27.37 - 34.00 dB。这些结果凸显了 Few TensoRF 是一个针对多种场景的实时三维重建的高效且数据利用率高的解决方案。

## Abstract
This paper presents Few TensoRF, a 3D reconstruction framework that combines TensorRF's efficient tensor based representation with FreeNeRF's frequency driven few shot regularization. Using TensorRF to significantly accelerate rendering speed and introducing frequency and occlusion masks, the method improves stability and reconstruction quality under sparse input views. Experiments on the Synthesis NeRF benchmark show that Few TensoRF method improves the average PSNR from 21.45 dB (TensorRF) to 23.70 dB, with the fine tuned version reaching 24.52 dB, while maintaining TensorRF's fast \(\approx10-15\) minute training time. Experiments on the THuman 2.0 dataset further demonstrate competitive performance in human body reconstruction, achieving 27.37 - 34.00 dB with only eight input images. These results highlight Few TensoRF as an efficient and data effective solution for real-time 3D reconstruction across diverse scenes.

---

## 论文详细总结（自动生成）

以下是对论文《Few TensoRF: Enhance the Few-shot on Tensorial Radiance Fields》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的神经辐射场（NeRF）在处理**少样本（Few-shot）**输入（即仅有极少数视角图像）时，容易出现严重的过拟合和高频伪影。虽然 TensoRF 引入张量分解大幅提升了训练速度，但在稀疏视角下依然表现不佳。
*   **研究动机**：旨在结合 **TensoRF 的高效张量表示**与 **FreeNeRF 的频率驱动正则化技术**，开发一种既能保持极快训练速度（10-15 分钟），又能在稀疏输入下生成高质量 3D 重建结果的通用框架。

### 2. 方法论
Few TensoRF 的核心思想是在 TensoRF 的张量分解架构中引入频率掩码和几何约束：
*   **张量组件频率掩码（Frequency Masking Tensor Components）**：对密度张量（$A$）和外观颜色张量（$A_c$）应用动态频率掩码。在训练初期限制高频分量的权重，迫使模型先学习低频的粗糙结构，随着训练进行逐渐释放高频细节，从而避免过快收敛导致的高频伪影。
*   **外观网格频率正则化**：对外观特征网格 $G_c$ 和视角方向 $d$ 的位置编码应用类似的频率过滤，减轻 MLP 网络的过拟合。
*   **遮挡正则化（Occlusion Regularization）**：引入遮挡损失函数，抑制相机附近的“漂浮物”（floaters）密度，迫使模型在更远的实际物体位置解释几何形状。
*   **算法流程**：输入稀疏图像及位姿 -> 张量分解建模（VM 分解） -> 应用频率掩码与遮挡约束 -> 梯度下降优化 -> 输出 3D 模型或新视角图像。

### 3. 实验设计
*   **数据集**：
    *   **Synthesis NeRF (Blender)**：标准的 8 个合成场景（Lego, Chair, Drums 等），用于评估少样本性能。
    *   **THuman 2.0**：复杂的人体数据集，用于测试模型在真实、复杂几何体上的泛化能力。
*   **对比基准（Benchmark）**：主要使用 **PSNR**（峰值信噪比）作为核心评价指标。
*   **对比方法**：
    *   **FreeNeRF**（少样本状态下的 SOTA 方法，基于传统 NeRF）。
    *   **TensoRF**（原始张量辐射场方法）。
    *   **Few TensoRF (Raw & Fine-tuned)**（本文提出的方法及其微调版本）。

### 4. 资源与算力
*   **硬件**：使用 Google Colab 平台，配备 **NVIDIA T4 Tesla GPU**。
*   **训练时长**：
    *   Few TensoRF 训练 15,000 次迭代仅需 **10-15 分钟**。
    *   相比之下，FreeNeRF（50,000 次迭代）需要约 **4 小时 55 分钟**。
*   **批处理大小**：Ray batch size 设置为 1024。

### 5. 实验数量与充分性
*   **实验规模**：在 Synthesis NeRF 的 8 个场景上进行了完整对比，并在 THuman 2.0 数据集上选择了 2 个代表性人体对象（0300 和 0525）进行深度测试。
*   **充分性与公平性**：
    *   实验涵盖了从 3 张到 10 张图像的不同稀疏程度。
    *   对比实验在相同的迭代次数（15,000 次）和硬件环境下进行，确保了时间效率对比的客观性。
    *   **不足之处**：在 THuman 2.0 数据集上仅测试了 2 个对象（总数 500 个），样本量较小，不足以构成全面的基准测试。

### 6. 主要结论与发现
*   **性能提升**：在 Synthesis NeRF 上，Few TensoRF 将平均 PSNR 从 TensoRF 的 21.45 dB 提升至 **23.70 dB**（微调后达 **24.52 dB**）。
*   **效率优势**：在获得与 FreeNeRF 相当甚至更优的图像质量时，训练速度快了约 **20-30 倍**。
*   **人体建模**：在仅有 8 张输入图像的情况下，能够重建出具有基本细节（如衣服、手指）的人体 3D 模型，PSNR 达到 27.37-34.00 dB。

### 7. 优点
*   **高效平衡**：成功实现了“快速训练”、“极少图像输入”与“高重建质量”三者之间的平衡。
*   **即插即用**：频率掩码机制对原始 TensoRF 架构改动较小，易于集成。
*   **鲁棒性**：通过频率控制有效解决了 TensoRF 在稀疏视角下的不稳定性问题。

### 8. 不足与局限
*   **复杂场景挑战**：在“Drums”等具有极复杂几何细节和隐藏角度的场景中，模型仍会产生伪影，表现不如其他简单场景。
*   **噪声问题**：尽管在 THuman 2.0 上 PSNR 较高，但渲染出的图像和 3D 网格仍存在明显的噪声，精细度有待提升。
*   **泛化验证不足**：人体实验仅针对极少数样本，尚未在大规模、多样化的人体姿态数据集上进行全面验证。

（完）
