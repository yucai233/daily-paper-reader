---
title: Z-Order Transformer for Feed-Forward Gaussian Splatting
title_zh: 用于前馈高斯泼溅的 Z-Order Transformer
authors: "Can Wang, Lei Liu, Wei Jiang, Dong Xu"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13465v1"
tags: ["query:q7"]
score: 8.0
evidence: 基于Transformer的前馈高斯泼溅架构，用于加速渲染
tldr: 针对传统3D高斯泼溅（3DGS）优化慢的问题，本文提出一种基于Transformer的前馈架构。通过引入Z-order策略将无序的高斯基元组织成空间相干序列，并结合稀疏注意力机制和自适应冗余抑制，实现了在单次前馈过程中高效预测高斯属性。该方法在减少基元数量的同时，显著提升了新视角合成的速度与质量。
selection_source: fresh_fetch
motivation: 传统3DGS依赖缓慢的迭代优化，而现有前馈方法在处理高斯基元冗余和渲染质量方面存在不足。
method: 提出一种利用Z-order空间排序策略的Transformer架构，通过稀疏注意力机制建模上下文并自适应压缩冗余基元。
result: 实验表明，该方法能以更少的高斯基元实现快速且高质量的新视角合成。
conclusion: Z-order策略能有效提升前馈3DGS的效率与表现，为实时三维重建提供了新思路。
---

## 摘要
近期 3D 高斯泼溅（3DGS）的进展在逼真新视角合成方面取得了显著进步。然而，传统的 3DGS 依赖于缓慢的迭代优化过程，这限制了其在需要实时结果的场景中的应用。为了克服这一瓶颈，近期的前馈方法旨在直接从图像中预测高斯属性，但它们往往在处理高斯原语（Gaussian primitives）的冗余和渲染质量方面面临困难。在这项工作中，我们提出了一种专门为前馈高斯泼溅设计的基于 Transformer 的架构。我们的核心见解是，通过 Z-order 策略将无结构的高斯集合组织成空间一致的序列，可以利用稀疏注意力机制有效捕捉高斯之间的空间和语义关系。此外，我们结合这种 Z-order 策略来动态抑制冗余，同时保留关键的结构细节。这使得 Transformer 能够在单次前馈过程中高效地建模上下文、压缩高斯原语并预测高斯属性。全面的实验表明，我们的方法能够以更少的高斯原语实现快速且高质量的新视角合成。

## Abstract
Recent advances in 3D Gaussian Splatting (3DGS) have enabled significant progress in photorealistic novel view synthesis. However, traditional 3DGS relies on a slow, iterative optimization process, which limits its use in scenarios demanding real-time results. To overcome this bottleneck, recent feed-forward methods aim to predict Gaussian attributes directly from images, but they often struggle with the redundancy of Gaussian primitives and rendering quality. In this work, we introduce a transformer-based architecture specifically designed for feed-forward Gaussian Splatting. Our key insight is that spatial and semantic relationships among Gaussians can be effectively captured through a sparse attention mechanism, enabled by a Z-order strategy that organizes the unstructured Gaussian set into a spatially coherent sequence. Furthermore, we incorporate this Z-order strategy to adaptively suppress redundancy while preserving critical structural details. This allows the transformer to efficiently model context, compress Gaussian primitives, and predict Gaussian attributes in a single forward pass. Comprehensive experiments demonstrate that our method achieves fast and high-quality novel view synthesis with fewer Gaussian primitives.

---

## 论文详细总结（自动生成）

以下是对论文《Z-Order Transformer for Feed-Forward Gaussian Splatting》的结构化总结：

### 1. 论文的核心问题与整体含义
传统的 3D 高斯泼溅（3DGS）依赖于针对每个场景的迭代优化，过程缓慢且缺乏泛化能力。现有的前馈（Feed-forward）方法虽然能直接预测高斯属性，但存在两大痛点：
*   **像素级方法**：为每个像素生成高斯基元，导致基元数量巨大，内存和渲染开销极高。
*   **体素级方法**：通过离散化空间减少冗余，但会引入量化误差，导致细节模糊。
**研究动机**：提出一种既能保持空间局部性、又能高效压缩高斯基元数量，且能实现高质量实时重建的前馈架构。

### 2. 论文提出的方法论
核心思想是利用 **Z-order（莫顿曲线）** 将无结构的 3D 高斯点集序列化为具有空间相干性的 1D 序列，并结合 Transformer 进行处理。
*   **Z-order 序列化**：将 3D 坐标通过位交错（Bit-interleaving）映射为 1D 编码。这使得在 3D 空间中邻近的点在序列中也相互靠近，便于局部特征聚合。
*   **ZFormer Block（核心模块）**：
    *   **稀疏注意力机制**：包含**分组注意力（Group Attention）**捕捉局部信息，以及**Top-k 注意力**选择最相关的块，在降低计算复杂度的同时保留关键细节。
    *   **Z-order 池化**：通过对 Z-order 编码进行位右移操作，将邻近的高斯点聚类并聚合，从而大幅减少基元数量。
*   **多级预测**：通过堆叠 ZFormer 块，在不同压缩率下生成多级高斯表示。
*   **最大覆盖视角选择算法**：针对推理时可能存在的密集视角，提出一种基于 Z-order 的贪婪算法，通过最大化空间覆盖率来剔除冗余视角，提升推理效率。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K**：大规模室内外场景。
    *   **DL3DV**：高分辨率真实场景。
    *   **ACID**：用于跨数据集泛化性评估。
*   **Benchmark 与对比方法**：
    *   **优化类方法**：3DGS, MipSplatting。
    *   **前馈类方法**：DepthSplat, AnySplat, 以及补充材料中的 PixelSplat, MVSplat, MonoSplat 等。
*   **评估指标**：PSNR, SSIM, LPIPS, 推理时间, 高斯基元数量。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练时长**：总计 100K 次迭代，耗时约 **2 天**。
*   **参数设置**：Batch size 为 2，使用了 AdamW 优化器和 FlashAttention 加速。

### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **定量对比**：在 2、4、8、12 个输入视角下分别进行了测试。
*   **跨数据集测试**：在未见过的 ACID 数据集上验证了模型的泛化能力。
*   **消融实验**：涵盖了 Z-order 结构、稀疏注意力、SH 初始化、深度图微调、层数选择以及视角选择策略等 6 组实验。
*   **效率对比**：详细对比了不同方法在推理耗时和高斯基元数量上的表现。

### 6. 论文的主要结论与发现
*   **高效性**：相比 3DGS 优化方法，速度提升约 1000 倍；相比其他前馈方法，高斯基元数量减少了 2 到 3 倍。
*   **高质量**：在稀疏视角（如 2 视图）下表现尤为稳健，PSNR 等指标显著优于现有前馈方法。
*   **空间局部性**：证明了 Z-order 策略在处理非结构化 3D 点云时，比固定体素网格更具灵活性和效率。

### 7. 优点
*   **创新的序列化策略**：巧妙地将 Z-order 引入 3DGS，解决了无序点集在 Transformer 中难以高效处理局部性的问题。
*   **自适应冗余抑制**：通过 Z-order 池化实现了显著的基元压缩，降低了渲染负担。
*   **端到端设计**：整合了深度估计、特征提取和高斯预测，且对输入视角数量具有良好的鲁棒性。

### 8. 不足与局限
*   **超高分辨率限制**：在处理 1K 以上的超高分辨率图像时，受限于内存约束，可能难以捕捉极细微的纹理。
*   **压缩与质量的权衡**：虽然 Z-order 块能减少基元，但实验发现超过 2 层后会出现明显的性能下降，如何实现更高倍率的无损压缩仍是挑战。
*   **深度依赖**：模型性能在一定程度上受限于初始深度估计的准确性，尽管有联合训练补偿，但在极端遮挡场景下可能仍存在伪影。

（完）
