---
title: Differentiable Ray Tracing with Gaussians for Unified Radio Propagation Simulation and View Synthesis
title_zh: 基于高斯的可微光线追踪，用于统一的无线电传播模拟与视图合成
authors: "Niklas Vaara, Lam Huynh, Pekka Sangi, Miguel Bordallo López, Janne Heikkilä"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07781v1"
tags: ["query:q1"]
score: 8.0
evidence: 在视觉重建的神经场景中进行可微分光线追踪
tldr: 本研究针对3D高斯泼溅（3DGS）在射频（RF）仿真中缺乏可相交几何的问题，提出了一种将高斯原语嵌入硬件加速光线追踪结构的微分框架。该方法实现了视觉重建场景中的点对点多径传播计算，无需手动构建网格即可生成物理意义明确的信道脉冲响应。这一工作证明了神经重建可以作为电磁传播仿真与照片级视图合成的统一空间表示，为跨模态数字孪生提供了新思路。
selection_source: fresh_fetch
motivation: 现有的3D高斯泼溅等神经表示侧重于光学合成，缺乏射频仿真所需的确定性射线相交几何结构。
method: 将高斯原语集成到硬件加速的光线追踪结构中，构建了一个支持微分射频传播模拟和视觉渲染的统一框架。
result: 成功从仅包含视觉信息的重建中提取出具有物理意义的信道脉冲响应，验证了跨模态仿真的可行性。
conclusion: 神经场景重建能够作为电磁传播模拟与高质量视图合成的通用底层表示。
---

## 摘要
显式神经表示（如 3D 高斯泼溅，3DGS）能够实现高保真且实时的实时新视图合成，但其优化目标是 alpha 混合的视觉外观，而非可进行光线求交的几何结构。相比之下，射频（RF）数字孪生需要确定的多反弹路径，其中几何结构决定了轨迹及其相关的衰减和延迟。我们引入了一个框架，能够直接在视觉重建的神经场景中进行可微 RF 传播模拟，允许在任意 3D 位置之间进行点对点路径计算，同时保持高质量的视觉渲染。与依赖手动构建网格的传统 RF 模拟流程不同，我们将高斯原语嵌入到硬件加速的光线追踪结构中，作为底层的空间表示。通过从仅包含视觉信息的重建中提取具有物理意义的信道脉冲响应，我们提供了跨模态证据，证明神经重建可以作为电磁传播模拟和照片级真实感视图合成的统一空间表示。

## Abstract
Explicit neural representations such as 3D Gaussian Splatting (3DGS) enable high-fidelity and real-time novel view synthesis, yet optimize for alpha-composited optical appearance rather than ray-intersectable geometry. In contrast, radio-frequency (RF) digital twins require deterministic multi-bounce paths, where the geometry dictates trajectories and their associated attenuation and delay. We introduce a framework enabling differentiable RF propagation simulation directly within visually reconstructed neural scenes, allowing point-to-point path computation between arbitrary 3D locations while preserving high-quality visual rendering. Unlike conventional RF simulation pipelines that rely on manually constructed meshes, we embed Gaussian primitives into a hardware-accelerated ray tracing structure as the underlying spatial representation. By extracting physically meaningful channel impulse responses from visual-only reconstructions, we provide cross-modal evidence that neural reconstructions can serve as unified spatial representations for both electromagnetic propagation simulation and photorealistic view synthesis.

---

## 论文详细总结（自动生成）

这是一份关于论文《Differentiable Ray Tracing with Gaussians for Unified Radio Propagation Simulation and View Synthesis》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了**计算机视觉（CV）与无线电传播（RF）仿真**之间的跨模态统一问题。
*   **研究动机**：虽然视觉图像和无线电波都受同一物理环境（几何结构和材料）的影响，但两者的建模方式长期割裂。
*   **核心痛点**：
    *   **3D高斯泼溅（3DGS）**：虽能实现高质量视图合成，但其本质是基于Alpha混合的体积表示，缺乏物理上可进行射线求交（Ray-intersectable）的精确几何表面。
    *   **RF仿真**：传统的射线追踪（RT）依赖手动构建的CAD或网格模型，成本高且难以扩展。
*   **整体含义**：提出一个统一的框架，利用视觉重建的神经场景（高斯表示）直接进行可微的射频传播模拟，消除手动建模瓶颈，实现视觉渲染与电磁仿真的底层表示统一。

### 2. 方法论
核心思想是将高斯原语嵌入硬件加速的光线追踪结构中，并引入可微的电磁计算。
*   **高斯几何表示**：采用**2D高斯（平面高斯）**而非3D椭球，使其更符合表面特性。每个高斯被近似为一个轻量级的多边形网格，以便利用现代GPU的硬件加速RT内核。
*   **可微渲染与正则化**：
    *   通过Alpha混合渲染颜色、法线、深度和边缘概率。
    *   **损失函数**：结合了RGB损失、法线一致性损失（利用单目法线估计MoGe-2监督）、深度正则化和边缘正则化，确保几何结构在物理上是合理的。
*   **传播路径计算**：
    *   **粗略路径搜索**：利用体素网格和可见性矩阵快速识别候选路径。
    *   **路径精细化**：基于**费马最短时间原理**，通过梯度下降优化交互点，使其满足镜面反射条件。
    *   **路径剪枝**：利用学习到的边缘概率（$\gamma$）剔除发生在几何边缘的无效反射路径。
*   **可微电磁建模**：将介电常数（$\epsilon_r$）和电导率（$\sigma_c$）设为可学习参数，通过匹配模拟与实测的信道脉冲响应（CIR）进行优化。

### 3. 实验设计
论文设计了多维度的实验来验证视觉质量和射频仿真的准确性：
*   **射频验证场景**：
    1.  **走廊场景（140 GHz）**：对比实测的非视距（NLoS）数据。
    2.  **大礼堂场景（234 GHz）**：包含1个发射端（TX）和18个接收端（RX），用于验证多径传播。
    3.  **实验室场景（3.85 GHz）**：使用iPhone采集数据，验证消费级设备的适用性。
*   **Benchmark与对比方法**：
    *   **RF仿真对比**：NimbusRT v0/v1（点云RT）、Sionna（网格RT）、Wireless Insite（商业软件）。
    *   **视觉渲染对比**：3DGS、2DGS、StopThePop、3DGRT等。
*   **评估指标**：RMS延迟扩展（$\tau_{rms}$）、接收信号强度（RSS）、PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **硬件平台**：实验主要在一台配备 **NVIDIA GeForce RTX 5090 GPU** 的 PC 上运行。
*   **训练时长**：
    *   视觉重建部分：在Mip-NeRF360等标准数据集上，平均训练时间约为 **13.3至27.2分钟**（取决于场景复杂度）。
    *   RF参数优化：在18个RX位置的场景下，优化1000次迭代。
*   **效率提升**：通过缓存光线求交结果，避免了反向传播时的二次RT操作，显著降低了开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **视觉部分**：在3个主流公开数据集（Mip-NeRF360, Deep Blending, Tanks&Temples）的多个场景上进行了测试。
    *   **RF部分**：使用了2个真实世界的毫米波/太赫兹测量数据集和1个自采的低频段数据集。
    *   **消融实验**：针对深度正则化、法线正则化、路径剪枝策略以及损失函数组件（SD、Pruning等）做了详细的消融研究。
*   **充分性评价**：实验设计较为全面，涵盖了从合成数据到真实测量、从高频（太赫兹）到低频（3.85GHz）的跨度。通过交叉验证（6个Train-Test Split）评估了RF参数学习的泛化能力，结果具有客观性。

### 6. 主要结论与发现
*   **统一表示可行性**：证明了仅通过视觉信息重建的神经场景，在经过适当的几何正则化后，可以提取出具有物理意义的射频传播路径。
*   **几何精度的重要性**：深度和法线的正则化对于RF仿真至关重要，缺乏这些约束会导致路径计算完全失效。
*   **可微优化的优势**：通过可微框架优化材料参数，可以显著降低模拟与实测信道特征之间的误差（如$\tau_{rms}$误差）。
*   **性能表现**：在保持与Sionna等网格仿真器相当的RF精度时，提供了照片级的视觉渲染能力，且无需手动建模。

### 7. 优点
*   **跨模态融合**：首次在3D高斯表示下实现了全流程可微的射频路径追踪。
*   **硬件加速**：巧妙地将高斯转化为多边形，利用了现代GPU的RTX硬件加速特性，保证了效率。
*   **自动化建模**：摆脱了对CAD模型的依赖，使得大规模、现场特定的无线数字孪生部署成为可能。
*   **边缘感知**：引入边缘概率学习，有效解决了点云/高斯表示中常见的边缘虚假反射问题。

### 8. 不足与局限
*   **物理模型简化**：目前的仿真主要关注镜面反射，尚未考虑**衍射（Diffraction）**、**漫反射/散射（Scattering）**和穿透效应，这在复杂室内环境中可能导致路径缺失。
*   **场景覆盖有限**：虽然验证了两个真实场景，但对于更大规模（如城市级）或动态场景的验证尚不充分。
*   **数据依赖**：RF参数的优化仍依赖于一定数量的实测信道数据，且在缺乏某些关键反射路径时，优化可能会陷入局部最优。
*   **路径搜索开销**：尽管使用了硬件加速，但在处理极高阶反射（如3次以上）时，计算复杂度仍是挑战。

（完）
