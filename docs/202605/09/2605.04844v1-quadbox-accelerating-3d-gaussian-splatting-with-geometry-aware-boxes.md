---
title: "QuadBox: Accelerating 3D Gaussian Splatting with Geometry-Aware Boxes"
title_zh: QuadBox：利用几何感知框加速 3D 高斯泼溅
authors: "Xinze Li, Bohan Yang, Pengxu Chen, Yiyuan Wang, Hongcheng Luo, Wentao Cheng, Weifeng Su"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04844v1"
tags: ["query:q7"]
score: 8.0
evidence: 利用几何感知框加速 3D 高斯投影
tldr: 3D Gaussian Splatting (3DGS) 在实时视图合成中表现出色，但高斯基元与瓦片的相交计算效率仍有待提高。本文提出 QuadBox 方法，通过四个轴对齐边界框紧凑地包裹投影后的高斯分布。利用几何感知拉伸因子构建瓦片对齐的 QuadBox，并结合 QPass 单次遍历算法简化相交检测。实验表明，该方法在保持质量的同时，将 3DGS 渲染速度提升了 1.85 倍。
selection_source: fresh_fetch
motivation: 针对 3DGS 光栅化过程中高斯基元与瓦片相交计算效率低下的瓶颈问题，寻求更高效的几何表示和遍历算法。
method: 引入 QuadBox 几何表示，利用四个轴对齐边界框紧凑包裹高斯投影，并采用 QPass 单次遍历算法通过区间测试加速瓦片相交检测。
result: 实验证明，该方法在多个公开数据集上将 3DGS 的渲染速度显著提升了 1.85 倍。
conclusion: QuadBox 通过离散化的几何感知边界框优化了渲染管线中的相交检测，为 3DGS 的实时渲染提供了高效的加速方案。
---

## 摘要
3D 高斯泼溅 (3DGS) 已成为一种先进的实时新视角合成技术，它通过使用可微高斯基元来表示场景的几何形状和外观。然而，在光栅化流水线中，高效计算精确的高斯-图块（Gaussian-tile）交集仍然是一项关键任务。为此，我们提出了 QuadBox，这是一种利用四个轴对齐包围框以离散方式紧凑封装投影高斯的方法。首先，我们推导出一个几何感知拉伸因子，从而能够构建一个与图块对齐的 QuadBox，该 QuadBox 覆盖了椭圆投影并很大程度上排除了无关图块。其次，我们引入了 QPass，这是一种单次通过的图块遍历算法，它充分利用了 QuadBox 的离散特性，确保通过简单的区间测试即可完成图块交集检查。在公开数据集上的实验表明，我们的方法将 3DGS 的渲染速度提高了 1.85 倍。代码可在 https://github.com/Powertony102/QuadBox 获取。

## Abstract
3D Gaussian Splatting (3DGS) has emerged as an advanced technique for real-time novel view synthesis by representing scene geometry and appearance using differentiable Gaussian primitives. However, efficiently computing precise Gaussian-tile intersections remains a critical task in the rasterization pipeline. To this end, we propose QuadBox, a method that leverages four axis-aligned bounding boxes to tightly encapsulate projected Gaussians in a discrete manner. First, we derive a geometry-aware stretching factor that enables the construction of a tile-aligned QuadBox, which covers the elliptical projection and largely excludes irrelevant tiles. Second, we introduce QPass, a single-pass tile traversal algorithm that exhaustively exploits the discrete nature of QuadBox, ensuring that the tile intersection check is performed with simple interval tests. Experiments on public datasets show that our method accelerates the rendering speed of 3DGS by 1.85$\times$. Code is available at \href{https://github.com/Powertony102/QuadBox}{https://github.com/Powertony102/QuadBox}.

---

## 论文详细总结（自动生成）

这篇论文提出了 **QuadBox**，一种旨在加速 3D 高斯泼溅（3DGS）渲染效率的几何感知包围框方法。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3DGS 在光栅化过程中，需要计算 2D 投影高斯分布与图像瓦片（Tiles）的交集。原始 3DGS 使用松散的轴对齐包围框（AABB），这会导致大量不包含高斯信息的瓦片被错误包含，增加了内存开销、排序负担和渲染计算冗余。
*   **研究动机**：现有的改进方法（如 AdR-Gaussian）虽然引入了不透明度感知的包围框，但在处理具有强各向异性（长条状）和倾斜方向的高斯分布时，单一 AABB 依然存在显著的冗余。

### 2. 核心方法论
论文提出了两个核心组件：**QuadBox 几何构造**和 **QPass 遍历算法**。

*   **QuadBox 构造**：
    1.  **初始化**：基于预定义的不透明度阈值，将高斯分布简化为椭圆方程。
    2.  **DualBox 提取**：首先沿椭圆主轴方向保留两个象限的包围框，初步减少次轴方向的冗余。
    3.  **QuadBox 拉伸**：通过推导一个代数闭式解（Closed-form expression），计算出**几何感知拉伸因子 $f$**。该因子仅依赖于协方差矩阵的项，用于动态调整四个子包围框的大小，使其在离散瓦片空间内紧凑地包裹椭圆。
*   **QPass 遍历算法**：
    *   这是一种单次通过（Single-pass）的瓦片遍历机制。
    *   它不使用复杂的解析几何检查或嵌套循环，而是利用 QuadBox 的四个子框，通过简单的**整数区间测试**来确定每一扫描线上的活跃瓦片范围。
    *   该算法是无分支（Branch-free）且缓存友好的，避免了 GPU 线程束分歧（Warp Divergence）。

### 3. 实验设计
*   **数据集**：使用了三个主流的 NVS 评测数据集：**Mip-NeRF 360**、**Tanks & Temples** 和 **Deep Blending**。
*   **基准方法（Benchmark）**：
    *   **Vanilla 3DGS**：原始官方实现。
    *   **AdR-Gaussian (AdR-AABB)**：当时最先进的包围框优化方法。
*   **即插即用测试**：将 QuadBox 集成到 **DashGaussian** 和 **Compact-3DGS** 等变体中，验证其通用性。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100** 和 **RTX 4090** GPU 上进行。
*   **软件**：基于官方 3DGS 代码库实现的自定义可微光栅化器。
*   **训练/渲染时长**：文中未详细列出总训练时长，但重点对比了渲染帧率（FPS），结果显示在不同场景下均有显著提升。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了室内、室外等多种复杂场景，并提供了逐场景（Per-scene）的详细对比数据。
*   **消融实验**：对比了 3DGS、AdR-AABB、DualBox 以及带/不带 QPass 的 QuadBox，证明了每个组件对速度和质量的贡献。
*   **充分性评价**：实验设计较为全面，不仅对比了渲染速度，还监控了 PSNR、SSIM 和 LPIPS 等质量指标，确保加速没有以牺牲画质为代价。

### 6. 主要结论与发现
*   **渲染加速**：QuadBox 相比原始 3DGS 实现了平均 **1.85 倍**的渲染速度提升。
*   **性能优于 SOTA**：在所有测试场景中，QuadBox 的 FPS 均高于 AdR-AABB，且在某些场景下（如 Compact-3DGS 结合后）FPS 高达 684。
*   **质量保持**：在大幅提升速度的同时，渲染质量（PSNR 等）保持不变甚至略有提升，这得益于更精确的瓦片剔除减少了噪声干扰。

### 7. 优点
*   **数学严谨性**：推导出的拉伸因子 $f$ 具有纯代数形式，计算开销极低，避免了复杂的开方或三角函数运算。
*   **高效遍历**：QPass 算法利用了离散网格特性，将几何问题转化为简单的整数区间合并，非常适合 GPU 并行架构。
*   **高兼容性**：作为光栅化层的优化，它可以作为“即插即用”模块集成到几乎任何基于 3DGS 的改进模型中。

### 8. 不足与局限
*   **极端情况限制**：虽然使用了四个子框，但本质上仍是轴对齐矩形的组合。对于极度细长且呈 45 度倾斜的高斯分布，理论上仍存在极小比例的瓦片冗余（相比于完美的解析相交检测）。
*   **预处理开销**：虽然论文声称计算开销极低，但在高斯基元数量达到千万级的大规模场景中，每帧重新计算 QuadBox 边界可能仍会产生一定的 CPU/GPU 调度开销。
*   **应用范围**：该方法主要针对瓦片式光栅化器（Tile-based Rasterizer），对于非瓦片式的渲染架构（如基于 Ray-marching 的方法）不适用。

（完）
