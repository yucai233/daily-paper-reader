---
title: "CAGS: Color-Adaptive Volumetric Video Streaming with Dynamic 3D Gaussian Splatting"
title_zh: CAGS：基于动态 3D 高斯泼溅的色彩自适应体视频流传输
authors: "Daheng Yin, Yili Jin, Jianxin Shi, Isaac Ding, Miao Zhang, Fangxin Wang, Zhaowu Huang, Cong Zhang, Jiangchuan Liu, Fang Dong"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09279v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 体积视频流与3D高斯泼溅
tldr: 本研究针对体积视频流媒体中3D高斯泼溅（3DGS）面临的带宽消耗大和细节层次（LoD）质量下降问题，提出了CAGS系统。该系统引入了一种颜色自适应方案，利用矢量量化建立LoD，并通过服务器端渲染的低分辨率参考图像在客户端进行颜色校正。实验表明，CAGS在波动带宽下显著提升了渲染质量，且具备极高的运行效率和通用性，为实时沉浸式交互提供了有力支持。
selection_source: fresh_fetch
motivation: 现有的3DGS流媒体方案在带宽受限时存在严重的颜色失真和细节层次退化问题，难以满足实时高质量传输的需求。
method: 提出一种颜色自适应方案，结合矢量量化构建LoD，并利用服务器生成的低分辨率参考图像在客户端修复因压缩导致的颜色失真。
result: 在波动带宽环境下，CAGS的PSNR比现有系统提升了5至20 dB，且处理速度显著优于现有的可扩展高斯压缩方法。
conclusion: CAGS为实时体积视频流媒体提供了一个高效、通用且能显著提升视觉质量的颜色自适应优化框架。
---

## 摘要
体视频（VV）流传输实现了对远程 3D 环境的实时、沉浸式访问，赋能了远程呈现、生态监测和机器人远程操作。这些应用使 VV 流传输成为连接远程物理环境的实时接口，对照片级真实感的场景表示、低延迟交互以及异构网络下的鲁棒性能提出了新的系统级要求。3D 高斯泼溅（3DGS）已被广泛用于实时照片级渲染，具有优越的视觉质量和渲染性能，但面临带宽消耗的挑战。此外，作为自适应 VV 流传输的基础，现有的基于密度的细节层次（LoD）方法并不适用于高斯表示，会导致明显的空隙和严重的质量下降。最近的研究也探索了属性压缩技术以减少带宽消耗。我们的初步研究表明，激进的属性压缩主要引起色彩失真，而这种失真可以通过参考图像在渲染图像中得到有效纠正。受此启发，我们提出了一种用于自适应 VV 流传输的新型色彩自适应方案，该方案利用矢量量化（VQ）建立 LoD，并使用低分辨率参考图像纠正色彩失真。我们进一步提出了 CAGS，这是一个兼容多种高斯表示的自适应 VV 流传输系统，它通过在流媒体服务器上渲染参考图像并在客户端进行色彩恢复来集成色彩自适应方案。在原型系统上的大量实验表明，在波动带宽下，CAGS 在 PSNR 方面优于现有的自适应流传输系统 5~20 dB，运行速度显著快于现有的可扩展高斯压缩方法，并能推广到不同的高斯表示。

## Abstract
Volumetric video (VV) streaming enables real-time, immersive access to remote 3D environments, powering telepresence, ecological monitoring, and robotic teleoperation. These applications turn VV streaming into a real-time interface to remote physical environments, imposing new system-level demands for photorealistic scene representation, low-latency interaction, and robust performance under heterogeneous networks. 3D Gaussian Splatting (3DGS) has been widely used for real-time photorealistic rendering, offering superior visual quality and rendering performance, but it faces challenges due to bandwidth consumption. Furthermore, as the foundation of adaptive VV streaming, existing Levels of Detail (LoD) methods based on density are not well-suited to Gaussian representations, leading to visible gaps and severe quality degradation. Recent studies have also explored attribute compression techniques to reduce bandwidth consumption. Our preliminary studies reveal that aggressive attribute compression primarily causes color distortion, which can be effectively corrected in the rendered image using a reference image. Motivated by these findings, we propose a novel Color-Adaptive scheme for adaptive VV streaming that uses vector quantization (VQ) to establish LoDs and correct color distortions with low-resolution reference images. We further present CAGS, an adaptive VV streaming system compatible with diverse Gaussian representations, which integrates the Color-Adaptive scheme by rendering reference images on the streaming server and performing color restoration on the client. Extensive experiments on our prototype system demonstrate that CAGS outperforms the existing adaptive streaming systems in PSNR by 5$\sim$20 dB under fluctuating bandwidth, operates significantly faster than existing scalable Gaussian compression methods, and generalizes across different Gaussian representations.

---

## 论文详细总结（自动生成）

以下是对论文《CAGS: Color-Adaptive Volumetric Video Streaming with Dynamic 3D Gaussian Splatting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：体视频（Volumetric Video, VV）是实现元宇宙、远程医疗和机器人协作等下一代应用的基础。3D 高斯泼溅（3DGS）因其照片级的渲染质量和实时性能成为主流表示法，但其巨大的属性数据量给带宽受限的网络传输带来了挑战。
*   **核心问题**：
    1.  **现有 LoD 缺陷**：传统的基于密度（减少高斯点数量）的细节层次（LoD）方法在低比特率下会导致场景出现明显的结构空隙和质量骤降。
    2.  **压缩副作用**：激进的属性压缩（如矢量量化 VQ）虽然能保持结构完整，但会引起严重的色彩失真。
*   **研究动机**：作者发现色彩失真可以通过低分辨率的参考图像进行有效修复。因此，论文旨在设计一种“色彩自适应”流媒体方案，在极低带宽下通过牺牲色彩精度换取结构完整，再利用参考图像进行后期修复。

### 2. 方法论：核心思想与关键技术
CAGS 系统采用了**服务器端渲染参考图 + 客户端色彩恢复**的架构，核心技术包括：
*   **可扩展矢量量化（SVQ）**：
    *   **分层码本构建**：结合 KMeans 的效率和层次聚类（AHC）的结构，将高斯属性组织成一个基础层和多个增强层。
    *   **可扩展索引**：采用类似哈夫曼编码的索引方式，截断低位比特即可实现不同质量等级（LoD）的切换，支持快速索引解码。
*   **渲染后透视对齐（PRPA）**：
    *   由于网络延迟，服务器渲染的参考图视角与客户端实际视角存在偏差。PRPA 利用客户端渲染的深度图，将参考图像素重投影到当前视角。
    *   **误差侵蚀（Error Erosion）**：针对重投影产生的遮挡空隙，通过迭代平均邻域色彩来消除伪影。
*   **自适应视场角（Adaptive FoV）**：
    *   使用 **LSTM 模型**预测用户未来的头部运动，动态调整服务器渲染参考图的视场角（FoV），在覆盖范围（防止边缘缺失）和像素密度（保证清晰度）之间取得平衡。

### 3. 实验设计
*   **数据集**：使用了 N3DV、ST-NeRF、Meeting Room 和 Dynamic 3DGS 等多个具有代表性的动态 3D 场景数据集。
*   **Benchmark 与对比方法**：
    *   **LTS-F**：基于密度的 LoD 自适应流媒体系统。
    *   **V3-A**：基于 VQ 压缩但不支持 LoD 的流媒体系统。
    *   **消融实验**：对比了无 PRPA、无自适应 FoV、仅超分辨率（SR）或仅色彩恢复（CR）的变体。
    *   **通用性测试**：将 CAGS 应用于 4DGS、HiCoM 等不同的高斯准备方法。
*   **评价指标**：PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **硬件设备**：服务器和客户端均部署在配备 **NVIDIA RTX 3080** GPU 的机器上；部分性能分析在 **RTX 4090** 上完成。
*   **运行性能**：
    *   客户端渲染（1600x1200 分辨率）在 RTX 3080 上约为 9.32ms，PRPA 对齐 2.11ms，色彩恢复 6.5ms。
    *   系统支持 **30 FPS** 的流传输和 **60 FPS** 的本地渲染。
*   **训练时长**：文中未详细列出每个场景的完整训练总时长，但提到 SVQ 码本构建约需 36.8s，Draco 编码约 370ms。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个大类数据集、十余个具体视频序列上进行了测试。
*   **带宽场景**：涵盖了固定带宽（30-150 Mbps）和基于真实 5G 网络轨迹（5Gophers 数据集）的波动带宽测试。
*   **充分性评价**：实验设计非常充分。不仅对比了端到端的系统性能，还深入分析了 SVQ 各层的视觉贡献、PRPA 的对齐精度以及 LSTM 预测的有效性。通过跨不同 3DGS 变体（如 4DGS）的测试，证明了该方法的广泛适用性。

### 6. 主要结论与发现
*   **质量提升**：在波动带宽环境下，CAGS 的 PSNR 比现有最先进系统（如 LTS）高出 **5~20 dB**。
*   **效率优势**：SVQ 的解码速度远快于现有的可扩展神经压缩方法（如 HAC、CompGS），更适合实时交互。
*   **色彩修复潜力**：实验证明，利用低分辨率（400x300）的参考图可以极大地补偿高倍率 VQ 压缩带来的色彩损失，使低比特率下的视觉体验接近高比特率。

### 7. 优点（亮点）
*   **思路新颖**：避开了传统的“增加点云密度”思路，转而利用 2D 图像修复技术解决 3D 属性压缩问题。
*   **系统完备性**：考虑了实际流媒体中的视角预测误差，并设计了 PRPA 和 Adaptive FoV 进行补偿，具有很强的工程落地价值。
*   **高兼容性**：不绑定特定的高斯训练算法，可作为通用插件提升现有各种 3DGS 表示法的传输效率。

### 8. 不足与局限
*   **遮挡伪影**：虽然有误差侵蚀算法，但在剧烈视角变化或复杂遮挡区域，PRPA 仍可能残留少量视觉伪影。
*   **模型轻量化限制**：为了保证实时性，色彩恢复模型非常轻量，这可能限制了其处理极端复杂光影变化的能力。
*   **依赖深度图**：PRPA 的效果高度依赖于客户端渲染深度图的准确性，如果高斯模型本身几何结构破碎，修复效果会受限。

（完）
