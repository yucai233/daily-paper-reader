---
title: Low-Cost Neural Radiance Fields
title_zh: 低成本神经辐射场
authors: "Alice Huang, Prathamesh Sonawane, Yashdeep Thorat, Yug Rao"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09312v1"
tags: ["query:q7"]
score: 9.0
evidence: 加速NeRF变体和神经辐射架构优化
tldr: 针对神经辐射场（NeRF）训练耗时长且依赖密集视图的问题，本文研究了低计算和低数据场景下的加速变体。通过对比DS-NeRF、TensoRF和HashNeRF，并提出TensoRF-DS、MLP消融及HashNeRF架构改进等扩展方案，探讨了在受限资源下提升合成质量与效率的权衡，为未来低成本NeRF设计提供了参考。
selection_source: fresh_fetch
motivation: 旨在解决NeRF在计算资源有限和输入视图稀疏情况下的高成本与低效率问题。
method: 评估了三种加速NeRF变体，并提出了深度监督、MLP消融以及基于残差和卷积的HashNeRF架构改进。
result: 实验表明在相同训练时间下，所提扩展方案未能显著超越基准模型，但揭示了不同改进在受限设置下的性能权衡。
conclusion: 本研究明确了现有加速技术在低成本环境下的适用性，并为未来高效NeRF的架构设计提供了实验依据。
---

## 摘要
神经辐射场 (NeRF) 实现了高质量的新视角合成，但其漫长的训练时间和对密集输入视角的依赖限制了其普及性。我们对三种加速 NeRF 变体（DS-NeRF、TensoRF 和 HashNeRF）进行了对比研究，并探索了针对低计算、低数据场景的扩展。首先，我们将源自 COLMAP 关键点的深度监督损失添加到 TensoRF 中 (TensoRF-DS)，并在减少视角数量的情况下在 LLFF 数据集上对其进行了评估。其次，我们对 TensoRF 的特征解码 MLP 进行了消融实验，并研究了输入下采样对合成 Lego 场景中 PSNR 和运行时间的影响。第三，我们提出了 HashNeRF 颜色和密度网络的四种架构变体，包括残差和卷积设计，并报告了在相同迭代预算下 PSNR 与训练时间之间的权衡。在等时评估下，我们的扩展均未表现出明显优于已发布基准的结果，但实验刻画了哪些扩展可以迁移到受限设置中，并为未来的工作提出了设计问题。

## Abstract
Neural Radiance Fields (NeRF) achieve high-quality novel-view synthesis, but their long training times and reliance on dense input views limit accessibility. We present a comparative study of three accelerated NeRF variants - DS-NeRF, TensoRF, and HashNeRF and explore extensions targeted at the low-compute, low-data regime. First, we add a depth-supervision loss derived from COLMAP keypoints to TensoRF (TensoRF-DS) and evaluate it on the LLFF dataset under reduced view counts. Second, we ablate the feature-decoding MLP of TensoRF and study the effect of input downsampling on PSNR and runtime on the synthetic Lego scene. Third, we propose four architectural variants of the HashNeRF color and density networks, including residual and convolutional designs, and report PSNR/training-time tradeoffs under matched iteration budgets. Under iso-time evaluation, none of our extensions conclusively outperform the published baselines, but the experiments characterize which extensions transfer to constrained settings and surface design questions for future work.

---

## 论文详细总结（自动生成）

以下是对论文《Low-Cost Neural Radiance Fields》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的神经辐射场（NeRF）面临两个主要瓶颈：一是**训练时间极长**（通常需要10小时以上），二是**依赖密集的输入视图**（需要大量多角度照片）。
*   **研究动机**：为了降低 NeRF 的使用门槛，使其在有限计算资源（低算力）和有限输入数据（稀疏视图）的场景下依然可用。本文旨在通过对比和改进现有的加速变体（DS-NeRF、TensoRF 和 HashNeRF），探索在受限条件下的性能权衡。

### 2. 方法论
论文探索了三种改进方向：
*   **TensoRF-DS（深度监督）**：
    *   **核心思想**：将 DS-NeRF 的深度监督机制引入 TensoRF。
    *   **技术细节**：利用 COLMAP 算法提取的 3D 关键点作为“真值”深度。在训练时，除了颜色损失（MSE），还增加了一个深度损失项 $L_{\hat{D}}$，通过计算模型预测深度与 COLMAP 关键点深度之间的均方误差来约束几何结构。
*   **TensoRF 优化与消融**：
    *   **下采样**：对输入图像进行 4 倍下采样，以减少内存占用并加速计算。
    *   **解码器消融**：对 TensoRF 的特征解码 MLP 架构进行调整，尝试不同的线性层组合和激活函数，探究架构复杂度对 PSNR（峰值信噪比）的影响。
*   **HashNeRF 架构变体**：
    *   在多分辨率哈希编码之后，设计了四种不同的浅层神经网络架构：
        1.  **卷积层**：使用 1D 卷积代替全连接层，试图减少参数量。
        2.  **全局残差连接**：将初始输入连接到每一层，以保留空间信息。
        3.  **层间残差连接**：在相邻层之间建立跳跃连接。
        4.  **深度微调**：增加线性层深度以优化颜色表现。

### 3. 实验设计
*   **数据集/场景**：
    *   **LLFF 数据集**：用于测试 TensoRF-DS 在稀疏视图（仅 9 张图像）下的表现。
    *   **Synthetic Lego 场景**：用于 TensoRF 的消融实验和 HashNeRF 的架构对比。
*   **Benchmark（基准）**：原始 TensoRF、DS-NeRF 和 HashNeRF (Instant-NGP) 的官方实现。
*   **对比方法**：对比了不同数据量（全数据 vs. 低数据）、不同迭代次数以及不同网络架构下的 PSNR 和训练时长。

### 4. 资源与算力
*   **GPU 型号**：HashNeRF 实验明确使用了 **NVIDIA GeForce RTX 3090 Ti**。
*   **训练时长**：
    *   TensoRF 在下采样模式下，2500 次迭代的训练时间控制在 **10 分钟以内**。
    *   HashNeRF 变体在 1000 次迭代下的训练时间约为 **300-360 秒**。
*   **限制说明**：作者提到实验受到了有限 GPU 显存和 RAM 的严格限制。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 TensoRF-DS 进行了 3 种配置对比（全数据、低数据、低数据+深度监督）。
    *   针对 TensoRF 解码器进行了 4 组架构消融实验。
    *   针对 HashNeRF 进行了 4 种架构变体的等时（Iso-time）对比。
*   **充分性评价**：实验设计较为客观，遵循了“等时对比”原则。但由于资源限制，实验的迭代次数（如 TensoRF 仅 2500 次，远低于原论文的 30000 次）和场景覆盖量较少，可能无法完全代表模型收敛后的最终潜力。

### 6. 主要结论与发现
*   **深度监督局限性**：在 TensoRF 中加入深度监督仅带来了极微小的 PSNR 提升（约 0.01），原因是 COLMAP 关键点过于稀疏，仅能影响极小部分的体素空间。
*   **下采样的有效性**：对图像进行 4 倍下采样可以在大幅缩短时间的同时，保持与全分辨率模型相当的 PSNR（约 31），是低成本场景下的高效策略。
*   **架构权衡**：对于 HashNeRF，增加网络深度虽然能提升 PSNR 上限，但在相同的训练时间预算下，更复杂的架构并不一定优于原始的浅层设计。

### 7. 优点
*   **实用性强**：直接针对硬件资源受限的用户，提供了具体的参数调整建议（如下采样倍数）。
*   **多模型融合尝试**：尝试将一种模型的优势（DS-NeRF 的深度监督）迁移到另一种模型（TensoRF）中，具有启发性。
*   **等时评估**：在对比 HashNeRF 变体时，严格控制了训练时间，确保了对比的公平性。

### 8. 不足与局限
*   **改进效果不显著**：所提出的 TensoRF-DS 和 HashNeRF 架构变体在受限时间内均未能显著超越原始基准模型。
*   **实验偏差风险**：由于迭代次数设置较低，可能导致某些需要较长收敛时间的架构（如更深的网络）在实验中处于劣势。
*   **数据覆盖不足**：主要集中在 Lego 和部分 LLFF 场景，未在更复杂的 360° 场景或大规模室外场景中验证。

（完）
