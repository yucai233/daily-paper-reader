---
title: Predicting 3D structure by latent posterior sampling
title_zh: 通过潜后验采样预测3D结构
authors: "Azmi Haider, Dan Rosenbaum"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10830v1"
tags: ["query:q1"]
score: 8.0
evidence: 基于NeRF的表示与神经场建模
tldr: 该研究结合了NeRF的3D场景表示能力与扩散模型的概率建模优势，将3D重建视为具有不确定性的感知问题。通过将3D场景表示为随机隐变量，并利用扩散模型学习其先验，实现了基于观测数据的后验采样。该方法在单视图、多视图、噪声图像及稀疏数据等多种重建任务中表现出色，能有效建模不同观测条件下的不确定性，为3D结构预测提供了统一的概率推理框架。
selection_source: fresh_fetch
motivation: 旨在解决3D重建中固有的不确定性问题，通过结合生成模型与神经场表示来提升重建的鲁棒性和准确性。
method: 采用两阶段训练法，先训练带自动解码隐表示的NeRF重建模型，再利用扩散模型学习隐空间的先验分布，并通过得分推理进行后验采样。
result: 实验证明该方法能从单视图、多视图、噪声图像、稀疏像素及深度数据等多种异构观测中准确预测3D结构并建模不确定性。
conclusion: 该方法提供了一个通用的概率框架，能够灵活处理不同信息量下的3D重建任务，展现了强大的泛化能力和鲁棒性。
---

## 摘要
2D图像生成模型和3D场景神经场表示的显著成就，为整合这两种方法的优势提供了极具吸引力的机会。在这项工作中，我们提出了一种将基于NeRF的3D场景表示与使用扩散模型的概率建模和推理相结合的方法。我们将3D重建视为一个具有固有不确定性的感知问题，因此可以从概率推理方法中获益。核心思想是将3D场景表示为一个随机潜变量，我们可以为其学习先验，并在给定一组观测值的情况下利用该先验进行后验推理。我们利用扩散模型的基于分数的推理方法，结合包含体渲染的重建模型计算出的似然项，来构建后验采样。我们采用两阶段过程训练模型：首先在对3D场景数据集的潜表示进行自动解码的同时训练重建模型，然后使用扩散模型训练潜变量上的先验。通过使用该模型从后验分布中生成样本，我们证明了可以执行各种3D重建任务，这些任务因作为输入的观测类型而异。我们展示了从单视图、多视图、噪声图像、稀疏像素和稀疏深度数据进行的重建。这些观测值在为场景提供的信息量上各不相同，我们展示了我们的方法能够对与每项任务相关的不同程度的固有不确定性进行建模。我们的实验表明，这种方法提供了一种综合性的手段，能够从多种类型的观测中准确预测3D结构。

## Abstract
The remarkable achievements of both generative models of 2D images and neural field representations for 3D scenes present a compelling opportunity to integrate the strengths of both approaches. In this work, we propose a methodology that combines a NeRF-based representation of 3D scenes with probabilistic modeling and reasoning using diffusion models. We view 3D reconstruction as a perception problem with inherent uncertainty that can thereby benefit from probabilistic inference methods. The core idea is to represent the 3D scene as a stochastic latent variable for which we can learn a prior and use it to perform posterior inference given a set of observations. We formulate posterior sampling using the score-based inference method of diffusion models in conjunction with a likelihood term computed from a reconstruction model that includes volumetric rendering. We train the model using a two-stage process: first we train the reconstruction model while auto-decoding the latent representations for a dataset of 3D scenes, and then we train the prior over the latents using a diffusion model. By using the model to generate samples from the posterior we demonstrate that various 3D reconstruction tasks can be performed, differing by the type of observation used as inputs. We showcase reconstruction from single-view, multi-view, noisy images, sparse pixels, and sparse depth data. These observations vary in the amount of information they provide for the scene and we show that our method can model the varying levels of inherent uncertainty associated with each task. Our experiments illustrate that this approach yields a comprehensive method capable of accurately predicting 3D structure from diverse types of observations.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合神经辐射场（NeRF）与扩散模型（Diffusion Models）的概率框架，旨在解决3D重建中固有的不确定性问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
3D重建本质上是一个“病态问题”（ill-posed problem），即从有限的2D观测推断3D结构时存在多种可能性（不确定性）。现有的方法要么侧重于确定性重建（容易在缺失区域产生伪影），要么利用2D扩散模型生成3D（往往缺乏3D一致性）。
**研究动机**：将3D场景表示为随机隐变量，通过学习隐空间的先验分布，并结合观测数据的似然项进行后验采样，从而实现既具备3D一致性又能量化不确定性的概率重建。

### 2. 方法论
该方法采用两阶段训练流程和基于分数的后验推理：
*   **核心思想**：将3D场景压缩进一个紧凑的隐空间，在该空间内学习生成先验，推理时通过梯度引导将先验与具体观测对齐。
*   **关键技术细节**：
    1.  **第一阶段：自动解码与重建模型（RM）**：使用三平面（Tri-plane）表示法。对训练集中的每个场景优化一个隐向量 $z$，同时训练一个共享的解码器。解码器将 $z$ 映射为三平面特征，再通过MLP预测3D点的颜色和密度，最后通过体渲染生成图像。
    2.  **第二阶段：隐空间先验训练**：在第一阶段得到的隐向量集上训练一个DDPM（去噪扩散概率模型）。
    3.  **后验采样（推理）**：在给定新观测 $y$ 时，通过迭代采样过程：$\nabla_{z_t} \log p_t(z_t|y) \approx \nabla_{z_t} \log p_t(z_t) + \nabla_{z_{t-1}} \log p(y|\hat{z}_0)$。其中第一项由扩散模型提供（先验分数），第二项是观测似然的梯度（由重建模型计算误差并反向传播）。
*   **算法流程**：从高斯噪声开始，在每一步扩散迭代中，利用重建模型对当前估计的隐变量进行“引导”，使其逐步逼近符合观测数据的3D结构。

### 3. 实验设计
*   **数据集**：
    *   **SRN Cars**：3200个汽车场景。
    *   **Objaverse-lvis (Chair category)**：439个椅子场景，形状更复杂多样。
*   **对比方法与基准（Benchmark）**：
    *   **隐表示效率**：对比了 Functa，证明三平面结构在相同维度下重建质量更高。
    *   **3D一致性**：对比了 3Dim 和 Zero-1-to-3，通过在生成图像上训练NeRF并测量PSNR来评估一致性。
    *   **鲁棒性**：在噪声图像重建任务中对比了 TensoRF。
*   **任务场景**：单视图重建、半图重建、极稀疏像素（5%）重建、稀疏深度数据（5%）重建以及高噪声图像重建。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA GeForce RTX 4090** GPU。
*   **时长**：重建模型（第一阶段）训练约 **1天**；扩散模型（第二阶段）训练约 **1天**。
*   **参数设置**：隐向量维度为1024，图像分辨率为128x128。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从RGB到深度图、从完整图像到极稀疏像素的多种观测模态。
*   **充分性评价**：实验设计较为全面，不仅展示了视觉效果，还通过不确定性图（Uncertainty Map）量化了模型在未观察区域的预测变异性。对比实验涵盖了隐空间压缩、3D一致性和噪声鲁棒性等多个维度，逻辑闭环较好。但受限于算力，实验主要集中在物体级（Cars/Chairs）数据集，未涉及大规模复杂场景。

### 6. 主要结论与发现
*   **隐空间有效性**：1024维的压缩隐向量足以捕捉复杂的3D结构和纹理。
*   **不确定性建模**：当观测信息不足（如仅有5%像素）时，模型能生成多种合理的补全结果，且不确定性图能准确反映信息缺失区域。
*   **鲁棒性**：引入扩散先验后，模型在极高噪声（$\sigma=0.8$）下的重建表现远超传统的确定性优化方法（如TensoRF）。
*   **PSNR与多样性的权衡**：简单地对多个后验样本取平均虽然能提高PSNR分数，但会导致结果过度平滑，失去真实的细节。

### 7. 优点
*   **通用性强**：一个预训练模型可以处理多种下游任务（RGB、深度、稀疏输入等），无需针对特定任务重新训练。
*   **理论严谨**：将3D重建统一在概率后验采样的框架下，能够自然地处理噪声和缺失数据。
*   **3D一致性高**：相比基于2D扩散模型的提升方法，该方法在隐空间直接操作3D表示，保证了多视角下的一致性。

### 8. 不足与局限
*   **采样速度慢**：由于依赖扩散模型的迭代采样，且每一步都需要通过重建模型进行反向传播计算梯度，推理效率较低。
*   **数据规模限制**：目前仅在特定类别的物体数据集上验证，尚未证明其在开放域或大规模场景（如城市级别）的泛化能力。
*   **分辨率限制**：实验主要基于128x128分辨率，更高分辨率下的显存开销和训练稳定性尚待观察。

（完）
