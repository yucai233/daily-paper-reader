---
title: "GEMM-GS: Accelerating 3D Gaussian Splatting on Tensor Cores with GEMM-Compatible Blending"
title_zh: GEMM-GS：利用 GEMM 兼容混合在 Tensor Core 上加速 3D 高斯泼溅
authors: "Haomin Li, Bowen Zhu, Fangxin Liu, Zongwu Wang, Xinran Liang, Li Jiang, Haibing Guan"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02120v2"
tags: ["query:q7"]
score: 9.0
evidence: 在Tensor Core上使用GEMM兼容混合加速3D高斯泼溅
tldr: 3D高斯泼溅（3DGS）虽然提升了渲染速度，但仍难以满足极高性能实时需求，且现有优化未充分利用现代GPU的Tensor Core。本文提出GEMM-GS，通过将3DGS的混合过程等效重构为GEMM兼容形式，使渲染能利用Tensor Core加速。结合三阶段双缓冲CUDA内核设计，GEMM-GS在原始3DGS基础上实现了1.42倍加速，并能与其他优化方法叠加使用，显著提升了渲染性能。
selection_source: fresh_fetch
motivation: 现有的3DGS加速方案忽略了现代GPU中强大的Tensor Core，因为其渲染管线缺乏通用的矩阵乘法（GEMM）操作。
method: 将3DGS的混合过程等效重构为GEMM兼容形式，并设计了集成三阶段双缓冲流水线的高性能CUDA内核以重叠计算与访存。
result: 实验表明GEMM-GS比原始3DGS快1.42倍，且在结合现有加速方法时能额外提供平均1.47倍的加速。
conclusion: GEMM-GS通过利用Tensor Core显著提升了3DGS的渲染效率，为实时3D场景重建提供了更高效的硬件加速方案。
---

## 摘要
神经辐射场 (NeRF) 能够从多张 2D 图像重建 3D 场景，但由于其点采样设计，导致渲染延迟较高。3D 高斯泼溅 (3DGS) 通过显式场景表示和优化的流水线改进了 NeRF，但仍无法满足实际的实时需求。现有的加速工作忽略了现代 GPU 中不断演进的 Tensor Core，因为 3DGS 流水线缺乏通用矩阵乘法 (GEMM) 操作。本文提出了 GEMM-GS，这是一种通过 GEMM 友好型混合变换利用 GPU 上 Tensor Core 的加速方法。它将 3DGS 混合过程等效地重新表述为 GEMM 兼容形式，以利用 Tensor Core。设计了一个高性能 CUDA 核函数，集成了一个三阶段双缓冲流水线，实现了计算与内存访问的重叠。大量实验表明，GEMM-GS 相比原始 3DGS 实现了 1.42 倍的加速，并且在与现有加速方法结合时，平均可提供额外的 1.47 倍加速。代码已发布在 https://github.com/shieldforever/GEMM-GS。

## Abstract
Neural Radiance Fields (NeRF) enables 3D scene reconstruction from several 2D images but incurs high rendering latency via its point-sampling design. 3D Gaussian Splatting (3DGS) improves on NeRF with explicit scene representation and an optimized pipeline yet still fails to meet practical real-time demands. Existing acceleration works overlook the evolving Tensor Cores of modern GPUs because 3DGS pipeline lacks General Matrix Multiplication (GEMM) operations. This paper proposes GEMM-GS, an acceleration approach utilizing tensor cores on GPUs via GEMM-friendly blending transformation. It equivalently reformulates the 3DGS blending process into a GEMM-compatible form to utilize Tensor Cores. A high-performance CUDA kernel is designed, integrating a three-stage double-buffered pipeline that overlaps computation and memory access. Extensive experiments show that GEMM-GS achieves $1.42\times$ speedup over vanilla 3DGS and provides an additional $1.47\times$ speedup on average when combining with existing acceleration approaches. Code is released at https://github.com/shieldforever/GEMM-GS.

---

## 论文详细总结（自动生成）

以下是对论文 **《GEMM-GS: Accelerating 3D Gaussian Splatting on Tensor Cores with GEMM-Compatible Blending》** 的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3D 高斯泼溅（3DGS）虽然比 NeRF 快，但在处理大规模、高分辨率场景时仍难以达到理想的实时性。
*   **研究动机**：
    *   **硬件利用率失衡**：现代 GPU 的 Tensor Core（专门用于矩阵乘法 GEMM）算力远超传统的 CUDA Core，但 3DGS 的渲染管线（尤其是混合阶段）缺乏矩阵运算，导致 Tensor Core 处于闲置状态。
    *   **性能瓶颈**：分析表明，混合（Blending）阶段占据了 3DGS 总渲染时间的约 70%，是主要的性能瓶颈。
    *   **现有方案不足**：现有的加速方法多集中于剪枝、量化或预处理剔除，未充分挖掘 GPU 硬件架构的演进特性。

### 2. 方法论
*   **核心思想**：通过等效数学变换，将 3DGS 中非线性的不透明度（$\alpha$）计算过程重构为**点积（Dot Product）形式**，从而能够利用 Tensor Core 进行批量矩阵乘法加速。
*   **关键技术细节**：
    *   **块内坐标变换**：引入 Tile（瓦片）中心参考坐标，将像素与高斯中心的关系拆解为“高斯相对于 Tile 中心”和“像素相对于 Tile 中心”两部分。
    *   **GEMM 兼容公式重构**：将原本复杂的指数幂项 $power_{ij}$ 展开并重新组合，转化为两个 6 维向量 $\vec{v}_{gi}$（高斯相关）和 $\vec{v}_{pj}$（像素相关）的点积。
    *   **矩阵化表示**：将一个 Tile 内的所有高斯和像素分别构建为矩阵 $M_g$ 和 $M_p$，通过 $M_{power} = M_g \cdot M_p$ 一次性算出所有像素的贡献。
*   **算法流程与内核设计**：
    *   **三阶段流水线**：设计了“加载索引 -> 构建 $M_g$ 矩阵 -> GEMM 计算与体渲染”的三阶段 CUDA 内核。
    *   **异步与双缓冲**：利用 Ampere 架构后的异步内存拷贝技术和双缓冲（Double Buffer）机制，实现计算与访存的深度重叠。
    *   **PTX 指令优化**：使用底层的 `mma` PTX 指令直接调用 Tensor Core 执行 16x8x8 的微块乘法。

### 3. 实验设计
*   **数据集与场景**：使用了 3 个主流数据集，共 13 个场景：
    *   **Tank & Temples**（室外）：Train, Truck。
    *   **Deep Blending**（室内）：Playroom, Drjohnson。
    *   **Mip-NeRF 360**：包含 9 个室内外复杂场景（如 bicycle, garden, kitchen 等）。
*   **Benchmark 与对比方法**：
    *   **基准**：Vanilla 3DGS（原始版本）。
    *   **预处理优化类**：FlashGS, StopThePop, Speedy-Splat。
    *   **压缩类**：LightGaussian, c3dgs。
*   **消融实验**：测试了不同 Batch Size（16 到 256）以及不同分辨率（1x, 2x, 3x）对性能的影响。

### 4. 资源与算力
*   **GPU 型号**：实验主要在 **NVIDIA A100** 和 **H100** GPU 上进行。
*   **训练配置**：所有场景均按照官方 3DGS 流程训练 30,000 次迭代（30K iterations）。
*   **算力利用**：论文强调了对 Tensor Core（TF32/FP16 精度）的利用，通过对比展示了 Tensor Core 理论算力与实际利用的差距。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 13 个不同复杂度的场景，并在两代顶级 GPU 上进行了验证，实验数据较为详实。
*   **充分性**：
    *   **正交性验证**：不仅对比了原始 3DGS，还将 GEMM-GS 集成到其他 5 种先进加速方案中，证明了该方法具有普适性。
    *   **多维度评估**：包含了延迟分解、分辨率扩展性、Batch Size 敏感度分析，实验设计客观且公平。

### 6. 主要结论与发现
*   **显著加速**：GEMM-GS 相比原始 3DGS 平均实现 **1.42x** 的加速。
*   **叠加效应**：在与其他加速方法（如 Speedy-Splat）结合时，能额外提供平均 **1.47x** 的性能提升。
*   **高分辨率优势**：在 2x 和 3x 分辨率下，加速比分别提升至 1.73x 和 1.74x，说明场景越复杂、计算量越大，Tensor Core 的优势越明显。
*   **硬件友好**：无需修改硬件，直接部署于商用 GPU 即可获得收益。

### 7. 优点
*   **架构感知设计**：敏锐捕捉到 3DGS 算法与现代 GPU 硬件特性（Tensor Core）之间的不匹配，切入点准确。
*   **数学等效性**：通过数学变换实现加速，不损失渲染精度（Lossless），无需重新训练模型。
*   **高度兼容**：作为一种底层的计算优化，它与现有的剪枝、剔除等高层算法完全正交，可作为“插件”提升性能。

### 8. 不足与局限
*   **硬件依赖**：该方法高度依赖具有 Tensor Core 的 NVIDIA GPU（Volta 架构及以后），在旧款 GPU 或非 NVIDIA 硬件（如移动端 SoC）上无法发挥作用。
*   **内存开销**：虽然 $M_p$ 矩阵可以预计算，但在高分辨率或大 Batch Size 下，频繁在 Shared Memory 中存储和交换矩阵数据可能会带来额外的带宽压力。
*   **Batch Size 限制**：实验表明 Batch Size 较小时（如 < 64）加速效果不明显，这限制了其在某些极小规模渲染任务中的应用。

（完）
