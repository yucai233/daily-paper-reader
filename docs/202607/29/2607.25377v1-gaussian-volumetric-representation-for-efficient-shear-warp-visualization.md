---
title: Gaussian Volumetric Representation for Efficient Shear-Warp Visualization
title_zh: 用于高效剪切-扭曲可视化的高斯体表示
authors: "Mayuri Mathur, Ojaswa Sharma"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25377v1"
tags: ["query:q7"]
score: 8.0
evidence: 基于高斯的体积表示和蒙特卡洛估计用于高效体渲染
tldr: 针对医学图像体渲染计算成本高的问题，本文提出一种基于高斯分布的体积表示方法。通过蒙特卡洛体积估计和课程学习策略（结合稀疏体素采样与切片采样），该方法能在极少监督下学习复杂的解剖结构和纹理。该表示支持剪切-变形渲染，在保持高保真度的同时，实现了显著的压缩比和实时渲染速度，适用于MRI和冷冻切片等多种模态。
selection_source: fresh_fetch
motivation: 传统的医学图像体渲染在处理大规模数据集时计算开销巨大，难以在保持解剖细节的同时实现实时交互。
method: 提出一种基于高斯的体积表示，利用蒙特卡洛估计进行稀疏采样训练，并引入结合全局稀疏体素与局部切片采样的课程学习策略。
result: 1的数据压缩比。
conclusion: 研究证明了高斯体积表示在高效、高保真医学影像可视化中的有效性，为大规模多模态数据的实时渲染提供了新思路。
---

## 摘要
医学图像可视化需要体绘制算法在保持高渲染速度的同时，保留解剖学保真度。为了解决大型体数据集的高计算成本问题，我们提出了一种基于高斯的体表示方法，用于高效可视化密集医学体数据，且不损失结构和辐射细节。我们使用蒙特卡洛体估计来优化所提出的表示，这使得能够在高度稀疏的体素子集上进行训练，同时保持与密集目标的一致性。此外，我们引入了一种课程学习策略，在训练过程中逐步加入结构化的基于切片的采样。稀疏体素样本提供了体数据的早期全局覆盖，而切片样本则捕捉空间相关的区域，有助于几何结构和纹理的连续性。这种结合使得高斯表示能够从稀疏监督中学习各种结构的解剖细节及相应的纹理，同时显著降低了与密集体素处理相关的计算成本。学习到的表示支持基于切片的渲染方法（如剪切-扭曲体绘制），从而在保留解剖结构的同时，实现对包括 MRI 和冷冻切片体数据在内的多模态医学数据集的高效可视化。利用稀疏监督，我们的方法实现了高达 43.86 FPS 的渲染速度，压缩比达到 11.31:1。

## Abstract
Medical image visualization requires volumetric rendering algorithms that preserve anatomical fidelity while maintaining high rendering speeds. To address the high computational cost of large volumetric datasets, we propose a Gaussian-based volumetric representation for efficient visualization of dense medical volumes without compromising structural and radiometric details. We optimize the proposed representation using Monte Carlo volumetric estimation, which enables training on a highly sparse subset of voxels while maintaining consistency with the dense volumetric objective. In addition, we introduce a curriculum learning strategy that progressively incorporates structured slice-based sampling during training. Sparse voxel samples provide an early global coverage of the volume, while slice samples capture spatially correlated regions that aid geometric structure and texture continuity. This combination enables the Gaussian representation to learn anatomical details of various structures and corresponding textures from sparse supervision while significantly reducing the computational cost associated with dense voxel processing. The learned representation supports slice-based rendering methods such as shear-warp volume rendering, enabling efficient visualization of multimodal medical datasets including MRI and Cryosection volumes while preserving anatomical structures. Using sparse supervision, our method achieves up to 43.86 FPS rendering with a compression ratio of 11.31:1.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Gaussian Volumetric Representation** 的新型体积表示方法，旨在解决大规模医学影像（如 MRI 和冷冻切片数据）在实时可视化过程中计算开销大、存储需求高的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：医学体积数据（如 512³ 分辨率的 CT/MRI）具有极高的维度。传统的体渲染技术（如光线投射法 Ray Marching）计算成本极高，而现有的神经表示方法（如 NeRF 或 Transformer）在处理大规模密集内部结构时，要么推理速度慢，要么参数量巨大。
*   **研究动机**：现有的 3D 高斯泼溅（3D Gaussian Splatting）主要针对物体表面建模，难以直接应用于需要展示内部解剖结构的医学体积数据。因此，需要一种既能压缩数据、又能保持解剖细节、且支持实时交互式渲染的新型表示。

### 2. 方法论
*   **核心思想**：将体积数据表示为一组可学习的 3D 高斯核混合模型。每个高斯核包含位置（μ）、协方差（Σ）、颜色（C）和不透明度（α）。
*   **关键技术**：
    *   **蒙特卡洛体积估计（MCE）**：为了避免在全量体素上计算损失（计算量巨大），引入 MCE 采样策略。通过重要性采样（优先采样梯度大或误差大的区域），利用极少数体素子集来近似全量体积的优化目标，确保优化过程是无偏且高效的。
    *   **课程学习采样策略**：训练初期使用**稀疏体素采样**以获得全局结构的覆盖；随后逐步引入**结构化切片采样**（在随机方向的平面上采样像素）。切片采样能捕捉空间相关性，增强解剖结构的连续性和纹理细节。
    *   **剪切-扭曲（Shear-Warp）渲染器**：开发了一种基于 GPU 的渲染器，直接从高斯表示中提取切片纹理并进行合成。相比光线投射法，这种方法利用了高斯核的解析性质，显著提升了渲染帧率。

### 3. 实验设计
*   **数据集**：
    *   **MRI 模态**：BraTS 脑肿瘤数据集、小鼠胚胎及新生儿 MRI  atlas。
    *   **冷冻切片（Cryosection）模态**：来自 Visible Korean Project 的人体器官数据（肺、肝、心脏、大脑）。
*   **对比方法（Benchmark）**：
    *   **传统/深度学习基准**：Voxel MLP、Slice MLP、Transformer、3D UNet。
    *   **前沿渲染技术**：GNT-MOVE（Transformer 增强 NeRF）、CVT-xRF（对比学习 Transformer）、FreeSplatter（高斯泼溅变体）。
*   **评价指标**：PSNR、SSIM、MS-SSIM（质量）；FPS（速度）；压缩比（效率）。

### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，学习率为 $5 \times 10^{-4}$，训练 120 个 Epoch。
*   **算力说明**：论文提到使用了 GPU 进行加速渲染和训练，但**未明确指出具体的 GPU 型号（如 RTX 3090 或 A100）及具体的 GPU 数量**。训练时长在对比表中显示约为 6 小时左右（针对特定数据集）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 种不同的医学数据集/器官，对比了 7 种主流的神经表示和渲染方法。
*   **消融实验**：设计了 5 组消融实验（A1-A5），分别验证了随机采样、不同顺序的课程学习、纯切片监督以及纯 MCE 监督的效果。
*   **充分性评价**：实验设计较为全面，跨越了单通道（MRI）和三通道（彩色冷冻切片）数据，验证了方法在不同解剖结构上的泛化能力。对比实验涵盖了从基础 MLP 到最新的 Transformer 架构，评估较为客观。

### 6. 主要结论与发现
*   **高性能渲染**：在保持高保真度的前提下，该方法实现了高达 **43.86 FPS** 的实时渲染速度，比传统光线投射法快约 2.91 倍。
*   **高压缩比**：实现了最高 **11.31:1** 的数据压缩比，显著降低了存储负担。
*   **质量优越**：在 PSNR 和 SSIM 等指标上均优于基于 MLP 和 Transformer 的基准方法，尤其在保留精细解剖边界方面表现突出。

### 7. 优点（亮点）
*   **训练效率高**：通过 MCE 采样，仅需极少量的体素监督即可重建整个体积，极大降低了显存占用。
*   **渲染架构适配**：将高斯表示与经典的剪切-扭曲算法结合，避开了昂贵的逐像素光线采样，非常适合医学影像的切片查看需求。
*   **多模态适用**：证明了该方法在 MRI 和彩色解剖切片上均具有极高的鲁棒性。

### 8. 不足与局限
*   **平滑效应**：由于高斯核本质上是平滑的基函数，在处理极高频的细节或极尖锐的边缘时，可能会出现轻微的模糊。
*   **伪影问题**：在极度稀疏的监督下，重建结果偶尔会出现细小的“点状伪影”（dot-like artifacts）。
*   **动态支持不足**：目前的研究集中在静态体积数据，尚未探讨在 4D 动态医学影像（如心动周期 CT）中的应用。

（完）
