---
title: "AsySplat: Efficient Asymmetric 3D Gaussian Splatting for Long-Sequence Scene Modeling"
title_zh: AsySplat：用于长序列场景建模的高效非对称 3D 高斯泼溅
authors: "Yingji Zhong, Dave Zhenyu Chen, Fuzhao Ou, Youyu Chen, Zhihao Li, Lanqing Hong, Dan Xu"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.10995v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于高效神经体积渲染和场景建模的非对称架构
tldr: 针对长序列场景建模中 3D 高斯泼溅（3DGS）模型的计算冗余问题，本文提出 AsySplat。该方法通过非对称架构解耦几何与外观建模，利用粗粒度几何分支和细粒度外观分支的协同工作，在显著减少参数量和计算开销的同时，实现了高效的高质量新视角合成，相比优化类方法提速近 800 倍。
selection_source: fresh_fetch
motivation: 现有的通用 3DGS 模型在处理长序列时存在大量计算冗余，且几何与外观建模对计算资源的需求并不对称。
method: 提出一种非对称架构，通过参数量大的几何分支处理粗粒度特征，参数量小的外观分支处理细粒度特征，并通过双向连接进行交互。
result: 在 32 视图 960P 输入下，该模型在保持高性能的同时比优化方法快 800 倍，且以更少的参数量超越了现有最先进的通用模型。
conclusion: AsySplat 通过任务感知的非对称设计有效降低了计算冗余，显著提升了长序列场景建模的参数效率和推理速度。
---

## 摘要
最近的可泛化 3D 高斯泼溅（3D Gaussian Splatting）模型推动了长序列新视角合成（NVS）的发展，但其代价是大量的冗余计算。我们发现，基于以下两个观察可以缓解这种冗余：(i) 高质量的 NVS 并不严格要求高精度的几何结构；(ii) 外观学习通常比几何恢复更容易。受此启发，我们提出了一种将几何与外观建模解耦的非对称架构。几何分支利用大部分参数处理粗粒度 token 以进行多视图重建，而外观分支则在细粒度 token 上运行，以显著更少的参数捕捉细节。这两个分支通过双边连接进行交互，实现各自任务的相互引导。这种任务感知的非对称性减少了计算冗余，并更合理地分配了计算资源，从而提高了参数效率，使小型模型也能实现强大的性能。在 32 视图 960P 输入下，我们的模型在达到与基于优化的方法相当性能的同时，实现了近 800 倍的加速，并以显著更少的参数和更低的训练/推理开销，超越了最先进的可泛化模型的零样本性能，实现了整体效率的提升。

## Abstract
Recent generalizable 3D Gaussian Splatting models have advanced long-sequence novel view synthesis (NVS), but at the cost of substantial redundant computation. We identify that the redundancy can be mitigated based on two observations: (i) high-precision geometry is not strictly required for high-quality NVS; (ii) appearance learning is generally easier than geometry recovery. Motivated by these insights, we propose an asymmetric architecture that decouples geometry and appearance modeling. The geometry branch processes coarse-grained tokens with most of the parameters for multi-view reconstruction, while the appearance branch operates on fine-grained tokens to capture details using significantly fewer parameters. The two branches interact through bilateral connections, enabling mutual guidance for their respective tasks. This task-aware asymmetry reduces the computational redundancy and allocates the computation more judiciously, thereby increasing parameter efficiency and enabling smaller models to achieve strong performance. On 32-view 960P inputs, our model matches optimization-based methods while delivering nearly 800x speedup, and surpasses the zero-shot performance of state-of-the-art generalizable models with markedly fewer parameters and reduced training/inference overhead, achieving an overall efficiency improvement.

---

## 论文详细总结（自动生成）

以下是对论文《AsySplat: Efficient Asymmetric 3D Gaussian Splatting for Long-Sequence Scene Modeling》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的通用型 3D 高斯泼溅（3DGS）模型在处理高分辨率、长序列输入时，通常采用简单的层堆叠架构，导致严重的**计算冗余**和**参数效率低下**。
*   **研究动机**：作者观察到两个关键点：
    1.  **几何精度非唯一性**：高质量的渲染并不绝对依赖极高精度的几何，3DGS 的 Alpha 合成机制可以弥补几何的微小缺陷。
    2.  **任务难度不对称**：多视图几何恢复（需要跨帧匹配）比外观学习（主要是像素对齐的 RGB 映射）要困难得多。
*   **目标**：通过重新分配计算资源，构建一个更轻量、更高效的长序列场景建模模型。

### 2. 方法论
核心思想是提出一种**非对称架构（Asymmetric Architecture）**，将几何与外观建模解耦：

*   **几何分支（Geometry Branch）**：
    *   **输入**：粗粒度 Token（大 Patch Size，$p_c=16$）。
    *   **设计**：分配约 90% 的参数，采用三阶段分层结构，结合双向 Mamba 和**稀疏注意力机制**，进行耗时的跨帧交互以提取多视图一致的深度。
*   **外观分支（Appearance Branch）**：
    *   **输入**：细粒度 Token（小 Patch Size，$p_f=8$），用于保留细节。
    *   **设计**：仅分配约 10% 的参数，采用轻量级 MambaVision，且计算限制在**帧内（Intra-frame）**，避免昂贵的跨帧开销。
*   **关键技术细节**：
    *   **双边连接模块（Bilateral Connection）**：两个分支通过交叉注意力机制交换信息，几何分支提供深度引导，外观分支提供高频细节。
    *   **稀疏注意力模块（Sparse Attention）**：将 Token 序列映射回 2D 网格并进行分区处理，将注意力复杂度从全局降低到局部网格内，显著缩短训练时间。
    *   **高斯预测**：融合两分支特征，预测每个像素对应的多个高斯属性。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DL3DV（包含超过 10k 个场景）。
    *   **测试/基准**：DL3DV-140、Tanks&Temples、MipNeRF-360、Deep Blending、Replica（合成数据集）。
*   **对比方法**：
    *   **基于优化的方法**：3DGS、Scaffold-GS。
    *   **通用型/前馈方法**：LongLRM（主要竞争对手）、pixelSplat、MVSplat、GS-LRM 等。
*   **实验设置**：主要针对 32 视图、540×960（960P）分辨率的长序列输入。

### 4. 资源与算力
*   **训练时长**：采用渐进式分辨率训练（256→512→960）。总训练时间约为 **54 小时**（相比 LongLRM 的 76 小时缩短了约 30%）。
*   **显存与计算量**：
    *   在 960P 推理时，显存占用约 **22G**（LongLRM 为 26G）。
    *   计算量为 **16.2 TFLOPs**（LongLRM 为 20.2 TFLOPs）。
    *   **GPU 型号**：文中未明确标注具体型号，但根据显存占用（最高 48GB）和 FlashAttention-2 的使用，推测为 A100 或 H100 级别的 GPU。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个主流基准数据集上进行了详尽对比。
    *   进行了**零样本（Zero-shot）**泛化能力测试。
    *   **消融实验**：针对分支设计、Token 粒度、层配置、参数分配比例等进行了 6 组以上的核心消融实验（见表 6）。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了渲染质量（PSNR/SSIM/LPIPS），还深入分析了参数效率、推理速度、高斯点数量以及后优化（Post-optimization）的效率，证明了其在相同时间预算下的性能优势。

### 6. 主要结论与发现
*   **性能领先**：在 32 视图输入下，AsySplat 的渲染质量媲美基于优化的方法，且速度快 **800 倍**。
*   **效率提升**：相比 LongLRM，参数量减少 **30%**，训练时间减少 **30%**，推理计算量减少 **20%**，生成的高斯点数量减少 **50%**。
*   **泛化性强**：在未见过的场景（如 MipNeRF-360）上，其零样本表现显著优于现有最先进模型。
*   **几何与外观的解耦是有效的**：实验证明，即使几何分支使用粗粒度 Token，只要外观分支保留细粒度信息，最终渲染效果依然出色。

### 7. 优点
*   **任务感知架构**：巧妙利用了几何与外观建模在难度和精度需求上的不对称性，打破了传统对称堆叠层的思维定式。
*   **稀疏注意力设计**：有效解决了长序列 Token 交互带来的计算爆炸问题，平衡了全局上下文和训练效率。
*   **参数效率极高**：用更小的模型实现了更强的泛化性能，尤其在后优化阶段表现出极高的收敛速度。

### 8. 不足与局限
*   **LPIPS 指标劣势**：与基于优化的方法相比，在 LPIPS（感知质量）上仍有差距，这主要是因为通用模型缺乏基于误差的“点云致密化（Densification）”机制。
*   **几何细节缺失**：由于几何分支处理的是粗粒度 Token，虽然渲染效果好，但在极精细的表面重建任务中可能不如高精度几何模型。
*   **应用限制**：目前仍依赖已知的相机位姿（Known Poses），尚未完全解决无位姿（Pose-free）的长序列重建问题。

（完）
