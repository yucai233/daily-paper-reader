---
title: "$K$-NeAS: Scalable Multi-Material CT Reconstruction Using Neural SDFs"
title_zh: $K$-NeAS：基于神经 SDF 的可扩展多材质 CT 重建
authors: "Daksh K. Shah, Emmanouil Nikolakakis, Razvan V. Marinescu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14415v1"
tags: ["query:q1"]
score: 8.0
evidence: 使用神经SDF恢复连续体积衰减场
tldr: 针对稀疏视图CT重建中多材料建模受限及参数需手动调节的问题，本文提出K-NeAS。该方法利用神经符号距离函数（SDF）和共享潜空间骨干网络，引入可微分的K材料顺序软选择器，实现了对任意数量重叠组织的自动化多材料表面重建。通过高斯混合模型自动确定衰减边界并结合辅助损失，在临床CBCT数据集上显著提升了重建精度和稀疏采样下的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的几何感知CT重建方法受限于手动调节的衰减边界和仅支持两种材料的约束，难以处理复杂的多组织场景。
method: 提出一种可扩展架构，采用共享骨干网络、可微分K材料选择器、基于GMM的自动衰减边界确定以及辅助浮点损失。
result: 在多个临床数据集上，该方法在处理多组织区域时PSNR提升高达1.88 dB，且在5至10个极稀疏视图下表现出更强的鲁棒性。
conclusion: K-NeAS成功实现了自动化的多材料CT重建，在提升三维体积保真度的同时，有效解决了稀疏视图下的几何伪影问题。
---

## 摘要
计算机断层扫描 (CT) 具有显著的电离辐射风险，这推动了对稀疏视图重建的需求。隐式场景表示 (ISR) 通过直接从稀疏投影中恢复连续的体衰减场来解决这一问题，而最近的几何感知扩展通过联合建模表面几何与衰减，提高了保真度，并实现了无需手动阈值处理的清晰组织分割。然而，这些方法仍受限于手动调节的衰减边界和僵化的双材质约束。本文提出了 $K$-NeAS，这是一种用于自动化、多材质表面重建的统一且可扩展的架构。我们用共享的潜在主干网络取代了独立的材质网络，并引入了一个全微分的 $K$ 材质顺序软选择器，以建模任意数量的重叠组织。为了消除手动调节，我们利用高斯混合模型 (GMM) 实现了衰减边界的自动化，并实施了调度辅助漂浮物损失，以减轻在极端稀疏情况下常见的几何幻觉。在四个临床锥形束 CT (CBCT) 数据集上的评估表明，$K$-NeAS 成功扩展到任意材质数量，在腹部等复杂多组织区域，当 $K=3$ 材质时实现了卓越的 3D 体保真度（3D PSNR 为 $33.28\text{ dB}$，相比单材质 NeAS 基线的 $31.40\text{ dB}$ 提升了 $+1.88\text{ dB}$）。此外，我们的模型在稀疏采样条件下表现出增强的鲁棒性，在 5 视图和 10 视图约束下，其 3D PSNR 优于基线高达 $1.17\text{ dB}$。

## Abstract
Computed Tomography (CT) carries significant ionizing radiation risks, driving the need for sparse-view reconstruction. Implicit scene representations (ISRs) address this by recovering continuous volumetric attenuation fields directly from sparse projections, and recent geometry-aware extensions jointly model surface geometry alongside attenuation to improve fidelity and enable clean tissue segmentation without manual thresholding. However, these methods remain limited by manually tuned attenuation bounds and rigid two-material constraints. This paper proposes $K$-NeAS, a unified and scalable architecture for automated, multi-material surface reconstruction. We replace independent material networks with a shared latent backbone and introduce a fully differentiable $K$-material sequential soft selector to model an arbitrary number of overlapping tissues. To eliminate manual tuning, we automate attenuation bounding using a Gaussian Mixture Model (GMM) and implement a scheduled auxiliary floater loss to mitigate geometric hallucinations common under extreme sparsity. Evaluated across four clinical Cone-Beam CT (CBCT) datasets, $K$-NeAS successfully scales to arbitrary material counts, achieving superior 3D volumetric fidelity at $K=3$ materials on complex multi-tissue regions such as the Abdomen ($33.28\text{ dB}$ 3D PSNR vs. $31.40\text{ dB}$ single-material NeAS baseline, a $+1.88\text{ dB}$ improvement). Furthermore, our model exhibits enhanced robustness under sparse-sampling conditions, outperforming baseline 3D PSNR by up to $1.17\text{ dB}$ under 5- and 10-view constraints.

---

## 论文详细总结（自动生成）

这是一份关于论文《K-NeAS: Scalable Multi-Material CT Reconstruction Using Neural SDFs》的结构化技术总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统的计算机断层扫描（CT）存在电离辐射风险，因此“稀疏视图重建”成为研究热点。现有的隐式场景表示（ISR）方法（如 NeAS）虽然能联合学习几何表面和衰减场，但存在两个主要局限：
    1.  **材质受限**：仅支持两种材质（如骨骼和肌肉），且使用不可微的硬选择器，难以扩展到复杂解剖结构。
    2.  **手动调参**：衰减边界（Attenuation Bounds）需要针对不同场景进行手动调节，缺乏自动化能力。
*   **研究背景**：该研究旨在通过神经符号距离函数（SDF）实现一种可扩展、自动化的多材质 CT 重建框架，以在极低剂量（稀疏视图）下保持高保真度的图像质量和清晰的组织分割。

### 2. 方法论
*   **核心思想**：引入一个共享的潜在主干网络和可微分的顺序软选择器，配合无监督的边界估计，实现对任意数量（K个）材质的联合建模。
*   **关键技术细节**：
    *   **共享主干架构**：使用多分辨率哈希编码，通过一个共享的 SDF 网络预测特征向量和多个材质的距离 $d$。衰减网络由共享主干和 $K$ 个轻量级预测头组成，极大地控制了参数量增长。
    *   **可微分 K 材质软选择器**：提出一种顺序占用过滤器。每个点的材质成员资格（Membership）是局部 SDF 占用情况的函数。通过 Sigmoid 函数计算每个材质的占用概率，并根据衰减程度从高到低进行加权聚合，确保了整个流程的全微分性。
    *   **自动衰减边界确定**：利用高斯混合模型（GMM）对单材质预训练模型的密度直方图进行拟合，自动识别不同组织的衰减区间，消除了人工调参。
    *   **调度辅助漂浮物损失（Floater Loss）**：针对稀疏视图下容易产生的“几何幻觉”（空腔中的伪影），在训练前 20% 阶段对已知为空白的射线施加正则化约束。

### 3. 实验设计
*   **数据集**：使用 NAF 临床锥形束 CT（CBCT）数据集，涵盖四个解剖区域：**腹部（Abdomen）、胸部（Chest）、足部（Foot）和下颌骨（Jaw）**。
*   **Benchmark 与对比方法**：
    *   以 **NeAS**（该领域的先进几何感知模型）作为主要基线。
    *   对比了不同材质数量（K=1, 2, 3, 4）下的表现。
*   **评估指标**：2D 投影域和 3D 体积域的峰值信噪比（PSNR）和结构相似性（SSIM）。
*   **稀疏度测试**：分别在 5、10、20、50 个视图的约束下进行鲁棒性评估。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA **A4000 GPU (16GB VRAM)**。
*   **训练细节**：每个配置训练 1000 个 epoch。
*   **效率**：由于采用了共享主干设计，材质数量从 K=1 增加到 K=4 时，参数量仅增加了约 0.14%（约 1.7 万个参数），显存占用和计算开销基本保持恒定。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同对比度和复杂度的解剖部位，每个配置均进行了 **3 次独立运行并取平均值**，确保了结果的客观性。
*   **消融实验**：专门针对“软选择器”、“漂浮物正则化”和“共享主干”进行了消融研究，验证了各模块的协同作用。
*   **充分性评价**：实验设计较为全面，不仅验证了精度提升，还重点测试了在极极端稀疏（5 视图）下的失效边缘，具有较高的学术严谨性。

### 6. 主要结论与发现
*   **精度提升**：在腹部等复杂多组织区域，K=3 的模型比单材质基线提升了 **1.88 dB (3D PSNR)**。
*   **稀疏鲁棒性**：在 10 视图的极端稀疏条件下，K-NeAS 的 3D PSNR 优于基线约 **1.07 dB**，证明了多材质先验有助于稳定几何结构。
*   **自动化成功**：GMM 能够有效替代人工调参，在大多数场景下自动找到合适的组织边界。

### 7. 优点
*   **可扩展性强**：打破了以往方法只能处理两种材质的限制，理论上可支持任意数量的组织分类。
*   **全微分设计**：顺序软选择器解决了硬选择器在边界处不可微的问题，使端到端优化更稳定。
*   **参数高效**：通过共享主干网络，以极微小的计算代价实现了多材质建模。
*   **自动化程度高**：引入 GMM 边界估计和辅助损失，降低了模型部署时的专业门槛。

### 8. 不足与局限
*   **高对比度场景失效**：在下颌骨（Jaw）场景中表现不如基线，原因是 GMM 在处理仅有“骨骼-空气”这种极高对比度且缺乏中间过渡组织的直方图时，容易出现边界误判。
*   **特定部位不稳定性**：在颅脑 CT 测试中（文中提到但未列入主表），由于颅骨与软组织对比度极端，哈希网格容易产生高频噪声，导致表面崩溃。
*   **依赖预训练**：GMM 边界估计依赖于单材质模型的初步收敛结果，增加了训练流程的复杂性（需两阶段处理）。

（完）
