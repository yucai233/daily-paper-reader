---
title: A Query-Efficient Stochastic Volume Rendering Framework for Time-Varying Implicit Neural Volumes
title_zh: 一种用于时变隐式神经体的高查询效率随机体渲染框架
authors: "Alper Sahistan, Haichao Miao, Zhimin Li, Peer-Timo Bremer, Joshua A Levine, Valerio Pascucci"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.28047v1"
tags: ["query:q3"]
score: 10.0
evidence: 神经体积的随机体渲染
tldr: 针对时变隐式神经体积（INR）渲染效率低下的问题，本文提出了一种基于Delta Tracking的高效随机体渲染框架。该框架利用RT核心进行遍历并结合Tensor核心进行批量神经推理，通过射线预算和查询剪枝策略显著减少了INR查询次数。该系统支持直接从原始表示进行交互式渲染，在保持高保真度的同时，实现了在RTX 4090上以30-40 FPS的速度渲染百万像素图像，并支持毫秒级的时域探索。
selection_source: fresh_fetch
motivation: 传统的隐式神经体积渲染受限于昂贵的神经推理开销，难以在时变数据上实现高效的交互式渲染。
method: 提出一种基于Delta Tracking的四阶段随机渲染管线，并结合射线预算与查询剪枝技术来优化神经查询效率。
result: 在RTX 4090上实现了1024x1024分辨率下30-40 FPS的渲染速度，且时间步更新仅需1-2毫秒。
conclusion: 该框架证明了通过结合随机采样与硬件加速，可以直接高效地渲染时变隐式神经体积，无需复杂的重采样或预计算。
---

## 摘要
时变隐式神经表示 (INR) 为科学体数据提供了一种紧凑的表示方式，对于动态 X 射线计算机断层扫描 (CT) 等模态，这通常是表示数据的唯一可行方法。然而，INR 的交互式体渲染具有挑战性，因为廉价的内存查找被昂贵的神经推理所取代，从而限制了性能。因此，传统的体渲染方法（如密集采样的光线步进）通常是不切实际的。虽然重采样、缓存和重新训练可以缓解这一成本，但它们会牺牲便利性和准确性，且对于时变数据而言变得难以实施。我们利用一种基于 delta tracking 的高查询效率随机体渲染框架来应对这些挑战。我们的系统采用了一个四阶段流水线，利用异构并行性，使用光线追踪核心进行遍历，并使用张量核心进行批量神经评估。此外，我们提出了通过光线预算和查询剪枝来减少 INR 查询的策略，从而提升了每帧性能。使用我们的渲染器，许多时变 INR 可以直接从其原始表示进行渲染。该系统在 RTX 4090 GPU 上以 1024x1024 分辨率实现了约 30-40 FPS 的帧率，并能收敛至高保真图像。此外，该系统支持对连续域进行交互式时间探索，时间步更新仅需约 1-2 毫秒。

## Abstract
Time-varying implicit neural representations (INRs) provide a compact representation of scientific volumes and, for modalities such as dynamic X-ray computed tomography (CT), are often the only practical way to represent the data. However, interactive volume rendering of INRs is challenging, as cheap memory lookups are replaced by expensive neural inferences, hindering the performance. Therefore, conventional volume rendering methods such as ray marching with dense sampling are often impractical. While resampling, caching, and retraining can mitigate this cost, they compromise convenience and accuracy and become impractical for time-varying data. We tackle these challenges using a query-efficient stochastic volume rendering framework based on delta tracking. Our system employs a four-stage pipeline that exploits heterogeneous parallelism, using ray tracing cores for traversal and tensor cores for batched neural evaluation. Furthermore, we present strategies to reduce INR queries via ray budgeting and query pruning, thereby increasing per-frame performance. Using our renderer, many time-varying INRs can be rendered directly from their original representation. The system achieves ~30-40 FPS at 1024x1024 resolution on an RTX 4090 GPU and converges to high-fidelity images. Moreover, the system enables interactive temporal exploration of the continuous domain, with timestep updates taking approximately 1-2 ms.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Query-Efficient Stochastic Volume Rendering Framework for Time-Varying Implicit Neural Volumes》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
隐式神经表示（INR）在科学可视化中因其极高的压缩比和对连续时空域的表达能力而备受关注，尤其适用于动态X射线CT等无法通过显式网格有效表示的数据。然而，**INR的交互式渲染存在严重的性能瓶颈**：传统的体渲染（如光线步进）需要密集的采样，而每次采样都涉及昂贵的神经网络推理（MLP计算），这比传统的内存查找（纹理采样）慢几个数量级。现有的优化方法（如重采样到网格或缓存）往往会损失精度，且在处理时变（Time-varying）数据时，缓存失效频繁，难以实现流畅的时间轴探索。

### 2. 方法论：核心思想与关键技术
本文提出了一种基于**随机采样（Stochastic Sampling）**的渲染框架，核心思想是将体渲染转化为一个查询效率问题，通过减少神经推理次数来提升速度。

*   **核心算法：Delta Tracking（德尔塔追踪）**
    *   利用Delta Tracking（又称Woodcock Tracking）进行随机采样。它通过一个全局或局部的消光系数上限（Majorant）来决定采样步长，将非均匀介质的采样转化为拒绝采样过程，从而大幅减少所需的神经查询总数。
*   **四阶段异步流水线（Wavefront Pipeline）**
    *   **阶段I：光线初始化**。
    *   **阶段II：遍历（Traversal）**。利用GPU的**RT核心（Ray Tracing Cores）**加速空间跳跃，定位包含数据的宏单元（Macrocells）。
    *   **阶段III：INR评估**。将收集到的采样点打包，利用GPU的**张量核心（Tensor Cores）**进行批量神经推理，最大化硬件利用率。
    *   **阶段IV：最终化**。累积采样结果并更新帧缓冲。
*   **查询减少策略**
    *   **自适应光线预算（Adaptive Ray Budgeting）**：结合时空蓝噪声（STBN）和残差信号，优先在视觉变化剧烈或未收敛的区域分配光线。
    *   **基于同质性的查询剪枝（Homogeneity-based Pruning）**：检测标量变化极小的宏单元，直接使用预估值代替神经推理。

### 3. 实验设计
*   **数据集**：使用了6个时变INR数据集，涵盖了三种不同的神经架构：
    *   **FFN (Fourier Feature Network)**：用于变形物体的X射线CT数据。
    *   **SIREN (Sinusoidal Representation Networks)**：用于CFD流体模拟数据（如cylinder, tangaroa）。
    *   **CoordNet**：残差正弦MLP，用于涡流场数据。
*   **Benchmark与对比方法**：
    *   **Baseline**：标准光线步进（Ray Marching）、原生Delta Tracking（未针对硬件优化）。
    *   **消融实验**：对比了使用DDA遍历与RT核心遍历的性能差异，以及不同光线预算策略（STBN vs. 残差直方图）的效果。

### 4. 资源与算力
*   **硬件环境**：实验主要在单块 **NVIDIA RTX 4090 GPU** 上完成。
*   **软件环境**：使用 CUDA 13 和 OptiX 9 框架。
*   **训练时长**：论文重点在于渲染推理阶段，未详细说明INR本身的训练时长（通常由前序工作完成），但提到宏单元（Macrocell）的构建和更新在毫秒级（1-55ms，取决于分辨率）。

### 5. 实验数量与充分性
*   **实验规模**：针对6个不同特征的数据集进行了详尽的性能测试。
*   **充分性**：实验涵盖了从每秒帧数（FPS）、图像质量（PSNR/FLIP）、内存占用、到时间步切换延迟等多个维度。
*   **客观性**：通过对比不同神经架构（FFN vs. SIREN vs. CoordNet），展示了框架的通用性，并诚实地指出了深度残差网络（如CoordNet）在张量核心上的利用率挑战。

### 6. 主要结论与发现
*   **高性能交互**：在1024²分辨率下，该框架能实现 **30-40 FPS** 的渲染速度，比未优化的神经渲染快约 **125倍**。
*   **低延迟时间探索**：由于INR的连续性，切换时间步只需更新极小的辅助结构，更新延迟仅为 **1-2 毫秒**，支持真正的实时动态探索。
*   **硬件协同效应**：解耦遍历与推理，使RT核心和张量核心能够并行工作，是实现交互式速率的关键。

### 7. 优点
*   **无需重新训练**：直接渲染原始INR模型，保持了数据的最高保真度，无重采样损失。
*   **异构计算优化**：巧妙地结合了RT核心的空间加速能力和张量核心的矩阵运算能力。
*   **时域友好**：相比缓存方案，该随机框架在处理连续时间变化时表现极其稳定，不会产生缓存失效导致的卡顿。

### 8. 不足与局限
*   **硬件依赖性**：高度依赖NVIDIA特定的硬件特性（RT Cores, Tensor Cores），在其他架构上的移植性受限。
*   **宏单元依赖**：宏单元的标量范围是通过采样估计的，如果采样不够密集，可能会遗漏极小的特征，导致渲染伪影（如块状缺失）。
*   **偏置风险**：查询剪枝策略虽然提升了速度，但本质上引入了统计偏置，在极端剪枝参数下会导致图像质量下降。
*   **网络深度敏感**：对于层数极深或架构复杂的网络，神经推理依然是绝对的瓶颈，性能提升空间受限于网络本身的计算复杂度。

（完）
