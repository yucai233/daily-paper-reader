---
title: "From Scalars to Time Series: Rethinking Implicit Neural Representations for Time-Varying Volumetric Data"
title_zh: 从标量到时间序列：重新思考时变体数据的隐式神经表示
authors: "Weihan Zhang, Xuan Zhao, Yenwen Peng, Yuqi Chen, Jun Tao"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20970v1"
tags: ["query:q1"]
score: 8.0
evidence: 时变体积数据的隐式神经表示
tldr: 本研究重新审视了时变体数据的隐式神经表示（INR）设计，提出将数据视为空间索引的时间序列而非离散的时空坐标点。通过引入序列级监督，该方法避免了密集的时空采样，显著降低了训练成本并提升了重建质量。该框架具有良好的兼容性，结合混合专家（MoE）架构后，能更有效地处理异构的时间动态，为时变科学数据的高效表示提供了新思路。
selection_source: fresh_fetch
motivation: 传统的坐标式采样在处理时变体数据时计算成本高昂，且未能有效利用时间维度的结构化信息。
method: 将数据重构为空间索引的时间序列集合，并利用序列级监督代替传统的逐点标量采样来训练INR。
result: 该方法在显著降低训练开销的同时，一致性地提升了多种INR架构的重建精度，并能通过MoE架构进一步优化动态分配。
conclusion: 从标量采样转向序列级表示是提升时变体数据建模效率和质量的关键，为隐式神经表示提供了更优的训练范式。
---

## 摘要
用于时变体数据的隐式神经表示（INRs）通常使用时空坐标上的密集采样进行训练，其中每个观测值对应于空间和时间中的单个点。这种基于坐标的公式在优化过程中需要大量的采样，导致高昂的计算成本和对时间结构利用的低效。在这项工作中，我们重新审视了这一设计选择，并证明密集时空采样对于学习时变场并非必要。相反，我们将数据表示为空间索引的时间序列集合，并使用每个空间位置上的序列级监督来训练 INR，而不是基于坐标的标量样本。这种重新表述消除了对密集时空采样的需求，转而以结构化的方式从每个空间位置的完整时间演化中进行学习。我们证明了这种表示方法与一系列现有的 INR 架构兼容，并能持续提高重建质量，同时显著降低训练成本。此外，我们展示了该公式可以与混合专家（MoE）架构相结合，并且我们的 MoE 实例化与基础重新表述以及现有的基于 MoE 的 INR 方法相比，进一步提高了重建质量，在异构时间动态下提供了更强的容量分配能力。

## Abstract
Implicit neural representations (INRs) for time-varying volumetric data are typically trained using dense sampling over spatiotemporal coordinates, where each observation corresponds to a single point in space and time. This coordinate-wise formulation requires extensive sampling during optimization, leading to high computational cost and inefficient use of temporal structure. In this work, we revisit this design choice and show that dense spatiotemporal sampling is not necessary for learning time-varying fields. Instead, we represent the data as a collection of spatially indexed time series and train INRs using sequence-level supervision over each spatial location, rather than coordinate-wise scalar samples. This reformulation eliminates the need for dense spatiotemporal sampling and instead learns each spatial location from its full temporal evolution in a structured manner. We demonstrate that this representation is compatible with a range of existing INR architectures and consistently improves reconstruction quality, while significantly reducing training cost. Furthermore, we show that this formulation can be combined with mixture-of-experts architectures, and that our MoE instantiation further improves reconstruction quality compared to both the base reformulation and existing MoE-based INR methods, providing a stronger capacity allocation under heterogeneous temporal dynamics.

---

## 论文详细总结（自动生成）

这篇论文《From Scalars to Time Series: Rethinking Implicit Neural Representations for Time-Varying Volumetric Data》对时变体数据的隐式神经表示（INR）进行了重新思考，提出了一种从“标量采样”转向“序列监督”的新范式。

以下是对该论文的详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的时变体数据 INR 通常将时间（$t$）与空间坐标（$x, y, z$）同等对待，采用 $(x, y, z, t) \to \text{scalar}$ 的映射方式。
*   **研究动机**：
    *   **采样效率低下**：为了捕捉动态变化，需要在时空维度进行极其密集的采样，导致训练计算成本高昂。
    *   **忽视时间结构**：逐点采样忽略了同一空间位置在时间轴上的连续性和结构化特征。
    *   **容量分配不均**：不同空间区域的时间动态差异很大（有的剧烈变化，有的平稳），传统方法难以针对性地分配模型容量。

### 2. 论文提出的方法论
*   **核心思想**：将数据重新表述为“空间索引的时间序列集合”。INR 的输入仅为空间坐标 $(x, y, z)$，输出则是该位置对应的完整时间序列向量。
*   **关键技术细节**：
    *   **序列级监督（Sequence-level Supervision）**：不再对单个时空点进行采样，而是对每个空间位置的整个时间演化进行建模。训练时，损失函数在整个序列上计算，从而利用了时间维度的结构化信息。
    *   **架构兼容性**：该方法是一个通用框架，可以集成到现有的 INR 架构（如 MLP、Hash-grid/Instant-NGP 等）中，只需将输出层从标量改为向量。
    *   **混合专家架构（MoE）集成**：引入 MoE 机制，根据空间位置的时间动态复杂程度，动态地将计算资源分配给不同的“专家”网络。这使得模型能更有效地处理异构的时间变化。

### 3. 实验设计
*   **数据集/场景**：使用了典型的科学可视化时变体数据集（如燃烧模拟、流体动力学等，这类数据通常具有复杂的时间演化特征）。
*   **Benchmark 与对比方法**：
    *   对比了多种主流 INR 架构，包括基于纯 MLP 的架构和基于特征网格（Feature Grids）的架构（如 Instant-NGP）。
    *   对比了现有的时空 INR 方法（如 ST-INR）以及其他基于 MoE 的改进方案。
*   **评估指标**：主要使用峰值信噪比（PSNR）、结构相似性（SSIM）以及训练收敛时间。

### 4. 资源与算力
*   **算力说明**：根据摘要和元数据，文中强调了该方法能“显著降低训练成本”。
*   **具体细节**：提供的文本片段中未明确列出具体的 GPU 型号（如 A100 或 RTX 4090）或具体的训练总时长。通常此类研究会在实验章节详细说明，但在此摘要层级的信息中缺失。

### 5. 实验数量与充分性
*   **实验规模**：论文声称在一系列现有的 INR 架构上验证了兼容性，并对比了基础模型与集成 MoE 后的性能。
*   **充分性评价**：实验设计较为全面，涵盖了从基础架构改进到高级架构（MoE）的演进对比。通过在多种不同动态特性的科学数据集上进行测试，证明了该方法的普适性和鲁棒性。

### 6. 论文的主要结论与发现
*   **密集采样非必要**：证明了学习时变场并不一定需要密集的时空坐标采样。
*   **序列表示更优**：将时间维度作为输出向量（序列）而非输入坐标，能显著提高重建质量并缩短训练时间。
*   **MoE 增益明显**：结合 MoE 架构后，模型在处理空间异构的动态数据时表现出更强的容量分配能力，进一步提升了精度。

### 7. 优点
*   **范式创新**：打破了“坐标即输入”的思维定式，利用时间序列的结构化特性简化了优化问题。
*   **高效性**：通过减少采样频率和优化监督方式，大幅提升了训练效率。
*   **灵活性**：作为一个通用的训练框架，它可以无缝嵌入现有的各种 INR 骨干网络中。
*   **自适应性**：MoE 的引入解决了科学数据中常见的局部剧烈变化问题。

### 8. 不足与局限
*   **显存压力**：将整个时间序列作为输出向量，当时间步长（Time steps）非常大时，可能会增加输出层的参数量和显存占用。
*   **外推限制**：由于时间不再作为连续的输入坐标，该方法在处理“时间插值”（即预测训练帧之间的时间点）时，可能需要额外的解码或插值机制，不如连续坐标表示那么直观。
*   **应用范围**：目前主要针对结构化网格的体数据，对于非结构化或极高维度的动态场景，其扩展性仍有待验证。

（完）
