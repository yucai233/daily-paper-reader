---
title: "Lossless-INR: Lossless Volumetric Implicit Neural Representations"
title_zh: Lossless-INR：无损体隐式神经表示
authors: "Kaiyuan Tang, Daniel Burke, Chaoli Wang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18150v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于体渲染的无损隐式神经表示
tldr: 隐式神经表示（INR）在体积数据表示中具有优势，但现有方法通常是有损的，误差会影响后续分析。本文提出Lossless-INR框架，通过位平面分解将体素重建转化为二进制分类任务。结合八叉树分块策略和三元特征网格网络，该方法在保持紧凑表示的同时，实现了科学体积数据的零位错误率和位精确重建，为高精度科学可视化和分析提供了可靠保障。
selection_source: fresh_fetch
motivation: 现有的隐式神经表示方法在处理体积数据时存在固有误差，这些误差会随渲染和下游分析传播，影响科学数据的准确性。
method: 采用位平面分解将重建任务转化为二进制分类，并结合八叉树自适应分块与三元特征网格网络进行优化。
result: 在多种体积数据集上实现了零位错误率（Zero BER）和位精确（Bit-exact）的无损重建。
conclusion: Lossless-INR 证明了通过特定架构设计，INR 可以在保持紧凑性的同时实现科学数据的完全无损表示。
---

## 摘要
隐式神经表示（INR）方法提供了连续的坐标到值的映射，并能自然地与直接体绘制相结合，使其在表示体数据方面具有吸引力。然而，现有的基于 INR 的体数据处理方法本质上是有损的，即使是微小的重建误差也可能通过绘制和下游分析进行传播。在这项工作中，我们探索了 Lossless-INR，这是一种基于位平面分解的 3D 科学体数据无损 INR 框架。通过将每个体素值分解为二进制位平面，我们将重建重新表述为逐位的二分类问题，从而使精确恢复简化为正确预测每一个位。为了在保持表示紧凑的同时使该优化过程易于处理，我们将一种能够自适应细分复杂区域的八叉树块划分策略与一个三元特征网格网络相结合，该网络的网格条目由一组三元值参数化。在多种体数据集上的实验表明，该设计可以实现零误码率和位精确重建，从而能够通过紧凑的表示实现忠实的绘制和下游分析。代码可在 https://github.com/TouKaienn/Lossless-INR 获取。

## Abstract
Implicit neural representation (INR) methods provide continuous coordinate-to-value mappings and integrate naturally with direct volume rendering, making them attractive for representing volumetric data. However, existing INR-based approaches for volumetric data are inherently lossy, and even small reconstruction errors can propagate through rendering and downstream analysis. In this work, we explore Lossless-INR, a lossless INR framework for 3D scientific volumetric data based on bit-plane decomposition. By decomposing each voxel value into binary bit-planes, we reformulate reconstruction as per-bit binary classification, so that exact recovery reduces to predicting every bit correctly. To make this optimization tractable while keeping the representation compact, we combine an octree block-partitioning strategy that adaptively subdivides complex regions with a ternary feature-grid network whose grid entries are parameterized by a ternary set of values. Experiments on diverse volumetric datasets show that this design can achieve zero bit-error rate and bit-exact reconstruction, enabling faithful rendering and downstream analysis with a compact representation. The code is available at https://github.com/TouKaienn/Lossless-INR.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Lossless-INR** 的新型框架，旨在实现科学体积数据的无损隐式神经表示（INR）。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的隐式神经表示（INR）方法（如 NeRF、Instant-NGP 等）在处理体积数据时本质上是**有损的**。对于科学可视化（如流体力学、燃烧模拟）而言，微小的重建误差会在体渲染和下游数值分析中不断累积和放大，导致结果不可靠。
*   **研究动机**：科学领域对数据的精度要求极高，通常需要“位精确”（Bit-exact）的重建。现有的 INR 压缩方法侧重于提高峰值信噪比（PSNR），但无法保证零误差。
*   **整体含义**：本文探索了如何通过架构创新，使 INR 从一种“近似表示工具”转变为一种“精确的、无损的数据存储与分析工具”。

### 2. 方法论：核心思想与关键技术
Lossless-INR 的核心是将传统的**回归任务**（预测连续数值）转变为**逐位的二进制分类任务**。
*   **位平面分解（Bit-plane Decomposition）**：将每个体素的标量值（如 8位或 16位整数）分解为多个二进制位平面。例如，一个 8 位数值被拆分为 8 个 0/1 信号。
*   **二分类重构**：不再使用均方误差（MSE）回归数值，而是针对每个位平面使用交叉熵损失函数进行二分类预测。只要模型对每一位的预测概率超过 0.5，即可实现 100% 准确的位恢复。
*   **八叉树自适应分块（Octree Partitioning）**：为了处理空间复杂度的异质性，采用八叉树对体积进行划分。简单区域使用大块，复杂区域（高频细节）进一步细分，从而优化参数分配。
*   **三元特征网格网络（Ternary Feature-Grid Network）**：
    *   特征网格的条目被限制在三元值集合 $\{-1, 0, 1\}$ 中。
    *   这种离散化设计不仅极大地压缩了模型体积，还配合符号函数（Sign Function）和直通估计器（STE）进行训练，增强了模型对二进制特征的捕捉能力。

### 3. 实验设计
*   **数据集**：涵盖了多种典型的科学体积数据集，包括燃烧模拟（Combustion）、超新星爆发（Supernova）、流体动力学（Jet）等，涵盖了不同的空间分辨率和数据特征。
*   **基准测试（Benchmark）**：
    *   对比了传统的有损 INR 方法（如 Instant-NGP 的变体）。
    *   重点评估指标是 **误码率（BER, Bit-Error Rate）**，目标是达到 **Zero BER**（零误码率）。
    *   同时对比了压缩比（Compression Ratio）和重建后的 PSNR（无损情况下 PSNR 应为无穷大）。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未详细列出具体的 GPU 型号和训练时长。通常此类基于特征网格（Feature Grid）的优化在单张消费级显卡（如 RTX 3090/4090）上即可完成，但具体的训练开销（Epochs）需参考论文正文或开源代码。
*   **代码可用性**：作者提供了 GitHub 开源代码链接，便于社区复现。

### 5. 实验数量与充分性
*   **实验规模**：论文在多个具有代表性的科学数据集上进行了测试，验证了方法在不同数据分布下的普适性。
*   **消融实验**：对八叉树深度、三元值约束以及位平面分解策略进行了消融研究，证明了各组件对实现“无损”目标的必要性。
*   **充分性评价**：实验设计较为充分，特别是通过 BER 指标直接证明了“无损”这一核心主张，比单纯对比 PSNR 更有说服力。

### 6. 主要结论与发现
*   **实现无损化**：Lossless-INR 成功在多个科学数据集上实现了 **Zero BER**，即位精确的无损重建。
*   **紧凑性与精确性的平衡**：通过三元网格和八叉树，该方法在保持无损的同时，依然能提供比原始原始数据更小的存储体积（具备压缩能力）。
*   **下游兼容性**：无损特性使得该表示可以直接用于高精度的科学分析和体渲染，不会引入伪影。

### 7. 优点
*   **精度突破**：打破了 INR 只能做有损压缩的固有印象，满足了科学计算的严苛需求。
*   **架构创新**：将位平面分解与三元神经网络结合，巧妙地利用了分类任务的容错性（只要预测概率 > 0.5 即可）来实现硬性的数值准确。
*   **自适应性**：八叉树结构使得模型能够根据数据的局部复杂度自动调整资源分配。

### 8. 不足与局限
*   **训练开销**：相比于简单的有损压缩，为了达到“零错误”，模型可能需要更长的收敛时间或更精细的超参数调优。
*   **动态数据挑战**：目前主要针对静态 3D 体积，对于随时间变化的 4D 时变科学数据，其存储效率和计算成本尚未深入探讨。
*   **位平面依赖**：对于高位深（如 32 位浮点数）的数据，位平面数量增加可能导致计算量显著上升。

（完）
