---
title: "ATSplat: Compact Feed-forward 3D Gaussian Splatting with Adaptive Token Expansion"
title_zh: ATSplat：具有自适应 Token 扩展的紧凑型前馈 3D 高斯泼溅
authors: "Cho In, Jeonghwan Cho, Mijin Yoo, Gim Hee Lee, Seon Joo Kim"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20417v1"
tags: ["query:q7"]
score: 8.0
evidence: 利用前馈架构加速3D高斯泼溅
tldr: ATSplat 针对现有前馈 3D 高斯泼溅（3DGS）方法中高斯分布冗余且缺乏场景自适应性的问题，提出了一种基于自适应 3D Token 的框架。该方法通过将粗糙的块级深度和相机线索提升为稀疏锚点 Token，并利用自适应 Token 扩展模块根据渲染误差预测不确定性，从而在复杂区域动态增加高斯数量。ATSplat 在保持极高渲染速度的同时，显著减少了高斯基元数量，实现了高效且高质量的场景重建。
selection_source: fresh_fetch
motivation: 现有的前馈 3DGS 方法通常采用像素对齐方式生成高斯，导致基元数量受限于分辨率而非场景复杂度，产生大量冗余。
method: 提出自适应 3D Token 框架，通过稀疏锚点 Token 结合可学习偏移量生成局部高斯，并利用自适应扩展模块在渲染误差较大的区域动态增加 Token。
result: 在 RealEstate10K 和 DL3DV 数据集上达到 SOTA 质量，高斯数量减少 5.7 倍以上，且在单 GPU 上实现亚秒级重建和超千帧的渲染速度。
conclusion: ATSplat 成功将 3DGS 的自适应分配能力引入前馈框架，实现了紧凑表示与高质量渲染的平衡。
---

## 摘要
3D 高斯泼溅 (3DGS) 通过在 3D 空间中优化自由放置的基元，并在重建不足的区域对其进行自适应加密，实现了高质量的新视角合成。然而，现有的前馈 3DGS 方法在很大程度上失去了这种场景自适应的容量分配能力，这些方法通常在输入像素处回归高斯分布，并沿相机射线将其提升。这种像素对齐的表述方式使得基元的数量和位置取决于图像分辨率和输入视角，而非场景复杂度，从而导致产生密集且通常冗余的高斯集合。我们提出了 ATSplat，这是一个前馈 3DGS 框架，通过自适应 3D Token 恢复了 3DGS 优化的自适应分配能力。ATSplat 首先将粗糙的块级深度和相机线索提升为稀疏的 3D 锚点 Token，形成场景的紧凑支架。随后，每个 Token 被回归为具有可学习 3D 偏移的局部高斯分布，从而将基元放置与输入图像网格解耦。自适应 Token 扩展模块预测 Token 级的不确定性分数，由渲染误差图监督，并通过可学习的扩展层选择性地扩展高不确定性 Token。这种从稀疏到自适应的表述使 ATSplat 能够在保持紧凑表示的同时，将基元集中在具有挑战性的区域。在两个代表性数据集 RealEstate10K 和 DL3DV 上的实验表明，与密集前馈 3DGS 方法相比，ATSplat 在实现最先进渲染质量的同时，将高斯分布的数量减少了 5.7 倍以上。对于分辨率为 512 × 960 的 12 张输入图像，ATSplat 在单个商用 GPU 上不到一秒即可完成重建，并能以 1136 FPS 的速度渲染高质量的新视角，且仅需 311K 个高斯分布。

## Abstract
3D Gaussian Splatting (3DGS) achieves high-quality novel-view synthesis by optimizing freely placed primitives in 3D and adaptively densifying them in under-reconstructed regions. However, this scene-adaptive capacity allocation is largely lost in existing feed-forward 3DGS methods, which commonly regress Gaussians at input pixels and lift them along camera rays. Such pixel-aligned formulations make the number and placement of primitives depend on image resolution and input viewpoints rather than scene complexity, resulting in dense and often redundant Gaussian sets. We present ATSplat, a feed-forward 3DGS framework that restores the adaptive allocation capability of 3DGS optimization through Adaptive 3D Tokens. ATSplat first lifts coarse patch-level depth and camera cues into sparse 3D anchor tokens, forming a compact scaffold of the scene. Each token is then regressed into local Gaussians with learnable 3D offsets, decoupling primitive placement from input image grids. An Adaptive Token Expansion module predicts a token-level uncertainty score, supervised by rendering error maps, and selectively expands high-uncertainty tokens through learnable expansion layers. This sparse-to-adaptive formulation enables ATSplat to concentrate primitives in challenging regions while maintaining a compact representation. Experiments on two representative datasets, RealEstate10K and DL3DV, show that ATSplat achieves state-of-the-art rendering quality while reducing the number of Gaussians by more than $5.7\times$ compared with dense feed-forward 3DGS methods. From 12 input images at $512 \times 960$ resolution, ATSplat completes reconstruction in less than a second using a single commercial GPU, and renders high-quality novel views at 1136 FPS ($512 \times 960$) with only 311K Gaussians.

---

## 论文详细总结（自动生成）

以下是对论文《ATSplat: Compact Feed-forward 3D Gaussian Splatting with Adaptive Token Expansion》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的前馈（Feed-forward）3D 高斯泼溅（3DGS）方法大多采用“像素对齐”架构，即在每个输入像素处生成一个高斯基元。
*   **研究动机**：
    *   **冗余性**：像素对齐导致高斯数量随分辨率和视角数线性增长，在平坦或无纹理区域产生大量冗余。
    *   **缺乏自适应性**：这种方式将表示能力固定在图像网格上，无法像原始优化版 3DGS 那样根据场景复杂度（如精细结构）动态分配资源。
    *   **效率瓶颈**：密集的高斯集合导致渲染速度变慢且存储成本高。

### 2. 方法论
ATSplat 提出了一个从“稀疏到自适应”的框架，核心包含三个关键步骤：
*   **稀疏锚点 Token 初始化**：
    *   利用多视角编码器提取粗糙的块级（Patch-level）特征。
    *   预测块级深度，并将其与相机射线结合，将特征提升（Unproject）到 3D 空间，形成稀疏的 3D 锚点 Token 支架。
*   **自适应 Token 扩展（ATE）模块**：
    *   **不确定性预测**：在解码器中，利用轻量级 MLP 预测每个 Token 的不确定性分数。
    *   **监督机制**：通过将中间层高斯渲染出的不确定性图与实际渲染误差图（D-SSIM）进行对比来监督该分数。
    *   **选择性扩展**：识别出重建不足的区域，将高不确定性的 Token 扩展为多个子 Token，从而在复杂区域集中表示能力。
*   **局部高斯回归与 3D 偏移**：
    *   每个精炼后的 Token 被解码为 $K$ 个局部高斯分布。
    *   **解耦设计**：高斯中心通过相对于锚点的 3D 偏移量（Offset）预测，而非强制约束在相机射线上，实现了基元放置与像素网格的解耦。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K**：主要用于室内场景的新视角合成评估。
    *   **DL3DV**：大规模、多样化的室内外场景数据集，用于测试模型处理复杂几何的能力。
*   **Benchmark 与对比方法**：
    *   **前馈方法**：pixelSplat, MVSplat, DepthSplat, iLRM, GS-LRM, TokenGS 等。
    *   **优化方法**（高分辨率下对比）：原始 3DGS, Mip-Splatting。
*   **评估指标**：PSNR, SSIM, LPIPS（质量）；高斯数量（紧凑性）；推理时间/FPS（效率）。

### 4. 资源与算力
论文详细列出了训练所需的算力资源：
*   **训练阶段**：
    *   **基础模型（RE10K）**：使用 4 张 **NVIDIA RTX 4090** GPU，训练约 2 天。
    *   **大规模场景（DL3DV）**：使用 4 张 **NVIDIA H200** GPU，训练 2 天。
    *   **高分辨率微调**：使用 8 张 **NVIDIA H200** GPU，训练少于 2 天。
*   **推理阶段**：在单张 **RTX 3090** GPU 上进行测量。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度测试，包括 2/4/6/12 个输入视角的不同配置，以及 256p 到 960p 的分辨率跨度。
*   **消融实验**：针对 3D 锚点设计（对比像素对齐、纯学习 Token 等）和 ATE 模块（对比随机选择、FPS 采样等）做了详细验证。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了 SOTA 前馈模型，还进行了跨数据集泛化（Zero-shot）实验和外插（Extrapolation）视角实验，证明了模型在未见数据和宽基线场景下的鲁棒性。

### 6. 主要结论与发现
*   **高效紧凑**：ATSplat 在达到或超过 SOTA 渲染质量的前提下，高斯基元数量比密集前馈方法减少了 **5.7 倍**以上。
*   **性能卓越**：在 DL3DV 高分辨率测试中，仅需 311K 个高斯即可实现高质量渲染，重建时间小于 1 秒，渲染速度高达 **1136 FPS**。
*   **自适应有效性**：实验证明，通过学习到的不确定性引导 Token 扩展，模型能准确地在薄结构和复杂纹理处分配更多基元。

### 7. 优点
*   **架构创新**：成功将 3DGS 的“自适应密度控制”思想引入了前馈网络，打破了像素对齐的固有局限。
*   **灵活性**：3D 偏移设计允许高斯基元自由分布在 3D 空间，而不受输入视角射线的严格限制，这在处理视角重叠较小的场景时优势明显。
*   **实用性强**：极高的渲染帧率和极低的高斯数量使其非常适合移动端或实时交互应用。

### 8. 不足与局限
*   **缺乏剪枝机制**：目前模型只做“扩展”不做“剪枝”，可能会在解码过程中保留一些最终变得冗余的 Token。
*   **扩展性挑战**：虽然在 960p 下表现良好，但对于极大规模城市级场景或更高分辨率的扩展仍需进一步研究架构的伸缩性。
*   **位姿依赖**：目前仍依赖于已知的相机位姿（Posed images），尚未完全解决无位姿输入下的鲁棒重建。

（完）
