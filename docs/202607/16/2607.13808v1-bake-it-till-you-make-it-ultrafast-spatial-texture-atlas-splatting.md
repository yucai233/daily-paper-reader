---
title: "Bake It Till You Make It: Ultrafast Spatial Texture-Atlas Splatting"
title_zh: 烘焙即成：超快速空间纹理图集泼溅
authors: "Neel Kelkar, Simon Niedermayr, Kaloian Petkov, Klaus Engel, Rüdiger Westermann"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13808v1"
tags: ["query:q7"]
score: 8.0
evidence: 通过解耦辐射表示和空间哈希网格烘焙实现超快渲染
tldr: 针对3D高斯泼溅（3DGS）在处理精细纹理时渲染开销大的问题，本文提出了一种超快速的空间纹理图集泼溅方法。该方法将低频几何与高频纹理表示解耦，利用2D面片捕捉几何特征，并将高频纹理烘焙至紧凑的纹理图集中。通过稀疏化优化大幅减少面片数量，在保持高保真度的同时，实现了比3DGS快5倍的渲染速度，支持在消费级硬件上进行4K 60FPS实时渲染。
selection_source: fresh_fetch
motivation: 现有的基于哈希网格的3DGS扩展虽然提升了细节，但导致片段渲染计算成本过高，难以实现超高清实时渲染。
method: 采用解耦的辐射场表示，将低频几何与高频纹理分离，并利用稀疏性优化将纹理烘焙到高效的2D纹理图集中。
result: 在保持最先进视觉质量的前提下，实现了比3DGS高出五倍的渲染速度，并在消费级GPU上达到了4K分辨率下的60 FPS。
conclusion: 该方法通过几何稀疏化和高效的纹理映射，成功解决了高质量神经渲染与实时性能之间的矛盾。
---

## 摘要
3D高斯泼溅（3DGS）的最新扩展利用基于哈希网格的外观参数化来捕捉精细的颜色细节，但在片元渲染过程中产生了高昂的计算开销。我们引入了一种解耦的辐射表示，利用2D面元（surfels）建模低频几何和视点相关的外观特征，同时通过烘焙到紧凑纹理图集中的视点无关空间哈希网格来表示高频纹理。通过引入惩罚半透明度和逐原语衰减的稀疏性增强优化，我们的方法能够激进地剪枝不重要的面元，并实现比先前工作显著更快且更稀疏的重建。利用几何稀疏性和高效的GPU纹理映射，我们的方法在保持最先进视觉保真度的同时，实现了比3DGS高达五倍的加速，从而在消费级硬件上实现了60 FPS的实时4K渲染。

## Abstract
Recent extensions of 3D Gaussian Splatting (3DGS) capture fine color details using hash-grid-based appearance parameterization but incur high computational cost during fragment rendering. We introduce a decoupled radiance representation that models low-frequency geometry and view dependent appearance features with 2D surfels while representing high-frequency textures via a view-independent spatial hash grid that is baked into a compact texture atlas. By including sparsity-enhancing optimizations that penalize semi-transparency and per-primitive falloff, our method aggressively prunes insignificant surfels and achieves significantly faster and sparser reconstructions than prior work. Exploiting geometric sparsity and efficient GPU texture mapping, our approach achieves up to a fivefold speedup over 3DGS while preserving state-of-the-art visual fidelity, enabling real-time 4K rendering at 60 FPS on consumer hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bake It Till You Make It: Ultrafast Spatial Texture-Atlas Splatting》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的 3D 高斯泼溅（3DGS）及其变体在处理精细纹理时，往往需要引入复杂的参数化方法（如基于哈希网格的神经表示）。虽然这提升了画质，但导致片元着色（Fragment Shading）阶段的计算开销剧增，难以在消费级硬件上实现超高清（4K）的实时渲染。
*   **研究背景**：传统的 3DGS 依赖大量高斯球，而 2DGS 引入了面元（Surfels）以更好地模拟表面。然而，为了捕捉高频细节，这些方法通常需要极高数量的原语或复杂的实时神经查询。本文旨在通过“解耦”和“烘焙”技术，打破画质与速度之间的权衡。

### 2. 方法论
*   **核心思想**：将辐射场表示解耦为**低频几何/外观**和**高频纹理**两部分。
    *   **低频部分**：使用 2D 面元（Surfels）建模几何形状和与视点相关的低频外观特征。
    *   **高频部分**：使用视点无关的空间哈希网格（Spatial Hash Grid）捕捉细节，并在训练完成后将其“烘焙”到紧凑的 2D 纹理图集（Texture Atlas）中。
*   **关键技术细节**：
    1.  **解耦表示**：每个面元存储位置、旋转、缩放、不透明度以及用于视点相关颜色的球谐函数（SH），而高频细节则由哈希网格提供。
    2.  **稀疏性优化**：引入了针对半透明度和逐原语衰减（Falloff）的惩罚损失函数。这促使模型在训练过程中剔除不重要的面元，使几何表示极其稀疏。
    3.  **烘焙流程**：训练结束后，将空间哈希网格中的高频信息映射并存储到 2D 纹理贴图中。渲染时，GPU 只需进行简单的纹理采样，而非复杂的哈希查找或 MLP 推理。
    4.  **高效渲染器**：利用烘焙后的纹理图集和稀疏的几何体，通过定制的 CUDA 渲染器实现极速泼溅。

### 3. 实验设计
*   **数据集**：采用了神经渲染领域的标准 Benchmark，包括 **Mip-NeRF 360**、**Tanks and Temples** 以及 **Deep Blending** 数据集。
*   **对比方法**：
    *   经典方法：3DGS, 2DGS。
    *   结构化/压缩方法：Scaffold-GS, Sugar。
    *   高性能/神经增强方法：如使用哈希网格的变体。
*   **评价指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似度）以及渲染速度（FPS）。

### 4. 资源与算力
*   **算力设备**：论文在性能评估中使用了 **NVIDIA RTX 4090** 显卡。
*   **渲染表现**：在 RTX 4090 上，该方法能够以 **60 FPS** 的帧率驱动 **4K 分辨率** 的实时渲染。
*   **训练时长**：文中未详细给出所有场景的精确总训练时长，但指出其稀疏化优化过程能显著加快重建速度，且烘焙过程是单次完成的额外开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了多个具有挑战性的室内外场景，进行了详尽的定量对比。
*   **消融实验**：论文对稀疏性惩罚项、纹理图集分辨率等关键组件进行了消融研究，验证了各模块对最终性能和速度的贡献。
*   **充分性评价**：实验设计较为充分，对比了当前最前沿的多种 GS 变体，且在不同复杂度的场景下均表现出一致的性能提升，结果具有较强的说服力。

### 6. 主要结论与发现
*   **速度突破**：该方法实现了比原始 3DGS 快 **5 倍** 的渲染速度。
*   **画质保持**：在大幅减少几何原语数量（稀疏化）的情况下，通过烘焙纹理补偿了细节损失，保持了与 SOTA 方法持平甚至更优的视觉保真度。
*   **实时 4K 成为可能**：证明了通过合理的解耦和硬件友好的烘焙技术，可以在消费级 GPU 上实现影视级的超高清实时神经渲染。

### 7. 优点
*   **极高的渲染效率**：通过将计算压力从实时查询转移到预计算纹理，极大地释放了 GPU 性能。
*   **几何稀疏性**：相比于产生数百万个高斯球的方法，该方法生成的面元数量更少，内存占用更优化。
*   **硬件友好**：利用了成熟的 GPU 纹理映射单元，而非依赖高负载的计算着色器逻辑。

### 8. 不足与局限
*   **内存开销**：虽然几何体稀疏了，但高分辨率的纹理图集（Texture Atlas）会占用额外的显存。
*   **静态限制**：烘焙过程假设场景纹理是静态的（或视点无关的部分占主导），对于具有极强动态光影变化的场景，烘焙效果可能受限。
*   **预处理时间**：虽然渲染极快，但训练过程中的稀疏化优化和最后的烘焙步骤增加了预处理的复杂性。

（完）
