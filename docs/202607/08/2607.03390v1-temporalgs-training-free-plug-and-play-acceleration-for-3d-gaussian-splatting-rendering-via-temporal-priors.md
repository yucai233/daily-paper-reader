---
title: "TemporalGS: Training-Free Plug-and-Play Acceleration for 3D Gaussian Splatting Rendering via Temporal Priors"
title_zh: TemporalGS：通过时间先验实现 3D 高斯泼溅渲染的无需训练、即插即用加速
authors: "Yuhongze Zhou, Zihao Yang, Xinxin Zuo, Juwei Lu"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03390v1"
tags: ["query:q7"]
score: 8.0
evidence: 通过时间先验加速3D高斯泼溅渲染
tldr: 3D Gaussian Splatting (3DGS) 在复杂场景下渲染高分辨率图像仍面临性能瓶颈。本文提出 TemporalGS，这是首个无需训练、即插即用的 3DGS 渲染加速算法。通过利用连续帧间的时空先验信息，该方法引入了时间动态剔除和选择性渲染策略，显著减少了高斯球预处理、排序和光栅化的冗余计算。实验表明，TemporalGS 在保持高质量渲染的同时，实现了高达 1.48 倍的加速，且具有良好的通用性。
selection_source: fresh_fetch
motivation: 针对 3DGS 在处理大量高斯椭球体时渲染速度受限的问题，探索一种无需额外训练或后处理的通用加速方案。
method: 利用几何和外观缓存等时空先验，通过时间动态剔除过滤低贡献高斯点，并结合选择性渲染仅更新无法由先验近似的图块。
result: 在多种场景下实现了最高 1.48 倍的渲染加速，且渲染质量与现有最先进的加速方法相当甚至更优。
conclusion: TemporalGS 为 3DGS 提供了一种简单高效、跨平台的即插即用加速方案，有效提升了实时渲染性能。
---

## 摘要
3D 高斯泼溅 (3DGS) 以其快速且高保真的渲染彻底改变了新视角合成。然而，在各种场景中实现高帧率和低延迟渲染仍然是一个挑战，特别是当场景中出现大量 3D 高斯椭球体时。为了解决这一问题，我们提出了 TemporalGS。据我们所知，这是首个无需训练、即插即用的算法方法，能够在不进行任何后期训练或后期处理的情况下加速 3DGS 渲染，该方法基于分块软件光栅化实现。其核心思想是，不同于 3DGS 独立渲染每一帧，我们利用由新型几何和外观缓冲区等表示的时间先验，来减少连续帧中高斯预处理、排序和光栅化操作的冗余。具体而言，我们提出了两种加速策略：(1) 时间动态剔除，过滤掉对当前帧渲染贡献较小的高斯体；(2) 选择性渲染，仅渲染一小部分无法通过时间先验进行近似的分块。通过调整和交替使用这两种策略，TemporalGS 为 3DGS 渲染加速提供了一种简单而有效的即插即用解决方案，且无需任何训练。大量实验表明，与现有的基于后期训练或后期处理的最先进 3DGS 渲染加速方法相比，TemporalGS 取得了相当甚至更好的性能。TemporalGS 可以显著提高各种 3DGS 方法的渲染速度，实现高达 1.48 倍的加速，同时保持极具竞争力的渲染质量。我们进一步将 TemporalGS 扩展到基于硬件光栅化的 3DGS，以展示我们算法的可移植性。

## Abstract
3D Gaussian Splatting (3DGS) has revolutionized novel-view synthesis with its fast and high-fidelity rendering. However, rendering at high FPS and low latency across various scenes remains a challenge, especially when large amounts of 3D Gaussian ellipsoids appear in the scene. To address this issue, we introduce TemporalGS, to the best of our knowledge, the first training-free plug-and-play algorithmic approach to accelerate 3DGS rendering without any post-training or post-processing, implemented on top of tile-based software rasterization. The key idea is that, instead of rendering frames independently as 3DGS, we leverage the temporal priors, represented by novel geometry and appearance buffers, etc., to reduce redundancy of Gaussian preprocessing, sorting, and rasterization operations of consecutive frames. Specifically, we propose two acceleration strategies: (1) temporal dynamic culling, which filters out Gaussians that contribute less to current frame rendering; (2) selective rendering, which renders only a small portion of tiles that cannot be approximated by the temporal priors. By adapting and interleaving these two strategies, TemporalGS yields a simple but effective plug-and-play solution for 3DGS rendering speed-up without any training. Extensive experiments show that TemporalGS achieves comparable or even better performance compared to existing state-of-the-art post-training or post-processing-based 3DGS rendering acceleration approaches. TemporalGS can significantly enhance the rendering speed of various 3DGS methods, achieving up to $1.48\times$ acceleration, while maintaining competitive rendering quality. We further extend our TemporalGS to hardware rasterization-based 3DGS to show the portability of our algorithm.

---

## 论文详细总结（自动生成）

这是一份关于论文《TemporalGS: Training-Free Plug-and-Play Acceleration for 3D Gaussian Splatting Rendering via Temporal Priors》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管 3D 高斯泼溅（3DGS）在实时渲染方面表现出色，但在处理包含数百万个高斯球的大规模场景时，其预处理、排序和光栅化步骤仍面临巨大的计算压力，难以在各种设备上维持极高帧率（FPS）和低延迟。
*   **研究动机**：
    *   现有的加速方案通常需要繁琐的**后期训练**（如剪枝、压缩）或**后期处理**（如可见性计算），且往往与特定硬件绑定。
    *   作者观察到，在连续渲染的视频流中，相邻帧之间存在极强的时间相干性（Temporal Coherence），即大部分高斯球的贡献和图像内容是重叠的。
    *   **目标**：开发一种**无需训练、即插即用**的算法，利用时间先验直接加速现有的 3DGS 渲染管线。

### 2. 方法论
TemporalGS 的核心思想是利用前一帧的渲染信息（时间先验）来指导当前帧的计算，主要包含以下关键技术：

*   **时间先验构建**：
    *   **时间几何缓冲区 (TGB)**：存储参考帧的深度图及其标准差，通过前向投影（Warping）预测当前帧的表面范围。
    *   **时间外观缓冲区 (TAB)**：缓存参考帧的颜色图像，用于快速近似当前帧内容。
    *   **时间高斯缓冲区**：记录在参考帧中实际产生贡献的高斯球集合。
*   **时间动态剔除 (TDC)**：
    *   **自适应视锥剔除 (AFC)**：根据上一帧贡献高斯球的范围动态调整视锥，过滤掉边界外冗余的高斯球。
    *   **时间遮挡剔除 (TOC)**：利用 TGB 预测的表面，过滤掉位于当前可见表面后方且在上一帧未被渲染的高斯球。
*   **选择性渲染 (SR)**：
    *   **分块检测**：将屏幕划分为 16x16 的图块（Tiles）。通过几何对应关系检测“遮挡图块”（出现新表面的区域）和“未匹配图块”（无法从前一帧投影的区域）。
    *   **混合渲染**：仅对上述检测到的“选定图块”执行完整的高斯渲染管线，其余稳定区域直接通过 TAB 进行反向投影（Warping）填充。
    *   **自适应参考帧插入**：当需要重新渲染的图块比例超过阈值（如 40%）时，强制更新参考帧以防止误差累积。

### 3. 实验设计
*   **数据集与场景**：
    *   **小规模场景**：Mip-NeRF-360（9个场景）。
    *   **大规模航拍/街景**：MatrixCity、Residence、Building、Rubble、Small City。
    *   **大规模室内**：OccluScene3D（具有复杂布局和遮挡）。
*   **对比方法 (Benchmarks)**：
    *   **基准**：Vanilla 3DGS, CityGaussian。
    *   **后期训练/压缩类**：LightGaussian, CP3DGS, C3DGS, StochasticSplats。
    *   **后期处理类**：RadSplat。
    *   **时间相干性类**：NeoG（同期工作）。
*   **评估指标**：PSNR, SSIM, LPIPS（质量指标）以及 FPS（速度指标）。

### 4. 资源与算力
*   **硬件平台**：所有实验均在单张 **NVIDIA RTX 4090 GPU** 上进行。
*   **训练时长**：由于该方法是**无需训练（Training-Free）**的，因此不存在后期训练耗时。它直接作用于已训练好的 3DGS 检查点。
*   **实现细节**：基于 CUDA 实现，并展示了在 WebGPU (web-splat) 等硬件光栅化管线上的可移植性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小规模物体到城市级大规模场景的多种类型，实验设置较为全面。
*   **消融实验**：详细分析了 AFC、TOC、SR 三个模块各自的贡献，并探讨了相机移动速度、超参数（如 λTDC, λSR, η）对性能的影响。
*   **客观性**：对比实验均使用了相同的场景检查点，确保了公平性。同时引入了真实用户漫游轨迹（Real User Trajectories）进行测试，增强了实验的实际说服力。

### 6. 主要结论
*   **显著加速**：TemporalGS 在各种 3DGS 变体上实现了 **1.48倍** 的渲染加速。
*   **质量保持**：在大幅提升 FPS 的同时，PSNR 的下降通常控制在很小的范围内（如 0.5 dB 左右），视觉质量与原始 3DGS 几乎无异。
*   **通用性强**：作为一种算法插件，它可以无缝集成到软件光栅化（CUDA）和硬件光栅化（OpenGL/WebGPU）管线中。
*   **优于同期工作**：相比 NeoG，TemporalGS 在大规模场景下的显存占用更低（仅增加约 0.05x），且加速效果更明显。

### 7. 优点与亮点
*   **即插即用**：无需对现有模型进行任何重新训练或微调，极大降低了部署成本。
*   **算法简洁高效**：通过简单的几何投影和分块选择策略，有效解决了 3DGS 渲染中的冗余计算问题。
*   **大规模场景友好**：在物体多、遮挡复杂的场景中，其剔除和选择性渲染策略表现出更强的优势。

### 8. 不足与局限
*   **运动限制**：该方法依赖于帧间相似性假设。当相机发生**剧烈、极快速移动**或瞬间跳变时，时间先验失效，加速效果会退化，甚至可能产生伪影。
*   **潜在闪烁**：如果自适应参考帧更新频率设置不当，在某些动态区域可能会出现轻微的渲染不一致或闪烁现象。
*   **小场景收益有限**：在视野开阔、遮挡极少的小规模场景中，由于可剔除的高斯球比例较低，加速效果不如大规模场景显著。

（完）
