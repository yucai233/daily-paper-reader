---
title: "RenderFormer++: Scalable and Physically Grounded Feed-Forward Neural Rendering"
title_zh: RenderFormer++：可扩展且符合物理规律的前馈神经渲染
authors: "Huangsheng Du, Haoran Zhu, Youcheng Cai, Jinyang Meng, Ligang Liu"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30380v1"
tags: ["query:q1"]
score: 8.0
evidence: 物理基础的前馈神经渲染
tldr: RenderFormer++ 针对现有 Transformer 神经渲染方法在物理一致性和扩展性上的不足，提出了可扩展且符合物理规律的前馈神经渲染框架。通过引入物理启发式传输引导（PITG）和分层以对象为中心的 Token 化（HOCT），该方法在降低计算成本的同时，实现了复杂大规模场景下具有物理一致性的全局光照渲染，显著提升了跨场景的泛化能力和渲染效率。
selection_source: fresh_fetch
motivation: 现有的神经渲染方法在处理大规模网格场景时，面临计算复杂度高以及缺乏物理一致性的挑战。
method: 提出了物理启发式传输引导（PITG）以增强光线传输建模，并利用分层以对象为中心的 Token 化（HOCT）来压缩特征并降低计算开销。
result: 实验证明该方法在复杂大规模场景中实现了高效、稳定且具有物理准确性的前馈全局光照渲染。
conclusion: RenderFormer++ 通过结合物理先验与高效的 Token 化策略，为大规模场景的实时神经渲染提供了一种极具扩展性的解决方案。
---

## 摘要
我们提出了 RenderFormer++，这是一个针对网格场景全局光照的可扩展且符合物理规律的前馈神经渲染框架。现有的基于 Transformer 的神经渲染方法（如 RenderFormer）虽然实现了良好的跨场景泛化能力，但由于三角形级分词（tokenization）带来的二次方注意力复杂度，面临着物理一致性有限和可扩展性差的问题。为了解决这些问题，我们引入了物理启发式传输引导（Physics-Informed Transport Guidance, PITG），它将渲染方程的归纳偏置嵌入到注意力机制中，并强制执行传输一致性损失，从而实现了符合物理规律的光线传输建模。我们进一步提出了分层以对象为中心的分词（Hierarchical Object-Centric Tokenization, HOCT），通过带有可学习查询的交叉注意力机制将三角形级特征聚合为紧凑的对象级标记（tokens），在保留几何和辐射信息的同时，大幅降低了计算和内存开销。大量实验表明，RenderFormer++ 在复杂的大规模场景中实现了可扩展、稳定且具有泛化能力的前馈全局光照渲染，与之前的神经渲染方法相比，在物理准确性和效率方面均有所提升。

## Abstract
We present RenderFormer++, a scalable and physically grounded feed-forward neural rendering framework for global illumination in mesh scenes. Existing Transformer-based neural rendering methods such as RenderFormer achieve promising cross-scene generalization, but suffer from limited physical consistency and poor scalability due to the quadratic attention complexity of triangle-level tokenization. To address these issues, we introduce Physics-Informed Transport Guidance (PITG), which embeds rendering-equation inductive biases into the attention mechanism and enforces transport consistency loss, enabling physically consistent light transport modeling. We further propose Hierarchical Object-Centric Tokenization (HOCT), which aggregates triangle-level features into compact object-level tokens via cross-attention with learnable queries, substantially reducing computational and memory costs while preserving geometric and radiometric information. Extensive experiments demonstrate that RenderFormer++ achieves scalable, stable, and generalizable feed-forward global illumination rendering across complex large-scale scenes with improved physical accuracy and efficiency over prior neural rendering methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **RenderFormer++**，这是一个旨在解决复杂网格场景中全局光照（Global Illumination, GI）渲染问题的神经渲染框架。以下是对该论文的结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：在离散三角形网格场景中实现高效、可扩展且符合物理规律的全局光照渲染。
*   **研究背景**：
    *   传统的路径追踪（Path Tracing）虽然精确但计算极其昂贵。
    *   现有的基于 Transformer 的神经渲染方法（如 RenderFormer）虽然具备跨场景泛化能力，但存在两个主要缺陷：
        1.  **物理一致性不足**：纯数据驱动的注意力机制难以显式遵循渲染方程的物理定律。
        2.  **扩展性差**：采用三角形级别的分词（Tokenization）策略，导致计算和内存开销随三角形数量呈二次方增长，无法处理大规模复杂场景。

### 2. 方法论
RenderFormer++ 提出了两个核心技术创新来解决上述问题：
*   **物理启发式传输引导（PITG）**：
    *   **发射编码器（Emission Encoder）**：显式提取光源的发射先验，将其注入全局场景 Token 中。
    *   **神经光线传输算子**：模拟渲染方程的递归特性，通过共享参数的 Transformer 层多次迭代（默认 $K=3$），在特征空间模拟光线的多次反弹和散射。
    *   **传输一致性损失（Transport Consistency Loss）**：在训练中约束相邻迭代状态之间的一致性，强制网络学习物理上合理的能量传输。
*   **分层以对象为中心的分词（HOCT）**：
    *   **分层聚合**：不再直接处理海量的三角形，而是先将每个对象的三角形特征通过交叉注意力机制（使用可学习的查询向量）聚合为固定数量（如 8 个）的对象级 Token。
    *   **降低复杂度**：这一策略显著缩短了输入 Transformer 的序列长度，将计算复杂度从三角形数量的二次方降至对象数量的二次方，极大提升了处理大规模场景的能力。
*   **几何引导解码器（GGD）**：
    *   在解码阶段引入 G-buffer（法线、反照率、粗糙度等）作为局部几何引导，结合全局传输特征，通过 Patch-based 解码器生成高动态范围（HDR）图像。

### 3. 实验设计
*   **数据集**：构建了一个包含 50 万个合成场景的大规模数据集（共 200 万张图像）。
    *   **小规模子集**：每个场景 3k-10k 个三角形。
    *   **大规模子集**：每个场景 10k-80k 个三角形（用于测试扩展性）。
*   **基准测试（Benchmark）**：使用 Blender 路径追踪生成的 4096 SPP 图像作为真值（GT）。
*   **对比方法**：主要对比了原始的 RenderFormer 及其微调版本。
*   **评估指标**：L1 损失、MAPE（平均绝对百分比误差）、LPIPS（感知相似度）和 SSIM。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 80GB GPU**。
*   **训练配置**：采用 AdamW 优化器，bfloat16 精度，Flash Attention 加速。
*   **时长**：总计训练了 **500k 次迭代**，耗时约 **7 天**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 2000 个未见场景（包含小规模和大规模）上进行了详尽测试。
*   **消融实验**：针对 PITG、传输一致性损失、迭代次数 $K$、GGD 解码器以及 HOCT 策略分别进行了消融实验。
*   **充分性评价**：实验设计较为全面，不仅验证了渲染质量的提升，还通过控制变量分析了 GPU 显存和运行时间随场景复杂度变化的趋势，客观地证明了该方法在扩展性上的优势。

### 6. 主要结论
*   RenderFormer++ 成功实现了在复杂、大规模网格场景下的前馈全局光照渲染。
*   通过引入物理先验，模型在处理间接光照、软阴影和颜色透射（Color Bleeding）等复杂效果时比纯数据驱动方法更准确。
*   HOCT 策略使得神经渲染能够处理以往方法无法负担的超大规模三角形场景，且保持了较高的推理效率。

### 7. 优点
*   **物理与神经的结合**：将渲染方程的归纳偏置引入 Transformer，提升了结果的真实感。
*   **卓越的扩展性**：对象级分词解决了 Transformer 处理长序列的瓶颈。
*   **强泛化性**：作为前馈模型，无需针对每个新场景进行优化，即可直接生成高质量渲染图。

### 8. 不足与局限
*   **材质简化**：目前的实验主要针对无纹理的对象，材质属性在对象内部是共享的，尚未充分测试复杂纹理贴图下的表现。
*   **对象依赖**：HOCT 依赖于场景的对象级组织结构，如果场景缺乏明确的对象划分（如单一巨大的连通网格），其效率优势可能会减弱。
*   **动态限制**：虽然是前馈的，但对于极高频的动态光影变化，其时间一致性尚未在文中深入探讨。

（完）
