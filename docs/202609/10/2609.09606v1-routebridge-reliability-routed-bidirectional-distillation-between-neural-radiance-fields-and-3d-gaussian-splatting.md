---
title: "RouteBridge: Reliability-Routed Bidirectional Distillation Between Neural Radiance Fields and 3D Gaussian Splatting"
title_zh: RouteBridge：神经辐射场与 3D 高斯溅射之间基于可靠性路由的双向蒸馏
authors: "YuanHang Wang, Xin Cao"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.09606v1"
tags: ["query:profile-1"]
score: 9.0
evidence: NeRF与3D高斯泼溅之间的蒸馏
tldr: RouteBridge是一种用于NeRF与3DGS之间双向蒸馏的框架。针对现有方法固定单一教师模型导致误差传播的问题，该研究提出了一种基于可靠性评估的自适应路由机制，能够根据光线特征动态选择监督方向。通过在NeRF和3DGS间双向传递颜色、不透明度和深度信息，该方法显著提升了两种表示形式的重建质量，在mip-NeRF 360等数据集上表现优于现有主流蒸馏方案。
selection_source: fresh_fetch
motivation: 现有跨表示蒸馏方法通常固定单一教师模型，容易导致局部重建误差在全局范围内传播。
method: 提出一种双向蒸馏框架，通过可靠性评估器结合光度残差与几何证据，为每条光线动态选择最优的监督方向。
result: 在mip-NeRF 360数据集上，3DGS导出质量提升1.56 dB，LPIPS指标降至0.207，优于现有NeRF-GS等方法。
conclusion: 自适应路由机制与几何射线目标是提升跨表示蒸馏效果的关键，证明了双向互补学习的有效性。
---

## 摘要
神经辐射场 (NeRF) 和 3D 高斯溅射 (3DGS) 以互补的归纳偏置对场景进行编码，但现有的跨表示蒸馏通常将一种表示固定为整个场景的教师。全局固定的教师可能会传播局部重建误差。我们提出了 RouteBridge，这是一个为每条射线选择教学方向的双向框架。其可靠性估计器将光度残差与特定表示的几何证据相结合，从而将监督信号从 NeRF 路由至 3DGS，从 3DGS 路由至 NeRF，或选择放弃。一个与渲染器无关的接口可以在不共享特征或点对应关系的情况下传输颜色、不透明度和归一化深度。在 mip-NeRF 360 数据集上，NeRF 和 3DGS 的导出结果分别达到了 28.56 dB 和 28.77 dB。3DGS 的导出结果较 3DGS 提升了 1.56 dB，较 NeRF-GS 提升了 0.45 dB，同时将 LPIPS 降低至 0.207。在静态三视图 DTU 数据集上，RouteBridge 获得了 21.12 dB 的成绩。消融实验表明，自适应路由和几何射线目标均对性能提升做出了贡献。

## Abstract
Neural radiance fields (NeRFs) and 3D Gaussian Splatting (3DGS) encode a scene with complementary inductive biases, but existing cross-representation distillation typically fixes one representation as teacher for the entire scene. A globally fixed teacher can propagate local reconstruction errors. We present RouteBridge, a bidirectional framework that selects the teaching direction for each ray. Its reliability estimator combines photometric residuals with representation-specific geometric evidence and routes supervision from NeRF to 3DGS, from 3DGS to NeRF, or abstains. A renderer-independent interface transfers color, opacity, and normalized depth without shared features or point correspondence. On mip-NeRF 360, the NeRF and 3DGS exports reach 28.56 and 28.77 dB, respectively. The 3DGS export improves over 3DGS by 1.56 dB and over NeRF-GS by 0.45 dB while reducing LPIPS to 0.207. On static three-view DTU, RouteBridge obtains 21.12 dB. Ablations show that both adaptive routing and geometric ray targets contribute to the improvement.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《RouteBridge: Reliability-Routed Bidirectional Distillation Between Neural Radiance Fields and 3D Gaussian Splatting》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：NeRF（隐式场）与 3DGS（显式基元）在场景表示上具有互补的归纳偏置。
*   **问题**：现有的跨表示蒸馏方法通常将一种表示固定为“教师”，这会导致局部重建误差在全局范围内传播。此外，现有方法往往忽略了不同表示在不同视角或区域下的相对优势。
*   **动机**：研究者提出，教学方向不应由表示类型决定，而应根据每条光线的局部证据动态选择。

### 2. 方法论：RouteBridge
*   **核心思想**：构建一个双向蒸馏框架，通过“可靠性路由”机制，在训练过程中动态决定 NeRF 教 3DGS、3DGS 教 NeRF，或两者均不进行蒸馏（放弃）。
*   **关键技术**：
    *   **独立分支**：NeRF 和 3DGS 分别独立初始化和训练，不共享参数或特征骨干，确保模型可独立部署。
    *   **可靠性估计器**：结合光度残差（Photometric Residuals）与特定表示的几何证据（NeRF 使用终止熵，3DGS 使用贡献深度方差），计算每条光线的可靠性分数。
    *   **路由逻辑**：基于可靠性分数差值与阈值，通过门控机制（Gate）选择监督方向。
    *   **渲染器无关接口**：通过颜色、不透明度和归一化深度进行蒸馏，无需点对应或复杂的特征对齐。

### 3. 实验设计
*   **数据集**：
    *   **mip-NeRF 360**：包含 9 个室内外场景，用于评估高质量新视角合成。
    *   **静态三视图 DTU**：包含 15 个场景，用于评估稀疏视角下的重建能力。
*   **对比基准**：
    *   对比了 Instant-NGP、3DGS、Scaffold-GS、Hash-GS、VDGS 以及最接近的联合优化方法 NeRF-GS。
    *   在稀疏视角下对比了 3DGS、2DGS 和 SplatFields。

### 4. 资源与算力
*   **说明**：论文中未明确列出具体的 GPU 型号、数量或总训练时长。
*   **训练预算**：文中提到所有变体均使用统一的训练预算：5k 步预热（Warm-up）+ 25k 步联合优化，且保持渲染光线数量一致，以确保对比的公平性。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了主对比实验（性能提升）、稀疏视角实验（鲁棒性验证）以及消融实验（验证路由机制与几何目标的有效性）。
*   **客观性**：通过对比固定教师策略与 RGB 仅蒸馏策略，证明了自适应路由和多模态（颜色+深度+不透明度）目标的必要性。实验设计较为严谨，且使用了公开 benchmark。

### 6. 主要结论与发现
*   **性能提升**：在 mip-NeRF 360 上，3DGS 导出质量较原版提升 1.56 dB，较 NeRF-GS 提升 0.45 dB，且 LPIPS 指标更优。
*   **双向互补**：证明了 NeRF 和 3DGS 可以作为彼此的局部教师，无需共享特征即可实现性能提升。
*   **路由有效性**：自适应路由优于全局固定教师策略，证明了局部可靠性评估在跨表示蒸馏中的关键作用。

### 7. 优点
*   **灵活性**：无需辅助转换网络，训练完成后两个分支均可独立部署，不增加推理开销。
*   **通用性**：渲染器无关的接口设计，使其能够兼容不同的 NeRF 或 3DGS 变体。
*   **公平性**：通过严格控制训练步数和光线预算，消除了因计算资源差异带来的性能偏差。

### 8. 不足与局限
*   **训练开销**：训练阶段需要同时维护两个表示，内存占用较高。
*   **启发式评估**：可靠性分数基于启发式规则，而非严格的概率不确定性估计，在某些极端情况下可能失效。
*   **局限性**：当两个分支在同一区域同时出现错误时，路由机制无法纠正误差；且该方法主要针对静态场景，未涉及动态场景的扩展。

（完）
