---
title: "WS-NeRF: A Mamba-Driven World-State-Aware Adaptive Deblurring Neural Radiance Field"
title_zh: WS-NeRF：一种基于 Mamba 驱动的世界状态感知自适应去模糊神经辐射场
authors: "Hang Jiang, Jinghao Wang, Yiming Zhang, Xinhong Wang, Luwei Ran, Yinfeng Yu"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21391v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于高质量3D重建的神经辐射场 (NeRF)
tldr: WS-NeRF 是一款针对模糊图像重建的神经辐射场模型。该研究将辐射场的交替优化建模为具有时间记忆的动态演化过程，引入 Mamba 架构捕捉长程依赖，并结合多维世界状态感知与专家混合机制，实现了自适应的去模糊处理，显著提升了在模糊输入下的 3D 重建质量与一致性。
selection_source: fresh_fetch
motivation: 现有 NeRF 方法高度依赖高质量清晰图像，难以处理现实场景中常见的模糊退化问题。
method: 提出一种基于 Mamba 的世界状态感知框架，通过动态演化过程与专家混合机制自适应调整去模糊先验。
result: 实验表明该方法在 PSNR、SSIM 和 LPIPS 指标上均优于现有技术，且迭代恢复过程更加稳定。
conclusion: WS-NeRF 有效解决了模糊环境下的 3D 重建难题，为复杂场景下的高质量视图合成提供了鲁棒的解决方案。
---

## 摘要
神经辐射场（NeRF）因其在高质量三维重建和多视角图像新视角合成方面的强大能力，近年来受到了广泛关注。现有方法通常依赖高质量的清晰输入，而现实世界中的图像采集极易受到模糊退化的影响，这严重制约了 NeRF 的重建质量。在本文中，我们提出了一种名为 WS-NeRF 的新型 Mamba 驱动的世界状态感知自适应去模糊神经辐射场，旨在解决图像退化和三维不一致性问题。我们将辐射场的交替优化过程构建为一个具有时间记忆的动态演化过程，并联合利用多维世界状态和专家混合机制，动态调整去模糊先验的置信度。实验结果表明，WS-NeRF 显著提升了模糊辐射场的重建质量，在 PSNR、SSIM 和 LPIPS 指标上均取得了更好的性能，同时表现出更稳定的迭代恢复行为。

## Abstract
Neural Radiance Fields (NeRF) have attracted extensive attention in recent years due to their strong capability for high-quality 3D reconstruction and novel view synthesis from multi-view images. Existing methods usually rely on high-quality sharp inputs, while real-world image acquisition is highly susceptible to blur degradation, which severely affects the reconstruction quality of NeRF. In this paper, we propose a novel Mamba-driven world-state-aware adaptive deblurring neural radiance field, termed WS-NeRF, to address image degradation and 3D inconsistency. We formulate the alternating optimization of radiance fields as a dynamic evolution process with temporal memory, and jointly exploit comprehensive multi-dimensional world states and a mixture-of-experts mechanism to dynamically adjust the confidence of deblurring priors. Experimental results show that WS-NeRF significantly improves blurry radiance field reconstruction quality, achieving better performance on PSNR, SSIM, and LPIPS, while exhibiting more stable iterative recovery behavior.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity。作为你的学术论文分析助手，我已对《WS-NeRF: A Mamba-Driven World-State-Aware Adaptive Deblurring Neural Radiance Field》进行了深入分析。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：NeRF 在高质量 3D 重建和新视角合成方面表现卓越，但其性能高度依赖于清晰的输入图像。
*   **痛点**：现实场景中，长曝光或相机运动导致的模糊会严重破坏 NeRF 的物理假设，导致 3D 重建质量下降。
*   **挑战**：现有的交替优化方法（2D 去模糊与 3D 重建）存在三个核心难题：2D 先验与 3D 几何约束的维度不匹配、先验融合缺乏置信度闭环、以及缺乏对优化过程的时间状态建模。

### 2. 方法论：WS-NeRF
*   **核心思想**：将辐射场去模糊建模为具有“时间记忆”的动态演化系统，通过感知世界状态来动态调节 2D 去模糊先验的置信度。
*   **关键技术**：
    *   **多维世界状态观察器 (WSO)**：采用双流架构，宏观上利用 DINOv2 提取语义差异，微观上利用 Sobel/Laplacian 算子提取物理残差，实时诊断几何退化。
    *   **Mamba 驱动的时间演化 (TEP)**：引入 Mamba 状态空间模型，捕捉优化轨迹的长程依赖，防止因盲目信任早期不稳定的 3D 先验而导致的误差累积。
    *   **自适应控制器与 MoE**：利用专家混合 (MoE) 机制，根据当前状态动态分配 2D 先验的权重，并引入“进度感知阻尼”机制，在优化后期抑制过拟合与震荡。
*   **优化目标**：构建了一个复合损失函数，通过置信度分数 $C_t$ 和阻尼指标 $I_{damp}$ 对先验引导损失进行实时调制。

### 3. 实验设计
*   **数据集**：
    *   **BlurRF-Synth**：包含 65 个训练场景和 10 个测试场景，涵盖 6-DOF 运动模糊和散焦模糊，并注入了 RAW 空间噪声。
    *   **BlurRF-Real**：包含 5 个真实低光照、严重运动模糊场景。
*   **对比方法**：与 Deblur-NeRF、BAD-NeRF、DP-NeRF、ExBluRF、PDRF、Deblurring-3DGS、BAGS 及 DeepDeblurRF 等主流方法进行了对比。
*   **评价指标**：PSNR、SSIM、LPIPS（合成场景）；NIQE、ARNIQA（真实场景）。

### 4. 资源与算力
*   **硬件**：单张 NVIDIA GPU。
*   **训练配置**：每场景训练 200k 次迭代，批次大小为 4096 条射线。
*   **其他**：DINOv2 (ViT-Small) 权重冻结，Adam 优化器，学习率采用指数衰减策略。

### 5. 实验数量与充分性
*   **实验覆盖**：包含定量对比（表 I）、消融实验（表 II）以及定性可视化分析（图 4）。
*   **客观性**：消融实验分别验证了世界状态感知 (WS)、Mamba 模块和多策略融合模块的有效性，证明了各组件对性能提升的贡献。实验设计较为严谨，涵盖了合成与真实场景，具有较好的说服力。

### 6. 主要结论与发现
*   WS-NeRF 在处理复杂模糊退化时表现出更强的鲁棒性，在 PSNR 和 SSIM 等指标上优于现有基准。
*   引入 Mamba 模块能有效减少交替优化过程中的震荡，使收敛过程更加稳定。
*   动态阻尼机制成功解决了早期优化阶段因错误先验引导导致的伪影问题。

### 7. 优点
*   **创新性**：首次将 Mamba 架构引入 NeRF 优化轨迹建模，解决了传统方法缺乏时间记忆的问题。
*   **闭环控制**：通过“感知-决策-反馈”的闭环机制，实现了对 2D 先验注入强度的精细化控制。
*   **多粒度感知**：结合了高层语义（DINOv2）与底层物理特征，感知能力更全面。

### 8. 不足与局限
*   **超参数敏感**：模型对初始超参数的设置较为敏感。
*   **场景局限**：在纹理缺失或曝光不足的场景中，2D 去模糊先验的泛化能力较弱，可能影响整体重建效果。
*   **未来方向**：作者指出未来将探索“无先验”的生成式物理反演，以及在严重退化下的联合位姿与辐射场校准。

（完）
