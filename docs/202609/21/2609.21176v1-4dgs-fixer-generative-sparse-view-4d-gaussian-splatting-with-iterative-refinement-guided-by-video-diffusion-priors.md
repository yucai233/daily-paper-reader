---
title: "4DGS-Fixer: Generative Sparse-View 4D Gaussian Splatting with Iterative Refinement Guided by Video Diffusion Priors"
title_zh: 4DGS-Fixer：基于视频扩散先验引导迭代优化的生成式稀疏视角 4D 高斯溅射
authors: "Haitao Huang, Shenghao Zhao, Boyuan Tian, Shin-Fang Chng, Songlin Yang, Sheila Lim, Huangying Zhan, Yi Xu, Anyi Rao, Frank Guan"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21176v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于动态场景合成的4D高斯泼溅 (Gaussian Splatting)
tldr: 针对稀疏视角下动态场景重建中几何信息缺失和初始化不佳的问题，本文提出了4DGS-Fixer框架。该方法通过多视角深度估计生成稠密点云以优化初始化，并利用预训练视频扩散模型对新视角渲染序列进行修复，作为伪监督信号迭代优化4D高斯表征，显著提升了动态场景的重建质量与一致性。
selection_source: fresh_fetch
motivation: 稀疏视角下的动态场景重建面临严重的病态问题，且传统COLMAP初始化导致几何信息严重缺失。
method: 通过多视角深度融合提供稠密几何初始化，并引入视频扩散模型对渲染序列进行修复，实现对4DGS的迭代式伪监督优化。
result: 在基准数据集上表现优异，PSNR指标较现有最优方法提升了近2 dB。
conclusion: 该方法有效解决了稀疏视角下动态场景重建的几何缺失问题，实现了高质量的4D场景合成。
---

## 摘要
本文旨在解决稀疏视角视频下的动态场景合成难题。现有方法通常采用几何先验、自适应优化或密度控制策略来改善稀疏观测下的 4D 高斯建模，但这些方法无法从根本上解决因观测不足和场景信息缺失所导致的不适定问题。此外，稀疏视角 4D 高斯溅射（4DGS）往往面临几何初始化质量较差的问题：在仅有少量输入视角的情况下，COLMAP 通常只能重建出稀疏且不完整的点云，导致场景中大片区域缺乏足够的高斯支撑，难以通过后续优化进行恢复。为解决上述局限，我们提出了一种基于视频扩散模型的迭代优化框架，以提升动态 4D 场景的完整性与一致性。具体而言，我们首先估计多视角深度图并将其融合为稠密点云，从而为动态 4DGS 表示提供更完整的几何初始化。随后，我们利用预训练的视频修复模型，对沿不同时间步长的新视角轨迹渲染出的序列进行优化。这些修复后的序列作为伪监督信号，用于正则化并迭代优化 4DGS 表示。在广泛使用的基准数据集上的实验表明，我们的方法显著优于现有基准，PSNR 指标较当前最优方法提升了近 2 dB。

## Abstract
This paper addresses the challenges of dynamic scene synthesis from sparse-view videos. Existing methods employ geometric priors, adaptive optimization, or density-control strategies to improve 4D Gaussian modeling under sparse observations. However, they cannot fundamentally resolve the ill-posed problem caused by insufficient observations and missing scene information. Moreover, sparse-view 4D Gaussian Splatting (4DGS) often suffers from poor geometric initialization: with only a few input views, COLMAP typically reconstructs sparse and incomplete point clouds, leaving large scene regions without sufficient Gaussian support and making them difficult to recover through subsequent optimization. To address these limitations, we propose a novel iterative refinement framework based on a video diffusion model to improve the completeness and consistency of dynamic 4D scenes. Specifically, we first estimate multi-view depth maps and fuse them into dense point clouds to provide more complete geometric initialization for a dynamic 4DGS representation. We then employ a pretrained video restoration model to refine sequences rendered along novel camera trajectories at different time steps. The restored sequences serve as pseudo-supervision to regularize and iteratively refine the 4DGS representation. Experiments on a widely used benchmark dataset demonstrate that our method substantially outperforms existing baselines, achieving nearly a 2 dB PSNR improvement over the previous best-performing method.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《4DGS-Fixer: Generative Sparse-View 4D Gaussian Splatting with Iterative Refinement Guided by Video Diffusion Priors》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：动态场景的 4D 高斯溅射（4DGS）通常依赖密集且同步的相机阵列。在实际应用中，往往只能获取极少数视角的视频，导致重建任务成为严重的“病态问题”。
*   **痛点**：
    *   **几何初始化缺失**：传统 COLMAP 在稀疏视角下无法生成完整点云，导致 4DGS 缺乏足够的初始支撑，后续优化难以恢复缺失区域。
    *   **观测信息不足**：现有方法受限于输入视角的视觉证据，无法推断出未观测区域的场景信息。

### 2. 方法论
*   **核心思想**：采用“稠密几何初始化 + 视频扩散模型引导的迭代优化”两阶段框架。
*   **关键技术**：
    *   **稠密几何初始化**：利用 `MVSAnywhere` 对四视角视频进行多视角深度估计，并将深度图融合为稠密点云，替代传统的稀疏 COLMAP 点云，为 4DGS 提供更强的几何先验。
    *   **视频扩散引导的迭代优化**：
        *   **轨迹采样**：在相机对之间采样新视角轨迹，渲染出包含伪影的视频序列。
        *   **参考引导修复**：利用预训练的 `CogVideoV2V` 模型，以原始观测视角为锚点，对渲染序列进行修复，生成高质量的伪监督信号。
        *   **迭代更新**：将修复后的视频作为伪监督，通过损失函数（STG 损失 + 生成损失）对 4DGS 进行迭代优化。
*   **算法流程**：通过缓存机制（Cache）周期性更新伪监督目标，平衡计算开销与生成随机性，确保 4DGS 随优化过程不断逼近真实场景。

### 3. 实验设计
*   **数据集**：使用 `Neural3DV` 基准数据集（包含 6 个动态场景，分辨率 2704×2028，30 FPS）。
*   **基准与对比**：选取 4 个最分散的视角作为训练集，其余作为测试集。对比方法包括：`STGS`、`4DGS`、`4DGaussians` 以及当前最优的 `4C4D`。

### 4. 资源与算力
*   **说明**：论文未明确列出具体的 GPU 型号、数量或总训练时长。仅在消融实验中提到，使用 `MVSAnywhere` 进行稠密初始化的预处理时间从 COLMAP 的约 20 分钟缩短至约 3 分钟。

### 5. 实验数量与充分性
*   **实验覆盖**：包含定量对比（PSNR, DSSIM, LPIPS 指标）、定性可视化对比以及详细的阶段性消融实验（对比 COLMAP 与 MVS 初始化、验证不同迭代轮次的效果）。
*   **评价**：实验设计较为客观，通过与当前 SOTA 方法（4C4D）的直接对比，证明了方法的有效性。消融实验清晰地拆解了各模块对性能提升的贡献。

### 6. 主要结论与发现
*   **性能提升**：4DGS-Fixer 在 `Neural3DV` 上表现优异，PSNR 指标较之前的最优方法 `4C4D` 提升了 1.89 dB，证明了生成式先验在稀疏视角重建中的巨大潜力。
*   **有效性**：稠密初始化显著提升了重建效率与几何完整性，而视频扩散模型的引入有效修复了未观测区域的空洞与模糊。

### 7. 优点
*   **创新性**：巧妙地将视频扩散模型（生成式先验）与 4DGS（显式表征）结合，解决了稀疏视角下的信息缺失问题。
*   **实用性**：通过缓存机制降低了扩散模型推理的计算压力，使得迭代优化在工程上可行。
*   **鲁棒性**：通过多视角深度融合，显著改善了初始化的质量，减少了对 COLMAP 的依赖。

### 8. 不足与局限
*   **计算开销**：尽管使用了缓存机制，但引入预训练视频扩散模型进行迭代优化依然具有较高的计算成本。
*   **生成依赖**：伪监督信号的质量高度依赖于预训练模型（CogVideoV2V）的生成能力，若模型产生伪影，可能会误导 4DGS 的优化方向。
*   **场景泛化**：目前主要在标准基准上验证，对于极其复杂或非刚性极强的真实世界场景，其泛化能力和稳定性仍需进一步探索。

（完）
