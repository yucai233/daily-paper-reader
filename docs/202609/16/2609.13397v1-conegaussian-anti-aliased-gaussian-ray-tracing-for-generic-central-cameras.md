---
title: "ConeGaussian: Anti-Aliased Gaussian Ray-Tracing for Generic Central Cameras"
title_zh: ConeGaussian：面向通用中心相机的抗锯齿高斯光线追踪
authors: "Deheng Zhang, Letian Shi, Runyi Yang, Zhendong Li, Lei Sun, Kanzhi Wu, Ajad Chhatkuli, Danda Pani Paudel, Luc Van Gool"
date: 2026-09-11
pdf: "https://arxiv.org/pdf/2609.13397v1"
tags: ["query:q7"]
score: 8.0
evidence: 用于神经渲染的抗锯齿高斯射线追踪
tldr: ConeGaussian 是一种通用的抗锯齿高斯射线追踪框架，旨在解决现有高斯渲染在不同相机模型下因忽略像素足迹导致的锯齿问题。该方法通过相机原生逆映射构建各向异性足迹，并推导出闭式解进行滤波，同时引入场景空间频率下限以优化渲染质量。它不仅适用于多种中心相机模型，还能在保持清晰度的同时显著提升渲染性能，在针孔和鱼眼相机上均表现出优异的抗锯齿效果。
selection_source: fresh_fetch
motivation: 现有高斯射线追踪通常将像素简化为单条中心射线，导致在缩小场景时产生锯齿，在放大时出现频率伪影。
method: 通过相机原生逆映射构建各向异性像素足迹，并结合局部线性近似与场景空间频率下限实现通用的抗锯齿渲染。
result: "在针孔和鱼眼相机数据集上，该方法显著提升了渲染质量，在 1/8 分辨率下 PSNR 提升高达 4.3 dB，鱼眼图像 LPIPS 降低 30%。"
conclusion: ConeGaussian 提供了一种与相机模型无关的通用抗锯齿方案，有效解决了复杂相机模型下的渲染失真问题。
---

## 摘要
在渲染中，相机是一个将每个有限像素映射为光束的采样算子。不同的相机模型会改变光束的几何结构，这使得建立统一且精确的渲染公式极具挑战性。因此，尽管高斯光线追踪通过逆光线映射支持通用相机（具有光学中心），但通常将每个像素简化为单条中心光线。这忽略了与相机相关的像素足迹，导致缩小显示时产生锯齿，而无约束的高斯函数在放大时会暴露出不支持的频率。我们提出了 ConeGaussian，这是一个用于基于高斯光线渲染的、与相机模型无关的抗锯齿框架。ConeGaussian 不在特定相机的图像平面上定义像素滤波器，而是直接利用相机原生逆映射产生的相邻光线构建各向异性足迹。我们推导了在有限像素足迹的局部线性、深度局部、矩匹配近似下的闭式响应，同时相同的几何结构定义了每个高斯的训练频率下限。值得注意的是，通过这种构建方式，我们的滤波原理可以在已校准的中心相机模型和多种高斯光线渲染主干网络中直接使用，无需修改。此外，与 mip-splatting 不同，我们的场景空间频率下限和滤波机制实现了渲染时的简单合成，从而消除了多余的模糊。在针孔相机和强畸变鱼眼相机的拍摄场景中，ConeGaussian 在两种不同的基于光线的主干网络上均实现了持续提升，在 1/8 分辨率下性能提升高达 4.3 dB，并将鱼眼相机的 LPIPS 指标降低了 30%，解决了透视屏幕平面足迹公式无法直接应用的问题。

## Abstract
In rendering, a camera is a sampling operator that maps each finite pixel to a bundle of rays. Different camera models change the geometry of this bundle, thus making a unified and faithful rendering formulation challenging. Consequently, Gaussian ray tracing supports generic cameras (with optical center) through their inverse ray mappings, yet typically reduces every pixel to a single center ray. This ignores the camera-dependent pixel footprint, causing aliasing under minification, while unconstrained Gaussians expose unsupported frequencies under magnification. We present ConeGaussian, a camera-model-agnostic anti-aliasing framework for Gaussian ray-based rendering. Instead of defining the pixel filter on a camera-specific image plane, ConeGaussian constructs an anisotropic footprint directly from neighboring rays produced by the camera's native inverse mapping. We derive a closed-form response under a locally linear, depth-local, moment-matched approximation of the finite pixel footprint, while the same geometry defines a per-Gaussian training-frequency floor. Notably, by construction, our filtering principle can be used unmodified across calibrated central camera models and multiple Gaussian ray-rendering backbones. Additionally, unlike in mip-splatting, our scene-space frequency floor and filtering enable trivial composition at render time, allowing us to remove excess blurring. On pinhole and strongly distorted fisheye captures, ConeGaussian consistently improves two distinct ray-based backbones, by up to 4.3 dB at 1/8 resolution, and reduces fisheye LPIPS by 30% where perspective screen-plane footprint formulations are not directly applicable.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《ConeGaussian: Anti-Aliased Gaussian Ray-Tracing for Generic Central Cameras》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：3D 高斯泼溅（3DGS）在渲染方面表现出色，但现有的射线追踪渲染器通常将每个像素简化为单条中心射线。
*   **问题**：这种简化忽略了相机相关的像素足迹（Pixel Footprint），导致在缩小（Minification）时产生严重的锯齿效应，而在放大（Magnification）时，由于缺乏频率约束，高斯函数会表现出不支持的高频伪影。
*   **目标**：提出一种与相机模型无关的通用抗锯齿框架，能够处理包括针孔和强畸变鱼眼在内的多种中心相机模型。

### 2. 方法论
*   **核心思想**：将像素视为一个有限的“光束”而非单条射线，通过相机原生的逆映射直接构建各向异性（Anisotropic）足迹。
*   **关键技术**：
    *   **射线足迹构建**：利用相邻光线的微分信息，在最大响应深度处构建一个各向异性协方差矩阵，用于描述像素的覆盖范围。
    *   **闭式响应推导**：在局部线性、深度局部和矩匹配假设下，推导出高斯函数与像素足迹卷积的闭式解。
    *   **频率下限（Frequency Floor）**：引入场景空间频率下限，通过限制高斯的最小尺度来抑制放大时的伪影。
    *   **边际合成（Marginal Composition）**：通过一种特殊的合成规则，仅在渲染时添加超出频率下限的方差，避免了多余的模糊。

### 3. 实验设计
*   **数据集**：涵盖了室内外场景，包括 ScanNet++（鱼眼）、Zip-NeRF（鱼眼）和 Mip-NeRF 360（针孔）。
*   **对比方法**：对比了 3DGEER、3DGUT 等射线追踪基准，以及 Mip-Splatting、Analytic-Splatting 等屏幕空间抗锯齿方法。
*   **评估指标**：PSNR、SSIM 和 LPIPS。

### 4. 资源与算力
*   **说明**：论文中未明确列出具体的 GPU 型号、数量或训练时长等硬件配置细节。

### 5. 实验数量与充分性
*   **实验覆盖**：包含多尺度训练与测试（MTMT）、单尺度训练与多尺度测试（STMT）、跨相机模型渲染（如鱼眼转针孔）以及详细的消融实验。
*   **客观性**：实验设计涵盖了多种相机类型和分辨率，通过与多种基准方法对比，证明了该方法的通用性和鲁棒性，实验设计较为充分且客观。

### 6. 主要结论与发现
*   **性能提升**：在 1/8 分辨率下，PSNR 提升高达 4.3 dB；在鱼眼相机上，LPIPS 指标降低了 30%。
*   **通用性**：该方法不依赖于特定的屏幕空间投影，因此可以无缝集成到不同的射线追踪渲染器（如 3DGEER 和 3DGUT）中。
*   **抗锯齿效果**：在缩小场景时有效抑制了锯齿，在放大场景时保留了细节，且在强畸变边缘处表现优于现有方法。

### 7. 优点
*   **相机无关性**：通过逆光线映射构建足迹，使其能够适配任何具有光学中心的相机模型。
*   **数学严谨性**：推导了闭式解，避免了复杂的采样过程，且通过边际合成规则解决了模糊问题。
*   **模块化**：该滤波框架可以作为插件直接嵌入现有的射线追踪渲染器中，具有很高的可移植性。

### 8. 不足与局限
*   **相机模型限制**：目前仅支持具有单一光学中心的中心相机模型。
*   **未覆盖场景**：对于折反射相机（Catadioptric cameras）或具有滚动快门（Rolling-shutter）效应的复杂相机模型，该方法尚不支持。
*   **假设依赖**：依赖于局部线性假设，在极端畸变或极近距离下，该近似可能存在误差。

（完）
