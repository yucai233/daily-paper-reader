---
title: Thread-Efficient Decoding for Neural Texture Compression
title_zh: 神经纹理压缩的线程高效解码
authors: "Janarbek Matai, Sho Ikeda, Lukasz Lipski, Takahiro Harada"
date: 2026-08-28
pdf: "https://arxiv.org/pdf/2608.27888v1"
tags: ["query:q7"]
score: 6.0
evidence: 神经纹理压缩与共享解码器以加速渲染
tldr: "神经纹理压缩（NTC）虽压缩率高，但因GPU线程分歧导致运行效率低。本文提出一种共享解码器MLP架构，结合渐进式解码器冻结训练方案和基于CLIP嵌入的语义聚类策略。该方法在保持渲染质量的同时，将线程分歧降低了25%-52%，在Radeon RX 9070 XT上实现了高达8.48倍的加速，显著提升了NTC在实时渲染中的实用性。"
selection_source: fresh_fetch
motivation: 神经纹理压缩在GPU上运行时存在严重的线程分歧问题，限制了其在实时渲染中的性能表现。
method: 提出共享解码器架构，通过CLIP语义聚类分组纹理，并采用渐进式解码器冻结策略进行稳定训练。
result: "在500多个纹理上验证，线程分歧减少了25%-52%，且在特定GPU上实现了最高8.48倍的解码加速。"
conclusion: 通过共享解码器和纹理聚类，该研究有效解决了NTC的线程分歧瓶颈，大幅提升了神经纹理的解码效率。
---

## 摘要
神经纹理压缩 (NTC) 实现了比 BCn 格式更高的压缩率，但受限于 GPU 线程分歧 (thread divergence)，这显著降低了运行时性能。在这项工作中，我们提出了一种共享解码器 MLP 架构——采用渐进式解码器冻结计划进行训练——并结合纹理聚类，在保持渲染质量的同时，将线程分歧减少了 25%-52%。我们在超过 500 个纹理和多个真实渲染场景上评估了我们的方法，结果表明，与非共享基准相比，在 Radeon RX 9070 XT GPU 上实现了高达 8.48 倍的加速。我们的主要贡献包括：(1) 一种统一的共享解码器架构，通过对纹理进行分组来减少分歧；(2) 一种带有渐进式解码器冻结的训练方案，提高了稳定性和重建精度；(3) 一种使用 CLIP 嵌入的语义聚类策略，将相似纹理分组以实现有效的解码器共享；以及 (4) 验证我们方法的全面性能评估和消融研究。

## Abstract
Neural texture compression (NTC) achieves higher compression ratios than BCn formats but suffers from GPU thread divergence, which significantly reduces runtime performance. In this work, we propose a shared decoder MLP architecture -- trained with a gradual decoder freezing schedule -- combined with texture clustering to reduce thread divergence by 25%-52% while preserving rendering quality. We evaluate our method on over 500 textures and multiple real rendering scenes, demonstrating up to 8.48x speedup on the Radeon RX 9070 XT GPU compared to non-shared baselines. Our key contributions include: (1) a unified shared decoder architecture that reduces divergence by grouping textures; (2) a training recipe with gradual decoder freezing that improves stability and reconstruction accuracy; (3) a semantic clustering strategy using CLIP embeddings that groups similar textures for effective decoder sharing; and (4) comprehensive performance and ablation studies validating our approach.