---
title: "ADATEX4D: adaptive texture capacity allocation for 4D gaussian splatting"
title_zh: ADATEX4D：面向4D高斯泼溅的自适应纹理容量分配
authors: "De Jiang, Peiqiang Wang, Kehong Yuan, Shaohua Ma"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.29963v1"
tags: ["query:q1"]
score: 8.0
evidence: 4D高斯泼溅渲染的自适应纹理分配
tldr: AdaTex4D是一种针对4D高斯溅射的自适应纹理容量分配方法。针对传统方法为所有图元分配相同纹理分辨率导致的存储浪费问题，该方法通过基于可见性和局部形变尺度，动态调整每个高斯图元携带的RGBA三平面分辨率，在显著降低存储成本的同时保持了高质量的重建效果。
selection_source: fresh_fetch
motivation: 现有4D高斯溅射中统一的纹理分配策略导致了低细节或弱可见区域的存储资源浪费。
method: 引入自适应纹理容量模块，根据归一化的屏幕空间梯度和形变局部尺度，独立调整每个高斯图元RGBA三平面的轴向分辨率。
result: 在N3DV和PanopticSports数据集上，该方法在减少超过一半纹理存储的同时，保持了重建质量，并在固定内存预算下优于统一分配策略。
conclusion: 动态、各向异性的纹理分配是提升4D高斯表示中局部外观容量分配效率的有效途径。
---

## 摘要
纹理高斯模型提升了局部外观的表现力，但为每个图元分配相同的纹理分辨率会导致在低细节或弱可见区域造成存储浪费。我们提出了AdaTex4D，这是一个用于基于形变的4D高斯泼溅（4D Gaussian Splatting）的自适应纹理容量模块。每个高斯图元携带压缩的RGBA三平面（triplanes），其两个轴根据可见性归一化的屏幕空间梯度和形变后的局部尺度独立增长。在N3DV和PanopticSports数据集上的实验表明，AdaTex4D在保持重建质量的同时，将纹理存储需求降低了一半以上。在固定的内存预算下，自适应分配相比均匀纹理分配不仅提升了质量，还降低了整体模型大小和峰值内存占用。这些结果表明，动态、各向异性的纹理分配为在4D高斯表示中分配局部外观容量提供了一种更高效的方法。

## Abstract
Textured Gaussians improve local appearance capacity, but assigning the same texture resolution to every primitive wastes storage on low-detail or weakly visible regions. We introduce AdaTex4D, an adaptive texture-capacity module for deformation-based 4D Gaussian Splatting. Each Gaussian carries packed RGBA triplanes whose two axes grow independently according to visibility normalized screen-space gradients and deformed local scales. Experiments on N3DV and PanopticSports show that AdaTex4D reduces texture storage by more than half while preserving reconstruction quality. Under fixed memory budgets, adaptive allocation also improves quality over uniform texture assignment and reduces overall model and peak memory. These results show that dynamic, anisotropic texture allocation provides a more efficient way to distribute local appearance capacity in 4D Gaussian representations.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《ADATEX4D: Adaptive Texture Capacity Allocation for 4D Gaussian Splatting》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：4D 高斯泼溅（4DGS）通过形变建模动态场景，但现有方法通常为每个高斯图元分配统一的纹理分辨率。
*   **问题**：这种“一刀切”的分配方式导致存储资源被浪费在低细节、弱可见或运动简单的区域，而关键细节区域的容量却不足。
*   **目标**：实现一种动态、各向异性的纹理容量分配机制，使纹理资源能够根据场景的动态特性（可见性、形变尺度、细节需求）进行精准投放。

### 2. 方法论：AdaTex4D
*   **核心思想**：在冻结的 4DGS 几何骨架基础上，引入一个可学习的残差纹理分支。每个高斯图元携带三个 RGBA 三平面（tri-planes），其分辨率可根据需求从 1×1 动态增长至 4×4。
*   **关键技术**：
    *   **动态证据聚合**：利用可见性归一化的图像空间梯度（反映细节需求）和形变后的局部尺度（反映几何覆盖范围）作为分配依据。
    *   **各向异性增长规则**：根据高斯图元在形变后的局部尺度比例，独立调整纹理平面的两个轴向分辨率。若某轴向需求高，则优先在该方向增长。
    *   **稳定性策略**：通过混合持久性需求（均值）和峰值需求（最大值）来确定分配分数，避免因瞬时噪声导致纹理参数频繁波动。

### 3. 实验设计
*   **数据集**：使用了 N3DV（6 个场景）和 PanopticSports（6 个场景，共 12 个场景）。
*   **对比方法**：主要对比了基础的 4DGaussians、Uniform Textured 4DGS（统一纹理分配）以及 STG（Spacetime Gaussian Feature Splatting）。
*   **Benchmark**：评估指标包括 PSNR、LPIPS、SSIM/DSSIM，以及存储占用（MiB）、峰值内存和渲染帧率（FPS）。

### 4. 资源与算力
*   **算力配置**：文中提到使用 8 个数据并行工作节点（workers）进行训练。
*   **训练时长**：平均训练时长约为 1.64 至 2.18 小时。
*   **硬件细节**：文中未明确指定具体的 GPU 型号，仅提到在渲染时仍需物化稠密纹理，这限制了当前的实时吞吐量提升。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了主质量对比、固定内存预算下的性能对比、固定高斯图元数量下的性能对比，以及详细的消融实验（验证时间峰值、各向异性增长、可见性归一化的有效性）。
*   **客观性**：采用了统一的随机种子（6666）和预计算相机调度，并使用配对的 Bootstrap 置信区间进行统计分析，实验设计严谨且具有较好的公平性。

### 6. 主要结论与发现
*   **存储效率**：AdaTex4D 在保持重建质量的前提下，平均减少了 47.3% 的纹理存储空间。
*   **性能提升**：在 80 MiB 的紧凑内存预算下，AdaTex4D 的 PSNR 指标显著优于统一纹理分配方法。
*   **动态分配的必要性**：实验证明，针对 4D 场景的动态、各向异性分配比静态分配策略更能有效利用有限的存储预算。

### 7. 优点
*   **高效的资源分配**：成功将有限的纹理容量从冗余区域转移到高细节区域，实现了“好钢用在刀刃上”。
*   **兼容性强**：作为一种附加模块，它可以在不改变原有 4DGS 几何骨架和形变场的情况下进行优化。
*   **各向异性设计**：考虑到高斯图元在形变后的非均匀分布，各向异性增长规则比简单的各向同性缩放更符合物理特性。

### 8. 不足与局限
*   **渲染开销**：由于当前渲染器仍需物化 4×4 的稠密纹理，存储节省尚未直接转化为渲染速度（FPS）的提升。
*   **参数依赖**：依赖于预训练的 4DGS 骨架，如果骨架本身存在严重的几何缺陷，纹理分配的优化空间有限。
*   **应用限制**：该方法主要针对基于形变的 4DGS，对于其他类型的动态场景表示（如基于网格或纯隐式表示）的适用性未做探讨。

（完）
