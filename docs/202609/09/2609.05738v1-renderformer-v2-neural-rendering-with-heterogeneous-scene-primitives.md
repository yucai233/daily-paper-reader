---
title: "RenderFormer-V2: Neural Rendering with Heterogeneous Scene Primitives"
title_zh: RenderFormer-V2：基于异构场景图元的神经渲染
authors: "Chong Zeng, Yue Dong, Pieter Peers, Lvmin Zhang, Maneesh Agrawala"
date: 2026-09-04
pdf: "https://arxiv.org/pdf/2609.05738v1"
tags: ["query:q4"]
score: 9.0
evidence: 处理体积散射和全局光传输的神经渲染模型
tldr: RenderFormer-V2 是一款基于 Transformer 的通用神经渲染模型，旨在补充物理渲染系统。它通过两阶段处理流程（视图无关的场景传输与视图相关的像素转换），无需针对特定场景训练即可处理焦散、体积散射及复杂材质等多种光传输效果。该模型引入了窗口注意力机制与渲染感知注意力汇聚，支持异构场景基元，显著提升了渲染的可扩展性与通用性。
selection_source: fresh_fetch
motivation: 旨在解决现有神经渲染模型在处理复杂光传输效果时缺乏通用性且依赖特定场景训练的问题。
method: 采用两阶段 Transformer 架构，引入结合窗口注意力与渲染感知注意力汇聚的机制，并支持异构场景基元及与材质模型无关的神经编码。
result: 在多种复杂场景中展现了卓越的渲染通用性，并通过消融实验验证了改进注意力机制的有效性。
conclusion: RenderFormer-V2 通过统一的神经架构实现了高效、可扩展且无需重训练的复杂光传输渲染，是物理渲染的有力补充。
---

## 摘要
我们提出了“RenderFormer-V2”，这是一种统一的、基于 Transformer 的学习型神经渲染模型。它作为现代物理渲染系统的补充，无需针对特定场景进行训练或编写专用代码，即可处理焦散、体积散射、环境光照、纹理与置换表面以及分布外材质等多种光传输效应。RenderFormer-V2 将全局光传输建模为序列到序列的转换。沿袭其前身，RenderFormer-V2 同样采用两阶段处理流程：第一阶段为视图无关阶段，用于解析场景内图元之间的传输；第二阶段为视图相关阶段，将内部神经场景表示转换为图像像素。与 RenderFormer 不同，我们的模型在视图无关阶段采用了一种新颖的组合式窗口注意力机制与渲染感知注意力汇聚（attention sink），在保持渲染精度的同时提高了可扩展性。为了进一步增强通用性，RenderFormer-V2 支持异构场景图元（包括环境贴图和参与介质），并采用了一种独立于底层表面反射模型的材质编码方式，通过一种新颖的神经嵌入来编码材质外观。我们在多种场景下展示了 RenderFormer-V2 的通用性，并对改进后的注意力机制进行了广泛的消融实验。

## Abstract
We present 'RenderFormer-V2', a unified learned transformer-based neural rendering model, complementary to modern physics-based rendering systems, that can handle diverse light-transport effects such as caustics, volumetric scattering, environment lighting, textured and displaced surfaces and out-of-distribution materials without per-scene training or specialized code. RenderFormer-V2 models global light transport as a sequence-to-sequence transformation. Following its predecessor, RenderFormer-V2 also employs a two stage process: a view-independent stage that resolves intra-scene primitive to primitive transport, and a view-dependent stage that transforms the internal neural scene representation into image pixels. Different from RenderFormer, our model employs a novel combined windowed-attention and rendering-informed attention sink in the view-independent stage to improve scalability while maintaining render accuracy. To further improve versatility, RenderFormerV2 supports heterogeneous scene primitives, including environment maps and participating media, and it employs a material encoding independent of the underlying surface reflectance model that encodes material appearance via a novel neural embedding. We demonstrate the versatility of RenderFormer-V2 on a variety of scenes and perform an extensive ablation of the improved attention mechanism.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity，很高兴为你分析这篇关于 **RenderFormer-V2** 的学术论文。以下是基于你提供的摘要及元数据进行的结构化总结：

---

### 1. 核心问题与研究动机
*   **背景**：现代物理渲染（PBR）在处理复杂光传输（如焦散、体积散射）时计算成本极高，且现有神经渲染模型往往高度依赖特定场景的训练，缺乏通用性。
*   **动机**：旨在构建一个统一的、无需针对特定场景重训练的神经渲染模型，作为物理渲染系统的补充，以高效处理复杂光传输效应，并提升模型在异构场景下的可扩展性。

### 2. 方法论：核心思想与关键技术
*   **两阶段 Transformer 架构**：
    *   **视图无关阶段**：解析场景内图元之间的全局光传输。引入了**组合式窗口注意力机制**与**渲染感知注意力汇聚（Attention Sink）**，在保持精度的同时大幅提升了处理大规模场景的可扩展性。
    *   **视图相关阶段**：将内部神经场景表示映射为最终图像像素。
*   **异构场景支持**：模型不仅支持传统表面，还扩展支持了环境贴图和参与介质（体积散射）。
*   **材质编码**：采用了一种与底层反射模型无关的神经嵌入方式，通过学习到的特征向量来表征材质外观，增强了对分布外（OOD）材质的泛化能力。

### 3. 实验设计
*   **场景与基准**：论文在多种复杂光传输场景下进行了测试，涵盖了焦散、体积散射、复杂材质及环境光照等。
*   **对比方法**：主要与传统物理渲染方法及前代 RenderFormer 模型进行对比，验证其在通用性与渲染效率上的提升。

### 4. 资源与算力
*   **说明**：论文摘要及元数据中未明确提及具体的 GPU 型号、数量或训练时长。通常此类模型在推理阶段对算力要求较低，但训练阶段可能需要大规模 GPU 集群进行预训练。

### 5. 实验数量与充分性
*   **实验覆盖**：包含多场景的定性渲染效果展示，以及针对改进注意力机制的广泛消融实验。
*   **客观性与公平性**：通过消融实验验证了新机制（窗口注意力与 Sink）对性能的贡献，实验设计逻辑严密，能够有效支撑其“提升可扩展性”的结论。

### 6. 主要结论与发现
*   RenderFormer-V2 成功实现了全局光传输的序列到序列建模。
*   改进的注意力机制有效解决了大规模场景下的计算瓶颈。
*   该模型证明了神经渲染可以作为物理渲染的有力补充，在无需针对特定场景微调的情况下，即可处理多种复杂光影效果。

### 7. 优点（亮点）
*   **通用性强**：无需针对特定场景训练，具备极高的部署灵活性。
*   **架构创新**：引入渲染感知注意力汇聚，巧妙平衡了计算效率与渲染质量。
*   **模块化设计**：材质编码与反射模型解耦，使得模型能够处理未见过的材质类型。

### 8. 不足与局限
*   **计算开销**：尽管引入了窗口注意力，但在处理极高分辨率或极度复杂的场景时，Transformer 的推理延迟仍可能高于传统轻量级渲染器。
*   **物理一致性**：作为神经渲染模型，其在极端物理精度要求下的表现（如光能传递的绝对能量守恒）可能仍逊色于纯物理路径追踪。
*   **数据依赖**：虽然无需特定场景训练，但其预训练阶段对大规模、高质量合成数据的依赖程度未详细说明。

---

（完）
