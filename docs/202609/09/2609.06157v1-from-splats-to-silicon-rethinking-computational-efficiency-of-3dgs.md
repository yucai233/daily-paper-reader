---
title: "From Splats to Silicon: Rethinking Computational Efficiency of 3DGS"
title_zh: 从高斯溅射到硅片：重新思考 3DGS 的计算效率
authors: "Minnan Pei, Qiwei Dong, Yihan Zhou, Gang Li, Yuchen Zhu, Wenju Zhao, Zhongtian Long, Siting Wang, Peisong Wang, Jian Cheng"
date: 2026-09-05
pdf: "https://arxiv.org/pdf/2609.06157v1"
tags: ["query:q7"]
score: 8.0
evidence: 重新思考3DGS渲染的计算效率与架构支持
tldr: 本文针对3D高斯泼溅（3DGS）在不同场景和硬件下计算效率差异巨大的问题，提出了一种以工作负载为中心的分析框架。通过对现有算法、GPU运行时及硬件架构的系统性评估与性能剖析，揭示了系统性能增益与工作负载缩减、执行粒度匹配及数据传输开销之间的深层联系，为3DGS的系统优化与评估提供了标准化指导。
selection_source: fresh_fetch
motivation: 现有3DGS研究在不同渲染路径和平台下的效率评估缺乏统一标准，难以量化各项优化对端到端性能的实际贡献。
method: 构建了一个以工作负载为中心的分析框架，结合文献综述与受控GPU性能剖析，追踪高斯选择、屏幕空间计算及数据移动对系统性能的影响。
result: 研究发现系统性能提升取决于工作负载的有效缩减、计算阶段的粒度匹配以及数据传输与同步开销的平衡。
conclusion: 未来3DGS系统设计应在渲染质量约束下进行更一致的评估，并重点关注数据流优化与硬件架构的协同设计。
---

## 摘要
3D 高斯溅射（3DGS）利用显式图元表示场景并支持实时新视角合成，但其系统效率在不同场景、视角、渲染路径和平台约束下存在显著差异。现有研究通过表示与算法设计、GPU 运行时优化以及架构支持来追求效率，但其报告的增益对应于渲染和更新路径上的不同点。将这些指标与端到端系统收益联系起来，需要追踪每项优化如何改变高斯选择、屏幕空间工作量、数据移动以及阶段或帧时间。因此，我们使用一个以工作负载为中心的框架，将表示与算法研究、GPU 运行时和硬件架构联系起来，并识别重复出现的工作负载模式。我们通过文献分析、复现测量以及对选定实现的受控 GPU 分析来补充研究，将工作负载计数与阶段时间和内存流量关联起来。这些比较共同表明，系统增益取决于工作负载的减少是否能传导至下游执行、各阶段的粒度匹配，以及数据传输、同步、缓存结果、梯度和优化器数据的成本。基于这些发现，我们讨论了在渲染质量约束下更一致的评估方法，并指出了未来系统设计的关键方向。

## Abstract
3D Gaussian splatting (3DGS) represents scenes with explicit primitives and supports real-time novel-view synthesis, yet its system efficiency varies substantially across scenes, viewpoints, rendering paths, and platform constraints. Existing studies pursue efficiency through representation and algorithm design, GPU runtime optimization, and architectural support, but their reported gains correspond to different points along the rendering and update paths. Connecting these indicators to end-to-end system benefit requires tracing how each optimization changes Gaussian selection, screen-space work, data movement, and stage or frame time. We therefore use a workload-centric framework to connect representation and algorithm research, GPU runtimes, and hardware architectures and to identify recurring workload patterns. We complement literature analysis with reproduced measurements and controlled GPU profiling of selected implementations, relating workload counts to stage time and memory traffic. Together, these comparisons show that system gains depend on workload reductions reaching downstream execution, granularity matching each stage, and the cost of data transfers, synchronization, and cached results, gradients, and optimizer data. Building on these findings, we discuss more consistent evaluation under rendering-quality constraints and identify key directions for future system design.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Splats to Silicon: Rethinking Computational Efficiency of 3DGS》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：3D 高斯溅射（3DGS）已成为实时新视角合成的主流技术，但其在不同场景、视角及硬件平台上的系统效率表现极不稳定。
*   **动机**：现有的效率优化研究（算法、GPU 运行时、硬件架构）往往关注渲染路径的不同环节，缺乏统一的评估标准。研究者难以判断某项优化对端到端性能的真实贡献，导致“局部加速”可能无法转化为“系统级收益”。

### 2. 方法论：以工作负载为中心的分析框架
*   **核心思想**：通过追踪 3DGS 从场景表示到最终渲染的完整路径，建立一套标准化的工作负载度量体系。
*   **关键指标**：定义了五个核心计数指标：
    *   $N_{resident}$（驻留高斯数）、$N_{active}$（活跃高斯数）、$N_{pair}$（高斯-瓦片关联数）、$N_{eval}$（评估次数）、$N_{blend}$（混合贡献数）。
*   **分析逻辑**：将所有优化方法映射到“投影-排序-混合”路径上，分析其如何通过“消除、重组、重映射或复用”工作负载来影响系统性能。

### 3. 实验设计
*   **基准与对比**：对比了多种代表性的 3DGS 实现（如 Graphdeco 原版、gsplat、FlashGS、Faster-GS 等）。
*   **场景设置**：使用了标准数据集（如 bicycle, bonsai, truck 等），并在受控条件下（固定分辨率、瓦片大小、视角）进行性能剖析。
*   **评估维度**：不仅关注渲染速度（FPS），还深入分析了内存流量（DRAM traffic）、内核执行时间占比及图像质量（PSNR/SSIM/LPIPS）。

### 4. 资源与算力
*   **硬件平台**：主要使用 NVIDIA RTX 3090 GPU 进行受控性能剖析。
*   **训练/推理**：论文未明确给出所有实验的统一训练时长，但强调了不同实现（如 gsplat vs. Graphdeco）在相同输入下的内存分配与带宽消耗差异。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从算法压缩、运行时调度到硬件架构设计的广泛文献（图 1 所示的数十种方法）。
*   **客观性**：论文通过“受控 GPU 性能剖析”和“相同输入下的后端对比”来确保公平性。实验设计非常严谨，特别是在分析 tile 大小对性能的影响时，展示了非线性的权衡关系。

### 6. 主要结论与发现
*   **工作负载传导**：上游的优化（如剪枝）必须能有效减少下游的计算或数据移动，否则无法提升系统性能。
*   **粒度权衡**：不同渲染阶段对粒度（如瓦片大小）的需求不同，不存在单一的最优粒度。
*   **瓶颈转移**：局部加速（如 rasterization）后，瓶颈往往会转移到其他环节（如 Adam 优化器更新或数据传输），系统设计需关注全路径。

### 7. 优点
*   **系统性强**：首次将算法、运行时和硬件架构整合在一个统一的分析框架下，极具学术价值。
*   **洞察深刻**：揭示了“评估次数减少不等于执行时间缩短”等反直觉现象，对后续研究具有极强的指导意义。
*   **工具开源**：提供了 GSPROF 工具包，方便社区进行标准化性能评估。

### 8. 不足与局限
*   **跨平台泛化性**：虽然分析了多种架构，但大部分受控实验基于 NVIDIA GPU，对移动端 NPU 或专用 ASIC 的评估依赖于文献数据，可能存在偏差。
*   **动态场景覆盖**：虽然提到了动态场景，但针对复杂动态 SLAM 系统的长期稳定性评估仍有待进一步细化。
*   **质量约束的复杂性**：不同优化方法对图像质量的损害程度不同，论文虽提及质量约束，但并未给出统一的“质量-效率”帕累托前沿对比。

（完）
