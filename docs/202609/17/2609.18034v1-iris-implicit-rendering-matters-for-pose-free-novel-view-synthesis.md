---
title: "IRIS: Implicit Rendering Matters for Pose-Free Novel View Synthesis"
title_zh: IRIS：隐式渲染对于无姿态新视角合成至关重要
authors: "Wenyu Li, Sidun Liu, Peng Qiao, Yong Dou, Tongrui Hu"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18034v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于隐式渲染的潜空间神经场
tldr: IRIS是一种用于无姿态多视图新视角合成的自监督框架。针对现有隐式渲染缺乏几何约束与显式表示优化困难的矛盾，IRIS通过将场景表示为潜在神经场，利用参考视图投影特征在采样点聚合，实现了灵活且具备几何结构的渲染。该方法在无需姿态监督的情况下，实现了高质量的新视角合成与准确的相机位姿估计。
selection_source: fresh_fetch
motivation: 现有方法在隐式渲染的灵活性与显式表示的几何约束之间难以平衡，且无姿态监督下的优化极具挑战。
method: IRIS通过将场景建模为潜在神经场，利用参考视图的投影特征在3D空间点进行聚合，从而在自预测相机参数下实现新视角渲染。
result: 实验表明，IRIS在完全自监督的条件下，能够实现高质量的新视角合成，并达到具有竞争力的相机位姿估计精度。
conclusion: IRIS通过引入几何结构化的隐式表示，为无姿态多视图合成提供了一种兼顾灵活性与优化稳定性的有效方案。
---

## 摘要
从无姿态的多视角图像中进行新视角合成仍然具有挑战性，因为模型必须在没有姿态监督的情况下联合学习场景表示和相机参数。现有的方法主要分为两个极端：隐式潜在空间渲染灵活且易于优化，但往往导致相机估计的几何基础较弱；显式 3D 表示提供了更强的几何基础，但引入了更繁重的参数化和更脆弱的优化过程。在本文中，我们提出了 IRIS，这是一个完全自监督的框架，在上述两种范式之间提供了一个实用的折中方案。IRIS 不解码自由的潜在标记，也不重建完全显式的 3D 基元，而是将场景表示为潜在神经场，并通过在自预测相机下查询该场来渲染新视角。具体而言，来自参考视角的投影特征在采样的 3D 点处进行聚合，形成点状潜在特征，然后沿目标射线进行组合以进行渲染。这种设计保留了隐式建模的灵活性和优化稳定性，同时引入了比无约束潜在渲染更强的几何结构。大量实验表明，IRIS 在完全自监督学习下实现了高质量的新视角合成，并具有极具竞争力的姿态估计精度。项目主页：https://leo-frank.github.io/IRIS

## Abstract
Novel view synthesis from unposed multi-view images remains challenging, as the model must jointly learn scene representations and camera parameters without pose supervision. Existing approaches largely fall into two extremes: implicit latent-space rendering is flexible and easy to optimize, but often yields weakly grounded camera estimation; explicit 3D representations provide stronger geometric grounding, but introduce heavier parameterization and more fragile optimization. In this paper, we present IRIS, a fully self-supervised framework that provides a practical middle ground between these two paradigms. Instead of decoding free latent tokens or reconstructing fully explicit 3D primitives, IRIS represents the scene as a latent neural field and renders novel views by querying this field under self-predicted cameras. Specifically, projected features from reference views are aggregated at sampled 3D points to form point-wise latent features, which are then composed along target rays for rendering. This design preserves the flexibility and optimization stability of implicit modeling, while introducing stronger geometric structure than unconstrained latent rendering. Extensive experiments show that IRIS achieves strong novel view synthesis quality with competitive pose accuracy under fully self-supervised learning. Our project page: https://leo-frank.github.io/IRIS

---

## 论文详细总结（自动生成）

这是一份关于论文 **《IRIS: Implicit Rendering Matters for Pose-Free Novel View Synthesis》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：新视角合成（NVS）通常依赖于预先获取的相机位姿（如通过 SfM 管道），这在弱纹理、遮挡或大规模数据场景下极不稳定且成本高昂。
*   **动机**：现有的无姿态（Pose-free）自监督方法存在两极分化：
    *   **隐式渲染（如 RayZer）**：灵活、易优化，但相机估计缺乏物理几何基础。
    *   **显式表示（如 E-RayZer）**：几何基础强，但参数化重、优化过程极其脆弱。
*   **核心目标**：IRIS 旨在寻找一种“中间地带”，既保留隐式建模的优化稳定性，又通过结构化的查询方式引入几何约束，实现高质量合成与准确的位姿估计。

### 2. 方法论：IRIS
*   **核心思想**：将场景表示为“潜在神经场（Latent Neural Field）”，通过在自预测相机下查询该场来渲染新视角，而非直接解码潜在标记或重建显式 3D 基元。
*   **关键流程**：
    1.  **共享多视图编码**：使用 Muskie 作为骨干网络，联合提取参考视图的视觉特征并预测相机参数（相对位姿与共享内参）。
    2.  **潜在场构建**：对于 3D 空间中的查询点，将其投影到所有参考视图中采样特征，通过 Transformer 聚合形成点级潜在特征。
    3.  **射线渲染**：沿目标射线采样多个点，利用第二个 Transformer 将点级特征组合为射线级特征，最终预测颜色。
*   **几何约束**：通过将渲染过程显式地与自预测相机参数挂钩，强制模型学习符合多视图几何一致性的表示。

### 3. 实验设计
*   **数据集**：涵盖了 Re10K、DL3DV、CO3Dv2、ARKitScenes 和 WildRGBD 等多种室内外场景。
*   **Benchmark**：主要在 DL3DV、NRGBD、7Scenes 和 ScanNet++ 上进行测试。
*   **对比方法**：
    *   **姿态条件方法**：MVSplat, LVSM。
    *   **姿态监督方法**：NoPoSplat。
    *   **自监督方法**：SelfSplat, SPFSplat, RayZer, E-RayZer。

### 4. 资源与算力
*   **硬件**：使用 8 块 A100 GPU 进行训练。
*   **训练时长**：在 DL3DV 数据集上训练约 7 天，在混合数据集设置下约 14 天。

### 5. 实验数量与充分性
*   **充分性**：实验设计较为全面，涵盖了单数据集训练、多数据集混合训练、稀疏视图（2-view）测试以及跨数据集泛化测试。
*   **消融实验**：对骨干网络初始化（Muskie vs. Random/DINOv3）、渲染器设计（隐式 vs. 体渲染/3DGS）进行了深入对比，证明了设计选择的合理性。
*   **公平性**：对比了多种基线，并针对 RayZer 仅适用于有序序列的局限性，专门设置了“随机顺序（Rand）”测试，体现了评估的客观性。

### 6. 主要结论与发现
*   **性能优势**：IRIS 在无姿态监督的情况下，渲染质量（PSNR/SSIM）优于现有的自监督方法，且在位姿估计精度上具有竞争力。
*   **泛化能力**：相比显式 3D 表示方法（E-RayZer），IRIS 在跨数据集泛化时表现出更强的鲁棒性。
*   **渲染机制**：学习到的射线聚合渲染器比传统的手工体渲染规则更具表达力，能更好地捕捉细节。

### 7. 优点
*   **平衡性**：成功在隐式灵活性与显式几何约束之间找到了平衡点，避免了 3DGS 等显式表示在自监督优化中的不稳定性。
*   **鲁棒性**：不依赖于图像的固定顺序，能够处理无序的多视图输入。
*   **高质量**：在完全自监督的设定下，其合成质量甚至在某些指标上超过了部分需要姿态监督的方法。

### 8. 不足与局限
*   **推理延迟**：由于采用了 Transformer 进行多视图聚合和射线聚合，推理延迟随参考视图和目标视图数量的增加而显著上升，比 E-RayZer 更慢。
*   **混合训练收益有限**：文中提到简单地混合多源数据并未带来预期的性能提升，暗示在异构数据上的统一表示学习仍有待突破。
*   **稀疏视图挑战**：在极度稀疏（2-view）的场景下，与依赖姿态监督的方法相比仍存在差距。

（完）
