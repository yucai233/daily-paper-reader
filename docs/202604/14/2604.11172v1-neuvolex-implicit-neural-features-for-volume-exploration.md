---
title: "NeuVolEx: Implicit Neural Features for Volume Exploration"
title_zh: NeuVolEx：用于体数据探索的隐式神经特征
authors: "Haill An, Suhyeon Kim, Donghyuk Choo, Younhyun Jung"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11172v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于直接体渲染的隐式神经表示
tldr: 本文提出NeuVolEx，将隐式神经表示（INR）的应用从体数据压缩扩展到体数据探索。针对现有方法在捕捉空间关联和处理有限监督时的不足，NeuVolEx利用INR训练中学习到的特征，并通过结构编码器和多任务学习增强空间一致性。该方法在传递函数设计和视点推荐任务中表现出色，支持稀疏监督下的准确分类和无监督聚类，提升了体数据探索的效率。
selection_source: fresh_fetch
motivation: 现有的体数据特征表示难以兼顾广域几何模式捕捉与有限监督下的鲁棒性，而隐式神经表示在特征提取方面的潜力尚未被充分利用。
method: 在基础INR之上增加结构编码器和多任务学习方案，利用训练过程中产生的隐式特征进行ROI刻画与分类。
result: 在多种模态数据集上，NeuVolEx实现了稀疏监督下的精确传递函数设计，并能通过无监督聚类推荐互补的观察视点。
conclusion: NeuVolEx证明了隐式神经特征在体数据探索中的有效性，在功能性和易用性上均优于传统显式或卷积特征方法。
---

## 摘要
直接体绘制 (DVR) 旨在帮助用户识别和检查体数据中的感兴趣区域 (ROI)，而支持有效 ROI 分类和聚类的特征表示在体数据探索中起着基础性作用。现有方法通常依赖于显式局部特征表示，或从原始体数据中学习到的隐式卷积特征表示。然而，显式局部特征表示在捕捉更广泛的几何模式和空间相关性方面存在局限性，而隐式卷积特征表示在用户监督通常有限的实际应用中，并不一定能确保鲁棒的性能。与此同时，隐式神经表示 (INR) 近期在用于体数据压缩的 DVR 中展现出巨大潜力，这归功于其紧凑参数化连续体场的能力。在这项工作中，我们提出了 NeuVolEx，这是一种神经体数据探索方法，将 INR 的作用扩展到了体数据压缩之外。与以往关注 INR 输出的压缩方法不同，NeuVolEx 利用 INR 训练过程中学习到的特征表示作为体数据探索的鲁棒基础。为了使这些特征表示更好地适应探索任务，我们通过结构编码器和多任务学习方案增强了基础 INR，从而提高了 ROI 表征的空间相干性。我们在两个基础体数据探索任务上验证了 NeuVolEx：基于图像的传递函数 (TF) 设计和视点推荐。NeuVolEx 在基于图像的 TF 设计中实现了稀疏用户监督下的准确 ROI 分类，并支持无监督聚类以识别紧凑的互补视点，从而揭示不同的 ROI 簇。在具有不同模态和 ROI 复杂度的多样化体数据集上的实验表明，NeuVolEx 在有效性和可用性方面均优于以往方法。

## Abstract
Direct volume rendering (DVR) aims to help users identify and examine regions of interest (ROIs) within volumetric data, and feature representations that support effective ROI classification and clustering play a fundamental role in volume exploration. Existing approaches typically rely on either explicit local feature representations or implicit convolutional feature representations learned from raw volumes. However, explicit local feature representations are limited in capturing broader geometric patterns and spatial correlations, while implicit convolutional feature representations do not necessarily ensure robust performance in practice, where user supervision is typically limited. Meanwhile, implicit neural representations (INRs) have recently shown strong promise in DVR for volume compression, owing to their ability to compactly parameterize continuous volumetric fields. In this work, we propose NeuVolEx, a neural volume exploration approach that extends the role of INRs beyond volume compression. Unlike prior compression methods that focus on INR outputs, NeuVolEx leverages feature representations learned during INR training as a robust basis for volume exploration. To better adapt these feature representations to exploration tasks, we augment a base INR with a structural encoder and a multi-task learning scheme that improve spatial coherence for ROI characterization. We validate NeuVolEx on two fundamental volume exploration tasks: image-based transfer function (TF) design and viewpoint recommendation. NeuVolEx enables accurate ROI classification under sparse user supervision for image-based TF design and supports unsupervised clustering to identify compact complementary viewpoints that reveal different ROI clusters. Experiments on diverse volume datasets with varying modalities and ROI complexities demonstrate NeuVolEx improves both effectiveness and usability over prior methods

---

## 论文详细总结（自动生成）

这是一份关于论文《NeuVolEx: Implicit Neural Features for Volume Exploration》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在直接体渲染（DVR）中，利用更鲁棒的特征表示来辅助用户识别和探索感兴趣区域（ROI）。
*   **研究动机**：
    *   **现有方法局限**：显式局部特征（如梯度、强度）难以捕捉广域几何模式；隐式卷积特征虽然能捕捉上下文，但维度过高（如75D-387D），在用户交互受限（如仅有少量涂鸦标注）的实际场景中表现不佳，且计算开销巨大。
    *   **INR的潜力**：隐式神经表示（INR）在体数据压缩领域表现出色，但其在训练过程中学习到的中间特征在“体数据探索”任务中的价值尚未被充分挖掘。

### 2. 方法论
NeuVolEx 的核心思想是将 INR 从单纯的“数据压缩工具”转变为“特征提取引擎”，并针对探索任务进行了优化。
*   **双路径架构**：
    1.  **位置表示路径**：使用多分辨率哈希网格编码（Hash Grid Encoding）将3D坐标映射为可训练特征。
    2.  **结构表示路径**：提取以坐标为中心的局部体素块（$5^3$），通过结构编码器（MLP）捕捉局部上下文。
*   **关键技术细节**：
    *   **FiLM 调制**：利用特征线性调制（FiLM）机制，用结构表示来调制位置表示，增强特征对边界和细微结构的敏感度。
    *   **多任务学习方案**：主 MLP 同时预测四个目标：强度（Intensity）、梯度（Gradient）、局部均值（Local Mean）和局部标准差（Local Std），以强制特征捕捉空间相干性和同质性。
*   **下游任务流程**：
    *   **TF 设计**：将学习到的特征与用户稀疏涂鸦结合，输入随机森林分类器，计算体素属于各 ROI 的概率，最后通过概率-强度渲染器成像。
    *   **视点推荐**：对特征进行无监督聚类（K-means），基于熵图计算各视点的“信息量”，选出一组互补的最优视点。

### 3. 实验设计
*   **数据集**：使用了 10 个涵盖医学、工业、合成及模拟的数据集（如 CT-Abdomen, MRA-Brain, CT-Tooth, CT-Engine, CTA-Head, CT-Bonsai, MR-Kiwi 等）。
*   **Benchmark（对比方法）**：
    *   **显式局部特征方法**：Soundararajan 等人的概率分类方法。
    *   **隐式卷积特征方法**：Quan 等人的 3D 卷积稀疏编码方法。
    *   **传统方法**：基于强度的 1D 传递函数（Intensity-TF）。
*   **评估指标**：F1-score（分类准确性）、涂鸦敏感度分析（S1-S4 四个等级）、定性视觉比较。

### 4. 资源与算力
*   **硬件配置**：Intel Core i7-14700K CPU, 64GB RAM, **NVIDIA RTX 4090 GPU (24GB VRAM)**。
*   **训练时长**：特征训练（预处理）时间随分辨率线性增长。
    *   小型数据（CT-Tooth）：约 12 秒。
    *   大型数据（CTA-Cardiac, 512x512x452）：约 15 分钟。
*   **分类器训练**：在用户交互后，随机森林的训练时间在数秒到几分钟之间（取决于体素规模）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括 4 个数据集的定量分类对比、4 个数据集的定性视觉对比、3 个数据集的视点推荐展示。
*   **消融实验**：详细验证了基础 INR、结构编码、FiLM 调制和多任务学习四个组件的各自贡献。
*   **充分性评价**：实验设计较为全面，涵盖了不同模态（CT, MR, 模拟数据）和不同复杂度的 ROI（如细小血管、内部器官、机械零件），并特别测试了在“极稀疏监督”（仅 1 张切片涂鸦）下的鲁棒性，客观证明了方法的优越性。

### 6. 主要结论与发现
*   **性能领先**：NeuVolEx 在所有测试数据集上的 F1-score 均显著高于对比方法，尤其在复杂的 MRA-Brain 数据上提升巨大。
*   **稀疏监督鲁棒性**：在极少量用户标注（S1 等级）下，NeuVolEx 的表现甚至优于对比方法在大量标注下的表现。
*   **特征通用性**：INR 学习到的特征不仅适用于有监督的分类任务，也适用于无监督的聚类和视点推荐任务。
*   **空间一致性**：通过结构编码和多任务约束，INR 特征能有效解决相邻区域强度相似但属于不同 ROI 的分类难题。

### 7. 优点
*   **高效性**：相比于传统的深度学习方法，INR 的训练和推理速度极快，适合交互式工作流。
*   **易用性**：极大地降低了用户在 TF 设计时的交互负担，简单的涂鸦即可获得高质量的分割与渲染结果。
*   **创新性**：成功将 INR 的应用范畴从“数据表示/压缩”扩展到了“语义特征提取”。

### 8. 不足与局限
*   **预处理依赖**：虽然时间较短，但仍需在探索前进行一次性的特征训练预处理。
*   **显存压力**：对于超大规模体数据（如 1024^3 以上），哈希网格和特征存储可能会面临显存瓶颈。
*   **任务覆盖**：目前仅验证了 TF 设计和视点推荐，尚未扩展到多模态配准或时变数据的动态探索中。

（完）
