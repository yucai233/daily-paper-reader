---
title: "Factorized Multi-Resolution HashGrid for Efficient Neural Radiance Fields: Execution on Edge-Devices"
title_zh: 用于高效神经辐射场的分解式多分辨率哈希网格：在边缘设备上的执行
authors: "Kim Jun-Seong, Mingyu Kim, GeonU Kim, Tae-Hyun Oh, Jin-Hwa Kim"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02836v1"
tags: ["query:q1"]
score: 8.0
evidence: 高效神经辐射场与3D表示的参数编码
tldr: 针对神经辐射场（NeRF）在边缘设备上训练时面临的内存和计算资源限制，本文提出了一种名为 Fact-Hash 的新型参数编码方法。该方法结合了张量分解与哈希编码技术，通过将3D坐标投影到低维空间（2D或1D）后再进行哈希处理，显著降低了内存占用（减少超过三分之一），同时保持了高分辨率特征和渲染质量。实验证明，Fact-Hash 在边缘设备上具有极高的计算效率和能耗优势，为实现高效的端侧 NeRF 训练提供了新方案。
selection_source: fresh_fetch
motivation: 传统的 NeRF 编码方法因巨大的内存和计算开销，难以在资源受限的边缘设备上实现高效的端侧训练与实时应用。
method: 提出 Fact-Hash 编码，通过将 3D 坐标投影至低维平面或向量后再应用多分辨率哈希函数，并聚合特征以实现参数的高效压缩。
result: 在保持 PSNR 质量和渲染速度的前提下，Fact-Hash 相比现有方法节省了超过三分之一的内存使用，并在边缘设备上展现出更低的能耗。
conclusion: Fact-Hash 成功解决了特征网格表示中的内存瓶颈，是提升边缘设备上 NeRF 训练效率和质量的一种极具前景的解决方案。
---

## 摘要
我们引入了 Fact-Hash，一种用于在设备端训练神经辐射场的新型参数编码方法。神经辐射场 (NeRF) 已被证明在 3D 表示中至关重要，但由于其对计算资源的高需求，其应用受到了限制。设备端训练可以开辟广阔的应用领域，在应对通信限制、隐私保护以及对频繁变化的场景进行快速适应方面具有优势。然而，资源受限（GPU 显存、存储和功耗）等挑战阻碍了其部署。为了解决这一问题，我们引入了 Fact-Hash，这是一种融合了张量分解 (Tensor Factorization) 和哈希编码 (Hash-encoding) 技术的新型参数编码。这种集成带来了两个好处：利用丰富的高分辨率特征以及少样本鲁棒性。在 Fact-Hash 中，我们在应用哈希函数之前将 3D 坐标投影到多个低维形式（2D 或 1D），然后将其聚合为单个特征。与最先进方法的对比评估表明，Fact-Hash 在保持质量和渲染速度的同时，具有卓越的内存效率。与之前的编码方法相比，Fact-Hash 在保持 PSNR 值的同时，节省了超过三分之一的内存使用量。设备端实验验证了 Fact-Hash 在计算效率和能耗方面优于其他位置编码方法。这些研究结果表明，Fact-Hash 是一种极具前景的解决方案，可用于改进特征网格表示、解决内存约束并提高各种应用中的质量。项目页面：https://facthash.github.io/

## Abstract
We introduce Fact-Hash, a novel parameter-encoding method for training on-device neural radiance fields. Neural Radiance Fields (NeRF) have proven pivotal in 3D representations, but their applications are limited due to large computational resources. On-device training can open large application fields, providing strength in communication limitations, privacy concerns, and fast adaptation to a frequently changing scene. However, challenges such as limited resources (GPU memory, storage, and power) impede their deployment. To handle this, we introduce Fact-Hash, a novel parameter-encoding merging Tensor Factorization and Hash-encoding techniques. This integration offers two benefits: the use of rich high-resolution features and the few-shot robustness. In Fact-Hash, we project 3D coordinates into multiple lower-dimensional forms (2D or 1D) before applying the hash function and then aggregate them into a single feature. Comparative evaluations against state-of-the-art methods demonstrate Fact-Hash's superior memory efficiency, preserving quality and rendering speed. Fact-Hash saves memory usage by over one-third while maintaining the PSNR values compared to previous encoding methods. The on-device experiment validates the superiority of Fact-Hash compared to alternative positional encoding methods in computational efficiency and energy consumption. These findings highlight Fact-Hash as a promising solution to improve feature grid representation, address memory constraints, and improve quality in various applications. Project page: https://facthash.github.io/

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Fact-Hash** 的新型参数编码方法，旨在解决神经辐射场（NeRF）在边缘设备上部署时面临的显存、存储和计算资源限制问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 NeRF 模型虽然在 3D 重建上表现优异，但其巨大的计算开销和内存占用使其难以在资源受限的边缘设备（如无人机、AR/VR 眼镜、自动驾驶系统）上进行实时训练和推理。
*   **研究动机**：
    *   **端侧需求**：边缘设备需要快速适应环境变化（端侧训练）、保护隐私并减少通信带宽。
    *   **现有方法局限**：张量分解法（如 TensoRF）计算复杂度高且扩展性差；哈希编码法（如 Instant-NGP）在少样本（Few-shot）场景下容易过拟合，产生严重的伪影。
    *   **目标**：寻找一种既能保持高分辨率细节，又能极度压缩参数量，且对少样本输入具有鲁棒性的编码方案。

### 2. 方法论：Fact-Hash 核心思想
Fact-Hash 结合了**张量分解（Tensor Factorization）**的结构化优势和**多分辨率哈希编码（Hash-encoding）**的高效压缩能力。
*   **核心流程（Map-Reduce 模式）**：
    1.  **映射（Map）**：将 3D 坐标 $(x, y, z)$ 投影到三个相互垂直的 2D 平面（XY, YZ, ZX），即 Tri-plane 分解。
    2.  **哈希编码（Hash）**：对投影后的 2D 坐标应用多分辨率哈希函数。相比于 Instant-NGP 在 3D 空间随机碰撞，Fact-Hash 将碰撞限制在 2D 平面内，提高了学习的稳定性。
    3.  **聚合（Reduce）**：从哈希表中提取特征后，通过 **Hadamard 积（元素级乘法）** 将三个平面的特征融合为单个特征向量。
    4.  **解码**：使用轻量级 MLP 解码器预测密度和颜色。
*   **关键技术**：
    *   **多尺度特征**：利用指数增长的网格分辨率捕获从粗糙到精细的几何细节。
    *   **拒绝采样（Rejection Sampling）**：引入位场（Bit-field）或提案网络（Proposal Network）来跳过空白区域，加速渲染。

### 3. 实验设计
*   **数据集/场景**：
    *   **NeRF Synthetic**：包含 8 个合成物体，用于评估基础性能。
    *   **Tanks and Temples**：高分辨率真实户外场景。
    *   **San Francisco Mission Bay**：大规模城市街区数据，评估模型的扩展性。
*   **Benchmark（基准）**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似度）、推理时间、参数量及存储大小。
*   **对比方法**：Instant-NGP (iNGP)、TensoRF、K-planes。为了公平竞争，作者还开发了针对边缘设备优化的 TensoRF* 和 K-planes* 版本。

### 4. 资源与算力
*   **训练设备**：使用 NVIDIA **RTX 3090Ti** 进行模型训练。
*   **边缘执行设备**：**NVIDIA Jetson Xavier NX (16GB)**，分别在 10W 和 20W 功率模式下测试。
*   **训练细节**：迭代 30,000 次，Batch Size 为 4096。
*   **算力说明**：论文明确对比了在边缘设备上的渲染耗时（分钟级），证明了其方法在低功耗硬件上的可行性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了合成物体、真实户外、大规模城市三种尺度，并专门设置了**少样本（Few-shot）**实验（如仅使用 8 张图像或 10% 的数据）。
*   **消融实验**：针对哈希碰撞率、张量分解与哈希编码的互补性进行了深入分析。
*   **评价**：实验设计全面且客观。通过重新实现基准方法并使其适应边缘内存限制，确保了对比的公平性。

### 6. 主要结论与发现
*   **极高的压缩率**：Fact-Hash 的参数量仅为基准方法的 **1/2 到 1/5**，存储空间节省超过 **1/3**。
*   **渲染加速**：在边缘设备上，平均渲染速度比现有最优方法快约 **40%**。
*   **少样本鲁棒性**：在稀疏输入下，Fact-Hash 表现出比 iNGP 更好的稳定性，有效减少了“浮动伪影”（Floating artifacts）。
*   **性能保持**：在大幅减少参数的同时，PSNR 等质量指标与重量级模型持平。

### 7. 优点与亮点
*   **架构创新**：巧妙地将 2D 投影与哈希结合，解决了 3D 哈希碰撞导致的训练不稳定问题。
*   **端侧友好**：不仅关注推理速度，更强调了端侧训练的可能性，符合未来空间计算的发展趋势。
*   **通用性**：实验证明该方法在小物体到大城市尺度上均有效。

### 8. 不足与局限
*   **硬件依赖**：虽然在 Jetson 上表现良好，但仍依赖 CUDA 核心，在非 NVIDIA 架构的移动端芯片（如手机 SoC）上的表现尚未验证。
*   **大规模场景处理**：对于超大规模场景，仍需依赖块分解（Block decomposition）策略，未能完全消除分块带来的复杂性。
*   **未来工作**：论文提到在抗锯齿（Anti-aliasing）和复杂光照处理方面仍有提升空间。

（完）
