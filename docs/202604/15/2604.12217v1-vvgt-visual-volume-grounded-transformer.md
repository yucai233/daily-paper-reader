---
title: "VVGT: Visual Volume-Grounded Transformer"
title_zh: VVGT：视觉体锚定 Transformer
authors: "Yuxuan Wang, Qibiao Li, Youcheng Cai"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12217v1"
tags: ["query:q7"]
score: 9.0
evidence: 将体积数据映射到 3D Gaussian Splatting 以进行可视化的前馈框架
tldr: 针对传统体渲染（DVR）扩展性差及现有3DGS方法需逐场景优化的局限，本文提出VVGT框架。这是一种前馈式Transformer网络，能直接将体积数据映射为3D高斯表示。通过引入体积几何强迫机制和对极交叉注意力，VVGT在无需表面假设的情况下实现了高效的体积重建，显著提升了可视化速度和跨数据集的泛化能力，为体积可视化提供了新范式。
selection_source: fresh_fetch
motivation: 传统体渲染在大规模数据下扩展性受限，且现有3D高斯泼溅方法依赖耗时的逐场景优化，难以满足实时交互需求。
method: 采用双Transformer架构并引入体积几何强迫机制，通过对极交叉注意力将多视图观测直接映射为3D高斯原语。
result: VVGT在保持高质量可视化的同时，实现了数量级的转换速度提升，并展现出卓越的几何一致性和零样本泛化性能。
conclusion: 该研究成功构建了一种无需逐场景优化的前馈式体积可视化框架，实现了真正交互式且可扩展的体积数据处理。
---

## 摘要
体绘制长期以来由直接体渲染（DVR）主导，它在密集体素网格上运行，且随着分辨率和交互性需求的增加，其可扩展性受到限制。3D 高斯泼溅（3DGS）的最新进展提供了一种以表示为中心的替代方案；然而，现有的体素扩展仍依赖于昂贵的逐场景优化，限制了可扩展性和交互性。我们提出了 VVGT（视觉体锚定 Transformer），这是一个前馈、表示优先的框架，可将体数据直接映射到 3D 高斯泼溅表示，推进了超越 DVR 的体绘制新范式。与之前针对以表面为中心的重建而设计的前馈 3DGS 方法不同，VVGT 明确考虑了体渲染，其中每个像素聚合了沿射线的贡献。VVGT 采用双 Transformer 网络，并引入了体几何强迫（Volume Geometry Forcing），这是一种对极交叉注意力机制，可在无表面假设的情况下将多视图观测整合到分布式的 3D 高斯原语中。该设计消除了逐场景优化，同时实现了准确的体表示。大量实验表明，VVGT 实现了高质量的可视化，转换速度提高了几个数量级，几何一致性得到改善，并在不同数据集上表现出强大的零样本泛化能力，从而实现了真正的交互式和可扩展体绘制。代码将在论文接收后公开发布。

## Abstract
Volumetric visualization has long been dominated by Direct Volume Rendering (DVR), which operates on dense voxel grids and suffers from limited scalability as resolution and interactivity demands increase. Recent advances in 3D Gaussian Splatting (3DGS) offer a representation-centric alternative; however, existing volumetric extensions still depend on costly per-scene optimization, limiting scalability and interactivity. We present VVGT (Visual Volume-Grounded Transformer), a feed-forward, representation-first framework that directly maps volumetric data to a 3D Gaussian Splatting representation, advancing a new paradigm for volumetric visualization beyond DVR. Unlike prior feed-forward 3DGS methods designed for surface-centric reconstruction, VVGT explicitly accounts for volumetric rendering, where each pixel aggregates contributions along a ray. VVGT employs a dual-transformer network and introduces Volume Geometry Forcing, an epipolar cross-attention mechanism that integrates multi-view observations into distributed 3D Gaussian primitives without surface assumptions. This design eliminates per-scene optimization while enabling accurate volumetric representations. Extensive experiments show that VVGT achieves high-quality visualization with orders-of-magnitude faster conversion, improved geometric consistency, and strong zero-shot generalization across diverse datasets, enabling truly interactive and scalable volumetric visualization. The code will be publicly released upon acceptance.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《VVGT: Visual Volume-Grounded Transformer》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的体绘制（Direct Volume Rendering, DVR）依赖密层级采样，在大规模数据和高交互需求下扩展性差。虽然 3D 高斯泼溅（3DGS）提供了高效的替代方案，但现有的体素 3DGS 方法通常需要针对每个场景进行耗时的迭代优化，无法实现即时转换和交互。
*   **研究动机**：现有的前馈式（Feed-forward）重建模型多基于“表面假设”（Surface-centric），不适用于光线穿透并聚合多个空间点贡献的“体积数据”。
*   **整体含义**：VVGT 提出了一种全新的“表示优先”范式，通过前馈网络直接将体积数据映射为 3DGS 表示，消除了逐场景优化的需求，实现了高效、可扩展且交互式的科学可视化。

### 2. 方法论：核心思想与关键技术
VVGT 的核心是一个**双 Transformer 网络（Dual-Transformer Network, DTN）**，结合了**体积几何强迫（Volume Geometry Forcing, VGF）**机制：
*   **2D 外观 Transformer**：基于 DINOv2 架构，提取多视图图像的特征。它引入了相机位姿嵌入（Camera Pose Embedding），通过全局自注意力机制确保多视图间的外观一致性。
*   **3D 几何 Transformer**：基于 PTV3 框架。首先利用可变基映射（VBM）初始化一组 3D 高斯原语，然后通过 Transformer 聚合几何上下文，预测高斯属性的残差修正（中心、不透明度、旋转、缩放、颜色）。
*   **体积几何强迫（VGF）**：这是连接 2D 和 3D 的关键。利用**对极交叉注意力（Epipolar Cross-Attention）**，将 3D 高斯原语投影到 2D 视图上，采样多尺度视觉特征。通过计算几何距离偏置，强制 3D 表示吸收 2D 观测信息，从而在无表面假设的情况下准确学习体积属性。
*   **训练目标**：结合了 $\ell_1$ 损失、SSIM 结构相似性损失和 LPIPS 感知损失，确保渲染结果在物理和视觉上的双重准确。

### 3. 实验设计
*   **数据集**：使用了来自直接数值模拟（DNS）的 4096³ 高分辨率数据，包括“旋转分层湍流”和“各向同性湍流”。从中切分出 110 个 512³ 的子场景（100 个训练，10 个测试）。
*   **Benchmark**：每个场景使用 10 种精心设计的传递函数（TF）进行渲染，分辨率为 800x800。
*   **对比方法**：
    *   **优化类**：标准的 3DGS、iVRGS（针对体积优化的 3DGS）。
    *   **前馈类**：NoPoSplat、AnySplat（均为针对表面重建设计的最新模型）。
*   **评价指标**：PSNR、SSIM、LPIPS 以及转换耗时（Time）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA RTX Pro 6000 GPU**，每台配备 96GB 显存。
*   **训练时长**：总计训练约 **4 天**（40K 次迭代）。
*   **推理效率**：转换速度比优化类方法快几个数量级（秒级 vs 分钟级）。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含 100 个场景，测试集包含 10 个完全未见过的场景（零样本泛化测试）。
*   **消融实验**：针对 3D 几何 Transformer、VBM 初始化、VGF 机制分别进行了剔除实验，验证了各模块对消除模糊和提升几何一致性的必要性。
*   **充分性评价**：实验设计较为充分，涵盖了不同物理特性的湍流数据，并对比了当前最前沿的优化式和前馈式基准，客观地展示了模型在零样本泛化和后优化（Post-optimization）潜力上的优势。

### 6. 主要结论与发现
*   **性能领先**：VVGT 在零样本测试中显著优于现有的前馈模型（如 AnySplat），在 PSNR 和 SSIM 上有大幅提升。
*   **效率极高**：VVGT 仅需约 2-4 秒即可完成转换，而传统优化方法需 7-8 分钟。
*   **后优化潜力**：在 VVGT 预测的基础上仅进行 100 步微调（约 5 秒），其渲染质量即可超过经过 30,000 步迭代的传统 3DGS 方法。
*   **范式转变**：证明了体积可视化可以从“采样渲染”转向“前馈表示生成”。

### 7. 优点
*   **首创性**：第一个将前馈 3DGS 引入体积可视化领域，打破了表面假设的限制。
*   **高质量与高速度并存**：在保持科学数据精细结构（如湍流涡流）的同时，实现了近乎即时的表示生成。
*   **泛化能力强**：在未见过的体积场景和传递函数下表现稳健。

### 8. 不足与局限
*   **数据覆盖范围**：目前主要在模拟物理场（湍流）上验证，尚未在真实世界的大规模医疗数据（如高分辨率 CT/MRI 扫描）上进行全面测试。
*   **算力需求**：训练过程对显存要求极高（96GB 级别），且模型容量在处理超高分辨率解剖细节时可能面临扩展压力。
*   **应用限制**：对于极度稀疏或极度复杂的非结构化网格数据的适应性仍有待进一步探索。

（完）
