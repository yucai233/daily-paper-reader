---
title: "8DNA: 8D Neural Asset Light Transport by Distribution Learning"
title_zh: 8DNA：基于分布学习的8维神经资产光线传输
authors: "Liwen Wu, Haolin Lu, Bing Xu, Miloš Hašan, Ravi Ramamoorthi"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25129v1"
tags: ["query:q4"]
score: 8.0
evidence: 包含次表面散射的光线传输神经表示
tldr: 针对高保真3D资产中复杂的全局光照（如次表面散射、光泽间接反射等）模拟成本高昂的问题，本文提出了8DNA。该方法通过分布学习将完整8D光传输预烘焙到神经表示中，克服了以往6D方法仅支持远场照明的局限，实现了近场照明下的精确渲染。实验证明，8DNA在降低训练成本和方差的同时，能以极快的推理速度生成与路径追踪相媲美的高质量渲染效果。
selection_source: fresh_fetch
motivation: 传统的全局光照模拟计算开销巨大，且现有神经预烘焙方法多局限于远场照明，无法处理复杂的近场光传输。
method: 提出一种基于分布学习的8D神经资产表示，通过前向路径追踪样本直接学习完整的光传输函数。
result: 8DNA在多种场景配置下均能达到与路径追踪一致的渲染质量，且具有更低的优化方差和更快的推理速度。
conclusion: 该研究为复杂3D资产的高效、高质量近场光照渲染提供了一种全新的神经表示方案。
---

## 摘要
高保真3D资产展现出引人入胜的全局光照效果，如次表面散射、光泽间接反射和细微的纤维散射，这些效果通常涉及模拟成本高昂的长散射路径。我们引入了8维神经资产（8DNA），将这些光线传输效果预烘焙到神经表示中。与以往假设远场照明并将光线传输预计算为6维函数的方法不同，8DNA学习完整的8维光线传输，从而实现在近场照明下的准确渲染。我们的训练利用了一种分布学习公式，从前向路径追踪样本中学习光线传输，与以往基于回归的方法相比，在更低的训练预算下产生了更小的优化方差。实验表明，我们的8DNA渲染在各种场景配置下都与路径追踪结果紧密匹配，同时在具有挑战性的资产上实现了更好的方差缩减和快速的推理速度。

## Abstract
High-fidelity 3D assets exhibit intriguing global illumination effects like subsurface scattering, glossy interreflections, and fine-scale fiber scatterings, which often involve long scattering paths that are expensive to simulate. We introduce 8D neural assets (8DNA) to pre-bake these light transport effects into neural representations. Unlike prior methods that assume far-field lighting and precompute light transport into 6D functions, 8DNA learns the full 8D light transport, enabling accurate rendering under near-field illumination. Our training leverages a distribution-learning formulation that learns light transport from forward path-traced samples, which produces less optimization variance with lower training budget than the prior regression-based approaches. Experiments show our 8DNA rendering closely matches path-traced results under various scene configurations, yet it achieves improved variance reduction and fast inference speeds on challenging assets.

---

## 论文详细总结（自动生成）

这是一份关于论文《8DNA: 8D Neural Asset Light Transport by Distribution Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证高保真度的前提下，高效渲染具有复杂全局光照效果（如次表面散射、光泽间接反射、纤维散射）的3D资产？
*   **研究动机**：
    *   **计算开销大**：传统的蒙特卡洛路径追踪（PT）在处理长路径散射时收敛极慢，方差大。
    *   **现有方法局限**：之前的神经预烘焙方法（如RNA）大多基于**远场假设（6D函数）**，即假设光源无限远。当光源靠近物体（近场照明）时，这些方法无法准确捕捉因入射位置变化引起的遮挡和散射细节。
    *   **训练困难**：直接对8D光传输函数进行回归训练（L2 Loss）会面临极高的采样方差，难以收敛。

### 2. 方法论
*   **核心思想**：将完整的**8D光传输函数** $F(x_o, \omega_o, x_i, \omega_i)$ 分解为**生存概率（Albedo）**和**散射分布（Distribution）**，并利用**分布学习（Distribution Learning）**框架从前向路径追踪样本中学习。
*   **关键技术细节**：
    *   **函数分解**：将 $F$ 分解为 $\alpha(x_o, \omega_o)$（由MLP预测的颜色缩放因子）和 $p(x_i, \omega_i | x_o, \omega_o)$（由正规化流模型学习的条件概率分布）。
    *   **正规化流（Normalizing Flow）**：使用有理二次样条（Rational Quadratic Splines, RQS）构建自回归流模型，以捕捉复杂的8D散射分布。
    *   **参数化方案**：为了在欧几里得空间处理入射点 $x_i$，将入射射线映射到资产的轴对齐包围盒（AABB）上的交点 $u_i$，并采用柱坐标编码以避免极点奇异性。
    *   **训练目标**：不再使用回归损失，而是使用**负对数似然（NLL）损失**。这种方式只需前向路径采样，无需复杂的下一事件估计（NEE），训练方差更低。
    *   **直接-间接分离**：为了提升精度，解析地保留直接BSDF散射，仅让神经网络学习复杂的间接光传输部分。

### 3. 实验设计
*   **数据集/场景**：涵盖了多种材质和几何特征的资产，包括：
    *   **体积散射**：蜡烛（Candle）、牛奶（Milk）、玉玺（Seal）、龙（Dragon）。
    *   **纤维/表面**：卷发（CurlHair）、织物（Fabric）、茶具（Teaset）。
    *   **纯体积**：云（Cloud）。
*   **Benchmark（基准）**：以高样本数（如8192+ spp）的路径追踪（PT）结果作为地面真值（Ground Truth）。
*   **对比方法**：
    1.  **Standard Path Tracing (PT)**：标准的蒙特卡洛路径追踪。
    2.  **Far-field Baseline**：基于回归学习的6D远场神经表示（模拟前人方法）。
    3.  **NeuralSSS**：针对次表面散射的近场基准（补充实验中对比）。

### 4. 资源与算力
*   **硬件**：所有实验和渲染均在 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练配置**：
    *   使用 Adam 优化器，学习率 0.0005。
    *   训练 240K 步，Batch Size 为 32768。
    *   **时长**：体积类资产平均训练总耗时约 **2.23 小时**，表面类资产约 **1.78 小时**（包含在线数据生成和优化）。
*   **显存占用**：推理时显存占用约 3.7GB - 8.8GB（视资产复杂度而定）。

### 5. 实验数量与充分性
*   **实验规模**：测试了 10 种以上不同类型的资产，涵盖了从均匀介质到非均匀介质、从纤维到金属表面的广泛范围。
*   **消融实验**：
    *   验证了网络架构大小对精度/速度的影响。
    *   对比了有无“直接-间接分离”的效果。
    *   测试了不同包围几何体（包围盒、包围球、凸包）作为参数化代理的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了渲染精度（MSE），还深入分析了等效时间下的方差缩减（Equal-time Variance），证明了该方法在实际生产环境中的效率优势。

### 6. 主要结论与发现
*   **近场精度高**：8DNA 能够准确还原近场光源下的遮挡和阴影效果，而远场模型在这些区域会出现明显的亮度偏差。
*   **方差缩减显著**：在处理高反照率（High-albedo）资产（如牛奶、头发）时，8DNA 相比 PT 能实现 2-20 倍的渲染加速或显著的方差降低。
*   **训练效率优**：由于采用了分布学习而非回归，8DNA 在更少的采样预算下即可收敛，且训练过程对高光泽材质更鲁棒。

### 7. 优点
*   **全8D支持**：突破了远场假设，使神经资产能够无缝集成到任何复杂的近场照明场景中。
*   **低方差训练**：引入分布学习框架，解决了传统神经光传输方法在处理尖锐分布（如镜面反射）时难以训练的问题。
*   **易于集成**：模型可以作为自定义材质插件集成到现有的路径追踪器（如Mitsuba 3）中，支持多重要性采样（MIS）。

### 8. 不足与局限
*   **凸包限制**：该方法假设资产内部散射被包围盒/凸包界定，如果外部几何体侵入该包围空间（如物体穿插），预烘焙的光路会失效。
*   **高频细节平滑**：正规化流模型对极高频的信号（如复杂的焦散或闪烁）存在归纳偏置，可能导致渲染结果过于平滑。
*   **计算开销**：虽然比 PT 快，但正规化流的求导和采样计算比纯 MLP 的远场模型更重，在对精度要求不高的远场场景下性价比稍低。

（完）
