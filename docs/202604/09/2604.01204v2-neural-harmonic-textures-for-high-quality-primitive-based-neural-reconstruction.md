---
title: Neural Harmonic Textures for High-Quality Primitive Based Neural Reconstruction
title_zh: 神经调和纹理：用于高质量基于原语的神经重建
authors: "Jorge Condor, Nicolas Moenne-Loccoz, Merlin Nimier-David, Piotr Didyk, Zan Gojcic, Qi Wu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01204v2"
tags: ["query:q1"]
score: 8.0
evidence: 为基于原语的重建锚定潜特征的神经表示
tldr: 针对3D高斯泼溅等基于基元的重建方法在处理高频细节时表现不足的问题，本文提出了神经调和纹理（Neural Harmonic Textures）。该方法通过在基元周围构建虚拟支架并锚定潜在特征向量，结合受傅里叶分析启发的周期性激活函数，将Alpha混合转化为调和分量的加权和。该方法在保持实时渲染性能的同时，显著提升了重建质量，有效弥补了基元表示与神经场表示之间的差距。
selection_source: fresh_fetch
motivation: 现有的基于基元的重建方法由于单个基元的表达能力有限，难以捕捉复杂的高频细节。
method: 在基元周围建立虚拟支架锚定特征向量，利用周期性激活函数处理插值特征，并通过小型神经网络进行延迟解码。
result: 在实时新视角合成任务中达到了最先进的水平，并能无缝集成到3DGUT、2DGS等多种现有基元管线中。
conclusion: 神经调和纹理成功结合了基元方法的灵活性与神经场的高表达能力，为高质量神经重建提供了通用且高效的解决方案。
---

## 摘要
基于原语（Primitive-based）的方法，如 3D Gaussian Splatting，最近已成为新视角合成及相关重建任务的最先进技术。与神经场（neural fields）相比，这些表示形式更加灵活、具有自适应性，并且能更好地扩展到大规模场景。然而，单个原语有限的表达能力使得建模高频细节具有挑战性。我们引入了神经调和纹理（Neural Harmonic Textures），这是一种神经表示方法，它将潜在特征向量锚定在围绕每个原语的虚拟支架上。这些特征在射线相交点处的原语内部进行插值。受傅里叶分析的启发，我们对插值特征应用周期性激活，将 Alpha 混合（alpha blending）转化为调和分量的加权和。生成的信号随后在单个延迟通道中通过小型神经网络进行解码，从而显著降低了计算成本。神经调和纹理在实时新视角合成中取得了最先进的结果，同时弥合了基于原语和基于神经场的重建之间的差距。我们的方法可以无缝集成到现有的基于原语的流水线中，如 3DGUT、Triangle Splatting 和 2DGS。我们通过在二维图像拟合和语义重建中的应用，进一步展示了其通用性。

## Abstract
Primitive-based methods such as 3D Gaussian Splatting have recently become the state-of-the-art for novel-view synthesis and related reconstruction tasks. Compared to neural fields, these representations are more flexible, adaptive, and scale better to large scenes. However, the limited expressivity of individual primitives makes modeling high-frequency detail challenging. We introduce Neural Harmonic Textures, a neural representation approach that anchors latent feature vectors on a virtual scaffold surrounding each primitive. These features are interpolated within the primitive at ray intersection points. Inspired by Fourier analysis, we apply periodic activations to the interpolated features, turning alpha blending into a weighted sum of harmonic components. The resulting signal is then decoded in a single deferred pass using a small neural network, significantly reducing computational cost. Neural Harmonic Textures yield state-of-the-art results in real-time novel view synthesis while bridging the gap between primitive- and neural-field-based reconstruction. Our method integrates seamlessly into existing primitive-based pipelines such as 3DGUT, Triangle Splatting, and 2DGS. We further demonstrate its generality with applications to 2D image fitting and semantic reconstruction.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neural Harmonic Textures for High-Quality Primitive Based Neural Reconstruction》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于原语（Primitive-based）的方法（如 3D Gaussian Splatting, 3DGS）虽然在渲染速度和灵活性上优于连续神经场（NeRF），但单个原语的表达能力有限。为了捕捉高频细节，通常需要大幅增加原语数量，这导致内存消耗激增并降低渲染效率。此外，传统的球谐函数（SH）在处理强镜面反射等高频外观时表现不足。
*   **研究背景**：研究者希望结合“拉格朗日表示（显式原语）”的灵活性与“欧拉表示（神经场/位置编码）”的高表达能力，在不显著增加计算负担的前提下，提升重建质量。

### 2. 方法论
*   **核心思想**：引入“神经调和纹理（NHT）”，将原语重新解释为几何载体和局部位置编码。通过在每个原语周围构建虚拟支架并存储特征，结合周期性激活函数，实现高效的高频信号建模。
*   **关键技术细节**：
    1.  **原语绑定特征嵌入**：在每个原语（如高斯体或三角形）周围建立一个虚拟支架（如四面体）。将可学习的潜在特征向量锚定在支架顶点上。
    2.  **局部插值**：当射线穿过原语时，在响应最大的点处对顶点特征进行重心插值，获得局部特征向量。
    3.  **调和编码（Harmonic Texturing）**：受傅里叶变换启发，对插值后的特征应用正弦（sin）和余弦（cos）激活。这使得特征在空间中产生高频振荡，原语的不透明度则充当这些调和分量的振幅。
    4.  **神经延迟解码**：将沿射线累积的调和信号输入一个轻量级多层感知机（MLP），在图像空间进行单次解码（Deferred Shading），输出最终颜色。
*   **算法流程**：
    *   输入：射线方向和原语参数。
    *   过程：计算相交 -> 特征插值 -> sin/cos 激活 -> 沿射线 Alpha 混合累积特征 -> MLP 解码。
    *   公式表达：$c = MLP_\theta \left( \sum \alpha_i T_i [\sin(f_i), \cos(f_i)], SH(d) \right)$。

### 3. 实验设计
*   **数据集**：
    *   **新视角合成**：MipNeRF360（室内外场景）、Tanks & Temples、Deep Blending。
    *   **语义重建**：结合 LSEG 特征的 MipNeRF360。
    *   **2D 图像拟合**：14-bit HDR RAW 高分辨率图像数据集（45.7 MP）。
*   **Benchmark 与对比方法**：
    *   对比了纯神经场方法（ZipNeRF, Mip-NeRF 360）、纯原语方法（3DGS-MCMC, 2DGS, Triangle Splatting）以及混合方法（Radiance Meshes, NeST）。
    *   评估指标包括 PSNR、SSIM、LPIPS 以及语义一致性指标。

### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA RTX A6000 Ada GPU** 上进行。
*   **训练时长**：
    *   室内场景：约 45k 次迭代。
    *   室外场景：约 25k 次迭代。
    *   2D 图像拟合：约 1-3 分钟。
*   **渲染性能**：在 A6000 GPU 上可达到 **140+ FPS**，保持了实时渲染能力。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 3 个主流 3D 数据集、1 个语义重建任务和 1 个高分辨率 2D 拟合任务。
*   **消融实验**：非常充分。作者对特征维度、MLP 架构、编码函数（无编码 vs ReLU vs sin/cos）、原语数量（从 1k 到 4M）以及训练策略（EMA、学习率调度等）均做了详细对比。
*   **客观性**：作者在相同的框架（gsplat）下，通过控制变量（如固定原语数量和参数量）与 SOTA 方法（如 Spherical Voronoi）进行了公平对比，证明了 NHT 的优越性。

### 6. 主要结论
*   **性能领先**：NHT 在多个基准测试中达到了 SOTA 水平，尤其在捕捉高频细节和镜面反射方面表现卓越。
*   **高效性**：在低原语数量（如 100k 以下）的情况下，NHT 比传统 3DGS 提升了超过 2dB 的 PSNR。
*   **通用性**：该方法可无缝集成到 3DGS、2DGS、三角形泼溅等多种管线中，并能处理高维语义特征和 HDR 图像。

### 7. 优点与亮点
*   **拉格朗日特性**：特征随原语移动，天然支持场景编辑、变形和动画。
*   **解耦设计**：将几何（原语）与外观（调和纹理）解耦，使原语密度能更专注于几何复杂性而非外观细节。
*   **延迟渲染优化**：通过图像空间单次 MLP 评估，大幅降低了神经解码的计算开销。
*   **高频建模**：利用周期性激活函数有效地解决了神经网络在低维空间难以学习高频函数的“光谱偏差”问题。

### 8. 不足与局限
*   **过拟合风险**：在监督信息极度稀疏的情况下，高表达能力可能导致模型过度拟合特定视角。
*   **渲染开销**：虽然达到了实时，但由于增加了神经解码层，其渲染速度略慢于纯显式的 3DGS 方法。
*   **训练复杂性**：引入了额外的超参数（如特征维度、MLP 宽度等），且 MLP 的收敛速度通常慢于球谐函数。

（完）
