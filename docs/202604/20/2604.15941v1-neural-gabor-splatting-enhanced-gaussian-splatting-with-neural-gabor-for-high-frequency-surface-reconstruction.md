---
title: "Neural Gabor Splatting: Enhanced Gaussian Splatting with Neural Gabor for High-frequency Surface Reconstruction"
title_zh: 神经 Gabor 泼溅：利用神经 Gabor 增强高斯泼溅以实现高频表面重建
authors: "Haato Watanabe, Nobuyuki Umetani"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15941v1"
tags: ["query:q1"]
score: 8.0
evidence: 用于增强体渲染的神经Gabor泼溅
tldr: 针对3D高斯泼溅（3DGS）在处理高频细节时因单基元单颜色导致基元数量激增的问题，本文提出Neural Gabor Splatting。该方法通过为每个高斯基元引入轻量级MLP来建模复杂的颜色变化，并结合频率感知加密策略优化基元分布。实验证明，该方法在保持渲染效率的同时，显著提升了高频表面重建的精度，有效减少了基元冗余。
selection_source: fresh_fetch
motivation: 传统3DGS中每个基元仅能表示单一颜色，导致在重建具有高频细节的场景时需要海量基元，造成存储和计算负担。
method: 为高斯基元集成轻量级MLP以建模内部颜色变化，并引入基于频率能量的加密策略来精准控制基元的克隆与剪枝。
result: 在Mip-NeRF360和高频数据集上的实验表明，该方法能以更少的基元实现更高精度的复杂纹理和高频表面重建。
conclusion: Neural Gabor Splatting通过神经表示增强了高斯基元的表达能力，是解决大规模高频场景重建中效率与精度平衡问题的有效方案。
---

## 摘要
近年来，3D 高斯泼溅 (3DGS) 作为一种强大的 3D 重建和新视角合成方法迅速兴起。其利用高斯原语进行的显式表示实现了快速训练、实时渲染以及便捷的后期处理（如编辑和表面重建）。然而，3DGS 存在一个关键缺陷：对于具有高频外观细节的场景，原语数量会急剧增长，因为每个原语只能表示单一颜色，导致每个剧烈的颜色过渡都需要多个原语。为了克服这一限制，我们提出了神经 Gabor 泼溅 (Neural Gabor Splatting)，它通过轻量级多层感知器增强了每个高斯原语，从而在单个原语内建模广泛的颜色变化。为了进一步控制原语数量，我们引入了一种频率感知加密策略，该策略根据频率能量选择不匹配的原语进行剪枝和克隆。我们的方法实现了对具有挑战性的高频表面的准确重建。我们通过在 Mip-NeRF360 和高频数据集（如棋盘格图案）等标准基准测试上的广泛实验，以及全面的消融研究，证明了该方法的有效性。

## Abstract
Recent years have witnessed the rapid emergence of 3D Gaussian splatting (3DGS) as a powerful approach for 3D reconstruction and novel view synthesis. Its explicit representation with Gaussian primitives enables fast training, real-time rendering, and convenient post-processing such as editing and surface reconstruction. However, 3DGS suffers from a critical drawback: the number of primitives grows drastically for scenes with high-frequency appearance details, since each primitive can represent only a single color, requiring multiple primitives for every sharp color transition. To overcome this limitation, we propose neural Gabor splatting, which augments each Gaussian primitive with a lightweight multi-layer perceptron that models a wide range of color variations within a single primitive. To further control primitive numbers, we introduce a frequency-aware densification strategy that selects mismatch primitives for pruning and cloning based on frequency energy. Our method achieves accurate reconstruction of challenging high-frequency surfaces. We demonstrate its effectiveness through extensive experiments on both standard benchmarks, such as Mip-NeRF360 and High-Frequency datasets (e.g., checkered patterns), supported by comprehensive ablation studies.

---

## 论文详细总结（自动生成）

这篇论文由东京大学的研究人员提出，旨在解决 3D 高斯泼溅（3DGS）在处理高频纹理细节时面临的存储和表示效率问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统 3D 高斯泼溅（3DGS）中的每个高斯基元（Primitive）只能表示单一颜色。在重建具有复杂纹理或高频细节（如棋盘格、细密毛发）的场景时，系统必须通过堆叠大量微小的基元来模拟颜色变化，导致基元数量激增、内存占用巨大且渲染效率下降。
*   **研究背景**：虽然已有研究尝试通过 3D Gabor 噪声或贴图（Textured Gaussians）来增强基元表达力，但它们受限于预定义的基函数或固定的贴图分辨率。

### 2. 方法论
该论文提出了 **Neural Gabor Splatting**，核心思想是赋予每个高斯基元“神经纹理”表达能力：
*   **神经 Gabor 基元（Neural Gabor Primitive）**：在 2D 高斯泼溅（2DGS）的基础上，为每个基元集成一个极其轻量级的多层感知器（MLP）。
    *   **输入**：基元的局部 2D 坐标 $(u, v)$ 和观察视角方向 $\vec{d}$。
    *   **架构**：采用单隐层（6 个神经元）的 **SIREN** 网络（使用正弦激活函数），能够隐式捕捉高频信号而无需显式的频率编码。
    *   **输出**：该位置的 RGB 颜色。
*   **频率感知加密策略（Frequency-aware Densification）**：
    *   为了精准控制基元数量，引入了基于频率能量的误差分析。
    *   利用 **快速傅里叶变换（FFT）** 提取渲染图像与真实图像在不同频段（低、中、高三频段）的差异。
    *   将频域误差反向投影到 3D 空间，优先在缺乏高频细节的区域克隆或分裂基元，避免在低频区域产生冗余。

### 3. 实验设计
*   **数据集**：
    *   **Mip-NeRF360**：用于评估无边界场景的重建。
    *   **High-Frequency Dataset**：专门测试高频纹理（如条纹、靴子纹理）。
    *   **DTU** 与 **Tanks and Temples**：标准 3D 重建基准。
*   **对比方法（Benchmarks）**：
    *   基础方法：3DGS、2DGS。
    *   增强方法：3D Gabor Splatting、Textured Gaussians。
    *   最新神经泼溅方法：Neural Texture Splatting (NTS)、Neural Shell Texture Splatting (NEST)。
*   **实验设置**：在相同的总数据预算（存储空间）下，对比各方法的重建质量（PSNR, SSIM, LPIPS）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA GeForce RTX 3090 GPU**。
*   **训练时长**：所有实验统一运行 20k 次迭代。训练时间约为 2DGS 的 **2 倍**（主要是因为每个基元都需要更新 MLP 权重）。
*   **渲染性能**：推理速度在 **30—500 FPS** 之间，取决于场景复杂度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流数据集，包含室内、室外、物体等多种场景。
*   **消融实验**：对比了不同的加密策略（基于梯度的、基于误差的和基于频率感知的），验证了频率感知策略在控制基元数量上的有效性。
*   **公平性**：实验特别强调了在“相同内存预算”下进行对比，这对于评估压缩和表达效率至关重要。整体实验设计较为客观、充分。

### 6. 主要结论与发现
*   **表达力提升**：单个神经 Gabor 基元可以替代数十个单色高斯基元，显著降低了表示高频表面所需的基元总数。
*   **性能优越**：在受限的存储预算下，该方法在 PSNR 和 SSIM 等指标上一致优于 3DGS、2DGS 和 3D Gabor Splatting。
*   **鲁棒性**：相比于基于贴图的方法（如 Textured Gaussians），该方法在极低预算（如仅 1% 数据量）下表现更稳健，不会出现严重的几何坍塌。

### 7. 优点
*   **高频重建能力强**：SIREN 架构的引入使得模型对细节纹理的捕捉非常精准。
*   **存储效率高**：通过提升单个基元的“信息密度”，有效缓解了 3DGS 的存储压力。
*   **端到端优化**：不同于两阶段训练的方法，该方法支持从头开始的端到端优化，避免了预训练带来的偏差。

### 8. 不足与局限
*   **计算开销**：虽然基元数量减少了，但每个基元在渲染时都需要运行一次微型 MLP，这增加了计算负担，导致渲染速度慢于原始 3DGS。
*   **应用范围限制**：目前主要针对表面重建（基于 2DGS 框架），对于体积效应（如烟雾、透明物体）或动态场景的适用性尚未验证。
*   **低频冗余**：在极其平滑、缺乏纹理的区域，使用 MLP 可能存在过度参数化（Over-parameterization）的问题。

（完）
