---
title: "Hybrid Latents: Geometry-Appearance-Aware Surfel Splatting"
title_zh: 混合潜变量：几何-外观感知的面元泼溅
authors: "Neel Kelkar, Simon Niedermayr, Klaus Engel, Rüdiger Westermann"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14928v2"
tags: ["query:q1"]
score: 8.0
evidence: 用于渲染的混合神经辐射表示
tldr: 本研究提出了一种混合高斯-哈希网格的辐射场表示方法，旨在解决2D高斯泼溅中几何与外观纠缠的问题。通过结合每高斯潜变量与哈希网格特征，显式分解场景的高低频成分，并引入硬透明度衰减和BCE稀疏性损失。该方法在提升几何重建精度的同时，显著减少了基元数量，实现了高效且高质量的新视角合成。
selection_source: fresh_fetch
motivation: 针对现有模型中几何与外观纠缠导致高频纹理错误补偿几何缺陷的问题，寻求更清晰的场景表示。
method: 采用混合潜变量特征进行频率分解，并结合硬透明度衰减与概率剪枝技术实现高效的稀疏化表示。
result: 在多个数据集上实现了超越现有技术的重建精度，且所使用的基元数量比同类方法减少了一个数量级。
conclusion: 通过频率解耦和稀疏化策略，该方法显著提升了2D高斯模型在几何重建质量和渲染效率上的平衡。
---

## 摘要
我们引入了一种混合高斯-哈希网格辐射表示，用于从多视图图像中重建二维高斯场景模型。与 NeST splatting 类似，我们的方法减少了基于 NeRF 模型中常见的几何与外观之间的耦合，但在哈希网格特征的基础上增加了每个高斯的潜特征，以引导优化器分离场景的低频和高频成分。这种显式的基于频率的分解减少了高频纹理补偿几何误差的倾向。鼓励具有硬不透明度衰减的高斯分布进一步加强了几何与外观的分离，从而提高了几何重建质量和渲染效率。最后，概率剪枝结合诱导稀疏性的 BCE 不透明度损失，允许关闭冗余的高斯，从而产生足以表示场景的最小高斯集合。通过使用合成和真实世界数据集，我们与基于高斯的新视角合成的最先进技术进行了对比，并证明了在基元数量减少一个数量级的情况下，具有更优越的重建保真度。

## Abstract
We introduce a hybrid Gaussian-hash-grid radiance representation for reconstructing 2D Gaussian scene models from multi-view images. Similar to NeST splatting, our approach reduces the entanglement between geometry and appearance common in NeRF-based models, but adds per-Gaussian latent features alongside hash-grid features to bias the optimizer toward a separation of low- and high-frequency scene components. This explicit frequency-based decomposition reduces the tendency of high-frequency texture to compensate for geometric errors. Encouraging Gaussians with hard opacity falloffs further strengthens the separation between geometry and appearance, improving both geometry reconstruction and rendering efficiency. Finally, probabilistic pruning combined with a sparsity-inducing BCE opacity loss allows redundant Gaussians to be turned off, yielding a minimal set of Gaussians sufficient to represent the scene. Using both synthetic and real-world datasets, we compare against the state of the art in Gaussian-based novel-view synthesis and demonstrate superior reconstruction fidelity with an order of magnitude fewer primitives.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Hybrid Latents** 的新型辐射场表示方法，旨在通过几何与外观的深度解耦，实现高精度、高效率且极度稀疏的 3D 场景重建。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 高斯泼溅（3DGS）及其变体（如 2DGS）在处理高频纹理时，往往需要海量的几何基元（Primitives），导致内存开销巨大。
*   **研究动机**：
    *   **几何-外观纠缠**：现有的混合表示（如 NeST）虽然引入了哈希网格（Hash-grid），但几何基元往往会为了补偿纹理误差而发生“几何膨胀”（Dilation），偏离真实表面。
    *   **频率分布不均**：低频的几何信息和高频的纹理细节在优化过程中缺乏显式的分离机制。
    *   **冗余性**：传统方法产生的基元中包含大量半透明的冗余部分，影响渲染效率。

### 2. 方法论
该方法的核心思想是**“频率感知”的混合表示**，具体技术细节包括：
*   **混合潜变量表示（Hybrid Latents）**：
    *   每个 2D 面元（Surfel）携带一个可学习的**低频潜特征**（$f_g$），负责捕捉基础几何和光照。
    *   结合一个单分辨率的**哈希网格**提供**高频残差特征**（$f_h$）。
    *   在渲染时，将两者拼接后输入轻量级 MLP 解码器，这种归纳偏置强制面元贴合几何表面。
*   **可变形 Beta 核（Beta Kernels）**：
    *   使用 Beta 分布代替高斯分布作为面元形状函数。通过调节参数 $b$，面元可以在“软边缘球体”和“硬边缘圆盘”之间切换，有效减少重叠渲染（Overdraw）。
*   **优化策略**：
    *   **随机几何优化（MCMC）**：采用马尔可夫链蒙特卡罗采样（基于 Langevin 动力学）来更新面元位置，允许面元在场景中“重新定位”以逃离局部最优。
    *   **二元交叉熵（BCE）正则化**：引入 BCE 损失函数作用于不透明度，强制面元要么完全透明（被剪枝），要么完全不透明，从而实现极高的稀疏度。

### 3. 实验设计
*   **数据集**：
    *   **NeRF Synthetic**：用于评估合成场景的精细度。
    *   **Mip-NeRF 360**：用于评估无边界真实场景。
    *   **DTU**：专门用于评估几何重建的准确性（使用 Chamfer 距离）。
*   **对比方法（Baselines）**：
    *   经典方法：3DGS, 2DGS。
    *   纹理/混合方法：SuperGS, NeST-Splatting。
    *   频率/核函数改进方法：Beta-Splatting。
*   **评估指标**：PSNR, SSIM, LPIPS（视觉质量）；Points Count（稀疏度）；FPS（渲染速度）；Chamfer Distance（几何精度）。

### 4. 资源与算力
*   **硬件平台**：实验在一台 **NVIDIA RTX 5090 GPU** 上完成。
*   **训练细节**：
    *   总迭代次数为 30,000 次。
    *   包含 10,000 次的 2DGS 预热阶段。
    *   哈希表大小根据场景规模设定在 $2^{19}$ 到 $2^{21}$ 之间。
*   **渲染性能**：在高度稀疏化后，渲染速度可达 80 FPS 以上（取决于场景）。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准数据集上进行了详尽测试，涵盖了室内、室外、合成及真实物体。
*   **消融实验**：作者对混合特征、MCMC 优化、BCE 正则化以及 Beta 核的影响分别进行了消融分析，清晰展示了各组件对稀疏度和质量的贡献。
*   **客观性**：通过可视化“仅面元特征”和“仅哈希特征”的渲染图，直观证明了频率解耦的有效性。实验对比涵盖了最新的 SOTA 方法，评价标准全面且客观。

### 6. 主要结论与发现
*   **极高的稀疏度**：在保持甚至超过 SOTA 视觉质量的前提下，该方法使用的基元数量比 2DGS 或 NeST **减少了一个数量级**（例如从百万级降至十万级甚至更低）。
*   **几何保真度提升**：通过频率解耦，面元能够更紧密地贴合真实物理表面，DTU 数据集上的 Chamfer 距离显著优于 NeST。
*   **渲染效率**：由于基元数量大幅减少且 Beta 核降低了 Overdraw，渲染速度得到了显著提升。

### 7. 亮点
*   **频率解耦机制**：巧妙地利用每面元特征和哈希网格的特性，解决了混合表示中的几何膨胀问题。
*   **稀疏化方案**：结合 MCMC 的重定位能力和 BCE 的硬剪枝能力，实现了目前高斯泼溅领域极具竞争力的压缩效果。
*   **Beta 核的应用**：不仅提升了拟合能力，还通过硬边缘特性优化了渲染管线的剔除效率。

### 8. 不足与局限
*   **解码器瓶颈**：虽然基元少了，但由于使用了 256 维隐藏层的 MLP 解码器，其计算开销仍高于简单的球谐函数（SH）查询，限制了其在极低功耗设备上的表现。
*   **编辑困难**：由于外观部分依赖于隐式的哈希网格，相比于纯显式的 3DGS，对场景进行局部编辑或合并多个场景模型更具挑战性。
*   **预热依赖**：模型仍需要 2DGS 进行初始化预热，无法完全从零开始高效训练。

（完）
