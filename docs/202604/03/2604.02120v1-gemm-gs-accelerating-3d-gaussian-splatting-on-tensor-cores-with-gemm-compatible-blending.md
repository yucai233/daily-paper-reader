---
title: "GEMM-GS: Accelerating 3D Gaussian Splatting on Tensor Cores with GEMM-Compatible Blending"
title_zh: GEMM-GS：通过 GEMM 兼容的混合变换在 Tensor Core 上加速 3D 高斯泼溅
authors: "Haomin Li, Bowen Zhu, Fangxin Liu, Zongwu Wang, Xinran Liang, Li Jiang, Haibing Guan"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02120v1"
tags: ["query:q8"]
score: 8.0
evidence: 在张量核心上加速3D高斯泼溅
tldr: 3D高斯泼溅（3DGS）虽然提升了渲染速度，但仍难以满足极高性能实时需求，且现有优化未充分利用现代GPU的Tensor Core。本文提出GEMM-GS，通过将3DGS的混合过程等效重构为GEMM兼容形式，使渲染能利用Tensor Core加速。结合三阶段双缓冲CUDA内核设计，GEMM-GS在原始3DGS基础上实现了1.42倍加速，并能与其他优化方法叠加使用，显著提升了渲染效率。
selection_source: fresh_fetch
motivation: 现有的3DGS加速方案由于缺乏通用矩阵乘法（GEMM）操作，无法利用现代GPU中强大的Tensor Core硬件单元进行加速。
method: 将3DGS的混合计算过程重构为GEMM兼容形式，并设计了集成三阶段双缓冲流水线的高性能CUDA内核以重叠计算与访存。
result: 实验表明GEMM-GS比原始3DGS快1.42倍，且在结合现有加速方法时能额外提供平均1.47倍的性能提升。
conclusion: GEMM-GS证明了通过算法重构利用硬件专用计算单元是进一步提升3DGS渲染性能的有效途径。
---

## 摘要
神经辐射场 (NeRF) 能够从多张 2D 图像中重建 3D 场景，但其点采样设计导致了较高的渲染延迟。3D 高斯泼溅 (3DGS) 通过显式场景表示和优化的流水线改进了 NeRF，但仍无法满足实际的实时性需求。现有的加速工作忽略了现代 GPU 中不断演进的 Tensor Core，因为 3DGS 流水线缺乏通用矩阵乘法 (GEMM) 操作。本文提出了 GEMM-GS，这是一种通过

## Abstract
Neural Radiance Fields (NeRF) enables 3D scene reconstruction from several 2D images but incurs high rendering latency via its point-sampling design. 3D Gaussian Splatting (3DGS) improves on NeRF with explicit scene representation and an optimized pipeline yet still fails to meet practical real-time demands. Existing acceleration works overlook the evolving Tensor Cores of modern GPUs because 3DGS pipeline lacks General Matrix Multiplication (GEMM) operations. This paper proposes GEMM-GS, an acceleration approach utilizing tensor cores on GPUs via GEMM-friendly blending transformation. It equivalently reformulates the 3DGS blending process into a GEMM-compatible form to utilize Tensor Cores. A high-performance CUDA kernel is designed, integrating a three-stage double-buffered pipeline that overlaps computation and memory access. Extensive experiments show that GEMM-GS achieves $1.42\times$ speedup over vanilla 3DGS and provides an additional $1.47\times$ speedup on average when combining with existing acceleration approaches. Code is released at https://github.com/shieldforever/GEMM-GS.

---

## 论文详细总结（自动生成）

这是一份关于论文《GEMM-GS: Accelerating 3D Gaussian Splatting on Tensor Cores with GEMM-Compatible Blending》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：3D 高斯泼溅（3DGS）虽然比 NeRF 快，但在处理大规模、高分辨率场景时仍难以达到真正的实时渲染要求。
*   **研究动机**：现代 GPU 的 Tensor Core（张量核心）拥有极强的矩阵乘法（GEMM）算力，但 3DGS 的渲染流水线（尤其是混合阶段）主要依赖 CUDA Core 进行逐像素计算，缺乏 GEMM 操作，导致强大的 Tensor Core 资源处于闲置状态。
*   **背景**：混合阶段（Blending）占据了 3DGS 渲染总耗时的约 70%，是性能提升的关键瓶颈。

### 2. 方法论
*   **核心思想**：通过等效数学变换，将 3DGS 混合过程中非线性的不透明度（$\alpha$）计算重构为**通用矩阵乘法（GEMM）**形式，从而利用 Tensor Core 加速。
*   **关键技术细节**：
    *   **块内坐标变换**：利用 Tile（瓦片，通常为 16x16 像素）内像素的相对坐标。将原本复杂的指数幂项拆解为一个仅与高斯参数相关的 6 维向量和一个仅与像素相对位置相关的 6 维向量的内积。
    *   **矩阵化重构**：将一个 Batch 的高斯属性组成矩阵 $M_g$，将 Tile 内像素的相对位置预计算并组成矩阵 $M_p$。通过 $M_{power} = M_g \cdot M_p$ 一次性算出该 Batch 内所有高斯对所有像素的贡献参数。
    *   **高性能 CUDA 内核设计**：
        *   **三阶段流水线**：设计了“加载索引”、“构建 $M_g$ 矩阵”、“执行 GEMM 与体积渲染”三个阶段。
        *   **双缓冲（Double Buffer）**：利用异步内存拷贝（Asynchronous Copy）技术，实现计算与访存的重叠（Overlap）。
        *   **PTX 指令优化**：直接调用底层的 `mma`（矩阵乘累加）PTX 指令，以实现极高的硬件利用率。

### 3. 实验设计
*   **数据集/场景**：使用了三个主流的 3D 重建数据集，共 13 个场景：
    *   **Tank & Temples** (Outdoor): Train, Truck.
    *   **Deep Blending** (Indoor): Playroom, Drjohnson.
    *   **Mip-NeRF 360**: 包括 bicycle, garden, kitchen 等 9 个室内外场景。
*   **Benchmark 与对比方法**：
    *   **基准**：Vanilla 3DGS（原始版本）。
    *   **预处理优化类**：FlashGS, StopThePop, Speedy-Splat。
    *   **模型压缩类**：LightGaussian, c3dgs。
*   **实验目的**：验证 GEMM-GS 的独立加速能力，以及它与现有优化方法结合后的叠加加速效果（正交性）。

### 4. 资源与算力
*   **硬件平台**：使用了 **NVIDIA A100** 和 **NVIDIA H100** GPU。
*   **训练配置**：所有场景均按照官方标准训练 30,000 次迭代。
*   **测试配置**：对测试集进行 10 次完整渲染取平均延迟。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 13 个不同复杂度的场景，并在两种顶级架构的 GPU 上进行了测试。
*   **消融/敏感性实验**：
    *   **Batch Size 影响**：测试了不同 Batch 大小对 Tensor Core 利用率的影响。
    *   **分辨率敏感性**：测试了在 1x、2x、3x 分辨率下的加速比，验证了高分辨率下的扩展性。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了原始版本，还证明了该方法作为一种“底层算子优化”，可以无缝集成到其他高层优化算法中，具有很强的说服力。

### 6. 主要结论与发现
*   **显著加速**：GEMM-GS 相比原始 3DGS 在 A100 上实现了 **1.42x** 的平均加速。
*   **强大的兼容性（正交性）**：当集成到 FlashGS 或 Speedy-Splat 等现有最先进方法中时，能额外提供平均 **1.47x** 的性能提升。
*   **高分辨率优势**：在 2x 和 3x 分辨率下，加速比进一步提升至 **1.73x** 以上，说明场景越复杂，Tensor Core 的优势越明显。
*   **硬件友好**：无需修改硬件，直接在商用 GPU 上部署。

### 7. 优点
*   **硬件利用率高**：填补了 3DGS 无法利用 Tensor Core 的空白，极大地释放了现代 GPU 的潜力。
*   **即插即用**：作为一种等效变换，它不改变渲染质量（Lossless），且不需要重新训练模型。
*   **设计精巧**：通过块内相对坐标变换将二次型问题转化为线性点积，数学推导简洁高效。

### 8. 不足与局限
*   **硬件门槛**：虽然支持商用 GPU，但主要针对具有 Tensor Core 的架构（如 Volta 及以后），在旧款 GPU 或移动端设备上可能无法发挥作用。
*   **共享内存限制**：由于需要存储 $M_g$ 和 $M_p$ 矩阵，对 GPU 的 Shared Memory 占用较高，这限制了 Batch Size 的进一步扩大。
*   **算子覆盖**：目前主要优化了混合阶段的幂项计算，对于体积渲染中的指数函数（exp）等非线性部分，仍需回到 CUDA Core 处理。

（完）
